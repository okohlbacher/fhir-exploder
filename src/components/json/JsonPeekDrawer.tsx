/**
 * Wave 0 stub — Plan 02 (52-02) will replace this with the full implementation.
 *
 * Exists only so that src/__tests__/peek-drawer.test.tsx can import it without
 * a module-not-found error. The stub renders nothing, which causes the
 * PEEK-01/02/03 tests to be RED (expected Wave 0 behavior).
 */
export function JsonPeekDrawer() {
  return null;
}
