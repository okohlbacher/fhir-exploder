/**
 * Patient list page placeholder — fully implemented in Task 2 of Plan 03-01.
 *
 * Created as a minimal stub so the /patients route registered in App.tsx
 * compiles after Task 1. Task 2 replaces this file with the full search UI
 * and SearchControl-backed results table.
 */
export function PatientListPage() {
  return null;
}
