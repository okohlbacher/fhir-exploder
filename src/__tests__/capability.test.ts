import { describe, it } from 'vitest';

describe('parseResourceTypes', () => {
  it.todo('parses CapabilityStatement rest resources into ParsedResourceType[]');
  it.todo('extracts search parameters from each resource');
  it.todo('extracts operations from each resource');
  it.todo('assigns FHIR category to each resource type');
  it.todo('handles empty CapabilityStatement gracefully');
});
