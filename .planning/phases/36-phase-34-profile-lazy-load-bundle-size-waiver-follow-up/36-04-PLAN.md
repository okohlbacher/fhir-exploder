---
phase: 36
plan: 04
type: execute
wave: 3
depends_on:
  - 36-01
  - 36-02
  - 36-03
files_modified:
  - .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html
  - .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-04-BUNDLE-DELTA.md
autonomous: true
requirements:
  - MII-EXT-12
must_haves:
  truths:
    - "rollup-plugin-visualizer treemap shows initial-load bundle delta vs Phase 34 baseline ≤ 100 KB gz; before/after HTMLs committed under .planning/phases/36-.../ (verbatim ROADMAP success criterion 3)"
    - "npm test 1054+ passing / 0 failing; npx tsc -b --noEmit clean; npm run build clean (verbatim ROADMAP success criterion 4)"
    - "The treemap visually shows the extension SD JSON files in async-only chunks (NOT in the initial-load critical path)"
  artifacts:
    - path: ".planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html"
      provides: "Post-refactor treemap from `ANALYZE=1 npm run build` after Plans 02+03 land"
    - path: ".planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-04-BUNDLE-DELTA.md"
      provides: "Authoritative delta computation: before bytes, after bytes, delta KB gz, gate result, async-chunk evidence"
  key_links:
    - from: "36-04-BUNDLE-DELTA.md"
      to: "36-visualizer-before.html"
      via: "Reads BASELINE_GZ_BYTES from the HTML comment in the before treemap"
      pattern: "BASELINE_GZ_BYTES"
    - from: "36-04-BUNDLE-DELTA.md"
      to: "36-visualizer-after.html"
      via: "Captures POST_REFACTOR_GZ_BYTES from the new build"
      pattern: "POST_REFACTOR_GZ_BYTES"
---

<objective>
Close Phase 36 by measuring the bundle-size delta and running the full regression gate.

Three deliverables:
1. **`36-visualizer-after.html`** — copy of `dist/bundle-stats.html` from `ANALYZE=1 npm run build` AFTER Plans 02 + 03 are merged. Carries a `POST_REFACTOR_GZ_BYTES: {N}` HTML comment.
2. **`36-04-BUNDLE-DELTA.md`** — authoritative report containing:
   - Baseline bytes (from Plan 01's `BASELINE_GZ_BYTES` comment)
   - Post-refactor bytes (from Task 1 below)
   - Delta in bytes AND KB gz
   - Gate result (PASS if delta ≤ 100 KB gz; FAIL otherwise)
   - Async-chunk evidence: list of post-refactor `dist/assets/*.js` files containing extension JSON content (sorted by size)
   - Per Pitfall 6: explicit confirmation that both measurements used the same `find … -exec gzip -9c {} \; | wc -c` formula
3. **Full regression gate** — `npm test`, `npx tsc -b --noEmit`, `npm run build` all green.

Purpose: Closes ROADMAP success criteria 3 and 4. This is the phase exit gate; if delta > 100 KB gz, the phase needs a Plan 05 gap-closure (manualChunks adjustment per RESEARCH §"Code Examples" Example 5) BEFORE phase completion.

Output: Two new artifacts in the phase directory; phase exits with all 4 ROADMAP success criteria satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-RESEARCH.md
@.planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-VALIDATION.md
@.planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-before.html
@.planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-01-SUMMARY.md
@.planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-02-SUMMARY.md
@.planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-03-SUMMARY.md
@.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md
@vite.config.ts

<interfaces>
Inputs:
  - Plan 01's `36-visualizer-before.html` carries `<!-- BASELINE_GZ_BYTES: {N} -->`. Read N to compute delta.
  - The expected baseline is in the 920k-960k byte range (Phase 34 measured 948,449 B; Phase 35 added small UI ~few KB; Plan 01 captured the precise number).

Outputs:
  - `<!-- POST_REFACTOR_GZ_BYTES: {N} -->` appended to `36-visualizer-after.html`
  - `36-04-BUNDLE-DELTA.md` with a fixed format (see Task 2 action)

Gate: delta = POST_REFACTOR_GZ_BYTES − BASELINE_GZ_BYTES; PASS if delta ≤ 100 × 1024 = 102,400 B.

Per RESEARCH Pitfall 2: Vite/Rollup may merge tiny dynamic imports back into one big async chunk. That is ACCEPTABLE for Phase 36 — the gate is initial-load delta; an async chunk is non-blocking. If the after treemap shows the extension JSONs in a single `extensions-*.js` async chunk, document this in the delta MD as expected behavior.

Per RESEARCH Open Question 2: re-measure on current main (Phase 35 HEAD) so the delta is honest. Plan 01 already captured this baseline. DO NOT re-baseline.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build with ANALYZE=1, capture post-refactor treemap, measure post-refactor gz total</name>
  <files>.planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html</files>
  <read_first>
    - .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-RESEARCH.md (Common Pitfalls Pitfall 6 — measurement parity; Pitfall 2 — async-chunk grouping is acceptable)
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md (lines 100-130 — exact measurement formula precedent)
    - vite.config.ts (confirms ANALYZE=1 gates the visualizer; OUTPUT goes to dist/bundle-stats.html)
    - .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-before.html (last 5 lines — to extract BASELINE_GZ_BYTES for Task 2's reference)
  </read_first>
  <action>
    Run a fresh `ANALYZE=1` build and capture both the treemap AND the post-refactor on-disk gz total.

    Exact commands:
    ```bash
    rm -rf dist
    ANALYZE=1 npm run build 2>&1 | tail -20
    cp dist/bundle-stats.html .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html
    ```

    Then measure on-disk gz total using the SAME formula Plan 01 used:
    ```bash
    find dist/assets -type f \( -name "*.js" -o -name "*.css" \) -exec gzip -9c {} \; | wc -c
    ```

    Append a single-line comment to the bottom of `36-visualizer-after.html`:
    ```
    <!-- POST_REFACTOR_GZ_BYTES: {N} -->
    ```
    where `{N}` is the exact integer from `wc -c`.

    ALSO capture an inventory of `dist/assets/*.js` files sorted by gz-size (for Task 2's async-chunk evidence section). Pipe to a temp file:
    ```bash
    for f in dist/assets/*.js; do
      gz=$(gzip -9c "$f" | wc -c)
      echo "$gz $f"
    done | sort -rn > /tmp/36-after-chunk-sizes.txt
    head -20 /tmp/36-after-chunk-sizes.txt
    ```

    The treemap (visual inspection in browser) should show extension JSON content under one or more async chunks distinct from the initial `index-*.js` bar — this is the structural proof the lazy-load worked. If extension JSON content STILL appears in the initial-load chunk, the refactor did not take effect; investigate before proceeding to Task 2.

    Failure modes:
    - `npm run build` fails with TS errors → tsc was supposed to be clean from Plans 02+03. Stop, fix the regression, re-run.
    - `dist/bundle-stats.html` doesn't appear after build → ANALYZE=1 didn't reach the visualizer plugin. Verify env var spelling; check `vite.config.ts:11` for the `=== '1'` strict check.
    - `dist/assets/` is empty after build → upstream build error swallowed; scroll back through full `npm run build` output.
  </action>
  <verify>
    <automated>test -f .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html &amp;&amp; grep -c "POST_REFACTOR_GZ_BYTES:" .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html &amp;&amp; npm run build 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html` exits 0
    - `grep -c "POST_REFACTOR_GZ_BYTES:" .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html` returns exactly `1`
    - The captured POST_REFACTOR byte count is plausibly in the `~700,000-750,000` range (baseline ~930k − 225k extension chunk shifted to async ≈ 705k). If > 850,000 the lazy-load did not take effect; if < 600,000 something else broke.
    - HTML file size > 100 KB (sanity check)
    - `dist/assets/*.js` contains at least 1 file matching `extensions` or appears in /tmp/36-after-chunk-sizes.txt under an async-chunk name (Vite's chunk naming uses content hashes, so the EXACT name varies — visual treemap inspection is the structural proof)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Post-refactor treemap captured; gz total measured and embedded as HTML comment; build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author 36-04-BUNDLE-DELTA.md with delta computation, gate result, and async-chunk evidence</name>
  <files>.planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-04-BUNDLE-DELTA.md</files>
  <read_first>
    - .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-before.html (last 10 lines for BASELINE_GZ_BYTES)
    - .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html (last 10 lines for POST_REFACTOR_GZ_BYTES)
    - /tmp/36-after-chunk-sizes.txt (Task 1 chunk-size inventory)
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md (§3 for the comparison-table style precedent)
  </read_first>
  <action>
    Write `.planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-04-BUNDLE-DELTA.md` with EXACTLY this structure (substitute the {N} placeholders with real measurements):

    ```markdown
    # Phase 36 — Bundle-Size Delta Report (MII-EXT-12 lazy-load gate)

    **Authored:** {today's date YYYY-MM-DD}
    **Method:** `find dist/assets -type f \( -name "*.js" -o -name "*.css" \) -exec gzip -9c {} \; | wc -c` (per Pitfall 6, identical formula used for both before and after measurements; matches Phase 34 `34-06-UAT.md:100`).

    ## Measurements

    | Metric | Value | Source |
    |--------|-------|--------|
    | Baseline gz (Phase 35 HEAD, pre-Phase-36) | {BASELINE_GZ_BYTES} B / {BASELINE_KB} KB | `36-visualizer-before.html` (BASELINE_GZ_BYTES comment) |
    | Post-refactor gz (Phase 36 HEAD) | {POST_REFACTOR_GZ_BYTES} B / {POST_KB} KB | `36-visualizer-after.html` (POST_REFACTOR_GZ_BYTES comment) |
    | **Delta** | **{DELTA_BYTES} B / {DELTA_KB} KB gz** | post − before |

    ## Gate Result (ROADMAP Success Criterion 3)

    | Gate | Threshold | Measured | Result |
    |------|-----------|----------|--------|
    | Initial-load bundle delta | ≤ 100 KB gz (102,400 B) | {DELTA_KB} KB gz | **{PASS|FAIL}** |

    {if PASS:}
    Phase 36 closes ROADMAP success criterion 3. The +227.81 KB gz overage from Phase 34 (D-23 WAIVE-AND-DEFER) is reduced to {DELTA_KB} KB gz by moving the extension-profile JSON payload out of the initial-load critical path into async-only chunks.

    {if FAIL:}
    Delta exceeds 100 KB gz threshold. Investigation required:
    - Run `node scripts/inspect-chunks.mjs` (or visualize `36-visualizer-after.html`) to identify which chunk(s) account for the residual delta.
    - Likely culprit: Vite chunk-grouping merged dynamic imports back into the initial chunk (Pitfall 2). Mitigation: add `build.rollupOptions.output.manualChunks` per RESEARCH §Code Examples Example 5.
    - Open a Phase 36 gap-closure plan (`/gsd-plan-phase 36 --gaps`) before declaring phase complete.

    ## Async-Chunk Evidence (Structural Proof of Lazy-Load)

    Top 20 dist/assets/*.js files by gzipped size (post-refactor):

    ```
    {paste contents of /tmp/36-after-chunk-sizes.txt | head -20}
    ```

    **Interpretation:**
    - The largest initial-load chunk should be `index-*.js` at ~330 KB gz (unchanged from Phase 34).
    - Extension-profile JSON content should appear in async-only chunks (Vite naming pattern: `chunk-*.js` or per-package groupings). These are FETCHED ON DEMAND when `getExtensionProfileForUrl` is first called for a URL — they do NOT block initial paint.
    - Per Pitfall 2: if Vite consolidates all 483 JSONs into ONE big async chunk (e.g. `chunk-DEADBEEF.js` ~225 KB gz), the gate STILL passes because the chunk is non-blocking. v1.6 may want `manualChunks` config for finer-grained per-package chunks; that is OUT OF SCOPE for Phase 36.

    ## Visual Treemap Inspection

    Open `.planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html` in a browser. Confirm:
    1. The "Initial bundle" / `index-*.js` bar is in the ~330 KB gz neighborhood (Phase 34 baseline).
    2. Extension SD content (filenames matching `Extension-mii-*` or `Procedure-mii-*` etc.) appears under async chunks, NOT under the initial bundle.
    3. No regression in non-extension chunks (compare to `36-visualizer-before.html`).

    ## Sources

    - `.planning/phases/36-.../36-visualizer-before.html` (Phase 35 HEAD baseline, captured Plan 01)
    - `.planning/phases/36-.../36-visualizer-after.html` (Phase 36 HEAD, captured this plan Task 1)
    - `.planning/phases/34-14-.../34-06-UAT.md:100-130` (measurement methodology precedent)
    - `.planning/phases/36-.../36-RESEARCH.md` §Common Pitfalls Pitfall 2 (acceptable async-chunk grouping) + Pitfall 6 (measurement parity)
    ```

    To compute the substitutions: extract BASELINE_GZ_BYTES from `36-visualizer-before.html` and POST_REFACTOR_GZ_BYTES from `36-visualizer-after.html` (both are HTML comments at the bottom of each file). Compute `DELTA_BYTES = POST − BASELINE`. Convert to KB by `/1024` (round to 2 decimals). Write the actual numbers into the MD, NOT the {N} placeholders.

    Inline computation example (run after Task 1 completes):
    ```bash
    BEFORE=$(grep "BASELINE_GZ_BYTES:" .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-before.html | grep -oE '[0-9]+')
    AFTER=$(grep "POST_REFACTOR_GZ_BYTES:" .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html | grep -oE '[0-9]+')
    DELTA=$((AFTER - BEFORE))
    DELTA_KB=$(awk "BEGIN { printf \"%.2f\", $DELTA / 1024 }")
    BASELINE_KB=$(awk "BEGIN { printf \"%.2f\", $BEFORE / 1024 }")
    POST_KB=$(awk "BEGIN { printf \"%.2f\", $AFTER / 1024 }")
    echo "Before: $BEFORE B ($BASELINE_KB KB)"
    echo "After:  $AFTER B ($POST_KB KB)"
    echo "Delta:  $DELTA B ($DELTA_KB KB)"
    if [ "$DELTA" -le "102400" ]; then echo "GATE: PASS"; else echo "GATE: FAIL"; fi
    ```

    Use the printed numbers verbatim in the MD.
  </action>
  <verify>
    <automated>test -f .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-04-BUNDLE-DELTA.md &amp;&amp; grep -E "Delta.*KB gz" .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-04-BUNDLE-DELTA.md &amp;&amp; grep -E "GATE.*(PASS|FAIL)" .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-04-BUNDLE-DELTA.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-04-BUNDLE-DELTA.md` exits 0
    - `grep -c "Baseline gz" .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-04-BUNDLE-DELTA.md` returns ≥ `1`
    - `grep -c "Post-refactor gz" .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-04-BUNDLE-DELTA.md` returns ≥ `1`
    - `grep -c "Delta" .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-04-BUNDLE-DELTA.md` returns ≥ `1`
    - `grep -E "(PASS|FAIL)" .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-04-BUNDLE-DELTA.md` matches at least 1 line (gate verdict written)
    - The numerical delta value (in bytes) extracted from the MD MUST be `(POST_REFACTOR_GZ_BYTES − BASELINE_GZ_BYTES)` from the two HTML comments (cross-check by re-running the inline computation)
    - The "Async-Chunk Evidence" section contains a code block listing at least 5 chunk files
  </acceptance_criteria>
  <done>Delta MD written with verbatim numbers; gate verdict (PASS/FAIL) recorded; async-chunk evidence section populated; ROADMAP success criterion 3 closed (PASS) or escalated to gap-closure (FAIL).</done>
</task>

<task type="auto">
  <name>Task 3: Final regression gate — npm test + tsc + npm run build all clean</name>
  <files></files>
  <read_first>
    - .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-04-BUNDLE-DELTA.md (Task 2 result; this task confirms the OTHER 3 ROADMAP success criteria from #4)
    - .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-03-SUMMARY.md (last test counts on record from Plan 03)
  </read_first>
  <action>
    Run the full Phase 36 exit gate. NO file modifications in this task — purely verification.

    ```bash
    npm test 2>&1 | tail -10
    ```
    Expected: `Tests {N} passed | {M} todo` where N ≥ 1060 and 0 failed.

    ```bash
    npx tsc -b --noEmit 2>&1 | tail -5
    ```
    Expected: exit 0, no error output.

    ```bash
    npm run build 2>&1 | tail -10
    ```
    Expected: exit 0, "built in {time}" line at the bottom; no errors.

    If any of the three commands fails, capture the exact error output in the SUMMARY for this plan and DO NOT proceed to phase verification — the failure must be addressed by either (a) a hotfix to the relevant Plan 02/03 file, or (b) a Plan 05 gap-closure if the failure indicates a deeper integration issue.

    Record the exact test count number in the plan SUMMARY for ROADMAP traceability.

    Note on Plan 04 not modifying source code: this task is the explicit verification gate for ROADMAP success criterion 4 (`npm test ≥ 1054 passing / 0 failing; npx tsc -b --noEmit clean; npm run build clean`). It is intentionally separate from Tasks 1+2 so a regression triggered by the bundle-rebuild step is identified independently of the delta-computation step.
  </action>
  <verify>
    <automated>npm test 2>&amp;1 | tail -5 | grep -E "Tests +([0-9]+) passed" &amp;&amp; npx tsc -b --noEmit &amp;&amp; npm run build 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npm test 2>&1 | tail -5 | grep -E "Tests +([0-9]+) passed"` matches and the captured number is ≥ `1060` (Plan 01 added 5 — 4 trivially-passing + 1 loader-spy that turns GREEN under Plan 02's dedup Map; Plan 03 unskipped 1 → 1054 baseline + 5 + 1 = 1060)
    - `npm test 2>&1 | tail -5 | grep -E "Tests +.*failed"` returns NOTHING (0 failed)
    - `npx tsc -b --noEmit` exits 0 with NO error lines printed (sanity: pipe to `wc -l` and assert empty stderr)
    - `npm run build 2>&1 | grep -E "error|Error|ERROR"` returns NOTHING
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 3 gates green: test count ≥ 1060, tsc clean, build clean. ROADMAP success criterion 4 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → committed artifact | The visualizer HTML is generated by Vite and copied to a planning artifact; static, not consumed by the running app. |
| filesystem → MD report | The delta MD reads numbers from HTML comments (no executable content). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-07 | Information Disclosure | 36-visualizer-after.html, 36-04-BUNDLE-DELTA.md | accept | Treemap reveals chunk names + sizes only; no PHI, no secrets. Standard Vite analyze output. |

No high-severity threats. No auth/session/access/crypto changes.
</threat_model>

<verification>
- `36-visualizer-after.html` exists and carries `POST_REFACTOR_GZ_BYTES: {N}` HTML comment
- `36-04-BUNDLE-DELTA.md` exists with explicit Delta and PASS/FAIL verdict
- `npm test 2>&1 | tail -5 | grep -E "Tests +([0-9]+) passed"` shows ≥ 1060 passing, 0 failing
- `npx tsc -b --noEmit` exits 0
- `npm run build` exits 0
- The delta in `36-04-BUNDLE-DELTA.md` ≤ 102,400 B (= 100 KB gz) → PHASE 36 ROADMAP success criterion 3 SATISFIED. If FAIL, escalate to a gap-closure plan.
</verification>

<success_criteria>
All four ROADMAP success criteria for Phase 36 demonstrably satisfied:
1. ✓ Plan 02 — `extensions/index.ts` no longer uses static `import` for SD JSONs; per-canonical-URL `import()` resolved on first call.
2. ✓ Plan 03 — Production caller of `getExtensionProfileForUrl` exists in `useConformanceRun.ts` AND is exercised by an active unit test.
3. ✓ Plan 04 Task 2 — Treemap shows initial-load bundle delta ≤ 100 KB gz; before/after HTMLs committed.
4. ✓ Plan 04 Task 3 — `npm test` ≥ 1060 passing / 0 failing; `npx tsc -b --noEmit` clean; `npm run build` clean.
</success_criteria>

<output>
After completion, create `.planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-04-SUMMARY.md` recording:
- Final delta numbers (before, after, delta in B and KB)
- Gate verdict (PASS / FAIL)
- Final test count from the regression run
- If FAIL: link to the gap-closure plan that will close the residual delta
</output>
