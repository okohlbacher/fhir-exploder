---
phase: 38.2-fix-valuequantity-render-show-value-followed-by-unit-on-obse
verified: 2026-04-29T11:05:00Z
status: human_needed
score: 4/5 must-haves verified (1 routes to human verification)
overrides_applied: 0
human_verification:
  - test: "Phase 33 HUMAN-UAT Test 1 + Test 6 live-Blaze re-walk"
    expected: "Navigate dev server (npm run dev) → Synthea patient (e.g. DHOT622BDE5AAY4S) → Laborbefund tab → click a lab Observation row → Human-readable tab. Locate the `valueQuantity` row in the property table. The Value cell must show `<numeric value> <unit>` (e.g. `98 mg/dL`, `7.4 mmol/L`, `120 mm[Hg]`) — NEVER just the unit code, just the system URL, or just the LOINC code. Repeat for at least one additional Observation."
    why_human: "SUMMARY (lines 135-137) explicitly states the doc-level append to 33-HUMAN-UAT.md landed 'on the assumption that the regression-test gate is sufficient' — i.e. the actual live-Blaze visual re-walk of Tests 1 + 6 did NOT occur. Roadmap SC #4 + plan must-have truth #5 require visual confirmation against the live Blaze 1.6.2 + Synthea fingerprint pinned in 38-SESSION.md. The 4 colocated Vitest cases prove the dispatch invariant in jsdom but do not exercise the live render path. Plan Task 3 is a `checkpoint:human-verify gate=blocking` which the SUMMARY itself flags as 'orchestrator-owned' open question."
---

# Phase 38.2: Fix valueQuantity Render Verification Report

**Phase Goal:** Fix the value-rendering defect on Observation rows in the patient detail Laborbefund tab — render `<value> <unit>` (e.g. `98 mg/dL`) when both `valueQuantity.value` and `valueQuantity.unit` are present. Missing-field cases handled correctly. Existing test suite stays green. Phase 33 HUMAN-UAT Tests 1+6 re-walk evidence appended.

**Verified:** 2026-04-29T11:05:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| - | ----- | ------ | -------- |
| 1 | Observation rows render `<value> <unit>` (e.g. `98 mg/dL`) when both `valueQuantity.value` and `valueQuantity.unit` are present | ✓ VERIFIED | `ResourcePropertyTable.tsx:133-134` returns `<Text size="sm">{obj.value as number} {obj.unit as string}</Text>` when `hasValue && hasUnit`. Vitest case "renders BOTH value and unit when valueQuantity has { value: 98, unit: 'mg/dL' }" passes — `screen.getByText(/98\s*mg\/dL/)` resolves. |
| 2 | Missing-field cases handled: value-only → render `value`; unit-only → render em-dash (NEVER bare unit); neither → em-dash | ✓ VERIFIED | `ResourcePropertyTable.tsx:136-141` — `hasValue && !hasUnit` returns `<Text>{obj.value}</Text>`; otherwise returns dimmed em-dash (`—`). Vitest cases for value-only (42), unit-only ('mg/dL' alone — anti-regression), and empty `{}` all pass. |
| 3 | Regression test asserts BOTH `98` AND `mg/dL` render for valueQuantity { value: 98, unit: 'mg/dL' } | ✓ VERIFIED | `ResourcePropertyTable.test.tsx:127-138` — Phase 38.2 describe block, headline test asserts `screen.getByText(/98\s*mg\/dL/)` resolves. Co-located with the implementation file's existing UAT-FU-02 tests. |
| 4 | `npm test` exits 0 (baseline ≥ 1059) | ✓ VERIFIED | `npm test` → 1064 passed, 22 todo, 0 failed across 114 test files (3 skipped). Exceeds Phase 36 baseline of 1059 by +5 (4 new Phase 38.2 cases + 1 prior). |
| 5 | Re-walk of Phase 33 HUMAN-UAT Test 1 + Test 6 against the same Blaze + Synthea fingerprint (38-SESSION.md) confirms inline values render correctly | ? UNCERTAIN — needs human | The 33-HUMAN-UAT.md doc-level append IS present (lines 63-76 for Test 1, lines 192-203 for Test 6, citing commit `9dd7fde`). However, the SUMMARY (lines 135-137) explicitly states the append landed "on the assumption that the regression-test gate is sufficient" without an actual live-Blaze visual walk. Plan Task 3 is a `checkpoint:human-verify gate=blocking`. Cannot be verified programmatically — routes to human verification (see below). |

**Score:** 4/5 truths verified; 1 routes to human verification

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/components/explorer/ResourcePropertyTable.tsx` | Quantity branch in RenderValue handling all four valueQuantity input shapes | ✓ VERIFIED | 24-line block at lines 120-142 ahead of Coding branch (line 145). Contains `isQuantityShape` predicate, branches for `hasValue && hasUnit`, `hasValue` alone, and em-dash fallback. Old single-condition branch removed (`grep -c "if (obj.value !== undefined && obj.unit)"` returns 0). Plan-required substring "valueQuantity" present in commentary. |
| `src/__tests__/ResourcePropertyTable.test.tsx` | Regression test fixture rendering valueQuantity { value: 98, unit: 'mg/dL' } and asserting both fragments appear | ✓ VERIFIED | `describe('Phase 38.2: valueQuantity rendering on Observation', ...)` block at lines 126-189 with 4 cases. Plan-required substring "98 mg/dL" present implicitly via regex `/98\s*mg\/dL/`. All 4 tests pass (`npm test -- ResourcePropertyTable.test.tsx` → 8/8 pass). |
| `.planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md` | Append `→ fixed in Phase 38.2, commit <sha>` evidence on Test 1 + Test 6 rows | ✓ VERIFIED (doc append) | `grep -c "fixed in Phase 38.2"` = 2 (Test 1 line 63, Test 6 line 192). Each annotation cites commit `9dd7fde`, which matches the `fix(38.2):` commit. Phase 38.1 annotations preserved (`fixed in Phase 38.1` count = 3). Original `result: fail` markers preserved (`grep -c "result: fail"` = 8). **Caveat:** the append is correctly formatted per D-03 but the underlying live-Blaze walk it documents was not actually executed (see truth #5 / human verification). |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `MiiModuleTab.tsx` | `ResourceDetailPage.tsx` | `navigate(\`/patients/${patientId}/${r.resourceType}/${r.id}\`)` on Laborbefund row click | ✓ WIRED | `MiiModuleTab.tsx:60` declares `useNavigate()`; line 176 onClick handler calls `navigate(\`/patients/${patientId}/${r.resourceType}/${r.id}\`)`. |
| `ResourceDetailPage.tsx` | `HumanReadableView.tsx` | default 'Human-readable' tab renders HumanReadableView | ✓ WIRED | `ResourceDetailPage.tsx:9` imports `HumanReadableView`; line 186 renders `<HumanReadableView resource={resource} />` in the human-readable tab panel. |
| `HumanReadableView.tsx` | `ResourcePropertyTable.tsx` | ResourcePropertyTable wraps each FHIR field with RenderValue | ✓ WIRED | `HumanReadableView.tsx:33` renders `<ResourcePropertyTable resource={display} />`. |
| `ResourcePropertyTable.tsx` (RenderValue, Quantity branch) | DOM Text element | render `<Text>{obj.value} {obj.unit}</Text>` for Quantity-shaped objects | ✓ WIRED | Line 134: `return <Text size="sm">{obj.value as number} {obj.unit as string}</Text>`. Both fragments interpolated into the same Text node. Branch positioned at line 132 ahead of Coding branch at line 145 (verified via awk line-order check: q=132, c=145, q < c). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `ResourcePropertyTable.tsx` (Quantity branch) | `obj.value`, `obj.unit` | `resource[key]` from prop, where `resource` is the FHIR `Observation` fetched by `ResourceDetailPage` via MedplumClient | ✓ FLOWING (under jsdom) | Render path traced end-to-end. The Vitest cases instantiate real Observation fixtures and assert the rendered DOM contains both fragments. The data flow under live Blaze is gated by truth #5 (human re-walk). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| ResourcePropertyTable test file all green | `npm test -- src/__tests__/ResourcePropertyTable.test.tsx` | 8 passed (1 file) | ✓ PASS |
| Full vitest suite ≥ 1059 passing | `npm test` | 1064 passed, 22 todo, 0 failed (114 files) | ✓ PASS |
| Quantity branch precedes Coding branch | `awk '/typeof obj.unit/{q=NR} /obj.system && obj.code/{c=NR} END{exit !(q && c && q < c)}'` | q=132, c=145, exit 0 | ✓ PASS |
| Old single-condition Quantity branch removed | `grep -c "if (obj.value !== undefined && obj.unit)"` | 0 | ✓ PASS |
| Phase 38.2 describe block present in test file | `grep -c "Phase 38.2: valueQuantity rendering on Observation"` | 1 | ✓ PASS |
| Phase 38.2 fix annotations on Test 1 + Test 6 in 33-HUMAN-UAT.md | `grep -c "fixed in Phase 38.2"` | 2 | ✓ PASS |
| Phase 38.1 annotations preserved | `grep -c "fixed in Phase 38.1"` | 3 | ✓ PASS |
| Original `result: fail` markers preserved (D-03) | `grep -c "result: fail"` | 8 | ✓ PASS |
| Live-Blaze visual re-walk Test 1 + Test 6 | (requires `npm run dev` + browser) | — | ? SKIP (routes to human) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| (none) | 38.2-01-PLAN | Plan declares `requirements: []` (decimal phase, implementation-level fix to UAT-FU-06 follow-up gap; no new REQ-IDs per ROADMAP entry line 258) | N/A | Closes a UAT-FU-06 follow-up rendering gap; does not introduce or claim REQ-IDs. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | No TODO/FIXME/PLACEHOLDER/HACK markers, no empty handlers, no static-only fallbacks introduced by this phase | — | Clean. The fix is a small, focused dispatch reorder + 4 colocated tests. |

### Code Review Findings (from 38.2-REVIEW.md)

The standard-depth code review reported **0 critical, 0 warning, 3 info** findings — all enhancements, not defects:

- **IN-01:** Quantity with `code` but no `unit` falls through to value-only rendering (could fall back to `obj.code` as display unit for UCUM-pure payloads) — enhancement, not blocker.
- **IN-02:** `as number` cast in value-only branch is not type-gated; could tighten `hasValue` to `typeof obj.value === 'number'` — minor type-safety polish.
- **IN-03:** `!obj.coding` truthy-check would mismatch a defensively-empty `coding: []` — pathological, non-conformant payload only.

None of these block the goal. They are deferred refinements consistent with the goal-backward verification finding.

### Human Verification Required

#### 1. Phase 33 HUMAN-UAT Test 1 + Test 6 live-Blaze re-walk (BLOCKING checkpoint per plan)

**Test:** With the same Blaze 1.6.2 + 500-patient Synthea bundle pinned in `.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md`:

1. Start the dev server: `npm run dev` (Vite default port 5173).
2. Navigate to `/patients/DHOT622BDE5AAY4S` (or any Synthea patient that surfaced lab Observations during the Phase 38.1 re-walk).
3. Click the **Laborbefund** tab. Confirm it populates with rows (this validates the Phase 38.1 `_sort=-_lastUpdated` precondition).
4. Click any row whose Summary cell shows a lab test name (e.g. `Glucose [Mass/volume] in Serum or Plasma`).
5. ResourceDetailPage opens on the **Human-readable** tab. Locate the `valueQuantity` row.
6. Repeat steps 3-5 for at least one additional Observation to cover multiple UCUM unit codings.

**Expected:** The Value cell of each `valueQuantity` row shows `<numeric value> <unit>` together (e.g. `98 mg/dL`, `7.4 mmol/L`, `120 mm[Hg]`). It must NOT show:
  - The unit alone (`mg/dL`)
  - The UCUM code alone (`mg/dL` rendered as `<Code>` only)
  - The system URL (`unitsofmeasure.org`)
  - The LOINC code from the `code` field

**Why human:** Visual rendering against a live Blaze server cannot be programmatically verified from this verifier's environment. The plan explicitly designates Task 3 as `<task type="checkpoint:human-verify" gate="blocking">`. The SUMMARY admits the doc-level 33-HUMAN-UAT.md append landed "on the assumption that the regression-test gate is sufficient" without the actual visual walk. The 4 Vitest cases prove dispatcher correctness in jsdom against synthetic fixtures, which is necessary but not sufficient for SC #4 ("Re-walk of Phase 33 HUMAN-UAT Test 1 / Test 6 confirms values render correctly inline against live Blaze + Synthea").

**Resume signal expected by plan:** `approved — re-walk passed and 33-HUMAN-UAT.md updated with commit <sha>`, OR detailed failure description (rendered text, row, Observation ID).

### Gaps Summary

No code-level gaps. The dispatch fix is correct, the 4-shape regression matrix is comprehensive, the test suite is green at 1064/1064 passing (exceeds 1059 baseline by 5), and all key links are wired. The only outstanding item is the Phase 33 HUMAN-UAT live-Blaze re-walk, which the plan itself designates as a blocking human-verify checkpoint and which the SUMMARY admits has not been independently executed. Once the user performs the re-walk and confirms (or refutes) the visual rendering, the phase can flip to `passed` (or surface a new gap).

---

_Verified: 2026-04-29T11:05:00Z_
_Verifier: Claude (gsd-verifier)_
