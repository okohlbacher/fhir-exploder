---
phase: 38-v1.5-human-uat-live-blaze-smoke-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md
  - .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md
autonomous: false
requirements:
  - MII-EXT-04
  - MII-EXT-06
  - MII-EXT-07
  - MII-EXT-08
must_haves:
  truths:
    - "Pre-flight 5-step fail-fast gate executed BEFORE any test walk; all 5 checks pass (or plan halts and the halt is logged)"
    - "11-row per-resource-type counts table captured ONCE at session start with non-zero Patient + Observation counts"
    - "Blaze metadata.software.version snapshot captured for audit reproducibility"
    - "All 6 tests in 33-HUMAN-UAT.md have result: pass OR result: fail (not result: pending) with non-empty evidence: |"
    - "Single browser session preserved: dev server running, Blaze unchanged, Synthea bundle untouched between Plan 38-01 close and Plan 38-02 open"
  artifacts:
    - path: ".planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md"
      provides: "Session-start fingerprint: Blaze URL, metadata.software.version snapshot, ISO-8601 capture timestamp, 11-row counts table"
      min_lines: 25
      contains: "| Resource type | Count |"
    - path: ".planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md"
      provides: "6 tests updated with result: pass | fail + evidence: | block (Phase 23 YAML shape)"
      contains: "result: pass"
  key_links:
    - from: "public/settings.yaml fhir.serverUrl"
      to: "38-SESSION.md Blaze URL header"
      via: "verifier reads settings.yaml in pre-flight task 0 step 5"
      pattern: "serverUrl"
    - from: "Pre-flight counts queries (GET <blazeUrl>/<Type>?_summary=count for 11 types)"
      to: "38-SESSION.md counts table"
      via: "single session-start capture; verifier records the 11 counts"
      pattern: "_summary=count"
    - from: "33-HUMAN-UAT.md test 1..6 entries"
      to: "browser interactions on http://localhost:5173"
      via: "verifier walks each test in numbered order, records result + evidence"
      pattern: "result: (pass|fail)"
---

<objective>
Pre-flight + walk Phase 33's 6 live-Blaze HUMAN-UAT tests in a single browser session. Captures the per-resource-type counts table as the Synthea bundle fingerprint (D-12, D-14) ONCE at session start so Plan 38-02 can reuse it without re-querying. Records pass/fail per test inline in `33-HUMAN-UAT.md` using the Phase 23 `result: pass | fail` + `evidence: |` YAML shape (D-04). Failures route through severity-tiered protocol (D-01): critical → flag for sub-phase 38.1 in Plan 38-03 closure; minor/data-coverage → accepted override in evidence prose.

Purpose: Closes Phase 33 SC5 (live-Blaze UAT clause) by walking the 6 observational tests that programmatic verification cannot exercise: extraQuery URL append against real Synthea Observations, CSS color-token rendering for 4 timeline types, Drawer animation + DevTools network-tab no-fetch invariant, Dashboard scoping label legibility, deep-link auto-expand forward-compat, and per-patient Laborbefund population from live Blaze. These tests are why `33-VERIFICATION.md` is `status: human_needed`.

Output: `38-SESSION.md` (session fingerprint) + 6 updated test entries in `33-HUMAN-UAT.md`. NO `src/` edits. Single browser session; do not restart Blaze, do not re-import Synthea between this plan and Plan 38-02.

This plan runs Wave 1. Not autonomous — Task 1 (the walk) has a `checkpoint:human-verify` gate covering all 6 tests in one human-verifier hand-off (D-11). Task 0 (pre-flight) is automated by Claude using Bash; the gate condition itself is Bash-checkable, not browser-bound.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-CONTEXT.md
@.planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md
@.planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-VERIFICATION.md
@.planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md
@public/settings.yaml

<interfaces>
<!-- Authoritative file shapes the executor must preserve. -->

From `.planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md` (current shape — DO NOT change frontmatter status field; ONLY edit per-test result + evidence):

```yaml
---
status: partial                  # leave as-is; Plan 38-03 owns the verification re-flip, not this plan
phase: 33-mii-schema-foundation-extension-modules-collapse-ui
source: [33-VERIFICATION.md]
started: 2026-04-24T13:45:00Z
updated: 2026-04-24T13:45:00Z   # <-- bump to today's ISO-8601 timestamp on commit
---

### N. <test name>
expected: <unchanged>
result: [pending]                # <-- replace with `pass` or `fail`
                                  # <-- ADD an `evidence: |` block immediately below
                                  #     (Phase 23 shape; multi-line free-text per D-02 + D-04)
```

Phase 23 evidence pattern reference (`23-HUMAN-UAT.md`):

```yaml
result: pass
evidence: |
  Re-run 2026-04-22 against live Blaze (localhost:8080/fhir) ...
  <multi-line prose narrative; counts, observed values, sentence(s) per D-02 (free-text)>
```

Counts-table format (D-14) for `38-SESSION.md`:

```markdown
| Resource type        | Count |
| -------------------- | ----- |
| Patient              | <n>   |
| Condition            | <n>   |
| Observation          | <n>   |
| Encounter            | <n>   |
| Procedure            | <n>   |
| MedicationStatement  | <n>   |
| AllergyIntolerance   | <n>   |
| Consent              | <n>   |
| Immunization         | <n>   |
| DiagnosticReport     | <n>   |
| ServiceRequest       | <n>   |
```

Blaze count-query shape (already used in app per DashboardPage `useResourceCounts`):

```
GET <blazeUrl>/<ResourceType>?_summary=count
→ Bundle with `total: <n>` field
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight fail-fast gate + counts table + version snapshot → 38-SESSION.md</name>
  <files>
    .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md
  </files>
  <read_first>
    - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-CONTEXT.md (D-12, D-13, D-14, D-15)
    - public/settings.yaml (read fhir.serverUrl — this is the Blaze URL captured in 38-SESSION.md)
  </read_first>
  <action>
Per D-15, run a 5-step FAIL-FAST gate before doing anything else. Halt the entire phase BEFORE Task 1 (the walk) if any check fails — log the halt at the top of `38-SESSION.md` and stop. The halt does NOT consume the Task-1 checkpoint (D-15).

**Step 1: Read `public/settings.yaml`** and extract `fhir.serverUrl`. Persist as `BLAZE_URL` in shell context. Expected default: `http://localhost:8080/fhir`. If the file lacks `fhir.serverUrl`, halt with `pre_flight_halt: settings.yaml missing fhir.serverUrl`.

**Step 2: Verify Blaze metadata endpoint returns 200.**

```bash
curl -sf -o /tmp/blaze-metadata.json -w "%{http_code}" "$BLAZE_URL/metadata"
```

If the HTTP status is not 200 OR curl exits non-zero → halt with `pre_flight_halt: <BLAZE_URL>/metadata returned <status>` (D-15 step 1).

**Step 3: Verify Patient count ≥ 1 AND Observation count ≥ 1.**

```bash
curl -sf "$BLAZE_URL/Patient?_summary=count"
curl -sf "$BLAZE_URL/Observation?_summary=count"
```

Each response is a FHIR Bundle JSON; the `total` field carries the count. Use `jq -r .total /tmp/blaze-patient-count.json` (or equivalent) to extract. If either is `0`, missing, or non-numeric → halt with `pre_flight_halt: <Type> count = <value> (need ≥ 1)` (D-15 steps 2 + 3).

**Step 4: Verify dev server reachable.** Run `npm run dev` in the background (or confirm an existing instance) on the port configured by Vite (default 5173). Probe:

```bash
curl -sf -o /dev/null -w "%{http_code}" http://localhost:5173/
```

If the response is not 200, attempt to start the dev server (`npm run dev &` and poll for up to 30 seconds). If still unreachable → halt with `pre_flight_halt: dev server unreachable on http://localhost:5173/` (D-15 step 4).

**Step 5: Confirm Blaze instance identity matches verifier intent.** Pause and ask the user: "About to walk 12 HUMAN-UAT tests against `<BLAZE_URL>` (read from `public/settings.yaml`). Confirm this is the Blaze instance you want? (yes / no)". If `no` → halt with `pre_flight_halt: BLAZE_URL mismatch — verifier expected <other>`. (D-15 step 5; brief in-task confirmation, not a checkpoint task.)

**Step 6: Capture counts table for 11 types** (D-14). For each of `Patient`, `Condition`, `Observation`, `Encounter`, `Procedure`, `MedicationStatement`, `AllergyIntolerance`, `Consent`, `Immunization`, `DiagnosticReport`, `ServiceRequest`, run `curl -sf "$BLAZE_URL/<Type>?_summary=count" | jq -r .total`. A `0` is acceptable for any type EXCEPT Patient + Observation (already gated in Step 3) — Synthea legitimately produces zero Consent / Medikation per `33-01-INVESTIGATION.md`. Record each count.

**Step 7: Capture Blaze software version snapshot** (Claude's Discretion bonus per CONTEXT). From `/tmp/blaze-metadata.json` extract `software.name` + `software.version` (e.g. `jq -r '.software.name + " " + .software.version' /tmp/blaze-metadata.json`). If absent, record `software: unavailable`.

**Step 8: Write `38-SESSION.md`.** Exact shape below — fill in real values, ISO-8601 UTC timestamp, real counts:

```markdown
---
phase: 38-v1.5-human-uat-live-blaze-smoke-tests
captured_by: plan-38-01
captured_at: <ISO-8601 UTC, e.g. 2026-04-28T09:15:00Z>
status: pre_flight_passed
---

# Phase 38 Session Fingerprint

**Source-of-truth for Blaze + Synthea identity across Plans 38-01, 38-02, 38-03.**

Captured ONCE at the start of Plan 38-01 task 0 per CONTEXT D-13 (single-source-of-truth) and D-14 (counts-table format). Plans 38-02 and 38-03 read this file rather than re-querying Blaze.

## Blaze instance

- **URL:** `<BLAZE_URL>` (from `public/settings.yaml` `fhir.serverUrl`)
- **metadata.software:** `<software.name + " " + software.version, or "unavailable">`
- **metadata HTTP status:** `200`
- **Capture timestamp (UTC):** `<ISO-8601>`

## Pre-flight checklist (D-15)

| # | Check | Status |
| - | ----- | ------ |
| 1 | `<BLAZE_URL>/metadata` returns 200 | PASS |
| 2 | `Patient?_summary=count` ≥ 1 (actual: `<n>`) | PASS |
| 3 | `Observation?_summary=count` ≥ 1 (actual: `<n>`) | PASS |
| 4 | Dev server reachable on `http://localhost:5173/` | PASS |
| 5 | `public/settings.yaml` `fhir.serverUrl` confirmed by verifier | PASS |

## Synthea bundle fingerprint (per-resource-type counts)

Source: `GET <BLAZE_URL>/<Type>?_summary=count → Bundle.total`

| Resource type        | Count |
| -------------------- | ----- |
| Patient              | <n>   |
| Condition            | <n>   |
| Observation          | <n>   |
| Encounter            | <n>   |
| Procedure            | <n>   |
| MedicationStatement  | <n>   |
| AllergyIntolerance   | <n>   |
| Consent              | <n>   |
| Immunization         | <n>   |
| DiagnosticReport     | <n>   |
| ServiceRequest       | <n>   |

## Notes

Counts captured ONCE at session start per D-13. Plans 38-02 and 38-03 reuse this fingerprint without re-capture; do not re-import Synthea or restart Blaze between plan executions or this fingerprint becomes invalid (D-09 single-session invariant).
```

If ANY pre-flight check fails (Steps 2, 3, 4, 5), instead of writing the file shape above, write a HALT shape:

```markdown
---
phase: 38-v1.5-human-uat-live-blaze-smoke-tests
captured_by: plan-38-01
captured_at: <ISO-8601 UTC>
status: pre_flight_halted
halt_reason: "<the specific halt message>"
---

# Phase 38 Session — PRE-FLIGHT HALTED

Plan 38-01 stopped before Task 1. Verifier must fix the underlying issue and re-run Plan 38-01.

## Failure detail

<concrete details: which check failed, the observed value, what the verifier needs to fix>
```

Then exit Plan 38-01 with a non-zero exit so the orchestrator does NOT proceed to Task 1. Halt is logged but does NOT consume the Task-1 checkpoint (D-15).

Commit on success with: `docs(38-01): pre-flight passed; capture session fingerprint`. Commit on halt with: `docs(38-01): pre-flight halted — <reason>`.
  </action>
  <verify>
    <automated>test -f .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md && grep -q "captured_by: plan-38-01" .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md && grep -qE "^status: (pre_flight_passed|pre_flight_halted)$" .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md && (grep -q "pre_flight_halted" .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md || (grep -q "| Patient " .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md && grep -q "| Observation " .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md && grep -q "| ServiceRequest " .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md))</automated>
  </verify>
  <acceptance_criteria>
    - `38-SESSION.md` exists with frontmatter `phase: 38-v1.5-human-uat-live-blaze-smoke-tests`, `captured_by: plan-38-01`, ISO-8601 `captured_at`, and `status: pre_flight_passed` OR `status: pre_flight_halted`.
    - On `pre_flight_passed`: file contains the Blaze URL header line, the 5-row pre-flight checklist with all 5 PASS, the 11-row counts table including `Patient` + `Observation` rows with non-zero values, and the `metadata.software` line.
    - On `pre_flight_halted`: file contains a `halt_reason:` frontmatter field and a `## Failure detail` section; Plan 38-01 stops without entering Task 1; the halt does NOT consume Task 1's `checkpoint:human-verify` gate.
    - File is committed to git.
  </acceptance_criteria>
  <done>
    - `38-SESSION.md` written and committed.
    - On halt, Plan 38-01 returns control to orchestrator without entering Task 1.
    - On pass, Task 1 is reachable.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Walk Phase 33's 6 HUMAN-UAT tests in numbered order; record per-test result + evidence in 33-HUMAN-UAT.md</name>
  <files>
    .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md
  </files>
  <read_first>
    - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md (must show `status: pre_flight_passed`)
    - .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md (the 6 tests + their `expected:` clauses)
    - .planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md (Phase 23 `result: pass` + `evidence: |` shape — this is the YAML shape to mirror)
    - .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-01-INVESTIGATION.md (data-coverage rationale: Consent / Medikation may legitimately empty per Synthea)
    - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-CONTEXT.md (D-01 severity-tier protocol, D-04 evidence shape, D-05 net-tab judgment, D-10 numbered order, D-11 single checkpoint)
  </read_first>
  <what-built>
    Phase 33 shipped: helpers in `src/utils/mii-modules.ts`, widened MII module schema, MiiModuleTab Promise.all fan-out, MiiModuleTabs base/extension Collapse partition, ClinicalTimeline `findModuleForType`, DashboardPage Drawer click target with "Server-wide totals" scoping label, and the UAT-FU-06 extraQuery URL fix at `MiiModuleTab.tsx:81`. All 8 must-haves are programmatically VERIFIED in `33-VERIFICATION.md`. The 6 remaining items in `33-HUMAN-UAT.md` require live Blaze + Synthea + browser to confirm visual / behavioral / no-fetch invariants that jsdom cannot exercise (CSS color tokens; Drawer animation; DevTools network-tab observation; live extraQuery rendering).

    Pre-flight (Task 0) confirmed Blaze is up at the URL captured in `38-SESSION.md`, Patient + Observation counts are non-zero, dev server is running on http://localhost:5173/, and the verifier confirmed the Blaze instance identity. Counts table is committed for downstream reuse.
  </what-built>
  <how-to-verify>
    Open Chrome (or your preferred browser) at http://localhost:5173/ — KEEP THIS BROWSER SESSION OPEN through Plan 38-02 (D-09 single-session invariant). Walk the 6 tests in **numbered order 1 → 2 → 3 → 4 → 5 → 6** (D-10). For each test, record `result: pass` or `result: fail` plus a multi-line `evidence: |` block describing what you observed.

    **Test 1 — Synthea Laborbefund extraQuery (no leakage to non-lab Observations).** Navigate to `/patients`, pick any patient with Observations (count from `38-SESSION.md` confirms the resource exists), open the patient, click the **Laborbefund** tab. Expected: only Observations with `category=laboratory` render. Before the plan-33-01 fix the tab leaked ~25% non-lab (social-history, vital-signs, survey). Open DevTools Network to confirm the request URL ends with `&category=laboratory` (or whatever the module's extraQuery is). Evidence: count of entries shown + a sentence on whether all are lab observations + the request URL fragment.

    **Test 2 — Timeline color + German label for all 4 TIMELINE_RESOURCE_TYPES.** Open the **Zeitleiste** tab. Verify entries render with the correct color border-left + colored badge per type:
    - `Condition` → `Diagnose` label, **teal**
    - `Encounter` → `Fall` label, **indigo**
    - `Procedure` → `Prozedur` label, **violet**
    - `Observation` → `Laborbefund` label, **cyan**
    Evidence: a sentence per type confirming the German label + the color you saw, OR the specific mismatch.

    **Test 3 — Dashboard MII heading reads `MII Kerndatensatz · Server-wide totals`.** Navigate to `/dashboard`. Find the MII section. Expected heading text: literally `MII Kerndatensatz · Server-wide totals` (note the middle-dot `·`, not a regular `·` substitute). Visual typography readable. Evidence: quote the exact heading text + a sentence on whether the middle-dot rendered correctly.

    **Test 4 — Dashboard tile click opens Drawer (not navigate); no new fetches (DevTools Network).** On `/dashboard`, open DevTools → Network tab → clear current entries. Click any of the 7 base MII tiles (e.g., Person, Diagnose, Laborbefund). Expected: a right-edge **Drawer** opens (NOT a navigation to `/patients`). Drawer contents must show the German label + FHIR resource type(s) + server-wide count + an "Open in Explorer" button. Click "Open in Explorer" and confirm navigation to `/explorer/<primary-type>` AND the Drawer closes. CRITICAL: the Network tab must show **zero new GETs** between tile click and Drawer open (D-05: trust verifier judgment, no mandatory phrasing). Evidence: which tile you clicked + what opened (Drawer or nav) + a sentence on the Network tab observation + confirmation of the "Open in Explorer" navigation behavior.

    **Test 5 — MiiModuleTabs deep-link `?tab=<extension-key>` auto-expand (forward-compat).** On the same patient as Test 1, the URL still has `/patients/:id`. In Phase 33 NO extension modules exist, so the deep-link auto-expand path is inactive. Visually confirm the base tab row renders correctly: `Person, Fall, Diagnose, Prozedur, Consent, Laborbefund, Medikation` + a `Zeitleiste` tab. The "Show extension modules" toggle / Collapse section MUST NOT be visible (length-guarded — Phase 33 ships zero extension modules). Evidence: list the tabs you saw + confirmation the Extension toggle does not render.

    **Test 6 — Per-patient Laborbefund populated (UAT-FU-06 fix).** On the same patient with non-zero Observations, click the **Laborbefund** tab. Expected: ≥ 1 lab Observation entry renders (NOT empty). Note: other modules (Consent, Medikation) may legitimately render empty per `33-01-INVESTIGATION.md` and counts in `38-SESSION.md` — that is data-coverage reality, not a regression (D-01 data-coverage override path). Evidence: count of lab entries shown + a sentence confirming it is non-empty + a brief mention of any legitimately-empty modules you observed.

    **For each test, write into `33-HUMAN-UAT.md` directly under the test heading.** The `result: [pending]` line becomes `result: pass` or `result: fail`. Immediately below, add an `evidence: |` block (Phase 23 shape — see `23-HUMAN-UAT.md` for the exact YAML form). Free-text per D-02 + D-04. Length scales with the test — pass cases may be 1-3 sentences, fails or net-tab tests may be a paragraph. No mandatory schema, no required phrase (D-05 — trust verifier judgment on net-tab observation).

    **Failure routing (D-01) — record in evidence; sub-phase fires from Plan 38-03 closure, NOT here:**
    - **Critical/major UX defect** (e.g., Drawer doesn't open in Test 4; timeline mis-colored across all 4 types in Test 2; navigate-instead-of-Drawer regression) → `result: fail`, evidence describes the defect concretely. The user should mark `severity: critical` or use clear "blocker" / "regression" language in the evidence so Plan 38-03 closure can detect the trigger and route to `/gsd-insert-phase` for sub-phase 38.1.
    - **Minor/cosmetic** (single label-copy nit, tooltip mis-position) → `result: fail`, evidence states "Accepted as cosmetic; no fix in v1.5" or similar override rationale (D-01 minor path).
    - **Data-coverage gap** (e.g., test asserts something that requires data the Synthea bundle doesn't include) → `result: fail`, evidence cites the data shape + counts from `38-SESSION.md` and states "Accepted as data-coverage reality" (D-01 data-coverage path; mirrors Phase 33 `33-01-INVESTIGATION.md` precedent).

    **Update `updated:` frontmatter** to today's ISO-8601 UTC timestamp. Update the `## Summary` block at the bottom: `passed: <N>`, `pending: <6 - N>` becomes `pending: 0` once all tests have a non-pending result. Increment `issues:` for each `result: fail` row.

    **Do NOT change** the top-frontmatter `status: partial` field in this plan (Plan 38-03 owns the verification re-flip; this plan only updates per-test results). The HUMAN-UAT file's `status` is independent of `33-VERIFICATION.md` `status` and stays as-is here.

    When all 6 tests are recorded, type `approved` to resume the executor; the executor commits the file with message: `docs(38-01): record 6 Phase-33 HUMAN-UAT results (pass: <N>, fail: <M>)`.

    DO NOT close the browser, restart Blaze, or re-import Synthea between this resume and Plan 38-02 starting (D-09 single-session invariant; counts in `38-SESSION.md` must remain valid for Plan 38-02).
  </how-to-verify>
  <action>
See `<how-to-verify>` above. Claude has confirmed pre-flight passed in Task 0 and `38-SESSION.md` is committed. The user runs the 6 browser tests in order, edits `33-HUMAN-UAT.md` per-test, types `approved`, then Claude commits the file.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md && ! grep -qE "^result: \[pending\]$" .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md && [ $(grep -cE "^result: (pass|fail)$" .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md) -eq 6 ] && [ $(grep -cE "^evidence: \|$" .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md) -eq 6 ]</automated>
  </verify>
  <acceptance_criteria>
    - All 6 test entries in `33-HUMAN-UAT.md` have `result: pass` OR `result: fail` (NOT `result: [pending]`).
    - All 6 test entries have an `evidence: |` block immediately following the `result:` line, with non-empty multi-line content.
    - The numbered ordering 1 → 6 matches the source file (D-10).
    - `## Summary` block updated: `pending: 0`, `passed: <N>` reflects actual passes, `issues: <M>` reflects actual fails.
    - Top-frontmatter `updated:` field bumped to today's ISO-8601 UTC timestamp.
    - Top-frontmatter `status:` field UNCHANGED (`status: partial` stays — Plan 38-03 owns the verification re-flip).
    - `38-SESSION.md` from Task 0 has `status: pre_flight_passed` (sanity guard — Task 1 is unreachable on halt).
    - Browser session left running after `approved` so Plan 38-02 inherits the same session (D-09).
    - File committed to git.
  </acceptance_criteria>
  <done>
    - `33-HUMAN-UAT.md` updated with 6 results + 6 evidence blocks.
    - File committed.
    - Single browser session preserved for Plan 38-02 handoff.
  </done>
  <resume-signal>Type "approved" once all 6 tests in 33-HUMAN-UAT.md show result: pass | fail with non-empty evidence: | blocks. If a critical/major UX defect was observed, also call it out in your reply (so Plan 38-03 closure can route to sub-phase 38.1 via /gsd-insert-phase per D-01). Keep the browser session OPEN — Plan 38-02 reuses it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ localhost Blaze | DevTools observation runs against trusted local Blaze; Synthea data is synthetic (no real PHI) |
| verifier ↔ HUMAN-UAT.md | Free-text evidence is authoritative for downstream reviewers; verifier is the trust root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | `33-HUMAN-UAT.md` evidence could be hand-edited to fake a pass | accept | Phase 23 + Phase 38 trust the in-person verifier; rationale: a single-developer GSD workflow has no adversary |
| T-38-02 | Information Disclosure | Synthea bundle counts published in `38-SESSION.md` could leak deployment scale | accept | Synthea is synthetic; `38-SESSION.md` lives only in `.planning/`; no external publication |
| T-38-03 | Spoofing | Verifier could miss-identify Blaze instance | mitigate | Pre-flight Step 5 forces an explicit yes/no confirmation of the URL read from `public/settings.yaml` (D-15 step 5) |
| T-38-04 | Repudiation | A "pass" claim could be retracted later without audit trail | mitigate | Counts table + `metadata.software.version` snapshot in `38-SESSION.md` makes the session reproducible; commit history pins the timestamp |
</threat_model>

<verification>
- `38-SESSION.md` committed with frontmatter `status: pre_flight_passed`, Blaze URL, software version snapshot, ISO-8601 timestamp, and an 11-row counts table with non-zero Patient + Observation rows.
- `33-HUMAN-UAT.md` has 6 entries each carrying `result: pass | fail` (zero `pending`) plus a non-empty `evidence: |` block.
- `## Summary` block shows `pending: 0`.
- `updated:` frontmatter field bumped to today's UTC timestamp.
- Single browser session is preserved for handoff to Plan 38-02 (D-09 invariant — verifier did NOT close the browser, restart Blaze, or re-import Synthea).
</verification>

<success_criteria>
Plan 38-01 closes when:
- Pre-flight passes (or halts cleanly without consuming the checkpoint).
- All 6 Phase-33 tests have a recorded outcome with evidence.
- `38-SESSION.md` is the durable single source of truth for the Synthea fingerprint, available for Plan 38-02 and Plan 38-03 to consume.
</success_criteria>

<output>
After completion, create `.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-01-SUMMARY.md` documenting: pre-flight verdict (passed/halted), Blaze URL + software version captured, the 11-row counts table inlined for quick reference, per-test pass/fail tally for Phase 33's 6 tests, and any critical-severity flags raised for Plan 38-03 sub-phase routing.
</output>
