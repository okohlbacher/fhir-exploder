---
phase: 35
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/explorer/ResourceDetailPage.tsx
  - src/components/explorer/ClinicalRawView.tsx
  - src/__tests__/resource-detail.test.tsx
  - src/__tests__/display-modes.test.tsx
  - src/__tests__/reference-navigation.test.tsx
autonomous: true
requirements: [UAT-FU-03]
user_setup: []

must_haves:
  truths:
    - "ResourceDetailPage renders exactly 2 Tabs.Tab elements (Human-readable + JSON)"
    - "Tab labelled 'Developer' is renamed to 'JSON'; tab labelled 'Clinical + Raw' is gone"
    - "src/components/explorer/ClinicalRawView.tsx no longer exists"
    - "grep -rn 'ClinicalRawView' src/ --include='*.ts' --include='*.tsx' returns 0 hits"
    - "Reference click delegation in remaining 2 panels still works"
    - "Keyboard shortcut '1' still selects human-readable; '2' selects JSON; '3' is a dead key"
    - "npm test green; npx tsc -b --noEmit clean; npm run build clean"
  artifacts:
    - path: "src/components/explorer/ResourceDetailPage.tsx"
      provides: "2-tab Tabs UI with Human-readable + JSON; ClinicalRawView import deleted; '2' key remapped"
      contains: "<Tabs.Tab value=\"developer\">JSON</Tabs.Tab>"
    - path: "src/__tests__/resource-detail.test.tsx"
      provides: "Updated test mocks; Tabs.Tab count assertion = 2; JSON label assertion"
      exports: []
    - path: "src/__tests__/display-modes.test.tsx"
      provides: "describe('ClinicalRawView') block deleted; remaining display-mode tests untouched"
      exports: []
    - path: "src/__tests__/reference-navigation.test.tsx"
      provides: "vi.mock('../components/explorer/ClinicalRawView') block deleted"
      exports: []
  key_links:
    - from: "src/components/explorer/ResourceDetailPage.tsx"
      to: "src/components/explorer/HumanReadableView.tsx"
      via: "import + Tabs.Panel value='human-readable'"
      pattern: "<HumanReadableView resource"
    - from: "src/components/explorer/ResourceDetailPage.tsx"
      to: "src/components/explorer/DeveloperJsonView.tsx"
      via: "import + Tabs.Panel value='developer'"
      pattern: "<DeveloperJsonView resource"
    - from: "div onClick={handleReferenceClick}"
      to: "remaining 2 Tabs.Panel children"
      via: "click delegation wrapper preserved"
      pattern: "onClick=\\{handleReferenceClick\\}"
---

<objective>
Close UAT-FU-03 (Phase 30 UAT follow-up): remove the `Clinical + raw` display mode entirely from `ResourceDetailPage.tsx`, rename the `Developer` tab to `JSON`, delete `ClinicalRawView.tsx`, and clean up every test reference so a `grep -rn 'ClinicalRawView' src/` returns zero hits. Smallest, most mechanical of the four Phase-35 plans — runs Wave 1 to clear the deck before the larger plans.

Purpose: ROADMAP success criterion #3 + REQUIREMENTS UAT-FU-03 + CONTEXT D-10/D-11/D-12. Reduces tab count and dead code; surfaces a cleaner detail-page UX.

Output: 1 file deleted (`ClinicalRawView.tsx`), 4 files modified (ResourceDetailPage + 3 test files), green test suite, clean tsc, clean build.

CRITICAL — Research corrections honored (per RESEARCH P-09 / P-10 / P-11):
- The actual code uses `<Tabs>` with 3 tabs, NOT `<SegmentedControl>` with 4 options. The task is "3 → 2 Tabs.Tab elements" — D-10's "4 → 3" wording is reconciled here.
- D-13 localStorage migration is a NO-OP — `ResourceDetailPage.tsx:51` uses plain `useState`; no `useLocalStorage` for active mode. Migration code MUST NOT be added.
- ClinicalRawView is referenced from 7 grep hits across 4 files. ALL must be cleaned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-CONTEXT.md
@.planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-RESEARCH.md
@.planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VALIDATION.md
@.planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-UI-SPEC.md

@src/components/explorer/ResourceDetailPage.tsx
@src/components/explorer/ClinicalRawView.tsx
@src/__tests__/resource-detail.test.tsx
@src/__tests__/display-modes.test.tsx
@src/__tests__/reference-navigation.test.tsx

<interfaces>
<!-- Key facts the executor needs without exploring the codebase -->

ResourceDetailPage current Tabs structure (verified read at lines 182-203):
```tsx
<Tabs value={activeTab} onChange={setActiveTab}>
  <Tabs.List>
    <Tabs.Tab value="human-readable">Human-readable</Tabs.Tab>
    <Tabs.Tab value="clinical-raw">Clinical + Raw</Tabs.Tab>      // DELETE
    <Tabs.Tab value="developer">Developer</Tabs.Tab>              // RENAME label → "JSON"
  </Tabs.List>
  <div onClick={handleReferenceClick}>                            // PRESERVE wrapper
    <Tabs.Panel value="human-readable" pt="md"><HumanReadableView .../></Tabs.Panel>
    <Tabs.Panel value="clinical-raw" pt="md"><ClinicalRawView .../></Tabs.Panel>   // DELETE
    <Tabs.Panel value="developer" pt="md"><DeveloperJsonView .../></Tabs.Panel>
  </div>
</Tabs>
```

Keyboard handler (verified at lines 78-98):
```tsx
switch (e.key) {
  case '1': setActiveTab('human-readable'); break;
  case '2': setActiveTab('clinical-raw'); break;   // REMAP → setActiveTab('developer')
  case '3': setActiveTab('developer'); break;      // DELETE (no third tab)
}
```

Import to delete (line 10):
```tsx
import { ClinicalRawView } from './ClinicalRawView';
```

Test reference inventory (RESEARCH P-11 — 7 hits across 4 files):
- src/components/explorer/ClinicalRawView.tsx               → DELETE entire file
- src/components/explorer/ResourceDetailPage.tsx:10,196     → DELETE import + Panel
- src/__tests__/resource-detail.test.tsx:49-50              → DELETE vi.mock block
- src/__tests__/display-modes.test.tsx:3,22-40              → DELETE import + entire describe('ClinicalRawView') block (4 trivial assertions)
- src/__tests__/reference-navigation.test.tsx:61-62         → DELETE vi.mock block

Keyboard shortcut decision (UI-SPEC §Interaction Contract #4):
- '1' → human-readable (UNCHANGED)
- '2' → developer (REMAPPED — was clinical-raw → now skips deleted middle tab and lands on JSON)
- '3' → DEAD KEY (delete the case branch)
- URL deep-link `?tab=clinical-raw` → falls back to default human-readable (Mantine Tabs default behavior; no explicit handling needed)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED test — assert post-cleanup tabs invariants</name>
  <files>src/__tests__/resource-detail.test.tsx</files>
  <read_first>
    - src/__tests__/resource-detail.test.tsx (full)
    - src/components/explorer/ResourceDetailPage.tsx (lines 182-205)
    - .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VALIDATION.md (Wave 0 #3)
    - .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-UI-SPEC.md (§Interaction Contract #4 + §Copywriting UAT-FU-03)
  </read_first>
  <behavior>
    Add a new `describe('Tabs cleanup (UAT-FU-03)')` block to `src/__tests__/resource-detail.test.tsx` that codifies the post-cleanup contract. These tests must FAIL initially (RED) — they describe the FUTURE state that Task 2 will make true.

    Test cases (vitest + @testing-library/react):
    1. `it('renders exactly 2 Tabs.Tab elements')` — render `<ResourceDetailPage>` (using existing route + mocked client), wait for resource load, then `expect(screen.getAllByRole('tab')).toHaveLength(2)`.
    2. `it('does NOT render a tab labelled "Clinical + Raw"')` — `expect(screen.queryByRole('tab', { name: /Clinical \+ Raw/ })).toBeNull()`.
    3. `it('renders a tab labelled "JSON" (renamed from "Developer")')` — `expect(screen.getByRole('tab', { name: /^JSON$/ })).toBeInTheDocument()`. Also assert `expect(screen.queryByRole('tab', { name: /^Developer$/ })).toBeNull()`.
    4. `it('default active tab is Human-readable')` — `expect(screen.getByRole('tab', { name: /Human-readable/ })).toHaveAttribute('aria-selected', 'true')`.
    5. `it('keyboard "2" activates the JSON tab (remapped from clinical-raw)')` — fire `keydown` event with `key: '2'`; assert JSON tab becomes active.

    The existing `vi.mock('../components/explorer/ClinicalRawView', ...)` block at lines 49-50 stays in this commit (still needed because the source file still exists). It will be deleted in Task 2.

    Run `npx vitest run src/__tests__/resource-detail.test.tsx` — the 5 new tests in this describe block MUST FAIL (the third tab still renders, the label still says "Developer", the '2' key still maps to clinical-raw).
  </behavior>
  <action>
    Open `src/__tests__/resource-detail.test.tsx`. After the existing `describe(...)` blocks (or at the bottom of the file before the closing brace), append a new `describe('Tabs cleanup (UAT-FU-03)', () => { ... })` block containing the 5 test cases above. Use the existing test-file's render helper / wrapper conventions (do NOT introduce new wrapper utilities).

    For the keyboard test, dispatch the event on `document.body`: `fireEvent.keyDown(document.body, { key: '2' })`. Use `await waitFor(...)` if the tab activation is async.

    Do NOT modify `ResourceDetailPage.tsx` in this task. Do NOT delete the `vi.mock` for ClinicalRawView yet. Do NOT delete `display-modes.test.tsx` describe block yet. Commit the failing tests so the reviewer sees the deliberate RED → GREEN sequence.

    Commit message: `test(35-01): RED tests for ResourceDetailPage tabs cleanup (UAT-FU-03)`
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/resource-detail.test.tsx -t 'Tabs cleanup' 2>&1 | grep -E 'FAIL|fail|×' | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `src/__tests__/resource-detail.test.tsx` contains a new `describe('Tabs cleanup (UAT-FU-03)', ...)` block with exactly 5 `it(...)` tests matching the descriptions above
    - `npx vitest run src/__tests__/resource-detail.test.tsx -t 'Tabs cleanup'` reports at least 3 failing tests (count, "Clinical + Raw" absence, JSON label rename)
    - `git diff HEAD~1 src/__tests__/resource-detail.test.tsx` shows ONLY additions (no deletions)
    - No edits to `ResourceDetailPage.tsx` in this commit
    - `git log -1 --pretty=%B` matches `test(35-01): RED tests for ResourceDetailPage tabs cleanup (UAT-FU-03)`
  </acceptance_criteria>
  <done>RED test commit landed; 3+ tests fail confirming current production behavior is being correctly inverted by the upcoming Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — delete ClinicalRawView + Clinical+raw tab + remap keyboard + clean test references</name>
  <files>
    src/components/explorer/ResourceDetailPage.tsx,
    src/components/explorer/ClinicalRawView.tsx,
    src/__tests__/resource-detail.test.tsx,
    src/__tests__/display-modes.test.tsx,
    src/__tests__/reference-navigation.test.tsx
  </files>
  <read_first>
    - src/components/explorer/ResourceDetailPage.tsx (full — lines 1-208)
    - src/components/explorer/ClinicalRawView.tsx (full — confirms what will be deleted)
    - src/__tests__/resource-detail.test.tsx (lines 49-50 vi.mock block)
    - src/__tests__/display-modes.test.tsx (lines 1-45)
    - src/__tests__/reference-navigation.test.tsx (lines 60-65)
    - .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-RESEARCH.md (§ Pitfalls P-09, P-10, P-11, P-12 — read in full)
  </read_first>
  <behavior>
    Make the RED tests from Task 1 pass GREEN by performing the full UAT-FU-03 cleanup in a single commit. After this commit, `grep -rn 'ClinicalRawView' src/ --include='*.ts' --include='*.tsx'` MUST return 0 hits.
  </behavior>
  <action>
    Perform ALL of the following changes in ONE commit (mechanical, deterministic):

    A. **`src/components/explorer/ResourceDetailPage.tsx`** edits:
       1. DELETE line 10: `import { ClinicalRawView } from './ClinicalRawView';`
       2. In the keyboard handler (lines 83-93), DELETE the `case '2': setActiveTab('clinical-raw'); break;` line entirely AND DELETE the `case '3': setActiveTab('developer'); break;` branch. Then ADD a new `case '2': setActiveTab('developer'); break;` so the remapping is exactly: '1' → human-readable, '2' → developer.
       3. In the Tabs.List (around line 184-186), DELETE the line `<Tabs.Tab value="clinical-raw">Clinical + Raw</Tabs.Tab>`.
       4. Change the line `<Tabs.Tab value="developer">Developer</Tabs.Tab>` to `<Tabs.Tab value="developer">JSON</Tabs.Tab>` (label-only rename — value attribute UNCHANGED to preserve existing query-string contracts).
       5. In the Tabs.Panel block (around line 195-197), DELETE the `<Tabs.Panel value="clinical-raw" pt="md"> ... </Tabs.Panel>` element entirely (the 3 lines wrapping `<ClinicalRawView resource={resource} />`).
       6. PRESERVE the `<div onClick={handleReferenceClick}>` wrapper at line 190 untouched (per RESEARCH P-12 — click delegation must continue to work for the remaining 2 panels).
       7. PRESERVE `useState<string | null>('human-readable')` at line 51 untouched (default mode unchanged per D-12).

    B. **`src/components/explorer/ClinicalRawView.tsx`** — `git rm` the entire file.

    C. **`src/__tests__/resource-detail.test.tsx`** — DELETE the `vi.mock('../components/explorer/ClinicalRawView', ...)` block at lines 49-50 (2 lines including the `}));` closer if separate). The Task-1 `describe('Tabs cleanup (UAT-FU-03)')` block remains and now PASSES.

    D. **`src/__tests__/display-modes.test.tsx`** — DELETE line 3 `import { ClinicalRawView } from '../components/explorer/ClinicalRawView';`. DELETE the entire `describe('ClinicalRawView', () => { ... })` block at lines 22-40 (4 trivial `expect(ClinicalRawView).toBeDefined()` assertions). Leave any other describe blocks in this file untouched.

    E. **`src/__tests__/reference-navigation.test.tsx`** — DELETE the `vi.mock('../components/explorer/ClinicalRawView', () => ({ ClinicalRawView: () => <div data-testid="clinical-raw-view" /> }));` block at lines 61-62 (2 lines).

    F. Run `grep -rn 'ClinicalRawView' src/ --include='*.ts' --include='*.tsx'` — MUST return 0 hits.

    G. Run the test suite + tsc + build:
       - `npx vitest run` — 998+ passing, 0 failing (the 5 Task-1 tests now PASS; other tests unchanged)
       - `npx tsc -b --noEmit` — exit 0
       - `npm run build` — exit 0

    Commit message: `feat(35-01): drop ClinicalRawView + Clinical+Raw tab; rename Developer → JSON (UAT-FU-03)`
  </action>
  <verify>
    <automated>! grep -rn 'ClinicalRawView' src/ --include='*.ts' --include='*.tsx' && npx vitest run src/__tests__/resource-detail.test.tsx src/__tests__/display-modes.test.tsx src/__tests__/reference-navigation.test.tsx && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'ClinicalRawView' src/ --include='*.ts' --include='*.tsx'` returns exit code 1 (no matches) — verified via `! grep -rn ...` chained with `&&`
    - `src/components/explorer/ClinicalRawView.tsx` does not exist (`test ! -f src/components/explorer/ClinicalRawView.tsx` exit 0)
    - `src/components/explorer/ResourceDetailPage.tsx` no longer contains the strings `'clinical-raw'`, `'Clinical + Raw'`, `ClinicalRawView`
    - `src/components/explorer/ResourceDetailPage.tsx` contains exactly one `<Tabs.Tab value="developer">JSON</Tabs.Tab>` and exactly one `<Tabs.Tab value="human-readable">Human-readable</Tabs.Tab>` and NO `<Tabs.Tab value="clinical-raw"...>`
    - `src/components/explorer/ResourceDetailPage.tsx` keyboard switch contains `case '2': setActiveTab('developer');` and does NOT contain `case '3':`
    - `src/components/explorer/ResourceDetailPage.tsx` line 190 `<div onClick={handleReferenceClick}>` is unchanged (verify via diff)
    - `npx vitest run src/__tests__/resource-detail.test.tsx -t 'Tabs cleanup'` reports all 5 tests passing
    - `npx vitest run` reports ≥ 998 passing / 0 failing (preserves Phase 34 D-24 baseline; the 4 deleted display-modes.test stubs reduce count by 4 but the 5 new Task-1 tests add 5 → net +1)
    - `npx tsc -b --noEmit` exits 0
    - `npm run build` exits 0
    - `git log -1 --pretty=%B` matches `feat(35-01): drop ClinicalRawView + Clinical+Raw tab; rename Developer → JSON (UAT-FU-03)`
    - NO new file `localStorage` migration code added (D-13 confirmed no-op per RESEARCH P-10) — `git diff HEAD~2 -- src/` does NOT contain `clinical-raw` mapping/migration logic
  </acceptance_criteria>
  <done>UAT-FU-03 closed: 2 tabs render (Human-readable + JSON), ClinicalRawView fully deleted from source + tests, click-delegation wrapper preserved, keyboard shortcut '2' lands on JSON, test suite + tsc + build all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → keyboard handler | Key events on document; existing branches handle '1'/'2'/'3' only |
| URL `?tab=` query param → Tabs `value` prop | Mantine Tabs accepts unknown values gracefully; no input validation needed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01-01 | Tampering | Reference click delegation in `<div onClick={handleReferenceClick}>` wrapper at ResourceDetailPage.tsx:190 | mitigate | PRESERVE the wrapper unchanged when deleting the middle Tabs.Panel (RESEARCH P-12). Existing `isValidFhirReference` guard at lines 23-25 stays intact. Re-run `reference-navigation.test.tsx` post-change to confirm click delegation still functions for the remaining 2 panels. |
| T-35-01-02 | Tampering | URL deep-link `?tab=clinical-raw` arriving from external bookmark / stale link | accept | Mantine Tabs gracefully falls back to default tab when `value` prop doesn't match any `<Tabs.Tab>`. No error UI; no exposure. UI-SPEC §Interaction Contract #4 explicitly accepts this. |
| T-35-01-03 | Information Disclosure | Removal of `Clinical + Raw` view | accept | The view rendered the same FHIR resource data as the other two views (no unique data); deletion is purely UI-shape. No new attack surface. |
</threat_model>

<verification>
- `grep -rn 'ClinicalRawView' src/ --include='*.ts' --include='*.tsx'` returns 0 hits
- `screen.getAllByRole('tab')` in `ResourceDetailPage` test renders 2 elements
- `screen.getByRole('tab', { name: /^JSON$/ })` resolves; `screen.queryByRole('tab', { name: /Developer/ })` is null
- `npx vitest run` ≥ 998 passing / 0 failing (Phase 34 baseline preserved)
- `npx tsc -b --noEmit` exit 0
- `npm run build` exit 0
- Reference click in remaining panels still navigates within explorer (regression covered by existing `reference-navigation.test.tsx`)
</verification>

<success_criteria>
- `ResourceDetailPage.tsx` renders exactly 2 Tabs.Tab elements (`Human-readable`, `JSON`)
- `ClinicalRawView.tsx` is deleted; grep returns 0 hits across `src/`
- Keyboard `1` → human-readable; `2` → JSON; `3` is unmapped (dead key)
- Default active tab unchanged (`human-readable`)
- `<div onClick={handleReferenceClick}>` wrapper preserved untouched
- All test files cleaned of `ClinicalRawView` mocks/imports/stub assertions
- Test count net change: -4 (display-modes.test stubs) + 5 (Task-1 RED→GREEN tests) = +1 passing
- `npm test` ≥ 998 passing / 0 failing; `tsc -b --noEmit` clean; `npm run build` clean
- D-13 localStorage migration is a NO-OP (no migration code added) per RESEARCH P-10
- Two atomic commits landed (RED then GREEN) per D-26-style review-friendly cadence
</success_criteria>

<output>
After completion, create `.planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-01-SUMMARY.md` with:
- Files modified + deleted (with line counts)
- grep verification command output (0 hits)
- Test count delta (998 baseline → ≥ 999 post)
- Confirmation that D-13 localStorage migration was a confirmed no-op
- Confirmation that click-delegation wrapper preserved (RESEARCH P-12)
- Two commit SHAs (RED + GREEN)
</output>
