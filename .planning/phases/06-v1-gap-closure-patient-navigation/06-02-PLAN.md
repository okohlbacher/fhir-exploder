---
phase: 06-v1-gap-closure-patient-navigation
plan: 02
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [CONN-01, CONN-02, CONN-03, CONN-04, CONN-05]
files_modified:
  - .planning/REQUIREMENTS.md

must_haves:
  truths:
    - "REQUIREMENTS.md shows CONN-01..05 as [x] (checked) in the v1 Requirements list"
    - "REQUIREMENTS.md Traceability table shows CONN-01..05 Status column as 'Complete'"
    - "No other requirement statuses are altered (BRWS-07/PTNT-04/PTNT-05 remain 'Pending' — Plan 06-01 closes them functionally and a separate post-execution reconciliation will flip them, OR if Plan 06-01 has verifiably completed before this plan reaches the reconciliation step, this plan MAY also flip those three; default behavior is CONN-only)"
    - "Coverage totals at the bottom of REQUIREMENTS.md remain accurate (25/25 mapped — unchanged)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate post-v1.0 traceability state for archival"
      contains: "CONN-01.*Complete"
  key_links:
    - from: ".planning/REQUIREMENTS.md v1 Requirements section"
      to: ".planning/REQUIREMENTS.md Traceability table"
      via: "Both reflect same Complete status for CONN-01..05"
      pattern: "CONN-01.*\\[x\\]"
---

<objective>
Reconcile REQUIREMENTS.md traceability for CONN-01..05 which shipped in Phase 1 but were never flipped from "Pending" to "Complete" in either the checkbox list or the traceability table. Called out explicitly in v1.0-MILESTONE-AUDIT.md cross-phase tech debt section.

Purpose: The v1.0 archival audit expects REQUIREMENTS.md to accurately reflect the implementation state before milestone close. Phase 1 delivered all five CONN-* requirements (Foundation & Blaze Connectivity) and the audit confirmed `satisfied` status — the file just wasn't updated. Pure documentation hygiene; no code changes.

Output: Edited `.planning/REQUIREMENTS.md` with consistent Complete status across both the checklist and the traceability table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Current relevant state in REQUIREMENTS.md (lines cited for precision) -->

Lines 12-16 (v1 checklist — CONN section, all currently unchecked):
```md
- [ ] **CONN-01**: User can configure FHIR server URL...
- [ ] **CONN-02**: App reads settings.yaml at startup...
- [ ] **CONN-03**: App fetches and parses the server's CapabilityStatement...
- [ ] **CONN-04**: App displays clear error messages...
- [ ] **CONN-05**: App shows loading indicators during FHIR server requests
```

Lines 83-87 (Traceability table — CONN rows, all currently "Pending"):
```md
| CONN-01 | Phase 1, Phase 6 (reconcile) | Pending |
| CONN-02 | Phase 1, Phase 6 (reconcile) | Pending |
| CONN-03 | Phase 1, Phase 6 (reconcile) | Pending |
| CONN-04 | Phase 1, Phase 6 (reconcile) | Pending |
| CONN-05 | Phase 1, Phase 6 (reconcile) | Pending |
```

Audit evidence (v1.0-MILESTONE-AUDIT.md line 70):
```
| CONN-01..05 | Connection infra | 1 | 01-01..03 | human_needed (approved) | satisfied |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip CONN-01..05 checkboxes and traceability status to Complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — ~116 lines; verify current state of CONN rows in both the checklist and the traceability table)
    - .planning/v1.0-MILESTONE-AUDIT.md (lines 66-82 — Requirements Coverage table and the reconciliation note on line 81)
    - .planning/ROADMAP.md Phase 1 entry (lines 26-41) — confirms all three Phase 1 plans completed with SUMMARY.md
  </read_first>
  <action>
    Perform exactly two edits in `.planning/REQUIREMENTS.md`:

    **Edit A — v1 Requirements checklist (Connection & Configuration section):**

    Replace each of lines 12-16 so that CONN-01 through CONN-05 appear as `[x]`:
    ```md
    - [x] **CONN-01**: User can configure FHIR server URL, auth mode (open/basic/bearer), and credentials in a settings.yaml file
    - [x] **CONN-02**: App reads settings.yaml at startup and connects to the configured FHIR server
    - [x] **CONN-03**: App fetches and parses the server's CapabilityStatement to discover supported resource types, search parameters, and operations
    - [x] **CONN-04**: App displays clear error messages when the FHIR server is unreachable or returns errors
    - [x] **CONN-05**: App shows loading indicators during FHIR server requests
    ```

    Keep the description text byte-for-byte identical — only the `[ ]` → `[x]` marker changes.

    **Edit B — Traceability table (Connection rows):**

    Replace lines 83-87 so each CONN-* row reads `Complete` in the Status column and drops the "(reconcile)" phase annotation that no longer applies:
    ```md
    | CONN-01 | Phase 1 | Complete |
    | CONN-02 | Phase 1 | Complete |
    | CONN-03 | Phase 1 | Complete |
    | CONN-04 | Phase 1 | Complete |
    | CONN-05 | Phase 1 | Complete |
    ```

    Rationale for removing "Phase 6 (reconcile)" from the Phase column: the reconciliation IS this plan; once it runs, the annotation is retrospectively misleading. The audit states CONN-01..05 shipped in Phase 1. Keep the traceability crisp.

    **Update the "Last updated" footer (line 116):**

    Change:
    ```md
    *Last updated: 2026-04-11 after roadmap creation*
    ```
    to:
    ```md
    *Last updated: 2026-04-12 after v1.0 milestone audit — CONN-01..05 traceability reconciled (Phase 06-02)*
    ```

    **Do NOT:**
    - Do NOT touch BRWS-07, PTNT-04, or PTNT-05 in this plan. Plan 06-01 closes their functional gap; a separate post-execution reconciliation step (typically handled by `/gsd-transition` or `/gsd-complete-milestone`) will flip them once Plan 06-01's SUMMARY is signed off. Flipping them here would lie about state if Plan 06-01 hasn't completed.
    - Do NOT touch the v2 Requirements section, Out of Scope table, or Coverage footer totals (still 25/25 — count is unaffected by this status change).
    - Do NOT touch the "Defined:" line at the top (that's the original definition date, not a modification date).
    - Do NOT reformat unrelated whitespace, reorder rows, or change column widths.

    This addresses CONN-01, CONN-02, CONN-03, CONN-04, CONN-05 traceability hygiene.
  </action>
  <verify>
    <automated>grep -E "CONN-0[1-5].*\\[x\\]" .planning/REQUIREMENTS.md | wc -l | tr -d ' ' | grep -q '^5$' && grep -E "\\| CONN-0[1-5] \\| Phase 1 \\| Complete \\|" .planning/REQUIREMENTS.md | wc -l | tr -d ' ' | grep -q '^5$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CONN-0[1-5].*\[x\]" .planning/REQUIREMENTS.md` returns exactly 5 (all five checkboxes flipped)
    - `grep -c "CONN-0[1-5].*Pending" .planning/REQUIREMENTS.md` returns 0 (no stale "Pending" CONN status remains)
    - `grep -c "| CONN-0[1-5] | Phase 1 | Complete |" .planning/REQUIREMENTS.md` returns exactly 5 (traceability rows match expected shape)
    - `grep -c "Phase 6 (reconcile)" .planning/REQUIREMENTS.md` returns 0 (stale annotation removed from CONN rows; BRWS-07/PTNT-04/PTNT-05 may still carry "Phase 6 (gap closure)" — that's a different string and is intentional)
    - `grep -c "BRWS-07.*Pending" .planning/REQUIREMENTS.md` returns 1 (untouched — Plan 06-01's downstream reconciliation flips this separately)
    - `grep -c "25 total" .planning/REQUIREMENTS.md` returns 1 (coverage totals unchanged)
    - File remains valid markdown (no broken table rows; pipe-count consistent across Traceability table)
  </acceptance_criteria>
  <done>Two targeted edits applied. Grep acceptance checks all pass. REQUIREMENTS.md accurately reflects that Phase 1 shipped and CONN-01..05 are Complete. No other requirement statuses altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning doc (`.planning/REQUIREMENTS.md`) → Archive process | File is read by milestone-archival tooling and by humans reviewing v1.0 shipment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Repudiation | Traceability record accuracy | mitigate | Update the "Last updated" footer to cite Phase 06-02 so the change is attributable and timestamped. Audit trail (git commit + SUMMARY.md) provides secondary attribution. |
| T-06-02-02 | Integrity | Accidentally flipping requirements that are NOT yet complete (BRWS-07 / PTNT-04 / PTNT-05) | mitigate | Explicit negative acceptance-criteria guards: `grep -c "BRWS-07.*Pending"` must return 1. Task action explicitly lists requirements NOT to touch. |
| T-06-02-03 | Information Disclosure | None applicable | accept | REQUIREMENTS.md is a repo-internal planning doc; no secrets, no PHI, no PII. Local-only tool. |
| T-06-02-04 | Tampering | Markdown-table malformation breaks downstream tooling (gsd-tools requirement scans) | mitigate | Preserve exact table shape (3 columns, same pipe count). Verification grep includes the full literal row shape `| CONN-0X | Phase 1 | Complete |` to catch column-count regressions. |

**Note:** This is a docs-only plan with a 6-line edit surface. Threat surface is intentionally minimal.
</threat_model>

<verification>
1. `grep -E "CONN-0[1-5]" .planning/REQUIREMENTS.md` — 10 matches expected (5 checklist + 5 traceability), all showing `[x]` or `Complete`.
2. `grep -E "^\\|" .planning/REQUIREMENTS.md | awk -F'|' '{print NF}' | sort -u` — column count consistent (table integrity preserved).
3. `git diff .planning/REQUIREMENTS.md` — diff shows exactly 6 line modifications (5 checkboxes + 5 table rows + 1 footer = 11; 11 is the real expected number if each changes in one line). Should NOT show changes outside the CONN block and the footer.
</verification>

<success_criteria>
- CONN-01..05 reflected as Complete in both views within REQUIREMENTS.md
- All other rows untouched (spot-check: BRWS-07 still shows Pending with "Phase 6 (gap closure)" annotation)
- Markdown remains valid — no broken tables, no stray characters
- Footer dates Phase 6-02 as the reconciliation driver
- Diff is minimal and reviewable in < 30 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/06-v1-gap-closure-patient-navigation/06-02-SUMMARY.md`.

Summary must include:
- One-line description of the reconciliation
- Line count of the final diff
- Explicit list: CONN-01, CONN-02, CONN-03, CONN-04, CONN-05 flipped to Complete
- Explicit note: BRWS-07, PTNT-04, PTNT-05 INTENTIONALLY NOT flipped in this plan (Plan 06-01 closes the functional gap; checkbox flip happens at `/gsd-transition` or `/gsd-complete-milestone` time once the executor's BF-1 fix is signed off)
- Reference to v1.0-MILESTONE-AUDIT.md as the source of this hygiene requirement
</output>
