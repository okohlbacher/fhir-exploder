---
phase: 60-capabilitystatement-driven-reverse-reference-discovery
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - src/components/explorer/IncomingReferencesPanel.tsx
  - src/components/explorer/__tests__/IncomingReferencesPanel.test.tsx
autonomous: true
requirements: [REVR-04]

must_haves:
  truths:
    - "IncomingReferencesPanel reads dynamicCatalog from useConnectionContext() when connected"
    - "Panel shows the union of dynamic + curated entries for the viewed resource type, deduplicated by `${type}:${param}`"
    - "When the connection is not connected (no dynamicCatalog), the panel renders the curated catalog entries unchanged"
    - "When a resource type has no dynamic entries, the panel falls back to that type's curated entries with no toast or banner"
    - "Dynamic-discovered entries with extra reference types appear as additional cards alongside curated ones"
  artifacts:
    - path: "src/components/explorer/IncomingReferencesPanel.tsx"
      provides: "dynamic-catalog read + per-type curated fallback merge"
      contains: "useConnectionContext"
    - path: "src/components/explorer/__tests__/IncomingReferencesPanel.test.tsx"
      provides: "dynamic-render test (4b) + curated-fallback test (4c) + useConnectionContext mock for existing tests"
      contains: "useConnectionContext"
  key_links:
    - from: "src/components/explorer/IncomingReferencesPanel.tsx"
      to: "src/contexts/ConnectionContext.tsx"
      via: "useConnectionContext() to read state.dynamicCatalog"
      pattern: "useConnectionContext\\(\\)"
    - from: "src/components/explorer/IncomingReferencesPanel.tsx"
      to: "src/utils/reverseReferenceCatalog.ts"
      via: "curated fallback import"
      pattern: "reverseReferenceCatalog\\[resource.resourceType\\]"
---

<objective>
Thread the dynamic catalog from connected state into `IncomingReferencesPanel`, merging it with the curated catalog per-type and deduplicating by the established `${type}:${param}` composite key, then prove dynamic-render (4b) and silent curated-fallback (4c) with tests, and confirm the full build is clean.

Purpose: REVR-04 integration half (D-05, D-07, D-08, D-09). The panel must populate from what the server advertises while transparently falling back to the curated catalog when the dynamic catalog is empty for a type or the connection is not connected — with zero user-visible signal distinguishing the two sources (UI-SPEC INV-3, INV-4).

Output:
- `IncomingReferencesPanel.tsx` reads `state.dynamicCatalog`, merges with curated, dedups
- Extended `IncomingReferencesPanel.test.tsx` with 4b + 4c and a `useConnectionContext` mock that keeps the existing 4 tests green via the curated path
- Full `npm run build` clean
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-capabilitystatement-driven-reverse-reference-discovery/60-CONTEXT.md
@.planning/phases/60-capabilitystatement-driven-reverse-reference-discovery/60-RESEARCH.md
@.planning/phases/60-capabilitystatement-driven-reverse-reference-discovery/60-UI-SPEC.md
@.planning/phases/60-capabilitystatement-driven-reverse-reference-discovery/60-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase + plan 60-01 output. -->

Current src/components/explorer/IncomingReferencesPanel.tsx (the line to replace is the entries lookup, currently line 17):
```typescript
import type { Resource } from '@medplum/fhirtypes';
import { reverseReferenceCatalog } from '../../utils/reverseReferenceCatalog';
import { RelatedResourcesPanel } from './RelatedResourcesPanel';

export function IncomingReferencesPanel({ resource }: IncomingReferencesPanelProps) {
  const entries = reverseReferenceCatalog[resource.resourceType] ?? [];
  if (entries.length === 0) return null;
  if (!resource.id) return null;
  const refValue = `${resource.resourceType}/${resource.id}`;
  return (
    <RelatedResourcesPanel
      title="Referenced By"
      entries={entries}
      refValue={refValue}
      onCardNavigate={(e) => `/explorer/${e.type}?${e.param}=${refValue}`}
    />
  );
}
```

From src/contexts/ConnectionContext.tsx (after plan 60-01 — connected state now carries dynamicCatalog):
```typescript
export function useConnectionContext(): ConnectionContextValue; // { state, connect, disconnect }
// state.status === 'connected' → state.dynamicCatalog: ReverseReferenceCatalog
```

From src/components/explorer/RelatedResourcesPanel.tsx (the dedup key to reuse exactly):
```typescript
const entryKey = (e: ReverseReferenceEntry) => `${e.type}:${e.param}`;
```
RelatedResourcesPanel renders cards; line 144 already guards `{e.icon && <Text>{e.icon}</Text>}` so dynamic entries without icons render with no leading glyph and no layout shift (UI-SPEC INV-5). RelatedResourcesPanel is UNCHANGED by this plan.

From src/utils/reverseReferenceCatalog.ts:
```typescript
export type ReverseReferenceCatalog = Partial<Record<ResourceType, readonly ReverseReferenceEntry[]>>;
export interface ReverseReferenceEntry { type: ResourceType; param: string; icon?: string; }
```
</interfaces>

<ui_invariants>
<!-- From 60-UI-SPEC.md — the panel's visual output must be pixel-identical. -->
- INV-1: Same SimpleGrid + Card anatomy + "Referenced By" Title order={5}. RelatedResourcesPanel untouched.
- INV-3: Fallback is invisible — NO toast, banner, badge, or copy difference between dynamic vs curated rendering.
- INV-4: Merge/dedup by `${type}:${param}`; only the SET of cards may grow on rich CapabilityStatements.
- INV-5: Dynamic entries have no emoji icon — render label with no leading glyph, no placeholder icon, no layout shift.
</ui_invariants>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Merge dynamic + curated catalogs in IncomingReferencesPanel</name>
  <files>src/components/explorer/IncomingReferencesPanel.tsx</files>
  <read_first>
    - src/components/explorer/IncomingReferencesPanel.tsx (current entries lookup at line 17 to replace)
    - src/contexts/ConnectionContext.tsx (useConnectionContext signature + connected state with dynamicCatalog from 60-01)
    - src/components/explorer/RelatedResourcesPanel.tsx (the exact `entryKey = (e) => \`${e.type}:${e.param}\`` dedup pattern to mirror, and the icon guard on line 144)
    - src/utils/reverseReferenceCatalog.ts (curated catalog kept as fallback — ReverseReferenceEntry/ReverseReferenceCatalog types)
    - .planning/phases/60-capabilitystatement-driven-reverse-reference-discovery/60-RESEARCH.md (Finding 5 integration reference + Finding 8 dedup)
    - .planning/phases/60-capabilitystatement-driven-reverse-reference-discovery/60-UI-SPEC.md (INV-1, INV-3, INV-4, INV-5)
  </read_first>
  <behavior>
    - When `state.status === 'connected'` and `state.dynamicCatalog[resourceType]` has entries, the panel passes those dynamic entries to RelatedResourcesPanel first.
    - Curated entries for the same type whose `${type}:${param}` key is NOT already present in the dynamic entries are appended after the dynamic entries.
    - A (type, param) pair present in BOTH catalogs appears exactly once (dedup by the composite key).
    - When `state.status !== 'connected'`, dynamicCatalog is treated as `{}`, so the merged entries equal the curated entries for that type (curated fallback).
    - When dynamicCatalog has no entry for the viewed resource type, the merged entries equal that type's curated entries (per-type fallback).
    - When the merged entries array is empty, the component still returns null (unchanged guard). When `resource.id` is missing, returns null (unchanged guard).
  </behavior>
  <action>
    Modify `src/components/explorer/IncomingReferencesPanel.tsx`. Keep the existing `reverseReferenceCatalog` import and the `RelatedResourcesPanel` import. Add `import { useConnectionContext } from '../../contexts/ConnectionContext';`.

    Inside the component, before the entries computation: `const { state } = useConnectionContext();` and `const dynamicCatalog = state.status === 'connected' ? state.dynamicCatalog : {};`.

    Replace the current single line `const entries = reverseReferenceCatalog[resource.resourceType] ?? [];` with a per-type merge (D-07, RESEARCH Finding 5/8): `const dynamicEntries = dynamicCatalog[resource.resourceType] ?? [];` and `const curatedEntries = reverseReferenceCatalog[resource.resourceType] ?? [];`. Build `const seen = new Set(dynamicEntries.map((e) => \`${e.type}:${e.param}\`));` then `const entries = [...dynamicEntries, ...curatedEntries.filter((e) => !seen.has(\`${e.type}:${e.param}\`))];`. Use the EXACT `\`${e.type}:${e.param}\`` key string so it matches RelatedResourcesPanel's `entryKey` (INV-4) — do not extract a shared helper (RESEARCH Finding 8: 3 lines, single consumer, no over-abstraction).

    Keep `if (entries.length === 0) return null;` and `if (!resource.id) return null;` exactly as-is (order unchanged). Keep the `refValue` and the `<RelatedResourcesPanel title="Referenced By" entries={entries} refValue={refValue} onCardNavigate={...} />` JSX unchanged — only the `entries` value is now the merged array. Do NOT add any toast, banner, loading spinner, or visual marker for the fallback path (INV-3). Do NOT modify RelatedResourcesPanel.tsx or reverseReferenceCatalog.ts. Do NOT remove the curated import — it is the fallback (RESEARCH Finding 10 pitfall 3).
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/explorer/IncomingReferencesPanel.tsx` contains `useConnectionContext` and `state.status === 'connected' ? state.dynamicCatalog : {}`
    - File contains `reverseReferenceCatalog[resource.resourceType]` (curated kept as fallback)
    - File contains a dedup using a `${e.type}:${e.param}` key string and a `Set` (`grep -c "new Set" src/components/explorer/IncomingReferencesPanel.tsx` returns >= 1)
    - File still contains `if (entries.length === 0) return null;` and `if (!resource.id) return null;`
    - File still contains `title="Referenced By"` and does NOT contain any `notifications.show`, `Alert`, or `Banner` usage (no fallback signal)
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>IncomingReferencesPanel reads dynamicCatalog from context, merges it with the curated catalog per type using the `${type}:${param}` dedup key, falls back to curated entries transparently when no dynamic entries exist, and tsc -b --noEmit is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dynamic-render (4b) + curated-fallback (4c) tests and useConnectionContext mock; verify full build</name>
  <files>src/components/explorer/__tests__/IncomingReferencesPanel.test.tsx</files>
  <read_first>
    - src/components/explorer/__tests__/IncomingReferencesPanel.test.tsx (existing 4 tests + the vi.mock for react-router-dom and @medplum/react-hooks + the wrap() helper to reuse)
    - src/components/explorer/IncomingReferencesPanel.tsx (post-Task-1 — now calls useConnectionContext)
    - src/contexts/ConnectionContext.tsx (useConnectionContext shape: { state, connect, disconnect }; connected state with dynamicCatalog)
    - src/utils/reverseReferenceCatalog.ts (curated Observation entries used as the fallback assertion baseline)
    - .planning/phases/60-capabilitystatement-driven-reverse-reference-discovery/60-RESEARCH.md (Finding 7 test strategy 4b/4c, Finding 10 pitfall 5 — existing tests need a useConnectionContext mock)
  </read_first>
  <behavior>
    - Existing 4 tests still pass: the panel now calls useConnectionContext, so a module mock must supply a value or they throw "must be used within a ConnectionProvider".
    - 4b (dynamic render): with a mocked connected state whose dynamicCatalog has an Observation entry NOT in the curated catalog (e.g. `{ type: 'CarePlan', param: 'observation' }` — or any type+param absent from curated Observation), navigating an Observation resource shows a card for that dynamic-only source type.
    - 4c (curated fallback): with a mocked non-connected state (status 'idle' → dynamicCatalog `{}`), navigating an Observation resource still shows the curated DiagnosticReport card.
  </behavior>
  <action>
    Extend `src/components/explorer/__tests__/IncomingReferencesPanel.test.tsx`. Add a `vi.mock('../../../contexts/ConnectionContext', ...)` whose `useConnectionContext` returns a mutable value backed by a module-level `let mockConnectionState` variable (pattern: the factory returns `{ useConnectionContext: () => ({ state: mockConnectionState, connect: vi.fn(), disconnect: vi.fn() }) }`). Initialize `mockConnectionState` to a connected state with an EMPTY dynamicCatalog: `{ status: 'connected', client: stableClient as never, capability: {} as never, dynamicCatalog: {} }`. Reset it in `beforeEach` to that empty-catalog connected state so the existing 4 tests render via the curated path unchanged (this is the Pitfall-5 mock requirement — RESEARCH Finding 10).

    Keep all existing tests and existing mocks (react-router-dom, @medplum/react-hooks, ResizeObserver/matchMedia polyfills, wrap(), mockGet/mockNavigate) intact. The `mockGet` must keep returning a Bundle with `total > 0` for cards to populate (reuse the existing `Promise.resolve({ resourceType: 'Bundle', total: 5 })` pattern).

    Add test 4b "renders dynamic-catalog entry not present in curated catalog": set `mockConnectionState` to `{ status: 'connected', client: stableClient as never, capability: {} as never, dynamicCatalog: { Observation: [{ type: 'CarePlan', param: 'observation' }] } }` (choose a type+param pair NOT in the curated Observation list — confirm against reverseReferenceCatalog.Observation which is DiagnosticReport/Observation/Provenance). Mock `mockGet` to resolve `total: 5` for any URL. Render `<IncomingReferencesPanel resource={observation} />`, await "Referenced By", and assert `screen.getByText('CarePlan')` is present (the dynamic-only card) — proving success criterion 2 / 4b. Optionally also assert a curated card (e.g. 'DiagnosticReport') is present, confirming the union.

    Add test 4c "falls back to curated catalog when not connected (silent)": set `mockConnectionState = { status: 'idle' }`. Mock `mockGet` to resolve `total: 5`. Render the Observation panel, await "Referenced By", and assert `screen.getByText('DiagnosticReport')` (a curated Observation entry) is present, AND assert there is no error/alert text (e.g. `expect(screen.queryByRole('alert')).toBeNull()` and `expect(screen.queryByText(/error|unavailable/i)).toBeNull()`) — proving the fallback is transparent (INV-3 / success criterion 3).
  </action>
  <verify>
    <automated>npx vitest run src/components/explorer/__tests__/IncomingReferencesPanel.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/explorer/__tests__/IncomingReferencesPanel.test.tsx` contains `vi.mock('../../../contexts/ConnectionContext'` and a `mockConnectionState` variable reset in `beforeEach`
    - The file contains a test asserting a dynamic-only source-type card (e.g. `screen.getByText('CarePlan')`) renders for an Observation (4b)
    - The file contains a test that sets `status: 'idle'` and asserts a curated card (`DiagnosticReport`) renders with no alert/error text (4c)
    - All 6 tests (4 existing + 4b + 4c) pass: `npx vitest run src/components/explorer/__tests__/IncomingReferencesPanel.test.tsx` exits 0
    - The 4 pre-existing test names ("type not in catalog", "Observation renders cards from catalog", "missing resource.id", "card click on Observation panel navigates", "structural equivalence") remain present and unmodified in intent
    - `npm run build` exits 0 (tsc -b + vite build clean)
  </acceptance_criteria>
  <done>Tests 4b (dynamic render) and 4c (silent curated fallback) pass; the useConnectionContext mock keeps all existing IncomingReferencesPanel tests green; the full production build (npm run build) is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connected state → panel render | The dynamicCatalog (server-derived, already allowlist-filtered in 60-01) flows into the panel and produces navigable card URLs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04 | Tampering | Merged entries producing card-navigate URLs `/explorer/${e.type}?${e.param}=${refValue}` | mitigate | Dynamic entry `type` values are constrained by 60-01's COMMON_REFERENCE_TARGETS allowlist; `param` comes from the server's own searchParam names. URLs are in-app router paths (no external navigation). refValue is `${resourceType}/${id}` built from the resource already loaded. No raw HTML injection — values pass through React text/href, not dangerouslySetInnerHTML. |
| T-60-05 | Information Disclosure | Fallback path leaking which catalog source was used | accept | By design the fallback is silent (INV-3) — no signal distinguishes dynamic vs curated. No sensitive data either way (only resource-type + param names). |
</threat_model>

<verification>
- `npx vitest run src/components/explorer/__tests__/IncomingReferencesPanel.test.tsx` — 4b + 4c + 4 existing tests all green
- `npm run build` — exit 0 (tsc -b + vite build)
- `npx tsc -b --noEmit` — exit 0 (panel merge compiles)
- `grep -c "notifications.show\|<Alert\|Banner" src/components/explorer/IncomingReferencesPanel.tsx` returns 0 (silent fallback, INV-3)
</verification>

<success_criteria>
- `IncomingReferencesPanel` populates from the union of dynamic + curated entries deduped by `${type}:${param}`; dynamic-discovered extra reference types appear as additional cards (success criterion 2 / 4b)
- When the CapabilityStatement is unavailable (not connected) or a type has no dynamic entries, the panel falls back to the curated catalog transparently — no toast or banner (success criterion 3 / 4c)
- All IncomingReferencesPanel tests pass (existing 4 + 4b + 4c); `npx tsc -b --noEmit` exit 0; `npm run build` clean (success criterion 5)
</success_criteria>

<output>
After completion, create `.planning/phases/60-capabilitystatement-driven-reverse-reference-discovery/60-02-SUMMARY.md`
</output>
