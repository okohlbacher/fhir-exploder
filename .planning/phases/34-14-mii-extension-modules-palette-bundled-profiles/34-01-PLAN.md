---
phase: 34
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/color-design-audit.md
autonomous: true
requirements:
  - MII-EXT-09
  - MII-EXT-10
  - MII-EXT-11
must_haves:
  truths:
    - "color-design-audit.md exists with 14 module rows and 7 palette contrast-audit tables"
    - "Every extension module has a confirmed package version, canonical URL, fhirResourceType array, patientSearchParamOverrides map (possibly empty), badgeColor family assignment, Tabler icon name, German + FHIR label"
    - "Every new palette family has shade-6 hex + contrast ratios ≥ 4.5:1 (text-on-fill) and ≥ 3:1 (UI chrome) — failing palettes tweaked and re-measured"
    - "3 deuteranopia screenshots committed showing every adjacent 21-module pair discriminable by icon+color combination"
    - "Plan 34-02 can read seed hex strings from this doc and paste them into src/theme.ts with zero further research"
  artifacts:
    - path: ".planning/research/color-design-audit.md"
      provides: "Per-module spec table + WCAG AA audit + deuteranopia screenshots + icon picks with rationale"
      min_lines: 200
    - path: ".planning/research/color-design-audit/deuteranopia-dashboard.png"
      provides: "Screenshot 1: /dashboard with 21 MII tiles under deuteranopia emulation"
    - path: ".planning/research/color-design-audit/deuteranopia-tab-row.png"
      provides: "Screenshot 2: /patients/:id tab row showing 7 base + 14 extension tabs under deuteranopia emulation"
    - path: ".planning/research/color-design-audit/deuteranopia-timeline.png"
      provides: "Screenshot 3: ClinicalTimeline with one entry per module under deuteranopia emulation"
  key_links:
    - from: ".planning/research/color-design-audit.md §palette"
      to: "src/theme.ts (Plan 34-02 MantineColorsTuple seed hexes)"
      via: "planner copies 7 × 10 hex arrays verbatim into theme.ts"
      pattern: "const (oncology|imaging|genetics|pathology|bioanalysis|administration|patientReported): MantineColorsTuple"
    - from: ".planning/research/color-design-audit.md §per-module spec"
      to: "src/utils/mii-modules.ts (Plan 34-04 14 module entries)"
      via: "per-module row → one MII_MODULES entry with badgeColor + icon + patientSearchParamOverrides copied verbatim"
      pattern: "key: '(bildgebung|biobank|dokument|intensivmedizin|kardiologie|molekulargenetik|mtb|onkologie|pathologie|pro|seltene|studie|symptom|mikrobiologie)'"
---

<objective>
Produce the per-module research + palette + icon + WCAG + deuteranopia audit at `.planning/research/color-design-audit.md` that unblocks Plans 34-02 through 34-05.

Purpose: Phase 34 ships 14 MII extension module entries with palette + icons + patientSearchParamOverrides. The data for these entries (canonical URLs, current package versions, per-type search params, Mantine palette hexes, Tabler icon names, WCAG contrast ratios, deuteranopia discriminability) is NOT trivially derivable at plan-execution time — it must be researched against live MII IG pages + `packages.fhir.org` + Mantine Colors Generator + Chrome DevTools emulation BEFORE any code lands. This plan produces the audit doc that every downstream plan references.

Output: `.planning/research/color-design-audit.md` (committed; ROADMAP-locked path per CONTEXT D-02) + 3 deuteranopia screenshots under `.planning/research/color-design-audit/`. Zero code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-CONTEXT.md
@.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-RESEARCH.md
@.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-UI-SPEC.md
@.planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-01-INVESTIGATION.md
@src/utils/mii-modules.ts
@src/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create color-design-audit.md skeleton + per-module spec table (14 rows)</name>
  <files>.planning/research/color-design-audit.md</files>
  <read_first>
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-CONTEXT.md (D-01..D-09 — module ordering, palette family mapping, icon picks)
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-RESEARCH.md §Canonical Refs (14-package table with versions + canonical URLs from STACK.md 2026-04-23 probe)
    - .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-01-INVESTIGATION.md (Phase 33 live-Blaze patient probe pattern for `patient=` vs `subject=` verification)
    - src/utils/mii-modules.ts (current MiiModule interface + 7 base module shape — reference for 14 extension rows)
  </read_first>
  <action>
Create `.planning/research/color-design-audit.md` with this exact top-level structure (sections in order):

```markdown
# Phase 34 — Color, Icon, and Per-Module Spec Audit

**Authored:** 2026-04-24 (Plan 34-01)
**Status:** Required reading for Plans 34-02 (theme + schema) through 34-05 (empty-state UX). Frozen for Phase 34; revisions require a new audit committed under a versioned filename.
**Scope:** 14 MII extension modules — per-module IG + search-param verification; 7 palette families with WCAG AA audit; 21 Tabler icon assignments; deuteranopia discriminability pass.

## 1. Per-Module Spec Table (14 rows × 10 columns)

| # | Module key (D-01 alpha) | German label | Canonical IG URL | Package name | Version (verified YYYY-MM-DD) | Pre-GA? | Primary FHIR type | Secondary types (array) | patientSearchParamOverrides |
|---|------------------------|--------------|------------------|---------------|------------------------------|---------|-------------------|-------------------------|------------------------------|
| 1 | bildgebung | Bildgebung | https://www.medizininformatik-initiative.de/fhir/ext/modul-bildgebung | de.medizininformatikinitiative.kerndatensatz.bildgebung | 2026.0.0 | no | ImagingStudy | ['DiagnosticReport'] | {} |
| 2 | biobank | Biobank | ... | de.medizininformatikinitiative.kerndatensatz.biobank | 2026.0.1 | no | Specimen | [] | { Specimen: 'subject' } |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |

(14 rows total — alphabetical-by-German-label order per D-01: bildgebung, biobank, dokument, intensivmedizin, kardiologie, mikrobiologie, molekulargenetik, mtb, onkologie, pathologie, pro, seltene, studie, symptom)

## 2. Palette Assignment + Icon Picks (21 total)

### 2a. Base 7 (LOCKED — D-08)

| Module key | badgeColor | Icon (Tabler) |
|------------|-----------|----------------|
| person | blue | IconUser |
| fall | indigo | IconBedFlat |
| diagnose | teal | IconStethoscope |
| prozedur | violet | IconMedicalCross |
| consent | pink | IconFileSignature |
| laborbefund | cyan | IconFlask |
| medikation | orange | IconPill |

### 2b. Extension 14 (D-05 clinical-semantic grouping)

| Module key | badgeColor (palette family) | Icon (Tabler) | Icon rationale (1 sentence) |
|------------|-----------------------------|----------------|------------------------------|
| onkologie | oncology | IconRadioactive | Cancer / radiation therapy association |
| mtb | oncology | IconUsersGroup | Tumor board = multidisciplinary meeting |
| bildgebung | imaging | IconPhoto | Imaging = photo capture |
| studie | imaging | IconClipboardData | Study = data collection clipboard |
| molekulargenetik | genetics | IconDna | Genetics = DNA strand |
| seltene | genetics | IconPuzzle | Rare disease = "puzzle" diagnostic challenge |
| pathologie | pathology | IconMicroscope | Histology = microscope |
| mikrobiologie | pathology | IconBacteria | Microbe = bacteria |
| biobank | bioanalysis | IconTestPipe | Biobank = specimen test tubes |
| intensivmedizin | bioanalysis | IconBedFilled | ICU = filled bed |
| kardiologie | administration | IconHeartbeat | Cardio = heartbeat waveform |
| dokument | administration | IconFileDescription | Document = annotated file |
| symptom | patient-reported | IconMoodSmile | Symptom = self-reported mood marker |
| pro | patient-reported | IconQuestionnaire | PRO = patient questionnaire |

## 3. WCAG AA Contrast Audit (§wcag)

### 3a. Seed hexes (from Mantine Colors Generator)

| Palette family | Seed hex (input to generator) | Shade 6 (rendered) | Shade 1 (hover wash) |
|----------------|-------------------------------|---------------------|------------------------|
| oncology | #e03131 | {measured hex} | {measured hex} |
| imaging | #0c8599 | {measured hex} | {measured hex} |
| genetics | #8e4ec6 | {measured hex} | {measured hex} |
| pathology | #6741d9 | {measured hex} | {measured hex} |
| bioanalysis | #099268 | {measured hex} | {measured hex} |
| administration | #364fc7 | {measured hex} | {measured hex} |
| patient-reported | #c2255c | {measured hex} | {measured hex} |

### 3b. Contrast ratios

| Palette family | White text on shade-6 fill (≥ 4.5:1 required) | Shade-6 fill on #ffffff card (≥ 3:1 required) | Shade-6 fill on #e7ecff active-pill bg (≥ 3:1 required) | PASS/FAIL |
|----------------|----------------------------------------------|-----------------------------------------------|---------------------------------------------------------|-----------|
| oncology | {n.nn}:1 | {n.nn}:1 | {n.nn}:1 | PASS/FAIL |
| ... (7 rows total) | ... | ... | ... | ... |

### 3c. Failing palette tweaks (if any)

(Document any palette whose seed was darkened/lightened to pass. Format: "bioanalysis: initial seed #099268 → shade 6 contrast 4.2:1 (FAIL on white-on-fill); re-seeded to #087050 → shade 6 contrast 4.8:1 (PASS).")

## 4. Deuteranopia Discriminability Pass (§deuteranopia)

### 4a. Test setup
- Browser: Chrome {version}
- DevTools: Rendering > Emulate vision deficiencies → `deuteranopia`
- Fixture: `.planning/research/color-design-audit/deuteranopia-fixtures.json` (synthetic timeline with 1 entry per module; drive via manual localStorage seed or a throwaway component render harness).

### 4b. Screenshots (committed under `.planning/research/color-design-audit/`)
- `deuteranopia-dashboard.png` — /dashboard with 21 MII tiles
- `deuteranopia-tab-row.png` — /patients/:id tab row with 7 base + 14 extension tabs
- `deuteranopia-timeline.png` — /patients/:id timeline with 21-entry synthetic fixture

### 4c. Adjacent-pair check

| Palette family | Module pair | Icon A | Icon B | Discriminable? |
|----------------|-------------|---------|---------|-----------------|
| oncology | onkologie + mtb | IconRadioactive | IconUsersGroup | ✅ |
| imaging | bildgebung + studie | IconPhoto | IconClipboardData | ✅ |
| genetics | molekulargenetik + seltene | IconDna | IconPuzzle | ✅ |
| pathology | pathologie + mikrobiologie | IconMicroscope | IconBacteria | ✅ |
| bioanalysis | biobank + intensivmedizin | IconTestPipe | IconBedFilled | ✅ |
| administration | kardiologie + dokument | IconHeartbeat | IconFileDescription | ✅ |
| patient-reported | symptom + pro | IconMoodSmile | IconQuestionnaire | ✅ |

### 4d. Cross-family collisions (if any)

(Record any adjacent tabs/tiles/timeline-entries across different palette families that appear perceptually identical under deuteranopia. Document the icon distinctiveness that prevents collapse.)

## 5. Provenance
- IG URLs + package versions probed: `npm view <package> version` (run 2026-04-24)
- Mantine Colors Generator: https://mantine.dev/colors-generator/
- Contrast ratios measured via: `npx wcag-contrast <fg> <bg>` (one-off; NOT a runtime dep)
- Deuteranopia emulation: Chrome DevTools Rendering panel (manual)
- Downstream plans: 34-02 (theme), 34-04 (module entries), 34-05 (empty-state UX)
```

Fill in actual values for:

**Section 1 (per-module table):** Use the exact 14 canonical URLs from 34-RESEARCH.md §Canonical Refs (lines 1001-1014 of that file — Onkologie = https://www.medizininformatik-initiative.de/fhir/ext/modul-onko, etc.). Run `npm view <package-name> version` for each of the 14 packages to probe current (2026-04-24) versions — flag `PRE-GA` in the Pre-GA column when the version string matches `PRE_GA_PATTERN = /-(alpha|beta|rc|ballot|draft)/i`. For each `(module, type)` pair, decide `patientSearchParamOverrides` based on R4 spec defaults (per 34-RESEARCH.md Pitfall P-02 — Specimen has no `patient` param, use `subject`; ResearchStudy has no per-patient param, use `enrollment`; DocumentReference uses `patient`). If you can probe a live Blaze server during audit, record empirical results; otherwise record R4-spec-derived defaults with a NOTE. Known exceptions from 34-RESEARCH.md Plan 34-04 §Per-type search param research: Biobank→Specimen `subject`, Studie→ResearchStudy `enrollment`, ImagingStudy both work (prefer `patient`), DocumentReference both work (prefer `patient`).

**Section 2b (extension icon picks):** Use the 14 recommendations from 34-UI-SPEC.md Icons table (lines 251-264 of that file) — these are the locked picks. Fallback candidates are noted in the UI-SPEC; use them only if a primary icon proves non-existent when verified against https://tabler.io/icons/icon/{name} (e.g., if `IconHeartbeat` doesn't exist use `IconHeart`).

**Section 3a + 3b (WCAG):** Use the K-02 PLACEHOLDER seed hexes from 34-RESEARCH.md (lines 1065-1071 of that file): oncology=#e03131, imaging=#0c8599, genetics=#8e4ec6, pathology=#6741d9, bioanalysis=#099268, administration=#364fc7, patient-reported=#c2255c. Paste each seed into https://mantine.dev/colors-generator/ and capture the 10-shade output. Record shade 6 (used at all three render sites per D-07) and shade 1 (hover wash). Measure 3 contrast ratios per palette (21 ratios total) using `npx wcag-contrast <fg> <bg>` — a 4.5:1 floor for text-on-fill and 3:1 floor for UI chrome per D-06. If any palette fails, darken the seed (`#e03131` → `#c92a2a` family) until shade-6 passes and document the tweak in section 3c.

**Section 4 (deuteranopia):** For this task, pre-populate sections 4a (setup), 4b (screenshot filenames), 4c (adjacent-pair table with expected PASS marks), and 4d (skeleton). The actual screenshot capture happens in Task 2.

Commit the audit doc (no screenshots yet — those land in Task 2).
  </action>
  <verify>
    <automated>test -f .planning/research/color-design-audit.md && grep -c '^## ' .planning/research/color-design-audit.md | grep -qE '^[4-9]$|^[0-9]{2,}$' && grep -qE '^\| [0-9]+ \| (bildgebung|biobank|dokument|intensivmedizin|kardiologie|mikrobiologie|molekulargenetik|mtb|onkologie|pathologie|pro|seltene|studie|symptom) \|' .planning/research/color-design-audit.md && [ $(grep -cE '^\| [0-9]+ \|' .planning/research/color-design-audit.md) -ge 14 ]</automated>
  </verify>
  <done>
    - File `.planning/research/color-design-audit.md` exists with ≥ 4 top-level `##` headings
    - Section 1 table has ≥ 14 rows, each with all 10 columns populated (pipe-delimited row count ≥ 14)
    - Every one of the 14 extension module keys (bildgebung, biobank, ... symptom) appears in a row at column 2
    - Section 2a (base 7) + 2b (extension 14) icon tables populated
    - Section 3a seed-hex table has 7 palette rows with shade-6 hex + shade-1 hex measured from Mantine Colors Generator
    - Section 3b contrast ratios has 7 palette rows with 3 ratios each (21 ratios total); any FAIL has a 3c tweak documented
    - Section 4 skeleton present (screenshots land in Task 2)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deuteranopia screenshot capture (human-verify — cannot automate browser emulation)</name>
  <files>
    .planning/research/color-design-audit/deuteranopia-dashboard.png
    .planning/research/color-design-audit/deuteranopia-tab-row.png
    .planning/research/color-design-audit/deuteranopia-timeline.png
    .planning/research/color-design-audit.md
  </files>
  <read_first>
    - .planning/research/color-design-audit.md §4 (deuteranopia setup + expected adjacent-pair table)
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-CONTEXT.md (D-09 — deuteranopia pass criterion)
  </read_first>
  <what-built>
    The Task 1 audit doc defines Section 4 with: expected Chrome DevTools setup, 3 screenshot filenames, and the 7-family adjacent-pair discriminability table. Because Phase 34 has no live code yet (14 modules haven't been added), the deuteranopia capture must happen against either (a) a pre-merge dev branch that applies the 14 module entries + icons as a throwaway local-only patch to enable the screenshot, OR (b) a dedicated fixture render harness. Claude cannot automate Chrome DevTools' Emulate vision deficiencies panel — it is a human-only browser action.
  </what-built>
  <how-to-verify>
    Execute this sequence:

    1. Apply a local throwaway stash that adds the 14 extension module entries + 21 icon references (copy the extension row shape from the future Plan 34-04 reference — a minimal `{ key, germanLabel, fhirResourceType: 'Condition' (placeholder), category: 'extension', badgeColor: 'oncology', icon: 'IconRadioactive', patientSearchParam: 'patient' }` skeleton per module; real values land in 34-04). This stash is NOT committed — it is discard-after-capture.
    2. Run `npm run dev` and open http://localhost:5173 in Chrome.
    3. Open Chrome DevTools > three-dot menu > More tools > Rendering.
    4. In the Rendering panel: Emulate vision deficiencies → `deuteranopia`.
    5. Navigate to `/dashboard`, wait for the MII tile grid to settle, capture full-viewport screenshot → save as `.planning/research/color-design-audit/deuteranopia-dashboard.png`.
    6. Navigate to `/patients/<any-synthetic-patient-id>`, click the "Extension modules (14)" toggle to expose the extension tab row, capture screenshot → save as `.planning/research/color-design-audit/deuteranopia-tab-row.png`.
    7. Navigate to `/patients/<patient-id>/timeline` (or click the Zeitleiste tab), capture screenshot → save as `.planning/research/color-design-audit/deuteranopia-timeline.png`.
    8. Revert the throwaway stash (`git stash drop` or `git restore`).
    9. Update section 4c of `color-design-audit.md`: for each of the 7 palette families, set the `Discriminable?` column to ✅ or ❌ based on what you see in the screenshots. If any pair is NOT discriminable, swap its icon assignment in Section 2b (use the fallback candidates from 34-UI-SPEC.md) until every pair is discriminable.
    10. Populate Section 4d (cross-family collisions) with any adjacent tab/tile pairs that look perceptually identical under deuteranopia.
    11. Commit the 3 PNG files + the updated audit doc.

    Expected outcome: every pair in 4c is ✅, 0 cross-family collisions in 4d, 3 PNGs exist on disk, audit doc committed with all sections populated.
  </how-to-verify>
  <resume-signal>Type "approved" once the 3 screenshots are committed + section 4c shows every pair ✅ + section 4d confirms no cross-family collisions (or documents the icon swaps that resolved them). If any failure blocks deuteranopia pass, describe the remaining discriminability issue and which icon swap you propose.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No network egress from the Phase 34 browser bundle. The audit doc + screenshots are local artifacts only. `npm view` runs against npm's public registry (no PHI, no auth).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Information Disclosure | deuteranopia screenshots | accept | Screenshots are of synthetic fixtures, no real patient data; captured on local dev server. |
| T-34-02 | Tampering | `npm view` output (package version probe) | accept | `npm view` hits public npm registry over HTTPS; no registry substitution likely at audit time; pinned versions in Plan 34-03 script re-verify. |
</threat_model>

<verification>
- `.planning/research/color-design-audit.md` is committed and contains 14 module rows with every column populated
- 3 deuteranopia screenshots are committed under `.planning/research/color-design-audit/`
- Every palette family's WCAG contrast table PASSes (or tweaks documented)
- Every adjacent-pair in section 4c is discriminable
</verification>

<success_criteria>
Plan 34-02 can read shade-6 hex arrays from section 3 of this doc; plan 34-04 can read per-module `(patientSearchParamOverrides, badgeColor, icon)` triples from sections 1 + 2b; no executor in downstream plans needs to consult external MII IG pages or the Mantine Colors Generator.
</success_criteria>

<output>
After completion, create `.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-01-SUMMARY.md` documenting: audit doc location, screenshot filenames, WCAG pass/fail counts, deuteranopia pass count, any icon swaps made during Task 2.
</output>
