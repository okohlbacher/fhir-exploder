---
phase: 03-patient-centric-browsing-mii-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/mii-modules.ts
  - src/components/patients/PatientsLayout.tsx
  - src/components/patients/PatientListPage.tsx
  - src/hooks/useBreadcrumbTrail.ts
  - src/App.tsx
  - src/__tests__/mii-modules.test.ts
  - src/__tests__/patient-list.test.tsx
autonomous: true
requirements: [PTNT-01]
user_setup: []

must_haves:
  truths:
    - "User can navigate to /patients from sidebar and see a patient list page"
    - "User can search patients by name, identifier, and birthDate"
    - "Patient list displays name, birthDate, gender, identifier columns"
    - "Clicking a patient row navigates to /patients/:patientId"
    - "Breadcrumb hook supports both /explorer/ and /patients/ base paths"
  artifacts:
    - path: "src/utils/mii-modules.ts"
      provides: "MII Kerndatensatz module configuration"
      exports: ["MiiModule", "MII_MODULES"]
    - path: "src/components/patients/PatientsLayout.tsx"
      provides: "Connection-gated patient route layout"
      exports: ["PatientsLayout", "PatientsOutletContext"]
    - path: "src/components/patients/PatientListPage.tsx"
      provides: "Patient search and list page"
      exports: ["PatientListPage"]
    - path: "src/App.tsx"
      provides: "Updated routes with /patients nested routes"
      contains: "PatientsLayout"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/patients/PatientsLayout.tsx"
      via: "Route element"
      pattern: "<Route.*PatientsLayout"
    - from: "src/components/patients/PatientsLayout.tsx"
      to: "src/contexts/ConnectionContext.tsx"
      via: "useConnection hook"
      pattern: "useConnection"
    - from: "src/components/patients/PatientListPage.tsx"
      to: "@medplum/react SearchControl"
      via: "SearchControl component"
      pattern: "SearchControl"
---

<objective>
Create the foundation for patient-centric browsing: MII module configuration, patient route layout with connection gating, patient list page with search, and refactored breadcrumb hook to support patient context URLs.

Purpose: Establishes routing, layout, and the patient list entry point (PTNT-01, D-01, D-02) so that Plan 02 can build the patient detail page on top.
Output: Working /patients route with searchable patient list, MII module config reusable across tabs and timeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-patient-centric-browsing-mii-modules/03-CONTEXT.md
@.planning/phases/03-patient-centric-browsing-mii-modules/03-RESEARCH.md
@.planning/phases/03-patient-centric-browsing-mii-modules/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/explorer/ExplorerLayout.tsx:
```typescript
export type ExplorerOutletContext = {
  capability: CapabilityStatement;
  client: MedplumClient;
};

export function ExplorerLayout() {
  const { state } = useConnection();
  // gates on state.status !== 'connected', wraps in MedplumProvider
}
```

From src/hooks/useBreadcrumbTrail.ts:
```typescript
export interface BreadcrumbEntry {
  resourceType: string;
  id: string;
  label?: string;
}

export function useBreadcrumbTrail() {
  // Currently hardcodes `/explorer/` prefix in push() and navigateTo()
  // Lines 24 and 33: navigate(`/explorer/${entry.resourceType}/${entry.id}`)
}
```

From src/hooks/useConnection.ts (via ConnectionContext):
```typescript
type ConnectionContextValue = {
  state: ConnectionState;
  connect: (settings: AppSettings) => Promise<void>;
  disconnect: () => void;
};
```

From src/App.tsx current route structure:
```typescript
<Route path="/explorer" element={<ExplorerLayout />}>
  <Route index element={<ResourceTypeLanding />} />
  <Route path=":resourceType" element={<SearchResultsPage />} />
  <Route path=":resourceType/:id" element={<ResourceDetailPage />} />
</Route>
<Route path="/patients" element={<PatientsPage />} /> // placeholder
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MII module configuration and PatientsLayout with routing</name>
  <files>src/utils/mii-modules.ts, src/components/patients/PatientsLayout.tsx, src/hooks/useBreadcrumbTrail.ts, src/App.tsx, src/__tests__/mii-modules.test.ts</files>
  <read_first>
    - src/components/explorer/ExplorerLayout.tsx (pattern to replicate for PatientsLayout)
    - src/hooks/useBreadcrumbTrail.ts (current implementation to refactor)
    - src/App.tsx (current route structure to extend)
    - src/contexts/ConnectionContext.tsx (useConnection import path)
    - .planning/phases/03-patient-centric-browsing-mii-modules/03-RESEARCH.md (Pattern 1, Pattern 4)
    - .planning/phases/03-patient-centric-browsing-mii-modules/03-UI-SPEC.md (MII Module Color Map)
  </read_first>
  <behavior>
    - Test: MII_MODULES array has exactly 6 entries with keys diagnose, prozedur, laborbefund, medikation, fall, consent
    - Test: Each MII module has germanLabel, fhirResourceType, badgeColor, patientSearchParam fields
    - Test: MII_MODULES[0] is { key: 'diagnose', germanLabel: 'Diagnose', fhirResourceType: 'Condition', badgeColor: 'teal', patientSearchParam: 'patient' }
    - Test: MII_MODULES Medikation entry has fhirResourceType 'MedicationStatement'
  </behavior>
  <action>
    1. Create `src/utils/mii-modules.ts`:
       - Export `MiiModule` interface with fields: `key: string`, `germanLabel: string`, `fhirResourceType: string`, `badgeColor: string`, `patientSearchParam: string`
       - Export `MII_MODULES: MiiModule[]` constant with exactly 6 entries per D-05 and UI-SPEC color map:
         - `{ key: 'diagnose', germanLabel: 'Diagnose', fhirResourceType: 'Condition', badgeColor: 'teal', patientSearchParam: 'patient' }`
         - `{ key: 'prozedur', germanLabel: 'Prozedur', fhirResourceType: 'Procedure', badgeColor: 'violet', patientSearchParam: 'patient' }`
         - `{ key: 'laborbefund', germanLabel: 'Laborbefund', fhirResourceType: 'Observation', badgeColor: 'cyan', patientSearchParam: 'patient' }`
         - `{ key: 'medikation', germanLabel: 'Medikation', fhirResourceType: 'MedicationStatement', badgeColor: 'orange', patientSearchParam: 'patient' }`
         - `{ key: 'fall', germanLabel: 'Fall', fhirResourceType: 'Encounter', badgeColor: 'indigo', patientSearchParam: 'patient' }`
         - `{ key: 'consent', germanLabel: 'Consent', fhirResourceType: 'Consent', badgeColor: 'pink', patientSearchParam: 'patient' }`

    2. Create `src/components/patients/PatientsLayout.tsx`:
       - Mirror `ExplorerLayout.tsx` exactly:
         - Import `useConnection` from `../../hooks/useConnection`
         - Import `MedplumProvider` from `@medplum/react`
         - Export type `PatientsOutletContext = { capability: CapabilityStatement; client: MedplumClient }`
         - Export function `PatientsLayout()` that:
           - Calls `useConnection()` to get state
           - If `state.status !== 'connected'`, renders Alert with `IconPlugConnectedX`, title "Not connected", link to `/` dashboard (same pattern as ExplorerLayout)
           - Otherwise renders `<MedplumProvider medplum={state.client}>` wrapping `<Outlet context={{ capability: state.capability, client: state.client } satisfies PatientsOutletContext} />`

    3. Refactor `src/hooks/useBreadcrumbTrail.ts`:
       - Add `basePath` parameter to `useBreadcrumbTrail(basePath: string = '/explorer')` with default `/explorer` for backward compatibility
       - Update `push` callback: `navigate(\`${basePath}/${entry.resourceType}/${entry.id}\`)`
       - Update `navigateTo` callback: `navigate(\`${basePath}/${entry.resourceType}/${entry.id}\`)`
       - Existing Phase 2 callers continue to work without changes (default param)

    4. Update `src/App.tsx`:
       - Import `PatientsLayout` from `./components/patients/PatientsLayout`
       - Import `PatientListPage` from `./components/patients/PatientListPage` (created in Task 2)
       - Import `PatientDetailPage` from `./components/patients/PatientDetailPage` (created in Plan 02)
       - Import `ResourceDetailPage` already imported
       - Replace the placeholder `<Route path="/patients" element={<PatientsPage />} />` with nested routes:
         ```
         <Route path="/patients" element={<PatientsLayout />}>
           <Route index element={<PatientListPage />} />
         </Route>
         ```
       - Remove the `PatientsPage` placeholder function
       - NOTE: Additional nested routes (`:patientId`, `:patientId/:resourceType/:resourceId`) will be added in Plan 02

    5. Create `src/__tests__/mii-modules.test.ts`:
       - Test MII_MODULES has 6 entries
       - Test each entry has all required fields (key, germanLabel, fhirResourceType, badgeColor, patientSearchParam)
       - Test specific entries match expected values (diagnose -> Condition/teal, medikation -> MedicationStatement/orange)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/mii-modules.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/mii-modules.ts exports `MiiModule` interface and `MII_MODULES` array with 6 entries
    - src/utils/mii-modules.ts contains `germanLabel: 'Diagnose'` and `fhirResourceType: 'Condition'`
    - src/utils/mii-modules.ts contains `germanLabel: 'Medikation'` and `fhirResourceType: 'MedicationStatement'`
    - src/components/patients/PatientsLayout.tsx contains `export type PatientsOutletContext`
    - src/components/patients/PatientsLayout.tsx contains `useConnection()` and `MedplumProvider`
    - src/hooks/useBreadcrumbTrail.ts contains `basePath: string = '/explorer'` parameter
    - src/hooks/useBreadcrumbTrail.ts contains `${basePath}/` in navigate calls (not hardcoded `/explorer/`)
    - src/App.tsx contains `<Route path="/patients" element={<PatientsLayout />}>`
    - src/App.tsx does NOT contain the `function PatientsPage()` placeholder
    - npx vitest run src/__tests__/mii-modules.test.ts exits 0
    - npx vitest run src/__tests__/search-state.test.ts exits 0 (breadcrumb refactor didn't break existing tests)
  </acceptance_criteria>
  <done>MII module config created with 6 modules matching D-05/UI-SPEC. PatientsLayout mirrors ExplorerLayout. Breadcrumb hook parameterized for both explorer and patient contexts. App routes updated with nested /patients routes.</done>
</task>

<task type="auto">
  <name>Task 2: Patient list page with search and SearchControl table</name>
  <files>src/components/patients/PatientListPage.tsx, src/__tests__/patient-list.test.tsx</files>
  <read_first>
    - src/components/explorer/SearchResultsPage.tsx (SearchControl usage pattern)
    - src/components/explorer/ExplorerLayout.tsx (ExplorerOutletContext pattern to replicate for PatientsOutletContext)
    - src/components/patients/PatientsLayout.tsx (created in Task 1, for PatientsOutletContext type)
    - .planning/phases/03-patient-centric-browsing-mii-modules/03-UI-SPEC.md (Patient List Page layout, copy, states)
    - .planning/phases/03-patient-centric-browsing-mii-modules/03-RESEARCH.md (Pattern 3: Patient-Scoped SearchControl)
  </read_first>
  <action>
    Create `src/components/patients/PatientListPage.tsx` per D-01, D-02, and UI-SPEC:

    1. Component structure:
       - Use `useOutletContext<PatientsOutletContext>()` to get capability and client
       - Use `useNavigate()` for patient row click navigation
       - State: `searchParams` object with `name: string`, `identifier: string`, `birthDate: string` (all initially empty)
       - State: `bundle: Bundle | undefined` for tracking loaded results
       - State: `searchTriggered: boolean` (false initially -- shows empty state until user searches or clicks "Search Patients" with no filters to list all)

    2. Search section (per UI-SPEC Patient List Page Structure):
       - Title: `<Title order={2}>Patients</Title>`
       - Three search fields in a `<Group>`:
         - `<TextInput label="Name" placeholder="Search by name..." value={searchParams.name} onChange={...} />`
         - `<TextInput label="Identifier" placeholder="Search by identifier..." value={searchParams.identifier} onChange={...} />`
         - `<TextInput label="Birth Date" placeholder="YYYY-MM-DD" value={searchParams.birthDate} onChange={...} />`
       - `<Button onClick={handleSearch}>Search Patients</Button>`
       - All three fields and the button should submit on Enter key (onKeyDown handler on Group or each input)

    3. Build SearchRequest from search params:
       ```typescript
       const searchRequest: SearchRequest = {
         resourceType: 'Patient',
         filters: [
           ...(searchParams.name ? [{ code: 'name', operator: 'eq' as const, value: searchParams.name }] : []),
           ...(searchParams.identifier ? [{ code: 'identifier', operator: 'eq' as const, value: searchParams.identifier }] : []),
           ...(searchParams.birthDate ? [{ code: 'birthdate', operator: 'eq' as const, value: searchParams.birthDate }] : []),
         ],
         fields: ['name', 'birthDate', 'gender', 'identifier'],
         count: 20,
       };
       ```

    4. Results section:
       - If not yet searched: show empty state per UI-SPEC:
         - `<Title order={3}>Browse Patients</Title>`
         - `<Text>Use the search fields above to find patients, or click Search with no filters to list all patients.</Text>`
       - If searched:
         - Result count: `<Text size="sm" c="dimmed">Showing {start}-{end} of {total}</Text>` (derive from bundle.total and current page)
         - `<SearchControl search={searchRequest} hideToolbar={true} hideFilters={true} onClick={(e) => navigate(\`/patients/${e.resource.id}\`)} onLoad={(e) => setBundle(e.response)} />`
         - Pagination using `<PaginationControls>` from `../explorer/PaginationControls` (reuse Phase 2 component) if bundle has next/prev links

    5. Error handling:
       - Wrap SearchControl errors: `<Alert color="red" title="Search failed">Search failed: Could not load patients. Check your server connection and try again.</Alert>`

    6. Create `src/__tests__/patient-list.test.tsx`:
       - Test that PatientListPage renders title "Patients"
       - Test that search fields render with correct labels (Name, Identifier, Birth Date)
       - Test that "Search Patients" button exists
       - Test that empty state shows "Browse Patients" heading before any search
       - Mock MedplumClient and PatientsOutletContext for rendering tests
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/patient-list.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/patients/PatientListPage.tsx contains `<Title order={2}>Patients</Title>`
    - src/components/patients/PatientListPage.tsx contains `label="Name"` and `placeholder="Search by name..."`
    - src/components/patients/PatientListPage.tsx contains `label="Identifier"` and `placeholder="Search by identifier..."`
    - src/components/patients/PatientListPage.tsx contains `label="Birth Date"` and `placeholder="YYYY-MM-DD"`
    - src/components/patients/PatientListPage.tsx contains `Search Patients` button text
    - src/components/patients/PatientListPage.tsx contains `resourceType: 'Patient'` in search request
    - src/components/patients/PatientListPage.tsx contains `navigate(\`/patients/${` for row click navigation
    - src/components/patients/PatientListPage.tsx contains `Browse Patients` empty state heading
    - src/components/patients/PatientListPage.tsx contains `SearchControl` import from `@medplum/react`
    - npx vitest run src/__tests__/patient-list.test.tsx exits 0
    - npx vitest run --reporter=verbose exits 0 (full suite green)
  </acceptance_criteria>
  <done>Patient list page renders with search by name/identifier/birthDate, SearchControl table with correct columns, row click navigation to /patients/:id, empty state and error handling per UI-SPEC copy. Per D-01, D-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User search input -> FHIR query | Patient name/identifier/birthDate search values constructed into FHIR search parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | PatientListPage search params | mitigate | Use SearchControl which handles FHIR parameter encoding internally; custom filter values passed through SearchRequest.filters which @medplum/core encodes safely |
| T-03-02 | Information Disclosure | Patient list display | accept | Local-only app connecting to local FHIR server; no cross-user data exposure risk. User has full server access by design. |
</threat_model>

<verification>
- `npx vitest run --reporter=verbose` passes (all tests green)
- `/patients` route renders patient list page (manual check)
- Search by name returns filtered patient results
- Clicking a patient row navigates to `/patients/:id` (will show blank until Plan 02)
</verification>

<success_criteria>
- /patients route accessible from sidebar, renders PatientListPage
- Patient search by name, identifier, birthDate works via SearchControl
- MII module config exported and tested
- Breadcrumb hook supports configurable base path
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-patient-centric-browsing-mii-modules/03-01-SUMMARY.md`
</output>
