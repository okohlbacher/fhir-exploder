---
phase: 01-foundation-blaze-connectivity
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/fhir/capability.ts
  - src/utils/fhir-categories.ts
  - src/hooks/useResourceCounts.ts
  - src/components/dashboard/ResourceTypeList.tsx
  - src/components/dashboard/ResourceTypeGroup.tsx
  - src/components/dashboard/ResourceTypeRow.tsx
  - src/components/dashboard/DashboardPage.tsx
  - src/components/dashboard/MedplumCompatGate.tsx
autonomous: false
requirements:
  - CONN-03
  - CONN-05

must_haves:
  truths:
    - "After connecting, user sees resource types grouped by FHIR category (Clinical, Diagnostics, Foundation, etc.)"
    - "Each resource type shows its name, count, number of search parameters, and number of operations"
    - "Resource counts load lazily with per-type spinners that resolve to count badges"
    - "Resource type names are clickable links (navigate to /explorer/{type})"
    - "Empty server shows 'No Resource Types Found' message"
    - "A Medplum React component successfully renders a FHIR resource fetched from Blaze (compatibility gate)"
  artifacts:
    - path: "src/fhir/capability.ts"
      provides: "CapabilityStatement parsing into ParsedResourceType[]"
      exports: ["parseResourceTypes", "ParsedResourceType"]
    - path: "src/utils/fhir-categories.ts"
      provides: "FHIR resource type to category mapping"
      exports: ["getResourceCategory", "groupByCategory"]
    - path: "src/hooks/useResourceCounts.ts"
      provides: "Lazy-loaded resource counts with concurrency limit"
      exports: ["useResourceCounts"]
    - path: "src/components/dashboard/ResourceTypeList.tsx"
      provides: "Grouped resource type display using Accordion"
      contains: "Accordion"
    - path: "src/components/dashboard/ResourceTypeGroup.tsx"
      provides: "Single category section with resource types"
      contains: "SimpleGrid"
    - path: "src/components/dashboard/ResourceTypeRow.tsx"
      provides: "Single resource type with count, params, operations"
      contains: "Badge"
    - path: "src/components/dashboard/MedplumCompatGate.tsx"
      provides: "Medplum React component compatibility verification"
      contains: "ResourceTable"
  key_links:
    - from: "src/components/dashboard/DashboardPage.tsx"
      to: "src/fhir/capability.ts"
      via: "parseResourceTypes(capability)"
      pattern: "parseResourceTypes"
    - from: "src/components/dashboard/ResourceTypeRow.tsx"
      to: "src/hooks/useResourceCounts.ts"
      via: "count display from hook"
      pattern: "counts\\[.*type"
    - from: "src/hooks/useResourceCounts.ts"
      to: "MedplumClient.search"
      via: "_summary=count queries"
      pattern: "search.*_summary.*count"
    - from: "src/components/dashboard/MedplumCompatGate.tsx"
      to: "@medplum/react"
      via: "ResourceTable rendering FHIR data"
      pattern: "ResourceTable"
---

<objective>
Parse the CapabilityStatement into grouped resource types, display them in a categorized accordion layout with lazy-loaded counts, verify Medplum React component compatibility with Blaze-fetched data, and verify the full connection-to-display pipeline with a human checkpoint.

Purpose: This completes the Phase 1 deliverable -- users can connect and see what's on their FHIR server. The resource type list is the primary "it works" moment, and the Medplum compatibility gate (ROADMAP Success Criterion 5) validates the Medplum-on-Blaze architecture before Phase 2.

Output: Fully functional CapabilityStatement display with categorized resource types, lazy-loaded counts, clickable navigation links, and a proven Medplum React compatibility gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-blaze-connectivity/01-CONTEXT.md
@.planning/phases/01-foundation-blaze-connectivity/01-RESEARCH.md
@.planning/phases/01-foundation-blaze-connectivity/01-UI-SPEC.md
@.planning/phases/01-foundation-blaze-connectivity/01-01-SUMMARY.md
@.planning/phases/01-foundation-blaze-connectivity/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs -->

From src/fhir/types.ts:
```typescript
export type ConnectionStatus = 'idle' | 'connecting' | 'connected' | 'error';

export type ConnectionState =
  | { status: 'idle' }
  | { status: 'connecting' }
  | { status: 'connected'; client: MedplumClient; capability: CapabilityStatement }
  | { status: 'error'; error: ConnectionError };
```

From src/hooks/useConnection.ts:
```typescript
export function useConnection(): {
  state: ConnectionState;
  connect: (settings: AppSettings) => Promise<void>;
  disconnect: () => void;
};
```

From src/components/dashboard/DashboardPage.tsx:
```typescript
// Currently has placeholder div for resource types section
// Props: settings, usingDefaults, connectionState, onConnect
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CapabilityStatement parsing, FHIR category mapping, resource counts hook, resource type display components, and Medplum compatibility gate</name>
  <files>src/fhir/capability.ts, src/utils/fhir-categories.ts, src/hooks/useResourceCounts.ts, src/components/dashboard/ResourceTypeList.tsx, src/components/dashboard/ResourceTypeGroup.tsx, src/components/dashboard/ResourceTypeRow.tsx, src/components/dashboard/MedplumCompatGate.tsx, src/components/dashboard/DashboardPage.tsx</files>
  <read_first>
    - src/components/dashboard/DashboardPage.tsx (current state -- must integrate resource types section)
    - src/fhir/types.ts (ConnectionState type, especially 'connected' variant with client and capability)
    - src/hooks/useConnection.ts (understand how to access client and capability)
    - .planning/phases/01-foundation-blaze-connectivity/01-RESEARCH.md (Pattern 3: CapabilityStatement parsing, Pattern 4: FHIR category mapping, Resource Counts hook with concurrency limit, Pitfall 4: count fetching overload)
    - .planning/phases/01-foundation-blaze-connectivity/01-UI-SPEC.md (Resource Type List layout, loading states, component inventory)
    - .planning/phases/01-foundation-blaze-connectivity/01-CONTEXT.md (D-07 through D-10)
  </read_first>
  <action>
1. **Create `src/utils/fhir-categories.ts`** -- Full FHIR R4 category mapping per RESEARCH.md Pattern 4:
- Copy the complete `CATEGORY_MAP` from RESEARCH.md (covers Clinical, Diagnostics, Medications, Foundation, Individuals, Entities, Workflow, Care Provision, Financial, Security, Documents, Communication, Research, Quality categories)
- Export `getResourceCategory(resourceType: string): string` -- returns category or 'Other' for unknown types
- Export `groupByCategory<T extends { category: string }>(items: T[]): Map<string, T[]>` -- groups items by category field
- Export `CATEGORY_ORDER: string[]` -- defines display order: ['Individuals', 'Clinical', 'Diagnostics', 'Medications', 'Care Provision', 'Workflow', 'Financial', 'Foundation', 'Entities', 'Security', 'Documents', 'Communication', 'Research', 'Quality', 'Other']

2. **Create `src/fhir/capability.ts`** -- CapabilityStatement parser per RESEARCH.md Pattern 3:
```typescript
import type { CapabilityStatement, CapabilityStatementRestResource } from '@medplum/fhirtypes';
import { getResourceCategory } from '../utils/fhir-categories';

export interface ParsedResourceType {
  type: string;
  searchParams: string[];
  operations: string[];
  category: string;
}

export function parseResourceTypes(capabilityStatement: CapabilityStatement): ParsedResourceType[] {
  const restResources = capabilityStatement.rest?.[0]?.resource ?? [];
  return restResources.map((resource: CapabilityStatementRestResource) => ({
    type: resource.type ?? 'Unknown',
    searchParams: (resource.searchParam ?? []).map(sp => sp.name ?? '').filter(Boolean),
    operations: (resource.operation ?? []).map(op => op.name ?? '').filter(Boolean),
    category: getResourceCategory(resource.type ?? ''),
  }));
}
```

3. **Create `src/hooks/useResourceCounts.ts`** per D-09, RESEARCH.md Pattern with concurrency limit of 4:
- Takes `client: MedplumClient | null` and `resourceTypes: string[]` as arguments
- Returns `Record<string, number | 'loading' | 'error'>`
- On mount (when client and resourceTypes change): initialize all types as 'loading'
- Fetch counts using `client.search(resourceType, '_summary=count')` -- read `bundle.total`
- Concurrency limiter: max 4 concurrent requests using a worker pool pattern (see RESEARCH.md example)
- Use `Promise.allSettled` so one failure doesn't block others
- On individual failure: set that type to 'error' (not blocking others)
- Include cleanup: use AbortController or a cancelled flag to prevent state updates after unmount

4. **Create `src/components/dashboard/ResourceTypeRow.tsx`** per D-08, D-09, D-10, UI-SPEC:
- Props: `resourceType: ParsedResourceType`, `count: number | 'loading' | 'error'`
- Resource type name as a react-router `Link` to `/explorer/${resourceType.type}` (dead link until Phase 2 per D-10), styled with Mantine blue accent color
- Count display:
  - `'loading'`: Mantine `Loader` size="xs" inline
  - `'error'`: Mantine `Badge` color="red" text "Error"
  - `number`: Mantine `Badge` color="blue" variant="light" showing the count (formatted with locale string for thousands separator)
- Search params count: `Text` size="xs" color="dimmed" showing "{N} search params"
- Operations count: `Text` size="xs" color="dimmed" showing "{N} operations"
- Layout: Mantine `Group` with name on left, count + metadata on right

5. **Create `src/components/dashboard/ResourceTypeGroup.tsx`** per D-07, UI-SPEC:
- Props: `category: string`, `resourceTypes: ParsedResourceType[]`, `counts: Record<string, number | 'loading' | 'error'>`
- Category name as section header: `Text` fw={600} size="xl" (20px semibold per UI-SPEC)
- Resource types in a `SimpleGrid` with `cols={{ base: 1, sm: 2, lg: 3 }}` per UI-SPEC
- Each cell renders a `ResourceTypeRow`

6. **Create `src/components/dashboard/ResourceTypeList.tsx`** per D-07, UI-SPEC:
- Props: `resourceTypes: ParsedResourceType[]`, `counts: Record<string, number | 'loading' | 'error'>`
- Uses Mantine `Accordion` with `multiple` and `defaultValue` set to all categories (all expanded by default)
- Groups resourceTypes by category using `groupByCategory` from fhir-categories
- Orders groups by `CATEGORY_ORDER` from fhir-categories
- Each `Accordion.Item` renders a `ResourceTypeGroup`
- Empty state: when resourceTypes is empty, show `Text` "No Resource Types Found" heading + "The server returned an empty CapabilityStatement. Verify the server has data loaded." body (per UI-SPEC copywriting)

7. **Create `src/components/dashboard/MedplumCompatGate.tsx`** -- Medplum React component compatibility gate (ROADMAP Success Criterion 5):
- This component validates that Medplum React components can render FHIR data fetched from Blaze.
- Props: `client: MedplumClient`
- On mount, fetch one resource from the server: use `client.searchResources(firstAvailableType, { _count: '1' })` where `firstAvailableType` is derived from the CapabilityStatement (e.g., pick the first resource type that has count > 0, or fall back to 'Patient', 'Observation', or 'CapabilityStatement').
- Accept an optional `resourceTypes` prop (string[]) to know which types are available. Try the first available type.
- Wrap the fetched resource in a Medplum `ResourceTable` component (from `@medplum/react`). `ResourceTable` renders a FHIR resource's properties in a table layout.
- IMPORTANT: `ResourceTable` and other @medplum/react components require `MedplumProvider` in the tree. Since we are NOT using MedplumProvider globally (we manage MedplumClient ourselves), wrap the `ResourceTable` in a `MedplumProvider` locally:
  ```typescript
  import { MedplumProvider } from '@medplum/react';
  // ...
  <MedplumProvider medplum={client}>
    <ResourceTable value={resource} />
  </MedplumProvider>
  ```
- Display states:
  - Loading: `Loader` with "Verifying Medplum compatibility..."
  - Success: Green `Alert` "Medplum compatibility verified" + the rendered `ResourceTable` showing the fetched resource
  - Error: Orange `Alert` "Medplum component rendering failed" with error details. This is a warning, not a blocker -- the rest of the app works, but Phase 2 may have issues.
- Render this component on the DashboardPage BELOW the resource type list, only when `connectionState.status === 'connected'`.

8. **Update `src/components/dashboard/DashboardPage.tsx`** to integrate resource type display and compatibility gate:
- When `connectionState.status === 'connected'`:
  - Parse capability: `const resourceTypes = parseResourceTypes(connectionState.capability)`
  - Render `ResourceTypeList` below the connection card area
  - Pass resource counts from `useResourceCounts(connectionState.client, resourceTypes.map(rt => rt.type))`
  - Render `MedplumCompatGate` below the resource type list, passing `client` and `resourceTypes`
- When `connectionState.status !== 'connected'`:
  - Show pre-connect empty state: `Text` heading "Not Connected", body "Configure your FHIR server in settings.yaml and click Connect to begin exploring." (per UI-SPEC copywriting)
- Maintain all existing DashboardPage functionality (ServerInfoCard, error alerts, warning banner)
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/Exploder && npm run build 2>&1 | tail -5 && echo "---" && grep -l "parseResourceTypes" src/fhir/capability.ts && grep -l "CATEGORY_MAP" src/utils/fhir-categories.ts && grep -l "useResourceCounts" src/hooks/useResourceCounts.ts && grep -l "Accordion" src/components/dashboard/ResourceTypeList.tsx && grep -l "ResourceTable" src/components/dashboard/MedplumCompatGate.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/fhir/capability.ts contains "export function parseResourceTypes" and "export interface ParsedResourceType" with type, searchParams, operations, category fields
    - src/utils/fhir-categories.ts contains "CATEGORY_MAP" with at least 30 resource type entries and "export function getResourceCategory" and "export function groupByCategory" and "export const CATEGORY_ORDER"
    - src/hooks/useResourceCounts.ts contains "export function useResourceCounts" and "_summary=count" and a concurrency mechanism (CONCURRENCY variable or similar)
    - src/hooks/useResourceCounts.ts does NOT fire all requests simultaneously (must have concurrency limiting logic)
    - src/components/dashboard/ResourceTypeRow.tsx contains "Link" to "/explorer/" and "Loader" and "Badge"
    - src/components/dashboard/ResourceTypeGroup.tsx contains "SimpleGrid" and "ResourceTypeRow"
    - src/components/dashboard/ResourceTypeList.tsx contains "Accordion" and "ResourceTypeGroup" and "No Resource Types Found"
    - src/components/dashboard/MedplumCompatGate.tsx contains "ResourceTable" and "MedplumProvider" and "compatibility"
    - src/components/dashboard/DashboardPage.tsx contains "parseResourceTypes" and "useResourceCounts" and "ResourceTypeList" and "MedplumCompatGate"
    - src/components/dashboard/DashboardPage.tsx contains "Not Connected" pre-connect empty state text
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>CapabilityStatement is parsed into categorized resource types. Resource types display in accordion groups ordered by FHIR category. Each type shows count (lazy-loaded with spinner), search param count, and operations count. Resource names are clickable links to /explorer/{type}. Counts load with concurrency limit of 4. Empty state handles no resources gracefully. Medplum React compatibility gate renders a FHIR resource via ResourceTable to validate the Medplum-on-Blaze architecture.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 connection flow end-to-end including Medplum compatibility</name>
  <files>none</files>
  <action>Human verification checkpoint. No code changes -- verify the full Phase 1 flow works end-to-end in the browser.</action>
  <what-built>
Complete Phase 1 Foundation: App shell with sidebar, settings loading, Blaze connection flow, CapabilityStatement parsing, categorized resource type display with lazy-loaded counts, and Medplum React component compatibility gate.
  </what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev`
2. Open http://localhost:5173 in a browser
3. Verify the app shell:
   - Left sidebar (240px, light gray background) shows "FHIR Exploder" title
   - Sidebar has 4 nav items: Explorer, Patients, Quality, Settings (with icons)
   - Connection status dot at top shows gray "Not connected"
4. Verify the landing page (pre-connect):
   - "Server Connection" heading visible
   - Server info card shows URL (http://localhost:8080/fhir) and Auth Mode (Open)
   - "Connect to Server" button is blue and enabled
   - "Not Connected" empty state text shown below card
5. Test error handling (with Blaze NOT running):
   - Click "Connect to Server"
   - Button should show spinner briefly then re-enable
   - Red error alert appears: "Cannot reach FHIR server at http://localhost:8080/fhir..."
   - Sidebar dot turns red, shows "Disconnected"
6. If Blaze IS available -- test successful connection:
   - Click "Connect to Server" (or "Retry Connection")
   - Button shows spinner, then changes to green "Connected"
   - Sidebar dot turns green, shows "Connected"
   - Resource types appear grouped by FHIR category in accordion sections
   - Per-type counts show small spinners that resolve to blue badges with numbers
   - Resource type names are blue clickable links
7. **Medplum compatibility gate (ROADMAP Success Criterion 5):**
   - Below the resource type list, look for a green "Medplum compatibility verified" alert
   - A Medplum ResourceTable should render showing properties of a FHIR resource fetched from Blaze
   - If it shows an orange warning instead, note the error -- this means Medplum React components may not be fully compatible with Blaze data
8. Navigate to /settings:
   - Click "Settings" in sidebar
   - Shows current config read-only (server URL, auth mode)
9. If settings.yaml is removed/renamed from public/:
   - Refresh app -- orange "Using default settings" warning banner should appear
10. Run tests: `npx vitest run --reporter=verbose` -- all scaffold tests should be skipped (todo), no failures
  </how-to-verify>
  <verify>
    <automated>cd /Users/kohlbach/Claude/Exploder && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User has verified the complete Phase 1 flow: app shell, settings, connection, error handling, resource type display, Medplum compatibility gate, and navigation all work as specified.</done>
  <resume-signal>Type "approved" if the full flow works, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Blaze /metadata | CapabilityStatement fetch from user-configured server |
| Browser -> Blaze /{type}?_summary=count | Multiple concurrent count queries per resource type |
| Browser -> Blaze /{type}?_count=1 | Single resource fetch for Medplum compatibility gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Task |
|-----------|----------|-----------|-------------|------------|------|
| T-1-08 | Denial of Service | Resource count fetching | mitigate | Concurrency limited to 4 simultaneous requests. Individual failures isolated via Promise.allSettled. No infinite retry. | Task 1 |
| T-1-09 | Tampering | CapabilityStatement parsing | accept | Parsed from user's own FHIR server. Malformed data results in empty/missing fields, not code execution. | Task 1 |
| T-1-10 | Information Disclosure | Resource type names/counts | accept | This is the app's primary purpose -- displaying server contents. No sensitive data beyond what the FHIR server already exposes. | Task 1 |
| T-1-11 | Tampering | Medplum ResourceTable rendering | accept | Renders one resource from user's own server. Medplum components sanitize output. Local-only tool. | Task 1 |
</threat_model>

<verification>
- `npm run build` succeeds
- All resource type display components render without errors
- CapabilityStatement parser extracts resource types with category, search params, operations
- Resource counts load lazily (not all at once -- verify with browser network tab)
- Grouped accordion display matches UI-SPEC layout
- Medplum ResourceTable renders a FHIR resource from Blaze without errors (compatibility gate)
- Full flow verified by human checkpoint
</verification>

<success_criteria>
- CapabilityStatement parsed into resource types with categories per FHIR R4 specification
- Resource types displayed in accordion groups matching FHIR categories (D-07)
- Each type shows count, search param count, operations count (D-08)
- Counts lazy-load with per-type spinners, max 4 concurrent (D-09)
- Resource type names link to /explorer/{type} (D-10)
- Empty state handled when no resources or no connection
- Medplum React component (ResourceTable) successfully renders a FHIR resource fetched from Blaze (ROADMAP Success Criterion 5)
- Human verification confirms end-to-end flow works
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-blaze-connectivity/01-03-SUMMARY.md`
</output>
