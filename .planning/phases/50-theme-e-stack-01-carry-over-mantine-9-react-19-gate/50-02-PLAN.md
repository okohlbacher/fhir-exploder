---
phase: 50
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [STACK-01]
must_haves:
  truths:
    - "REQUIREMENTS.md STACK-01 traceability row shows status `deferred` and points at v1.8 (or carries an equivalent v1.8 marker per the file's actual column layout)"
    - "PROJECT.md has a v1.8 deferred-items / candidates entry referencing STACK-01 with the D-04 re-attempt trigger condition (`npm view @medplum/react peerDependencies`)"
    - "ROADMAP.md Progress table Phase 50 row marks status as `deferred` (mirroring how Phase 45 closed in v1.6)"
    - "ROADMAP.md Phase 50 entry in the Phases list (line ~44) is updated from `[ ]` to `[~]` with a brief deferred annotation, mirroring the v1.6 Phase 45 line"
    - "No source files under src/, package.json, package-lock.json, or any test/build artifact are modified"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "STACK-01 row flipped to `deferred` with v1.8 target"
      contains: "deferred"
    - path: ".planning/PROJECT.md"
      provides: "v1.8 candidates / deferred-items block referencing STACK-01"
      contains: "v1.8"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 50 row → `deferred` in the Progress table; Phase 50 line item updated in v1.7 Phases list"
      contains: "Phase 50"
  key_links:
    - from: "REQUIREMENTS.md STACK-01 row"
      to: "v1.8 milestone"
      via: "status column flip + Phase column annotation"
      pattern: "STACK-01.*deferred"
    - from: "PROJECT.md v1.8 candidates block"
      to: "50-SUMMARY.md re-attempt trigger"
      via: "reproduces D-04 trigger command and the conditional branching"
      pattern: "STACK-01.*v1.8"
    - from: "ROADMAP.md Phase 50 row"
      to: "v1.7 milestone-close audit trail"
      via: "Progress-table status flip mirroring Phase 45 v1.6 close"
      pattern: "50.*deferred"
---

<objective>
Traceability rollover for Phase 50 closure. Flip STACK-01 from `pending`/active to `deferred` across the three planning files that surface its status, and add a v1.8 carry-forward entry mirroring the v1.6 → v1.7 carry pattern that already exists in PROJECT.md (line ~94).

Purpose: A future v1.8 planner running `/gsd-new-milestone` should immediately see STACK-01 as a v1.8 candidate with a clear re-attempt trigger, exactly the way v1.7 inherited it from v1.6. The Progress table and the in-flight requirements traceability row must reflect the deferred state so milestone-close audits don't trip on a "pending" STACK-01.

Output: Three existing planning files edited in place; no new files.

Closes: MH-3 (REQUIREMENTS.md), MH-4 (PROJECT.md), MH-5 (ROADMAP.md). Together with Plan 01 (MH-1, MH-2), this fully closes Phase 50 / STACK-01 as `deferred`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v1.6-phases/45-mantine-9-upgrade-stack-01/45-SUMMARY.md
@.planning/milestones/v1.6-REQUIREMENTS.md
@.planning/milestones/v1.6-ROADMAP.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Pure-doc edits to planning files. No code, no dependency change, no runtime behavior, no network call, no user input handling. Trust surface is empty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-07 | Spoofing | n/a | accept | Doc-only deferral; no code, dependency, or runtime change introduced |
| T-50-08 | Tampering | n/a | accept | Doc-only deferral; no code, dependency, or runtime change introduced |
| T-50-09 | Repudiation | n/a | accept | Doc-only deferral; no code, dependency, or runtime change introduced |
| T-50-10 | Information Disclosure | n/a | accept | Doc-only deferral; no code, dependency, or runtime change introduced |
| T-50-11 | Denial of Service | n/a | accept | Doc-only deferral; no code, dependency, or runtime change introduced |
| T-50-12 | Elevation of Privilege | n/a | accept | Doc-only deferral; no code, dependency, or runtime change introduced |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Flip REQUIREMENTS.md STACK-01 traceability row to `deferred` (v1.8 target)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current STACK-01 row layout — verify the actual table columns before editing; current columns are `| REQ-ID | Phase | Plan(s) | Status |`)
    - .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-CONTEXT.md (D-03 closure path, D-04 re-attempt trigger)
    - .planning/milestones/v1.6-REQUIREMENTS.md (precedent — v1.6 STACK-01 row in v1.6's traceability table; note v1.6 used 3 columns `| REQ-ID | Phase | Depends on |` with no Status column, so this differs by file)
  </read_first>
  <action>
Edit the existing v1.7 `.planning/REQUIREMENTS.md` Traceability table. The current STACK-01 row is:

```
| STACK-01 | Phase 50 | TBD | pending |
```

Replace that row with:

```
| STACK-01 | Phase 50 | — (no plans authored; pure-doc WAIVE-AND-DEFER) | deferred → v1.8 |
```

Also update the STACK-01 checkbox in the §"Theme E — Carry-over: Mantine 9 / React 19 Gate" section earlier in the file. The current line is:

```
- [ ] **STACK-01**: Re-run the `@medplum/react` peer-dependency gate ...
```

Change `[ ]` to `[~]` to mark deferred (mirrors the `[~]` notation used in `.planning/PROJECT.md` line ~90 for the v1.6 Phase 45 closure: "[~] **STACK-01** — Mantine 9 / React 19 upgrade — DEFERRED to v1.7"). Append at the end of that bullet, after the existing trailing sentence about "**This requirement closes either as `validated` (gate passed, upgrade shipped) or `deferred` (gate failed, pushed to v1.8) — both are acceptable closures per the v1.6 precedent.**", an additional sentence in italics:

```
*Closure status (v1.7): `deferred` — gate result MIXED at 2026-05-01 (Mantine 9 still pinned `^8.0.0` by `@medplum/react@5.1.10`; React 19 newly open). Per user decision D-02 in 50-CONTEXT.md, React/Mantine remain coupled; entire upgrade defers to v1.8. Re-attempt trigger: re-run `npm view @medplum/react peerDependencies` at v1.8 milestone start. See `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md`.*
```

Also update the "Last updated" footer line at the bottom of the file. After the existing two `*Last updated: 2026-05-01 ...*` lines, append a new italic line:

```
*Last updated: 2026-05-02 — Phase 50 (Theme E — STACK-01 carry-over) closed `deferred` via WAIVE-AND-DEFER. Gate result MIXED (Mantine 9 closed, React 19 open as of medplum/react@5.1.10); user decision D-02 keeps React/Mantine coupled; STACK-01 carries to v1.8 deferred-items list. Closure docs: `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md` + `50-VERIFICATION.md`.*
```

Do NOT modify any other row of the Traceability table. Do NOT change requirement IDs. Do NOT add `[x]` (validated) anywhere — STACK-01 is `deferred`, not `validated`.
  </action>
  <verify>
    <automated>grep -q "STACK-01.*deferred → v1.8" .planning/REQUIREMENTS.md && grep -q "\[~\] \*\*STACK-01\*\*" .planning/REQUIREMENTS.md && grep -q "Phase 50 (Theme E — STACK-01 carry-over) closed \`deferred\`" .planning/REQUIREMENTS.md && ! grep -q "STACK-01 | Phase 50 | TBD | pending" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` Traceability table row for STACK-01 contains the literal string `deferred → v1.8` and no longer contains `pending` for that row.
    - The STACK-01 bullet in §"Theme E — Carry-over: Mantine 9 / React 19 Gate" uses `[~]` (deferred marker) instead of `[ ]`.
    - That bullet's trailing italic sentence references `50-SUMMARY.md` and the `npm view @medplum/react peerDependencies` re-attempt command.
    - A new "Last updated: 2026-05-02 — Phase 50 ..." italic footer line is appended at the bottom (existing two 2026-05-01 lines preserved).
    - File still parses as valid Markdown (table structure preserved; no broken pipe counts).
    - No source files under `src/`, `package.json`, or `package-lock.json` were modified.
  </acceptance_criteria>
  <done>REQUIREMENTS.md STACK-01 row reads `deferred → v1.8`, the section bullet is marked `[~]` with a re-attempt trigger pointer, and a 2026-05-02 footer line records the WAIVE-AND-DEFER closure. No other row changed; no source diff outside `.planning/`.</done>
</task>

<task type="auto">
  <name>Task 2: Add v1.8 deferred-items / candidates entry to PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (current v1.7 candidates block at line ~92-94 — the format to mirror; current line is `- [ ] **STACK-01** (carried from v1.6): Mantine 9 / React 19 upgrade — re-run peer-dep gate at v1.7 milestone start; if `@medplum/react` peer range now allows `^9.x`, reactivate Phase 45 from `.planning/milestones/v1.6-phases/45-*/45-CONTEXT.md`)
    - .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-CONTEXT.md (D-04 re-attempt trigger; verbatim text to embed)
    - .planning/milestones/v1.6-phases/45-mantine-9-upgrade-stack-01/45-SUMMARY.md (precedent for the carry-forward annotation)
  </read_first>
  <action>
Edit `.planning/PROJECT.md`. Two surgical changes:

**Change A — Mark the existing v1.7 STACK-01 candidate as deferred-not-shipped.** The current §"Active (v1.7 candidates — pending scope)" section at line ~92-94 reads:

```
### Active (v1.7 candidates — pending scope)

- [ ] **STACK-01** (carried from v1.6): Mantine 9 / React 19 upgrade — re-run peer-dep gate at v1.7 milestone start; if `@medplum/react` peer range now allows `^9.x`, reactivate Phase 45 from `.planning/milestones/v1.6-phases/45-*/45-CONTEXT.md`
```

Change `[ ]` to `[~]` and append a closure annotation. The replaced section should read:

```
### Active (v1.7 candidates — pending scope)

- [~] **STACK-01** (carried from v1.6): Mantine 9 / React 19 upgrade — DEFERRED to v1.8 via Phase 50 WAIVE-AND-DEFER on 2026-05-02. Gate result MIXED at 2026-05-01: `@medplum/react@5.1.10` peers `@mantine/core: ^8.0.0` (Mantine 9 still closed) but `react: ^18.0.0 || ^19.0.0` (React 19 newly open). User decision D-02 keeps React/Mantine coupled, so the entire upgrade defers. See `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md`.
```

**Change B — Add a new v1.8 candidates / deferred-items section directly below the v1.7 block.** Insert this new block immediately after the modified v1.7 candidates section and before the next existing section (`## Current State`, currently at line ~96). Mirror the v1.6 → v1.7 carry-forward pattern.

```

### Active (v1.8 candidates — pending scope)

- [ ] **STACK-01** (carried from v1.6 → v1.7): Mantine 9 / React 19 upgrade — re-run peer-dep gate at v1.8 milestone start. **Mantine 9 watch:** the bottleneck remains `@medplum/react`'s `@mantine/core: ^8.0.0` peer pin. React 19 has independently unblocked since v1.6 close (current peer pin: `^18.0.0 || ^19.0.0`), but per user decision D-02 (50-CONTEXT.md) React and Mantine stay coupled until both gates open.
  - **Re-attempt trigger:** `npm view @medplum/react peerDependencies` — if `@mantine/core` peer range now includes `^9.x`, open a new phase (working name "Theme X — STACK-01 Mantine 9 / React 19 upgrade") in v1.8 with codemod + breaking-change sweep + visual regression UAT + bundle measurement scope per the original Phase 50 gate-PASS branch. If still pinned to `^8.0.0`, re-defer to v1.9 with another WAIVE-AND-DEFER.
  - **Reactivation source:** `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-CONTEXT.md` + `50-SUMMARY.md`; original v1.6 context still valid at `.planning/milestones/v1.6-phases/45-mantine-9-upgrade-stack-01/45-CONTEXT.md`.
  - **Out of scope (rejected workarounds — do NOT revisit without re-discussing D-05/D-06/D-07):** React-19-only upgrade (D-05), `--legacy-peer-deps` workaround (D-06), Mantine 9 codemod preview against scratch branch (D-07).
```

Append a new italic "Last updated" entry at the very bottom of the file (after the existing latest entry, currently the 2026-05-01 Phase 49 entry):

```
*Last updated: 2026-05-02 — Phase 50 (Theme E — STACK-01 carry-over) closed `deferred` via WAIVE-AND-DEFER. Gate result MIXED at 2026-05-01: `@medplum/react@5.1.10` peers `@mantine/core: ^8.0.0` (Mantine 9 still closed) but `react: ^18.0.0 || ^19.0.0` (React 19 newly open). Per user decision D-02 (50-CONTEXT.md), React/Mantine remain coupled; entire upgrade defers to v1.8 deferred-items list. v1.7 milestone now at 5/5 phases complete; STACK-01 traceability flipped to `deferred → v1.8` in REQUIREMENTS.md. Closure docs: `50-SUMMARY.md` + `50-VERIFICATION.md` (status `passed` per v1.6 Phase 45 pure-doc closure precedent). v1.8 candidates block added: STACK-01 watch with `npm view @medplum/react peerDependencies` re-attempt trigger.*
```

Do NOT touch any other section. Do NOT modify the §"Validated (v1.6 — shipped 2026-04-30)" list or the §"Validated (v1.5 — shipped 2026-04-29)" list. Do NOT change `## Constraints` or `## Key Decisions`.
  </action>
  <verify>
    <automated>grep -q "Active (v1.8 candidates — pending scope)" .planning/PROJECT.md && grep -q "\[~\] \*\*STACK-01\*\* (carried from v1.6): Mantine 9 / React 19 upgrade — DEFERRED to v1.8 via Phase 50 WAIVE-AND-DEFER on 2026-05-02" .planning/PROJECT.md && grep -q "carried from v1.6 → v1.7" .planning/PROJECT.md && grep -q "Mantine 9 watch" .planning/PROJECT.md && grep -q "npm view @medplum/react peerDependencies" .planning/PROJECT.md && grep -q "Phase 50 (Theme E — STACK-01 carry-over) closed \`deferred\` via WAIVE-AND-DEFER" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains the new section heading `### Active (v1.8 candidates — pending scope)`.
    - The v1.8 STACK-01 entry contains both `Mantine 9 watch` and `npm view @medplum/react peerDependencies`.
    - The v1.7 STACK-01 entry was changed from `[ ]` to `[~]` and now contains `DEFERRED to v1.8 via Phase 50 WAIVE-AND-DEFER`.
    - The new bottom-of-file footer line contains the literal string `Phase 50 (Theme E — STACK-01 carry-over) closed \`deferred\` via WAIVE-AND-DEFER`.
    - The v1.8 entry references all three rejected-workaround decision IDs (D-05, D-06, D-07).
    - No row of `## Validated (v1.5 — shipped ...)` or `## Validated (v1.6 — shipped ...)` was modified.
    - No source files under `src/`, `package.json`, or `package-lock.json` were modified.
  </acceptance_criteria>
  <done>PROJECT.md has a v1.8 candidates block referencing STACK-01 with the D-04 re-attempt trigger, the v1.7 candidates block is marked deferred, the optional "Mantine 9 watch" note is included (per CONTEXT.md "Claude's Discretion" recommendation), and a 2026-05-02 footer line records the closure. v1.6 / v1.5 validated lists untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Mark Phase 50 row `deferred` in ROADMAP.md Progress table + Phases list</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current Phase 50 Progress-table row at line ~230: `| 50. STACK-01 gate | v1.7 | TBD | Not started | - |`; current Phase 50 Phases-list line at ~44: `- [ ] Phase 50: Theme E — STACK-01 carry-over: Mantine 9 / React 19 upgrade gate (STACK-01)`)
    - .planning/milestones/v1.6-ROADMAP.md (Phase 45 close precedent: line 102 `[x] **Phase 45: Mantine 9 upgrade (STACK-01)** — Conditional on... defers to v1.7 with `WAIVE-AND-DEFER` if gate fails (completed 2026-04-30)`; line 250 progress row `| 45. Mantine 9 upgrade (STACK-01) | v1.6 | 1/0 | Complete    | 2026-04-30 |`)
    - .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-CONTEXT.md (D-03 closure path)
    - .planning/milestones/v1.6-phases/45-mantine-9-upgrade-stack-01/45-SUMMARY.md (precedent — v1.6 close marked Phase 45 as complete-but-deferred-to-v1.7)
  </read_first>
  <action>
Edit `.planning/ROADMAP.md`. Three surgical changes.

**Change A — Phases list (line ~44).** Current line:

```
- [ ] Phase 50: Theme E — STACK-01 carry-over: Mantine 9 / React 19 upgrade gate (STACK-01)
```

Replace with (mirrors v1.6 Phase 45 line 102 pattern using `[~]` to indicate deferred — note v1.6 used `[x]` because the v1.6 milestone shipped, but for in-flight v1.7 we use `[~]` consistent with PROJECT.md's existing `[~]` deferred-marker convention):

```
- [~] Phase 50: Theme E — STACK-01 carry-over: Mantine 9 / React 19 upgrade gate (STACK-01) — DEFERRED to v1.8 via WAIVE-AND-DEFER on 2026-05-02 (gate result MIXED 2026-05-01: `@medplum/react@5.1.10` peers `@mantine/core: ^8.0.0` only; React 19 independently unblocked but coupled-defer per user decision D-02)
```

**Change B — Progress table (line ~230).** Current row:

```
| 50. STACK-01 gate | v1.7 | TBD | Not started | - |
```

Replace with (mirrors v1.6 Phase 45 row 250 column shape; status string `Deferred` matches the deferred state):

```
| 50. STACK-01 gate | v1.7 | 2/0 | Deferred    | 2026-05-02 |
```

(Reasoning for `2/0`: 2 plans authored — Plan 01 closure docs + Plan 02 traceability rollover — but 0 implementation plans executed because the closure path is pure-doc; mirrors the v1.6 `1/0` shape used for Phase 45 where 1 doc plan ran and 0 implementation plans ran. The `2026-05-02` date is the closure date.)

**Change C — Deferred Items section (line ~14-22).** The current §"Deferred Items" section contains a `**Now scheduled in v1.7 (Phase 50 — conditional gate):**` block describing the v1.6 → v1.7 carry. Add a new subsection BELOW it but BEFORE the existing `**Deferred beyond v1.7 (re-evaluate at v1.8 milestone-new):**` block. Insert this new block:

```

**Now deferred to v1.8 (Phase 50 closed `deferred` 2026-05-02):**

- **STACK-01 (Mantine 9 / React 19 upgrade — second WAIVE-AND-DEFER):** v1.7 Phase 50 re-ran the peer-dep gate on 2026-05-01 against `@medplum/react@5.1.10`. Result MIXED: Mantine 9 still pinned `^8.0.0` (gate FAILS); React 19 newly allowed via `^18.0.0 || ^19.0.0` (gate PASSES). Per user decision D-02 (`.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-CONTEXT.md`), React and Mantine stay coupled until both gates open, so the entire upgrade defers. **Re-attempt trigger at v1.8 milestone start:** re-run `npm view @medplum/react peerDependencies`; if `@mantine/core` now includes `^9.x`, open a new v1.8 phase with codemod + breaking-change sweep + visual regression UAT + bundle measurement; else re-defer to v1.9. Reactivation source: `50-SUMMARY.md` + `50-CONTEXT.md` + original v1.6 `.planning/milestones/v1.6-phases/45-mantine-9-upgrade-stack-01/45-CONTEXT.md`.
```

Do NOT modify the existing "Deferred beyond v1.7 (re-evaluate at v1.8 milestone-new)" block. Do NOT modify the v1.0..v1.6 milestone summaries. Do NOT modify the Phase 46/47/48/49 progress rows. Do NOT modify the Backlog or Resolved Backlog sections.
  </action>
  <verify>
    <automated>grep -q "\[~\] Phase 50: Theme E — STACK-01 carry-over.*DEFERRED to v1.8 via WAIVE-AND-DEFER on 2026-05-02" .planning/ROADMAP.md && grep -q "| 50. STACK-01 gate | v1.7 | 2/0 | Deferred" .planning/ROADMAP.md && grep -q "Now deferred to v1.8 (Phase 50 closed \`deferred\` 2026-05-02)" .planning/ROADMAP.md && grep -q "STACK-01 (Mantine 9 / React 19 upgrade — second WAIVE-AND-DEFER)" .planning/ROADMAP.md && ! grep -q "| 50. STACK-01 gate | v1.7 | TBD | Not started" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phases list line for Phase 50 is changed from `[ ]` to `[~]` and contains `DEFERRED to v1.8 via WAIVE-AND-DEFER on 2026-05-02`.
    - The Progress table row for Phase 50 reads exactly `| 50. STACK-01 gate | v1.7 | 2/0 | Deferred    | 2026-05-02 |` (4-space pad after "Deferred" matching the v1.6 Phase 45 row's column-alignment style).
    - The §"Deferred Items" section contains a new `**Now deferred to v1.8 (Phase 50 closed \`deferred\` 2026-05-02):**` subsection inserted before the existing v1.8+ deferred block.
    - That new subsection contains the literal string `npm view @medplum/react peerDependencies` (re-attempt trigger).
    - The old `| 50. STACK-01 gate | v1.7 | TBD | Not started | - |` row is GONE.
    - Phase 46-49 progress rows are unchanged.
    - No source files under `src/`, `package.json`, or `package-lock.json` were modified.
  </acceptance_criteria>
  <done>ROADMAP.md Phase 50 row in the Progress table is `Deferred` (mirroring how Phase 45 closed in v1.6); the Phases-list line is `[~]` with a deferred annotation; the Deferred Items section has a v1.7 → v1.8 carry-forward block with the re-attempt trigger. Phase 46-49 rows untouched.</done>
</task>

</tasks>

<verification>
- REQUIREMENTS.md grep confirms `STACK-01.*deferred → v1.8`, `[~] **STACK-01**`, the new 2026-05-02 footer line, and absence of the prior `STACK-01 | Phase 50 | TBD | pending` row.
- PROJECT.md grep confirms `### Active (v1.8 candidates — pending scope)`, `Mantine 9 watch`, `npm view @medplum/react peerDependencies`, and the new 2026-05-02 footer.
- ROADMAP.md grep confirms `[~] Phase 50: Theme E — STACK-01 carry-over... DEFERRED to v1.8`, `| 50. STACK-01 gate | v1.7 | 2/0 | Deferred`, `**Now deferred to v1.8 (Phase 50 closed `deferred` 2026-05-02):**`, and the verbatim re-attempt command.
- `git status --porcelain` shows only the three planning files changed; no `src/`, `package.json`, `package-lock.json`, or test/build artifact appears in the diff.
</verification>

<success_criteria>
- A v1.8 milestone-new run can pick up STACK-01 from PROJECT.md's v1.8 candidates block without consulting the phase directory directly.
- A v1.7 milestone-close audit reads ROADMAP.md and immediately sees Phase 50 = `Deferred` with date and a deferral annotation in the phases list.
- REQUIREMENTS.md traceability is internally consistent: every requirement either has a phase + plan + status, and STACK-01's status is `deferred → v1.8`.
- Zero source diff outside `.planning/`.
</success_criteria>

<output>
After completion, create `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-02-SUMMARY.md` summarizing the three traceability flips.
</output>
