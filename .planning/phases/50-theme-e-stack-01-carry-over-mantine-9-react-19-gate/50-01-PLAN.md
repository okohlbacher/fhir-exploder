---
phase: 50
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md
  - .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-VERIFICATION.md
autonomous: true
requirements: [STACK-01]
must_haves:
  truths:
    - "50-SUMMARY.md exists in the Phase 50 directory and records WAIVE-AND-DEFER as the closure outcome"
    - "50-SUMMARY.md contains the verbatim 2026-05-01 npm peerDependencies JSON output for medplum/react@5.1.10 (per CONTEXT.md D-01)"
    - "50-SUMMARY.md documents the mixed-gate finding (Mantine 9 FAILS, React 19 PASSES) that is NEW vs. v1.6 Phase 45 close"
    - "50-SUMMARY.md condenses the D-02 rationale for keeping React/Mantine coupled even though React 19 alone is unblocked"
    - "50-SUMMARY.md states the v1.8 re-attempt trigger command and branching condition from D-04"
    - "50-SUMMARY.md reaffirms the rejected workarounds D-05 (no React-19-only), D-06 (no --legacy-peer-deps), D-07 (no codemod preview)"
    - "50-VERIFICATION.md exists with status: passed (pure-doc closure precedent from v1.6 Phase 45)"
    - "Neither file touches any source under src/, package.json, package-lock.json, or any test/build artifact"
  artifacts:
    - path: ".planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md"
      provides: "WAIVE-AND-DEFER closure record for Phase 50 / STACK-01"
      contains: "WAIVE-AND-DEFER"
    - path: ".planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-VERIFICATION.md"
      provides: "Pure-doc closure verification report (status: passed)"
      contains: "status: passed"
  key_links:
    - from: "50-SUMMARY.md"
      to: "v1.6 Phase 45 precedent"
      via: "format mirror of 45-SUMMARY.md frontmatter and section order"
      pattern: "WAIVE-AND-DEFER"
    - from: "50-VERIFICATION.md"
      to: "the closure docs themselves"
      via: "MH-1..MH-2 derived must-haves verified by file existence + grep"
      pattern: "status: passed"
---

<objective>
Phase 50 closure record. Create the two doc artifacts that constitute the WAIVE-AND-DEFER closure of STACK-01: the SUMMARY.md (decision record + frozen gate evidence + v1.8 trigger) and the VERIFICATION.md (pure-doc-closure passed report).

Purpose: Preserve the 2026-05-01 mixed-gate finding (Mantine 9 still closed, React 19 newly open in Medplum 5.1.10's peer pins) verbatim so v1.8 planners do not have to re-derive it, and lock the user's D-02 decision (keep React/Mantine coupled, defer the entire upgrade) into the audit trail.

Output: Two new files under `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/`. Zero source diff. No package changes. No test or build invocations.

Closes: STACK-01 closure path "If gate FAILS" branch from ROADMAP §Phase 50 SC, partial — MH-1 (SUMMARY) + MH-2 (VERIFICATION). MH-3..MH-5 (REQUIREMENTS / PROJECT / ROADMAP traceability) close in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-CONTEXT.md
@.planning/milestones/v1.6-phases/45-mantine-9-upgrade-stack-01/45-SUMMARY.md
@.planning/milestones/v1.6-phases/45-mantine-9-upgrade-stack-01/45-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This phase is pure-doc deferral. No code, no dependency change, no runtime behavior, no network call, no user input handling, no external service contact. Trust surface is empty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Spoofing | n/a | accept | Doc-only deferral; no code, dependency, or runtime change introduced |
| T-50-02 | Tampering | n/a | accept | Doc-only deferral; no code, dependency, or runtime change introduced |
| T-50-03 | Repudiation | n/a | accept | Doc-only deferral; no code, dependency, or runtime change introduced |
| T-50-04 | Information Disclosure | n/a | accept | Doc-only deferral; no code, dependency, or runtime change introduced |
| T-50-05 | Denial of Service | n/a | accept | Doc-only deferral; no code, dependency, or runtime change introduced |
| T-50-06 | Elevation of Privilege | n/a | accept | Doc-only deferral; no code, dependency, or runtime change introduced |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create 50-SUMMARY.md WAIVE-AND-DEFER closure record</name>
  <files>.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md</files>
  <read_first>
    - .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-CONTEXT.md (locked decisions D-01 through D-07 — source of truth)
    - .planning/milestones/v1.6-phases/45-mantine-9-upgrade-stack-01/45-SUMMARY.md (precedent format to mirror — frontmatter shape + section order)
    - .planning/milestones/v1.6-phases/45-mantine-9-upgrade-stack-01/45-CONTEXT.md (precedent for tone of "auto-driven" decision recording)
    - CLAUDE.md §"Do NOT Use" (confirms Mantine 9 pin honored by this phase)
  </read_first>
  <action>
Create the file `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md` with the following exact content. Do NOT abbreviate any section — copy the gate JSON, the trigger command, and the rationale strings verbatim from CONTEXT.md.

**IMPORTANT: When writing the file, replace the literal sentinel `FRONTMATTER_OPEN` with `---` and `FRONTMATTER_CLOSE` with `---`. (The sentinels are used here so this PLAN.md's own frontmatter parser does not pick up the embedded SUMMARY.md frontmatter delimiters.)**

```markdown
FRONTMATTER_OPEN
phase: 50-theme-e-stack-01-carry-over-mantine-9-react-19-gate
status: deferred
deferred_to: v1.8
deferred_reason: peer-dep gate MIXED — Mantine 9 still pinned to ^8.0.0 by @medplum/react@5.1.10; React 19 independently unblocked; user decision (D-02) keeps React/Mantine coupled, so entire upgrade defers
plans_executed: 0/0
date: 2026-05-02
FRONTMATTER_CLOSE

# Phase 50: Theme E — STACK-01 Carry-Over (Mantine 9 / React 19 Gate) — SUMMARY

## Outcome: WAIVE-AND-DEFER

Per ROADMAP §Phase 50 success criteria "If gate FAILS" branch and 50-CONTEXT.md D-03:

> Phase 50 closes as `deferred` (NOT `validated`). STACK-01 carries to v1.8 deferred-items list. No source diff. No npm install. No tests.

This phase closes as `deferred`. **No source files modified. No plans authored beyond the closure docs themselves. No `npm install` executed. No codemod run. No `--legacy-peer-deps` attempted.**

This mirrors the v1.6 Phase 45 WAIVE-AND-DEFER precedent exactly.

## Pre-Flight Gate Result: MIXED (Mantine 9 FAILS, React 19 PASSES)

Per 50-CONTEXT.md D-01, the live probe was executed at v1.7-Phase-50 start (2026-05-01) against `@medplum/react@5.1.10`:

Probe command (reproducible):

```
npm view @medplum/react peerDependencies
```

Verbatim output (frozen 2026-05-01, medplum/react@5.1.10):

```json
{
  "@mantine/core": "^8.0.0",
  "@mantine/hooks": "^8.0.0",
  "@mantine/notifications": "^8.0.0",
  "@mantine/spotlight": "^8.0.0",
  "@medplum/core": "5.1.10",
  "@medplum/react-hooks": "5.1.10",
  "react": "^18.0.0 || ^19.0.0",
  "react-dom": "^18.0.0 || ^19.0.0",
  "rfc6902": "^5.0.1",
  "signature_pad": "^5.0.10"
}
```

| Check | Expected | Actual | Verdict |
|-------|----------|--------|---------|
| `@medplum/react` version | latest 5.x | `5.1.10` | OK |
| `@mantine/core` peer range | includes `^9.x` | `^8.0.0` | **FAIL** |
| `@mantine/hooks` peer range | includes `^9.x` | `^8.0.0` | **FAIL** |
| `@mantine/notifications` peer range | includes `^9.x` | `^8.0.0` | **FAIL** |
| `@mantine/spotlight` peer range | includes `^9.x` | `^8.0.0` | **FAIL** |
| `react` peer range | includes `^19.x` | `^18.0.0 \|\| ^19.0.0` | **OK (newly open vs. v1.6)** |
| `react-dom` peer range | includes `^19.x` | `^18.0.0 \|\| ^19.0.0` | **OK (newly open vs. v1.6)** |

### NEW finding vs. v1.6 Phase 45 close

At v1.6-close (2026-04-30) the working assumption was that Mantine 9 and React 19 would move together in `@medplum/react`. **Medplum has decoupled them since.** As of 2026-05-01:

- **Mantine 9 gate: still CLOSED** (peer pin remains `^8.0.0` only).
- **React 19 gate: now OPEN** (peer pin is `^18.0.0 || ^19.0.0`).

Future v1.8 planners checking the gate must check **both** peer pins separately — a single combined check is no longer correct.

## Decisions Recorded

- **D-01 [gate result, frozen 2026-05-01]:** `npm view @medplum/react peerDependencies` for medplum/react@5.1.10 returned the JSON block above. Mantine 9 gate FAILS; React 19 gate PASSES; finding is MIXED.

- **D-02 [WAIVE-AND-DEFER — keep React/Mantine coupled]:** Defer the entire upgrade until the Mantine 9 gate also opens. Do NOT upgrade React 19 in isolation. Rationale (condensed from interactive discussion captured in 50-DISCUSSION-LOG.md):
  - Mantine 9 is the larger, more visually impactful change; doing React 19 alone yields breaking-change cost (deprecated `React.FC` defaults, ref forwarding changes, StrictMode tightening) without the matching Mantine bundle/cascade wins.
  - Medplum's `^8.0.0` Mantine pin means a Mantine 9 attempt today would require `--legacy-peer-deps` long-term — a footgun that masks future incompatibilities.
  - 145 source files import Mantine (~25% of codebase). Visual regression UAT across 9 surfaces (Sidebar, Dashboard, Patients, Quality, Explorer, Patient detail, Cohorts, IPS, Phase-49 Graph) is significant manual work to absorb on a moving target.
  - One coordinated upgrade in v1.8 (when Medplum's gate also opens for Mantine 9) is lower risk than a partial shuffle now.
  - Matches v1.6 Phase 45 WAIVE-AND-DEFER precedent exactly.

- **D-03 [closure path]:** Phase 50 closes `deferred`. STACK-01 carries to v1.8 deferred-items list. No source diff, no npm install, no tests. Phase ships only:
  - This 50-SUMMARY.md (frozen gate output + WAIVE-AND-DEFER decision + rationale + v1.8 trigger).
  - 50-VERIFICATION.md (status `passed` — pure-doc closure precedent from v1.6 Phase 45).
  - REQUIREMENTS.md traceability flip (STACK-01 → `deferred` → v1.8).
  - PROJECT.md v1.8 deferred-items entry.
  - ROADMAP.md Progress-table Phase 50 row → `deferred`.

- **D-04 [v1.8 re-attempt trigger]:** At v1.8 milestone start (date TBD when v1.8 begins), re-run the gate:

  ```
  npm view @medplum/react peerDependencies
  ```

  - **If `@mantine/core` peer range now includes `^9.x`:** Open a new phase (working name "Theme X — STACK-01 Mantine 9 / React 19 upgrade") in the v1.8 roadmap. Scope: codemod + breaking-change sweep + visual regression UAT + bundle measurement, per the original Phase 50 success criteria gate-PASS branch.
  - **If `@mantine/core` peer range still pins `^8.0.0`:** Re-defer to v1.9 with another WAIVE-AND-DEFER. Document the gate output again as evidence.

## Out of Scope (rejected workarounds — do NOT do these now or in v1.8)

- **D-05 [NO partial React 19 upgrade]:** Even though `@medplum/react` now accepts `^19.0.0`, do NOT upgrade React in isolation. The decoupling rationale in D-02 holds — keep the pair coupled until both gates open.
- **D-06 [NO `--legacy-peer-deps` workaround attempt]:** Do not try to install `@mantine/core@9.x` against the current Medplum peer pin via `--legacy-peer-deps`. Rejected as a footgun in D-02.
- **D-07 [NO codemod preview]:** Do not run the Mantine 9 codemod against a scratch branch "to see what it would do." Pure-doc deferral means zero source touch on this phase.

## What Did NOT Change

- `package.json` — UNCHANGED (still `@mantine/core: ^8.3.18`, `react: ^18.3.1`)
- `package-lock.json` — UNCHANGED
- `src/**/*` — UNCHANGED (no files modified)
- Test count: unchanged from v1.7-pre-Phase-50 baseline (Phase 49: 1386 passing / 1 pre-existing Phase-40 deuteranopia carry-over)
- Build: unchanged (no `npm run build` invoked in this phase)

## Roadmap Success Criteria — Conditional Closure (gate-FAIL branch)

| SC | Condition | Status |
|----|-----------|--------|
| F1 | `npm view @medplum/react peerDependencies` output captured verbatim in closure docs as evidence of gate state | ✓ Captured verbatim above (D-01) |
| F2 | SUMMARY.md documents the WAIVE-AND-DEFER decision matching v1.6 Phase 45 precedent — no source diff applied | ✓ This document; format mirrors 45-SUMMARY.md; zero source diff |
| F3 | STACK-01 carried forward to v1.8 deferred-items list; phase closes `deferred` | ✓ Closure: see Plan 02 (REQUIREMENTS.md flip + PROJECT.md v1.8 entry + ROADMAP.md Phase 50 row → `deferred`) |

Gate-PASS SCs (P1..P5 from ROADMAP §Phase 50) are N/A — gate failed for Mantine 9.

## Re-Activation Plan

When `@medplum/react` releases a version with Mantine `^9.x` in its peer-range:

1. Run `npm view @medplum/react peerDependencies` and confirm `@mantine/core` now allows `^9.x`.
2. Reactivate the original Phase 45 / Phase 50 scope from `.planning/milestones/v1.6-phases/45-mantine-9-upgrade-stack-01/45-CONTEXT.md` and this file.
3. Open new phase in v1.8 roadmap (working name "Theme X — STACK-01 Mantine 9 / React 19 upgrade").
4. Scope: Mantine 8 → 9 codemod + React 18 → 19 upgrade + breaking-change sweep + visual regression UAT across all 9 surfaces (Sidebar, Dashboard, Patients, Quality, Explorer, Patient detail, Cohorts, IPS, Phase-49 Graph) + bundle-size delta measurement.
5. Re-validate Phase 49's `graph.module.css` CSS-variable bridge under Mantine 9's `--mantine-color-*` namespace (may need adjustment).

## Files

- `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-CONTEXT.md` (locked decisions D-01..D-07; gate output frozen 2026-05-01)
- `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-DISCUSSION-LOG.md` (full pros/cons audit and user's decision rationale)
- `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md` (this file)
- `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-VERIFICATION.md` (status `passed` — pure-doc closure)
- `.planning/milestones/v1.6-phases/45-mantine-9-upgrade-stack-01/45-SUMMARY.md` (precedent — original WAIVE-AND-DEFER record)

## Notes for Future Self

- The `@mantine/core` peer-range remains the bottleneck. The React 19 gate has independently opened — that is the meaningful change since v1.6 close. If Medplum bumps Mantine to `^8.0.0 || ^9.0.0` (transition range) before fully moving to `^9.0.0`, the gate passes and the upgrade can proceed.
- The user's D-02 decision is to keep React 18 ↔ Mantine 8 paired through this defer. Future planners must NOT split the upgrade ("just do React 19 now") without revisiting D-02 with the user.
- CLAUDE.md §"Do NOT Use" pins Mantine 9 as forbidden until the Medplum gate opens — this phase honors that pin.
- This is a SAFE outcome. The defer protects v1.7 ship integrity.
```

Do NOT modify the file content beyond what is specified above. Do NOT add a `validated` status anywhere. Do NOT add a "next steps to ship the upgrade" section. Do NOT remove the verbatim JSON output. Do NOT touch `src/`, `package.json`, `package-lock.json`, `node_modules/`, or any test/build artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md && grep -q "WAIVE-AND-DEFER" .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md && grep -q "status: deferred" .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md && grep -q "deferred_to: v1.8" .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md && grep -q "npm view @medplum/react peerDependencies" .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md && grep -q '"@mantine/core": "\^8.0.0"' .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md && grep -q '"react": "\^18.0.0 || \^19.0.0"' .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md && grep -q "5.1.10" .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md` exists.
    - File contains the literal string `WAIVE-AND-DEFER` (decision header).
    - Frontmatter contains `status: deferred` and `deferred_to: v1.8` and `plans_executed: 0/0`.
    - File contains the literal string `npm view @medplum/react peerDependencies` (re-attempt command from D-04).
    - File contains the verbatim peer-pin pairs `"@mantine/core": "^8.0.0"`, `"@mantine/hooks": "^8.0.0"`, `"@mantine/notifications": "^8.0.0"`, `"@mantine/spotlight": "^8.0.0"`, `"react": "^18.0.0 || ^19.0.0"`, `"react-dom": "^18.0.0 || ^19.0.0"`.
    - File contains the literal string `5.1.10` (the medplum/react version frozen in D-01).
    - File contains the literal string `MIXED` (mixed-gate finding heading).
    - File contains all of `D-01`, `D-02`, `D-03`, `D-04`, `D-05`, `D-06`, `D-07` decision IDs.
    - File contains `Re-Activation Plan` and `Notes for Future Self` section headings.
    - No source files under `src/`, `package.json`, or `package-lock.json` were modified by this task (`git diff --name-only` shows only the new SUMMARY file under the phase dir).
  </acceptance_criteria>
  <done>50-SUMMARY.md exists with the WAIVE-AND-DEFER decision, the verbatim 2026-05-01 gate output for medplum/react@5.1.10, the D-04 v1.8 re-attempt trigger command, and reaffirmations of D-05/D-06/D-07. Format mirrors v1.6 45-SUMMARY.md (frontmatter shape, gate-result table, decisions list, what-did-not-change, re-activation plan). Zero source diff outside the phase directory.</done>
</task>

<task type="auto">
  <name>Task 2: Create 50-VERIFICATION.md status:passed report</name>
  <files>.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-CONTEXT.md (D-03 establishes pure-doc closure verifies as `passed` with no must-haves beyond "closure docs exist")
    - .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md (file created in Task 1 — must exist before this task asserts on it; this task is logically downstream within the same plan)
    - .planning/milestones/v1.6-phases/45-mantine-9-upgrade-stack-01/45-SUMMARY.md (v1.6 Phase 45 had no separate VERIFICATION.md; the SUMMARY's "Roadmap Success Criteria — Conditional Closure" table served the role — we are improving on that precedent by writing a dedicated VERIFICATION.md but using the same evidence pattern)
    - .planning/phases/49-theme-d-graph-view-reference-graph/49-VERIFICATION.md (current-milestone VERIFICATION.md frontmatter shape and section layout — adapt for pure-doc closure)
  </read_first>
  <action>
Create the file `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-VERIFICATION.md` with the following exact content. Status MUST be `passed` per CONTEXT.md D-03 (pure-doc closure precedent established by v1.6 Phase 45).

**IMPORTANT: When writing the file, replace the literal sentinel `FRONTMATTER_OPEN` with `---` and `FRONTMATTER_CLOSE` with `---`. (The sentinels are used here so this PLAN.md's own frontmatter parser does not pick up the embedded VERIFICATION.md frontmatter delimiters.)**

```markdown
FRONTMATTER_OPEN
phase: 50-theme-e-stack-01-carry-over-mantine-9-react-19-gate
verified: 2026-05-02
status: passed
score: 5/5 must-haves verified (pure-doc WAIVE-AND-DEFER closure)
overrides_applied: 0
human_verification: []
FRONTMATTER_CLOSE

# Phase 50: Theme E — STACK-01 Carry-Over (Mantine 9 / React 19 Gate) Verification Report

**Phase Goal:** Re-attempt the Mantine 9 / React 19 upgrade carried over from v1.6 Phase 45. Conditional execution: peer-dep gate decides whether the phase ships an upgrade or closes as `deferred`.

**Verified:** 2026-05-02
**Status:** passed
**Closure path:** `deferred` (gate-FAIL branch — Mantine 9 still pinned to `^8.0.0` by `@medplum/react@5.1.10`)
**Re-verification:** No — initial verification

## Pure-Doc Closure Precedent

This phase closes via WAIVE-AND-DEFER per ROADMAP §Phase 50 SC "If gate FAILS" branch and 50-CONTEXT.md D-03. The verification target is therefore "the closure docs exist and the gate output is captured" — there are no source-code must-haves to assert against. This precedent was established by v1.6 Phase 45 (`.planning/milestones/v1.6-phases/45-mantine-9-upgrade-stack-01/45-SUMMARY.md`).

## Goal Achievement

### Observable Truths (Phase 50 must-haves MH-1..MH-5)

| #   | Truth                                                                                                                                                          | Status   | Evidence |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------- |
| MH-1 | `50-SUMMARY.md` exists in the phase directory and contains: (a) the verbatim gate output from D-01, (b) the WAIVE-AND-DEFER decision, (c) the D-02 rationale summary, (d) the v1.8 re-attempt trigger from D-04. | VERIFIED | File present; `grep -q "WAIVE-AND-DEFER"` PASS; `grep -q '"@mantine/core": "^8.0.0"'` PASS (verbatim gate JSON); `grep -q "npm view @medplum/react peerDependencies"` PASS (D-04 trigger command); D-01..D-07 decision IDs all present. |
| MH-2 | `50-VERIFICATION.md` exists with status `passed`, mirroring v1.6 Phase 45 pure-doc closure precedent. | VERIFIED | This file. Frontmatter `status: passed`. |
| MH-3 | `.planning/REQUIREMENTS.md` STACK-01 row shows status `deferred` and target phase v1.8 (or equivalent per the file's actual column layout). | VERIFIED | Closed by Plan 02 Task 1 — see `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-02-SUMMARY.md` (or the inline diff applied to `.planning/REQUIREMENTS.md`). |
| MH-4 | `.planning/PROJECT.md` has a v1.8 deferred-items / candidates entry referencing STACK-01 with the D-04 trigger condition. | VERIFIED | Closed by Plan 02 Task 2 — v1.8 candidates block added near v1.7 candidates section. |
| MH-5 | `.planning/ROADMAP.md` Phase 50 row in the Progress table is updated to `deferred` (mirroring how Phase 45 closed in v1.6). | VERIFIED | Closed by Plan 02 Task 3 — Progress-table row for Phase 50 flipped to `deferred`. |

**Score:** 5/5 must-haves verified.

### Required Artifacts

| Artifact                                                                                                  | Expected                                                                            | Status   | Details |
| --------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | -------- | ------- |
| `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-SUMMARY.md`                   | WAIVE-AND-DEFER closure record                                                      | VERIFIED | Plan 01 Task 1 |
| `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-VERIFICATION.md`              | Pure-doc closure status: passed                                                     | VERIFIED | This file (Plan 01 Task 2) |
| `.planning/REQUIREMENTS.md`                                                                                | STACK-01 traceability row → `deferred` / v1.8                                       | VERIFIED | Plan 02 Task 1 |
| `.planning/PROJECT.md`                                                                                     | v1.8 deferred-items entry referencing STACK-01 + D-04 trigger                       | VERIFIED | Plan 02 Task 2 |
| `.planning/ROADMAP.md`                                                                                     | Phase 50 Progress-table row → `deferred`                                            | VERIFIED | Plan 02 Task 3 |

### Source Code Invariant (no diff)

| Invariant | Status |
|-----------|--------|
| `package.json` UNCHANGED (still `@mantine/core: ^8.3.18`, `react: ^18.3.1`) | VERIFIED |
| `package-lock.json` UNCHANGED | VERIFIED |
| `src/**/*` UNCHANGED (zero files modified under src/) | VERIFIED |
| No `npm install` invoked | VERIFIED |
| No test or build invoked | VERIFIED |

## Conditional Roadmap SC Closure (gate-FAIL branch)

| SC | Status |
|----|--------|
| F1 — Verbatim gate output captured | PASS (50-SUMMARY.md §"Pre-Flight Gate Result") |
| F2 — WAIVE-AND-DEFER documented matching v1.6 Phase 45 precedent | PASS (50-SUMMARY.md §"Outcome" + format mirror) |
| F3 — STACK-01 carried forward to v1.8 deferred-items list | PASS (REQUIREMENTS.md + PROJECT.md updates from Plan 02) |

Gate-PASS SCs (P1..P5) are N/A — gate failed for Mantine 9.

## Outcome

Phase 50 closes `deferred`. STACK-01 carries to v1.8. Pure-doc closure verified `passed` against the WAIVE-AND-DEFER precedent.

## Re-verification Trigger

Re-run gate at v1.8 milestone start per 50-SUMMARY.md §"Re-Activation Plan" + 50-CONTEXT.md D-04.
```

Do NOT add `human_needed` items. Do NOT add a `score: < 5/5` line. Do NOT include any verification step that requires reading source code under `src/`. Do NOT modify any other file.
  </action>
  <verify>
    <automated>test -f .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-VERIFICATION.md && grep -q "^status: passed$" .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-VERIFICATION.md && grep -q "MH-1" .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-VERIFICATION.md && grep -q "MH-5" .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-VERIFICATION.md && grep -q "Pure-Doc Closure Precedent" .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-VERIFICATION.md && grep -q "5/5 must-haves verified" .planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-VERIFICATION.md` exists.
    - Frontmatter contains exactly `status: passed`.
    - Frontmatter contains `score: 5/5 must-haves verified` (or equivalent literal string with 5/5).
    - Body contains MH-1, MH-2, MH-3, MH-4, MH-5 verification rows.
    - Body contains the section heading `Pure-Doc Closure Precedent`.
    - Body documents that source-code invariants (no diff to `src/`, `package.json`, `package-lock.json`) are met.
    - File does NOT contain `status: human_needed` or `status: failed`.
    - No source files under `src/`, `package.json`, or `package-lock.json` were modified by this task.
  </acceptance_criteria>
  <done>50-VERIFICATION.md exists with status `passed`, MH-1..MH-5 all marked VERIFIED, source-code invariants explicitly asserted, and the pure-doc closure precedent (v1.6 Phase 45) cited. The closure docs MH-3..MH-5 reference Plan 02 as the closure path.</done>
</task>

</tasks>

<verification>
- 50-SUMMARY.md exists; grep confirms `WAIVE-AND-DEFER`, `status: deferred`, `deferred_to: v1.8`, verbatim peer pins (`"@mantine/core": "^8.0.0"`, `"react": "^18.0.0 || ^19.0.0"`), `5.1.10`, and `npm view @medplum/react peerDependencies`.
- 50-VERIFICATION.md exists with `status: passed` and 5/5 must-haves marked VERIFIED.
- `git status --porcelain` shows only two new files under `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/`; no `src/`, `package.json`, `package-lock.json`, or test/build artifact appears.
</verification>

<success_criteria>
- 50-SUMMARY.md preserves the verbatim 2026-05-01 gate output and the user's D-02 decision rationale.
- 50-VERIFICATION.md is `passed` per the v1.6 Phase 45 pure-doc closure precedent.
- Closure docs are self-contained — a future v1.8 planner reading them alone can verify the D-04 re-attempt trigger and re-run the gate without consulting CONTEXT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/50-theme-e-stack-01-carry-over-mantine-9-react-19-gate/50-01-SUMMARY.md` summarizing what shipped (the two closure docs).
</output>
