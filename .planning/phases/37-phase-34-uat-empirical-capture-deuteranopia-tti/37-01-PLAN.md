---
phase: 37
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-dashboard.png
  - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row.png
  - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-timeline.png
  - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
autonomous: false
gap_closure: false
requirements:
  - MII-EXT-11
tags: [uat, deuteranopia, screenshots, reconciliation, mii-extension]
must_haves:
  truths:
    - "3 deuteranopia PNGs exist on disk (deuteranopia-dashboard.png, deuteranopia-tab-row.png, deuteranopia-timeline.png) under .planning/phases/37-.../"
    - "Each PNG is non-empty (test -s passes — defeats Pitfall 1 silent-skip under --auto)"
    - "37-EMPIRICAL.md §1 contains a 7-row Within-family adjacent-pairs table mirroring color-design-audit.md §4b row order, with an Empirical column populated for each row"
    - "37-EMPIRICAL.md §2 contains a 14-row Cross-family adjacent-pairs table mirroring color-design-audit.md §4c row order, with an Empirical column populated for each row"
    - "37-EMPIRICAL.md §3, §4, §5, §7 carry placeholder text '[populated by Plan 37-02]' (§3) or '[populated by Plan 37-03]' (§4, §5, §7) so Plan 37-02 and Plan 37-03 own those sections — clean wave-1 file ownership"
    - "Each empirical PASS/FAIL judgement on the 2 borderline pairs (mikrobiologie↔molekulargenetik HIGH; pro↔seltene MEDIUM-HIGH) is recorded so Plan 37-03 can decide whether contingency contradicts paper"
  artifacts:
    - path: ".planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-dashboard.png"
      provides: "Full-viewport screenshot of /dashboard under Chrome DevTools deuteranopia emulation; shows 21 MII tiles (7 base + 14 extension under 'Show extension modules' partition)"
    - path: ".planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row.png"
      provides: "Full-viewport screenshot of /patients/<deterministic-id> with the extension Collapse expanded (7 base + 14 extension tabs visible)"
    - path: ".planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-timeline.png"
      provides: "Full-viewport screenshot of the same patient's ClinicalTimeline (Zeitleiste) showing per-module dot entries"
    - path: ".planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md"
      provides: "Mirror-the-paper reconciliation document with all six required sections (§1-§7); §1+§2 populated, §3 §4 §5 §7 stubbed for downstream plans"
      min_lines: 80
      contains: "## §1 Within-family"
  key_links:
    - from: ".planning/research/color-design-audit.md §4b"
      to: ".planning/phases/37-.../37-EMPIRICAL.md §1"
      via: "row-by-row: each §4b paper-pass row maps to one §1 row with an Empirical column"
      pattern: "\\| onkologie \\+ mtb \\|.*\\|.*\\| (PASS|FAIL)"
    - from: ".planning/research/color-design-audit.md §4c"
      to: ".planning/phases/37-.../37-EMPIRICAL.md §2"
      via: "row-by-row: each §4c paper-pass row maps to one §2 row with an Empirical column"
      pattern: "\\| mikrobiologie \\| molekulargenetik \\|.*\\|.*\\| (PASS|FAIL)"
    - from: "deuteranopia-tab-row.png + deuteranopia-timeline.png"
      to: "37-EMPIRICAL.md §1 + §2 Empirical column"
      via: "human visual judgement of icon+color pair distinctness under Chrome's Machado 2009 deuteranopia simulation"
      pattern: "Empirical"
---

<objective>
Capture the three deuteranopia screenshots that Plan 34-06 carried forward as `checkpoint:human-verify` gates, then write `37-EMPIRICAL.md` with §1 (7 within-family pairs) and §2 (14 cross-family pairs) reconciliation tables filled from the screenshots. Closes the empirical leg of Phase 34 MII-EXT-11.

Purpose: Phase 34 shipped 21 MII modules (7 base + 14 extension) with a 7-family palette and 21 Tabler icons. Plan 34-01's audit doc §4b/§4c made paper-only predictions of deuteranopia discriminability for all 21 adjacent pairs (7 within-family + 14 cross-family). Empirical screenshot capture was deferred to Plan 34-06, which executed under `--auto` mode and hit Pitfall 1 (auto-mode silently approved the human-verify gates without artifacts existing — see RESEARCH.md §Pitfall 1). Phase 37 redoes the capture with `<automated><verify>test -s ...</verify></automated>` post-checkpoint gates so `--auto` cannot skip the artifact production again.

Output: 3 PNG files + `37-EMPIRICAL.md` skeleton (§1+§2 filled by this plan; §3 by Plan 37-02; §4+§5+§7 by Plan 37-03; §6 cross-reference set by Plan 37-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-CONTEXT.md
@.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md
@.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-VALIDATION.md
@.planning/research/color-design-audit.md
@.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-PLAN.md
@.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md

<interfaces>
<!-- Paper predictions to mirror verbatim. Empirical column added per row. -->
<!-- Mirror order EXACTLY so a reviewer can scan paper-vs-empirical in lockstep. -->

From .planning/research/color-design-audit.md §4b (lines 175-184) — 7 within-family pairs (paper predicted 7/7 PASS):
| Pair | Palette family | Icon A | Icon B | Paper |
| onkologie + mtb | oncology (red) | IconRadioactive | IconUsersGroup | PASS |
| bildgebung + studie | imaging (cyan) | IconPhoto | IconClipboardData | PASS |
| molekulargenetik + seltene | genetics (grape) | IconDna | IconPuzzle | PASS |
| pathologie + mikrobiologie | pathology (violet) | IconMicroscope | IconVirus | PASS  (audit named IconBacteria; Plan 34-04 swapped to IconVirus)
| biobank + intensivmedizin | bioanalysis (teal) | IconTestPipe | IconBedFilled | PASS |
| kardiologie + dokument | administration (indigo) | IconHeartbeat | IconFileDescription | PASS |
| symptom + pro | patient-reported (pink) | IconMoodSmile | IconListCheck | PASS  (audit named IconQuestionnaire; Plan 34-04 swapped to IconListCheck)

From .planning/research/color-design-audit.md §4c (lines 191-208) — 14 cross-family pairs (paper predicted 14/14 PASS; 2 borderline):
| Left | Right | Palette pair | Risk | Paper |
| medikation | bildgebung | orange vs imaging-cyan | LOW | PASS |
| bildgebung | biobank | imaging-cyan vs bioanalysis-teal | MEDIUM | PASS |
| biobank | dokument | bioanalysis-teal vs administration-indigo | MEDIUM | PASS |
| dokument | intensivmedizin | administration-indigo vs bioanalysis-teal | MEDIUM | PASS |
| intensivmedizin | kardiologie | bioanalysis-teal vs administration-indigo | MEDIUM | PASS |
| kardiologie | mikrobiologie | administration-indigo vs pathology-violet | MEDIUM | PASS |
| mikrobiologie | molekulargenetik | pathology-violet vs genetics-grape | HIGH | PASS (icon-distinct only) |   ← borderline #1
| molekulargenetik | mtb | genetics-grape vs oncology-dark-red | LOW | PASS |
| mtb | onkologie | within oncology family | N/A | PASS |
| onkologie | pathologie | oncology-red vs pathology-violet | MEDIUM | PASS |
| pathologie | pro | pathology-violet vs patient-reported-pink | MEDIUM | PASS |
| pro | seltene | patient-reported-pink vs genetics-grape | MEDIUM-HIGH | PASS |   ← borderline #2
| seltene | studie | genetics-grape vs imaging-cyan | LOW | PASS |
| studie | symptom | imaging-cyan vs patient-reported-pink | LOW | PASS |
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Capture 3 deuteranopia screenshots via Chrome DevTools Rendering panel</name>
  <files>
    .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-dashboard.png
    .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row.png
    .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-timeline.png
  </files>
  <read_first>
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-CONTEXT.md (D-02, D-03 — filenames + Chrome DevTools procedure)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md §Capture Procedure (deuteranopia) — Chrome DevTools step-by-step (lines 300-321)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md §Pitfall 1 — `test -s` post-checkpoint gate rationale
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md §6a — original capture worklist
  </read_first>
  <what-built>
    Phase 34 has shipped 21 MII modules (7 base + 14 extension), 7 custom MantineColorsTuples, 21 Tabler icons rendered at 3 sites (ClinicalTimeline dot, MiiModuleTabs pill, DashboardPage tile + Drawer). The codebase is at current `main` HEAD (post-Phase-36 lazy-load) — for the visual capture this is fine because Phase 36 only changed bundle-loading mechanics, not the rendered UI. `npm run dev` serves the same component tree as Phase 34 HEAD for the dashboard, tab row, and timeline. The Blaze server at localhost:8080 has 975 Synthea patients loaded.

    The DevTools "Emulate vision deficiencies → deuteranopia" toggle (Machado 2009 simulation matrix) and the Cmd+Shift+P → "Capture full size screenshot" command are the human-only browser actions that produce the 3 PNGs. Claude cannot drive the Rendering panel.
  </what-built>
  <how-to-verify>
    Executor: prepare the dev server FIRST, then pause for human capture.

    Executor preparation steps (auto, before pausing):
    1. `git status` — confirm working tree is clean on `main`.
    2. `npm run dev` (run in background) — wait for "Local: http://localhost:5173/" line.
    3. Pick a deterministic Synthea patient family-name-first via:
       ```
       curl -s "http://localhost:8080/fhir/Patient?_summary=true&_count=1000&_elements=id,name" \
         | python3 -c "
       import json, sys
       data = json.load(sys.stdin)
       def famof(p):
           n = p.get('name') or [{}]
           return n[0].get('family') or ''
       items = [(famof(e['resource']), e['resource'].get('id'))
                for e in data.get('entry', [])]
       items_with_family = [t for t in items if t[0]]
       items_with_family.sort(key=lambda t: t[0].lower())
       fam, pid = items_with_family[0]
       print(f'patient_id={pid} family={fam}')"
       ```
       Print the result so the human sees `patient_id=<X> family=<Y>` (per RESEARCH §Code Examples lines 372-392). Do NOT hard-code a known ID — Synthea bundle may have re-imported.
    4. Print this resume-block to the user (verbatim, not a paraphrase):

       ```
       === HUMAN CAPTURE REQUIRED — DEUTERANOPIA SCREENSHOTS ===

       Vite dev server is up at http://localhost:5173/. Deterministic patient: <patient_id> (family <family>).

       Steps (Chrome on macOS):
         1. Open Chrome (Incognito recommended — fewer extensions = cleaner screenshot)
         2. Open DevTools (Cmd+Option+I)
         3. DevTools three-dot menu (top-right) > More tools > Rendering
         4. Rendering panel > Emulate vision deficiencies > select "deuteranopia"
            (the viewport color shifts immediately)
         5. Capture screenshot 1:
            - Navigate to http://localhost:5173/dashboard
            - Wait for the MII tile grid to settle (21 tiles: 7 base + 14 extension under "Show extension modules" toggle — click the toggle if needed)
            - Cmd+Shift+P > type "screenshot" > select "Capture full size screenshot"
              (NOT "Capture screenshot" — full-size includes content below the fold)
            - Save as: .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-dashboard.png
         6. Capture screenshot 2:
            - Navigate to http://localhost:5173/patients/<patient_id>
            - Click the "Show extension modules (14)" Collapse toggle to expose all 21 tab pills
            - Cmd+Shift+P > "Capture full size screenshot"
            - Save as: .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row.png
         7. Capture screenshot 3 (same patient page):
            - Click the "Zeitleiste" (Timeline) tab
            - Wait for at least a few timeline entries to render (Synthea data may be sparse for extension modules — base-module entries are sufficient per RESEARCH Open Question #2)
            - Cmd+Shift+P > "Capture full size screenshot"
            - Save as: .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-timeline.png
         8. Reset DevTools: Rendering panel > Emulate vision deficiencies > "No emulation"
         9. Reply "approved" once all 3 PNGs exist on disk at the paths above

       === END HUMAN CAPTURE BLOCK ===
       ```
    5. Pause executor (this is the `checkpoint:human-verify` boundary).

    On resume (after user types "approved"):
    6. Run the post-checkpoint `<automated><verify>` block (see <verify> tag below) — this MUST pass before proceeding. If any of the 3 files is missing or empty, the verify exits non-zero and the executor halts (defeats Pitfall 1).
    7. Stop the background dev server: `pkill -f "vite.*--config" 2>/dev/null || true` (or note the PID from step 2 and `kill <pid>`).
    8. Commit with message:
       ```
       docs(37-01): capture 3 deuteranopia screenshots (Chrome DevTools Rendering panel)
       ```
       Files: the 3 PNGs.
  </how-to-verify>
  <action>See <how-to-verify> above. Executor automates dev server + deterministic-patient probe + resume-block printing; the human operates Chrome DevTools and saves the 3 PNGs to the exact paths in CONTEXT D-02; the executor commits.</action>
  <verify>
    <automated>test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-dashboard.png && test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row.png && test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-timeline.png</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist and are non-empty: `test -s deuteranopia-dashboard.png && test -s deuteranopia-tab-row.png && test -s deuteranopia-timeline.png` exits 0.
    - Each PNG is at least 50,000 bytes (rules out a corrupted save / placeholder): `[ $(wc -c < deuteranopia-dashboard.png) -gt 50000 ] && [ $(wc -c < deuteranopia-tab-row.png) -gt 50000 ] && [ $(wc -c < deuteranopia-timeline.png) -gt 50000 ]`.
    - File magic confirms PNG: `file .planning/phases/37-.../deuteranopia-*.png | grep -c "PNG image data"` returns 3.
    - The 3 PNGs are committed (not just on disk): `git ls-files .planning/phases/37-.../deuteranopia-*.png | wc -l` returns 3.
  </acceptance_criteria>
  <done>
    - 3 PNG files exist on disk at the canonical filenames per CONTEXT D-02
    - `test -s` and `[ $(wc -c < ...) -gt 50000 ]` both pass for each PNG
    - PNGs committed in a single commit titled `docs(37-01): capture 3 deuteranopia screenshots (Chrome DevTools Rendering panel)`
    - Background `npm run dev` process stopped (no port 5173 listener after task ends)
  </done>
  <resume-signal>Type "approved" once all 3 PNGs exist on disk at the canonical paths. If you cannot capture (Chrome misbehaving, Blaze offline, etc.), reply with the blocker and DO NOT type "approved".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 37-EMPIRICAL.md with §1 + §2 reconciliation tables and §3/§4/§5/§7 placeholder stubs</name>
  <files>
    .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
  </files>
  <read_first>
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-CONTEXT.md (D-07 — full §1-§6 schema; D-09 — borderline pair handling)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md §Reconciliation Table Skeleton (lines 437-462)
    - .planning/research/color-design-audit.md §4b (lines 171-185 — 7 within-family rows in canonical order)
    - .planning/research/color-design-audit.md §4c (lines 187-208 — 14 cross-family rows in canonical order)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-dashboard.png (visual reference for §1+§2 judgements)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row.png (visual reference)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-timeline.png (visual reference)
  </read_first>
  <action>
Write `.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md` with the EXACT structure below. The executor renders the §1 + §2 tables from the audit doc rows verbatim and asks the human (via inline prompts visible in the file before commit) to record PASS/FAIL judgements for each pair based on the 3 PNGs. Sections §3, §4, §5, §7 are written as placeholder stubs naming the downstream owner plan.

**File structure (write verbatim, fill empirical column from human inspection):**

```markdown
---  <!-- literal YAML frontmatter delimiter; preserve as-is when writing 37-EMPIRICAL.md -->
phase: 37
plan: 01
authored: 2026-04-26
status: in-progress
sections_owned_by:
  "§1 Within-family pairs": "Plan 37-01 (this plan)"
  "§2 Cross-family pairs": "Plan 37-01 (this plan)"
  "§3 TTI": "Plan 37-02"
  "§4 Contradictions": "Plan 37-03"
  "§5 Contingency commits triggered": "Plan 37-03"
  "§6 34-06-UAT.md update": "Plan 37-03"
  "§7 Future hardening": "Plan 37-03 (optional, brief, no commitment)"
---  <!-- literal YAML frontmatter delimiter; preserve as-is when writing 37-EMPIRICAL.md -->

# Phase 37 — Empirical Reconciliation

**Authored:** 2026-04-26 (Plan 37-01 starts; Plans 37-02 and 37-03 fill remaining sections)
**Status:** Plan 37-01 fills §1 + §2 from the 3 deuteranopia PNGs; downstream plans own the rest.
**Scope:** Reconcile color-design-audit.md §4b/§4c paper predictions against empirical Chrome DevTools deuteranopia screenshots. Capture TTI before/after at commits `048e99c` and `a7e4544`. Fire contingency icon swap iff a HIGH or MEDIUM-HIGH borderline pair empirically fails.

**Source artifacts:**
- `deuteranopia-dashboard.png` — `/dashboard` 21-tile grid under deuteranopia
- `deuteranopia-tab-row.png` — `/patients/<id>` with extension Collapse expanded
- `deuteranopia-timeline.png` — same patient's ClinicalTimeline
- `tti-snapshot.json` (written by Plan 37-02)

**Methodology:** Chrome DevTools Rendering panel "Emulate vision deficiencies → deuteranopia" (Machado 2009 simulation matrix). Captured 2026-04-26.

---  <!-- literal YAML frontmatter delimiter; preserve as-is when writing 37-EMPIRICAL.md -->

## §1 Within-family adjacent pairs (mirrors color-design-audit.md §4b)

7 pairs — same palette family, two different icons. Paper predicted 7/7 PASS via icon-shape distinctness alone (no color help by design).

| # | Pair | Palette family | Icon A | Icon B | Paper | Empirical | Agree? | Notes |
|---|------|----------------|--------|--------|-------|-----------|--------|-------|
| 1 | onkologie + mtb | oncology (red) | IconRadioactive | IconUsersGroup | PASS | <PASS|FAIL> | <yes|no> | Radial trefoil vs clustered circles |
| 2 | bildgebung + studie | imaging (cyan) | IconPhoto | IconClipboardData | PASS | <PASS|FAIL> | <yes|no> | Frame+lens vs clipboard-with-lines |
| 3 | molekulargenetik + seltene | genetics (grape) | IconDna | IconPuzzle | PASS | <PASS|FAIL> | <yes|no> | DNA helix vs interlocking tiles |
| 4 | pathologie + mikrobiologie | pathology (violet) | IconMicroscope | IconVirus | PASS | <PASS|FAIL> | <yes|no> | Tall apparatus vs polyhedral cluster (audit named IconBacteria; Plan 34-04 swapped to IconVirus per package availability — see RESEARCH §Pitfall 5) |
| 5 | biobank + intensivmedizin | bioanalysis (teal) | IconTestPipe | IconBedFilled | PASS | <PASS|FAIL> | <yes|no> | Vertical cylinder vs horizontal rectangle |
| 6 | kardiologie + dokument | administration (indigo) | IconHeartbeat | IconFileDescription | PASS | <PASS|FAIL> | <yes|no> | Waveform vs rectangle-with-lines |
| 7 | symptom + pro | patient-reported (pink) | IconMoodSmile | IconListCheck | PASS | <PASS|FAIL> | <yes|no> | Circle-face vs linear checklist (audit named IconQuestionnaire; Plan 34-04 swapped to IconListCheck — see RESEARCH §Pitfall 5) |

**Within-family summary:** Empirical agreement <X>/7 pairs. (Filled after column populated.)

---  <!-- literal YAML frontmatter delimiter; preserve as-is when writing 37-EMPIRICAL.md -->

## §2 Cross-family adjacent tabs (mirrors color-design-audit.md §4c)

14 pairs across the final tab order. Paper predicted 14/14 PASS. Two borderline pairs flagged for empirical attention:
- **mikrobiologie ↔ molekulargenetik** — HIGH color-collapse risk (violet vs grape both shift bluish-purple under deuteranopia)
- **pro ↔ seltene** — MEDIUM-HIGH color-collapse risk (dark pink vs grape both reddish-purple family)

| # | Left module | Right module | Palette pair | Risk | Paper | Empirical | Agree? |
|---|-------------|--------------|--------------|------|-------|-----------|--------|
| 1 | medikation | bildgebung | orange vs imaging-cyan | LOW | PASS | <PASS|FAIL> | <yes|no> |
| 2 | bildgebung | biobank | imaging-cyan vs bioanalysis-teal | MEDIUM | PASS | <PASS|FAIL> | <yes|no> |
| 3 | biobank | dokument | bioanalysis-teal vs administration-indigo | MEDIUM | PASS | <PASS|FAIL> | <yes|no> |
| 4 | dokument | intensivmedizin | administration-indigo vs bioanalysis-teal | MEDIUM | PASS | <PASS|FAIL> | <yes|no> |
| 5 | intensivmedizin | kardiologie | bioanalysis-teal vs administration-indigo | MEDIUM | PASS | <PASS|FAIL> | <yes|no> |
| 6 | kardiologie | mikrobiologie | administration-indigo vs pathology-violet | MEDIUM | PASS | <PASS|FAIL> | <yes|no> |
| 7 | mikrobiologie | molekulargenetik | pathology-violet vs genetics-grape | **HIGH** | PASS | <PASS|FAIL> | <yes|no> |
| 8 | molekulargenetik | mtb | genetics-grape vs oncology-dark-red | LOW | PASS | <PASS|FAIL> | <yes|no> |
| 9 | mtb | onkologie | within oncology family | N/A | PASS | <PASS|FAIL> | <yes|no> |
| 10 | onkologie | pathologie | oncology-red vs pathology-violet | MEDIUM | PASS | <PASS|FAIL> | <yes|no> |
| 11 | pathologie | pro | pathology-violet vs patient-reported-pink | MEDIUM | PASS | <PASS|FAIL> | <yes|no> |
| 12 | pro | seltene | patient-reported-pink vs genetics-grape | **MEDIUM-HIGH** | PASS | <PASS|FAIL> | <yes|no> |
| 13 | seltene | studie | genetics-grape vs imaging-cyan | LOW | PASS | <PASS|FAIL> | <yes|no> |
| 14 | studie | symptom | imaging-cyan vs patient-reported-pink | LOW | PASS | <PASS|FAIL> | <yes|no> |

**Cross-family summary:** Empirical agreement <X>/14 pairs. (Filled after column populated.)

**HIGH/MEDIUM-HIGH attention pairs (drives Plan 37-03 contingency decision):**
- Pair #7 (mikrobiologie ↔ molekulargenetik): empirical <PASS|FAIL> → if FAIL, Plan 37-03 considers icon swap candidates (NB: per RESEARCH §Pitfall 5, the audit's named swap target IconVirus is ALREADY shipped; alternative candidates need a Tabler 3.41.1 availability probe).
- Pair #12 (pro ↔ seltene): empirical <PASS|FAIL> → same logic for IconListCheck.

---  <!-- literal YAML frontmatter delimiter; preserve as-is when writing 37-EMPIRICAL.md -->

## §3 TTI before/after

**Owner:** Plan 37-02 (Wave 1, parallel with this plan).
**Status:** [populated by Plan 37-02 from `tti-snapshot.json`]

Plan 37-02 captures TTI at commits `048e99c` (Phase 33 tail, baseline) and `a7e4544` (Phase 34 HEAD, post_phase) using the twin-worktree pattern (RESEARCH §Pattern 1). Three runs per checkout, median value, no throttling. Dual gate: PASS iff `delta_ms ≤ 100` AND `delta_pct ≤ 10%` (CONTEXT D-08).

| Metric | Value | Source |
|--------|-------|--------|
| baseline_ms (median of 3 runs at 048e99c) | [populated by Plan 37-02] | tti-snapshot.json.baseline_ms |
| post_phase_ms (median of 3 runs at a7e4544) | [populated by Plan 37-02] | tti-snapshot.json.post_phase_ms |
| delta_ms | [populated by Plan 37-02] | post_phase_ms - baseline_ms |
| delta_pct | [populated by Plan 37-02] | (delta_ms / baseline_ms) * 100 |
| dual-gate verdict (D-08) | [populated by Plan 37-02] | PASS iff delta_ms ≤ 100 AND delta_pct ≤ 10 |

If FAIL, §3 must add a culprit-attribution paragraph referencing chunks in `.planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html` (CONTEXT D-08 names this as the post-Phase-36 chunk treemap that represents the actual current state).

---  <!-- literal YAML frontmatter delimiter; preserve as-is when writing 37-EMPIRICAL.md -->

## §4 Contradictions

**Owner:** Plan 37-03 (Wave 2).
**Status:** [populated by Plan 37-03]

Plan 37-03 reads §1 + §2 above. For each row where `Agree? = no` (Paper says PASS but Empirical says FAIL, or vice versa), Plan 37-03 enumerates the contradiction here. If 0 rows disagree, Plan 37-03 writes "0 contradictions — paper analysis confirmed empirically."

LOW-risk pair contradictions are informational only (CONTEXT D-10). HIGH or MEDIUM-HIGH borderline pair contradictions trigger §5 contingency processing.

---  <!-- literal YAML frontmatter delimiter; preserve as-is when writing 37-EMPIRICAL.md -->

## §5 Contingency commits triggered

**Owner:** Plan 37-03 (Wave 2).
**Status:** [populated by Plan 37-03]

Plan 37-03 first runs a Tabler 3.41.1 availability probe (RESEARCH §Pitfall 5 — IconBacteria/IconQuestionnaire/IconFileSignature are NOT exported; IconVirus/IconListCheck/IconFileCertificate are ALREADY shipped) to identify candidate alternative icons if a HIGH or MEDIUM-HIGH borderline pair fails empirically. If no shape-superior candidate is unambiguously available in 3.41.1, Plan 37-03 writes "no swap fired — no superior candidate available" instead of forcing a swap. Otherwise: swap the icon in `src/utils/mii-icons.ts` + `src/utils/mii-modules.ts`, re-capture the affected screenshot(s) (preserving pre-swap PNGs as `*-pre-swap.png` per CONTEXT D-09), and append a `## Revision (2026-04-26, Phase 37)` note to `color-design-audit.md` §4d.

If §4 has 0 contradictions, this section reads: "None — no contingency triggered."

---  <!-- literal YAML frontmatter delimiter; preserve as-is when writing 37-EMPIRICAL.md -->

## §6 34-06-UAT.md update

**Owner:** Plan 37-03 (Wave 2).
**Status:** [populated by Plan 37-03]

Plan 37-03 updates `.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md` per CONTEXT D-11:
- §1d filled with the §4 contradictions list (or "0 contradictions").
- §2 TTI table populated from §3 above.
- §6 closure checklist marks deuteranopia + TTI items DONE.

This section records the commit SHA(s) that landed the §1d + §2 + §6 fills.

---  <!-- literal YAML frontmatter delimiter; preserve as-is when writing 37-EMPIRICAL.md -->

## §7 Future hardening (optional)

**Owner:** Plan 37-03 (Wave 2). Optional per CONTEXT Discretion bullet 2.
**Status:** [populated by Plan 37-03 — recommended brief, no commitment]

Two candidates noted for v1.6+ backlog (no commitment):
- (a) Headless deuteranopia simulation in Vitest using a Brettel/Machado JS implementation against rendered DOM colors — would catch icon+color regressions on every PR.
- (b) ΔE2000 perceptual-distance lint over `MII_MODULES` palette adjacency — fails CI if any two adjacent tabs fall below threshold.
```

After writing the file, INSPECT the 3 PNGs (load via the Read tool — Claude Code is multimodal and renders PNGs visually) and FILL each row's Empirical column with PASS or FAIL based on whether the icon+color pair is visually disambiguable in the screenshot. Specific judgement criteria:
- For §1 rows: look at the tab row PNG (and the Dashboard PNG for the tile pairs) — can a viewer tell the two same-family modules apart by icon shape alone? If yes → PASS. If the icons read as visually similar at the rendered size → FAIL.
- For §2 rows: look at the tab row PNG — can a viewer tell the two adjacent tabs apart by icon shape AND/OR color? If yes via either channel → PASS. If both channels collapse → FAIL.
- For pair #7 (mikrobiologie ↔ molekulargenetik) and pair #12 (pro ↔ seltene): apply EXTRA scrutiny — these are the borderline pairs that drive contingency decisions.
- The Agree? column is computed: `Agree = (Paper == Empirical)` → "yes" if both PASS or both FAIL; "no" otherwise.

After filling all 21 Empirical + Agree? cells, populate the two summary lines ("X/7 pairs", "X/14 pairs"). Replace the `<X>` placeholders in the HIGH/MEDIUM-HIGH attention block at the bottom of §2 with the actual PASS/FAIL judgement for pairs #7 and #12.

Commit with message:
```
docs(37-01): write 37-EMPIRICAL.md with §1+§2 reconciliation tables filled from screenshots
```
  </action>
  <verify>
    <automated>test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && grep -q "^## §1 Within-family adjacent pairs" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && grep -q "^## §2 Cross-family adjacent tabs" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && grep -q "^## §3 TTI" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && grep -q "^## §4 Contradictions" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && grep -q "^## §5 Contingency commits triggered" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && grep -q "^## §6 34-06-UAT.md update" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && [ $(awk '/^## §1/,/^## §2/{ if (/^\| [0-9]/) c++ } END{print c+0}' .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md) -ge 7 ] && [ $(awk '/^## §2/,/^## §3/{ if (/^\| [0-9]+ \|/) c++ } END{print c+0}' .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md) -ge 14 ] && ! grep -q "<PASS|FAIL>" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists and non-empty: `test -s .planning/phases/37-.../37-EMPIRICAL.md`
    - All 6 mandatory CONTEXT D-07 sections present: `grep -c "^## §[1-6]" 37-EMPIRICAL.md` returns 6
    - §1 has 7 data rows (one per within-family pair): `awk '/^## §1/,/^## §2/{ if (/^\| [0-9] \|/) c++ } END{print c+0}'` returns 7
    - §2 has 14 data rows (one per cross-family pair): `awk '/^## §2/,/^## §3/{ if (/^\| [0-9]+ \|/) c++ } END{print c+0}'` returns 14
    - No `<PASS|FAIL>` placeholders remain (every Empirical column populated): `grep -c "<PASS|FAIL>" 37-EMPIRICAL.md` returns 0
    - §3 §4 §5 §6 §7 carry "[populated by Plan 37-02]" or "[populated by Plan 37-03]" placeholders so downstream owners know to fill: `grep -c "\[populated by Plan 37-0[23]\]" 37-EMPIRICAL.md` returns ≥ 4
    - Pairs #7 and #12 (the HIGH and MEDIUM-HIGH borderline pairs) have empirical judgements recorded: `grep -E "mikrobiologie \| molekulargenetik" 37-EMPIRICAL.md | grep -Ev "<PASS\\|FAIL>" | wc -l` returns ≥ 1; same for `pro \| seltene`.
    - File committed: `git ls-files .planning/phases/37-.../37-EMPIRICAL.md` returns the path.
  </acceptance_criteria>
  <done>
    - 37-EMPIRICAL.md exists with all 6 mandatory CONTEXT D-07 sections (§1-§6) and an optional §7 stub
    - §1 + §2 fully populated with empirical PASS/FAIL judgements for all 21 pairs
    - §3, §4, §5, §6, §7 carry explicit "[populated by Plan 37-02]" / "[populated by Plan 37-03]" placeholders so wave-2 owners know what to fill
    - Within-family summary line ("X/7 pairs") and cross-family summary line ("X/14 pairs") populated
    - Borderline pair attention block at bottom of §2 populated with PASS/FAIL for pairs #7 + #12
    - File committed in a single commit titled `docs(37-01): write 37-EMPIRICAL.md with §1+§2 reconciliation tables filled from screenshots`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new runtime trust boundary introduced. Screenshots are local artifacts captured against `localhost:5173` (vite dev server) reading from `localhost:8080` (Blaze with Synthea synthetic data). No production servers, no real PHI.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-16 (inherited) | Information Disclosure | Synthetic PHI in screenshots committed under `.planning/phases/37-.../` | accept | Capture against Synthea synthetic patients only (Blaze instance loads synthetichealth/synthea data — verified via identifier-system probe at research time). Not real PHI. Phase 34 already accepted this disposition. |
| T-37-01 (new) | Information Disclosure | Risk of misconfigured local Blaze pointing at a production / non-Synthea source, leaking real PHI in committed PNGs | mitigate | Executor printing the deterministic-patient block (Task 1, step 3) shows `family=<Y>` to the user before capture; user verifies the family name is Synthea-shaped (e.g. Abbott, Smith — common synthetic surnames) and NOT a real-patient identifier before saving the PNGs. If the resolved patient family looks suspicious, the user aborts the capture and reconfigures `settings.yaml` to point at the Synthea fixture. |
</threat_model>

<verification>
- 3 deuteranopia PNGs exist on disk + are non-empty + are committed
- 37-EMPIRICAL.md exists with all 6 mandatory sections per CONTEXT D-07
- §1 has 7 data rows mirroring color-design-audit.md §4b verbatim row order
- §2 has 14 data rows mirroring color-design-audit.md §4c verbatim row order
- Every Empirical column cell in §1 + §2 is populated with PASS or FAIL (no `<PASS|FAIL>` placeholders remain)
- §3, §4, §5, §6, §7 carry explicit "[populated by Plan 37-XX]" placeholders for downstream plans
- Both borderline pairs (mikrobiologie↔molekulargenetik HIGH; pro↔seltene MEDIUM-HIGH) have empirical PASS/FAIL judgements recorded — these drive Plan 37-03's contingency decision
</verification>

<success_criteria>
Plan 37-01 is closeable when:
- All 3 PNGs exist, non-empty, committed; PNG magic confirmed
- 37-EMPIRICAL.md exists with §1 + §2 fully populated and §3/§4/§5/§6/§7 stubbed for downstream plans
- The two borderline pairs have empirical judgements that Plan 37-03 can act on
- `npm test` baseline preserved (no test changes in this plan; verify by running `npx vitest run --reporter=dot --no-coverage` once at end if convenient — should remain at 1059+ passing)
</success_criteria>

<output>
After completion, create `.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-01-SUMMARY.md` documenting:
- 3 PNG file sizes (bytes) for the audit trail
- The deterministic-patient ID + family name used in the tab-row + timeline screenshots
- The 21 pair empirical judgements summarized as a single line: "Within-family X/7 PASS; Cross-family Y/14 PASS"
- Specific empirical results for borderline pairs #7 and #12 so Plan 37-03 can plan its contingency work
- Any deviations from the plan (e.g., if a screenshot had to be re-taken because of a Chrome glitch)
</output>
