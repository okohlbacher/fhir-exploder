---
phase: 37
plan: 03
type: execute
wave: 2
depends_on:
  - "37-01"
  - "37-02"
files_modified:
  - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
  - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md
  - .planning/research/color-design-audit.md
  - src/utils/mii-icons.ts
  - src/utils/mii-modules.ts
  - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row-pre-swap.png
  - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-timeline-pre-swap.png
  - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row.png
  - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-timeline.png
autonomous: false
gap_closure: false
requirements:
  - MII-EXT-11
  - D-22
tags: [uat, contingency, icon-swap, phase-34-closure, mii-extension]
must_haves:
  truths:
    - "37-EMPIRICAL.md §3 populated from tti-snapshot.json (baseline_ms, post_phase_ms, delta_ms, delta_pct, dual-gate verdict)"
    - "37-EMPIRICAL.md §4 lists every Paper-vs-Empirical disagreement from §1+§2 (or '0 contradictions — paper analysis confirmed empirically')"
    - "37-EMPIRICAL.md §5 records contingency decision: either swap commits + audit-doc revision note (if HIGH/MEDIUM-HIGH pair failed AND a superior alternative icon was found in @tabler/icons-react@3.41.1 per RESEARCH §Pitfall 5), or 'no swap fired — no superior candidate available', or 'None — no contingency triggered' (if all borderline pairs PASS empirically)"
    - "37-EMPIRICAL.md §6 cross-references the commit SHA(s) that updated 34-06-UAT.md §1d/§2/§6"
    - "37-EMPIRICAL.md §7 brief future-hardening section authored (RESEARCH Open Question #3 — 4-line stub naming v1.6+ headless deuteranopia simulation + ΔE2000 lint candidates, no commitment)"
    - "34-06-UAT.md §1d filled (no `(This subsection is populated by the human verifier...)` placeholder remains)"
    - "34-06-UAT.md §2 TTI table no longer has `⏳ pending` cells in the value column"
    - "34-06-UAT.md §5 closure checklist: deuteranopia + TTI items marked done with [x]"
    - "If a contingency icon swap fired: color-design-audit.md §4d carries a `## Revision (2026-04-26, Phase 37)` note; affected pre-swap PNG renamed to `deuteranopia-{tab-row|timeline}-pre-swap.png` and post-swap takes the canonical filename; mii-modules + mii-icons consistent (icon string in MII_MODULES has a matching ICON_MAP entry); npm test stays green"
    - "If no swap: src/utils/mii-icons.ts and src/utils/mii-modules.ts UNCHANGED; only doc files modified"
  artifacts:
    - path: ".planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md"
      provides: "Final reconciliation document with §3-§7 populated; §1+§2 inherited from Plan 37-01"
      min_lines: 100
      contains: "## §3 TTI"
    - path: ".planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md"
      provides: "Phase 34 closure UAT — §1d, §2 TTI table, §5 sign-off updated with empirical results"
      contains: "0 contradictions"
  key_links:
    - from: ".planning/phases/37-.../37-EMPIRICAL.md §1 + §2 Empirical column"
      to: ".planning/phases/37-.../37-EMPIRICAL.md §4 Contradictions"
      via: "rows where Agree? = no aggregate into §4"
      pattern: "Contradictions"
    - from: "tti-snapshot.json"
      to: "37-EMPIRICAL.md §3"
      via: "fields baseline_ms, post_phase_ms, delta_ms, delta_pct, verdict transcribed"
      pattern: "delta_ms.*[0-9]+"
    - from: "37-EMPIRICAL.md §4 + §5"
      to: "34-06-UAT.md §1d + §6"
      via: "Phase-34 closure update mirrors Phase-37 reconciliation"
      pattern: "1d|§6"
---

<objective>
Wave 2 closure plan. Reads Plan 37-01's §1+§2 reconciliation tables and Plan 37-02's `tti-snapshot.json`, then:
1. Populates 37-EMPIRICAL.md §3 (TTI summary) + §4 (Contradictions) + §5 (Contingency) + §6 (cross-ref) + §7 (Future hardening stub).
2. If a HIGH or MEDIUM-HIGH borderline pair failed empirically, runs a Tabler 3.41.1 availability probe (per RESEARCH §Pitfall 5) and either fires an icon swap or documents "no superior candidate available."
3. Updates 34-06-UAT.md §1d / §2 / §5 closure checklist per CONTEXT D-11 — flips Phase 34's UAT from CONDITIONALLY COMPLETE to fully closed.

Purpose: Close the human-gated work that Plan 34-06 carried forward. CONTEXT D-09 names a contingency swap path that RESEARCH §Pitfall 5 has revealed is unfireable as written (the named primaries `IconBacteria` and `IconQuestionnaire` are NOT exported by `@tabler/icons-react@3.41.1`; the named fallbacks `IconVirus` and `IconListCheck` are ALREADY shipped). This plan handles three branches:

- Branch A — All 21 pairs PASS empirically: §4 = "0 contradictions"; §5 = "None — no contingency triggered"; no source code edits; only doc files modified. **Plan is autonomous** in this branch (no second human checkpoint needed).
- Branch B — A LOW-risk pair fails: §4 logs the disagreement; §5 = "None — LOW-risk failure is informational only per CONTEXT D-10"; no source code edits.
- Branch C — A HIGH or MEDIUM-HIGH borderline pair fails: §5 runs the Tabler probe → either C1 (workable alternative found) or C2 (no superior candidate). C1 fires the swap + re-capture (introduces a sub-checkpoint for re-capture, so this plan is `autonomous: false`). C2 documents the failure honestly.

Output: 37-EMPIRICAL.md fully populated, 34-06-UAT.md closure cells filled, optionally an icon-swap commit + audit-doc revision note + re-captured screenshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-CONTEXT.md
@.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md
@.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-VALIDATION.md
@.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
@.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json
@.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-01-SUMMARY.md
@.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-02-SUMMARY.md
@.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md
@.planning/research/color-design-audit.md
@src/utils/mii-icons.ts
@src/utils/mii-modules.ts

<interfaces>
<!-- Inputs for branch detection. Read at plan start to determine path. -->

§1+§2 borderline-pair empirical results (extract from 37-EMPIRICAL.md after Plan 37-01 commits):
- Pair #7 (mikrobiologie ↔ molekulargenetik) — HIGH risk — empirical = ?
- Pair #12 (pro ↔ seltene) — MEDIUM-HIGH risk — empirical = ?

Branch decision matrix:
  All 21 PASS empirically                     → Branch A (no contingency)
  Only LOW-risk pair(s) FAIL                  → Branch B (informational only per D-10)
  Pair #7 FAIL or pair #12 FAIL (or both)     → Branch C (probe + swap-or-document)

Tabler 3.41.1 availability probe (run in Branch C only):
  node -e "const I = require('@tabler/icons-react');
   const candidates = ['IconBiohazard','IconParasite','IconRadar','IconScan','IconClipboardList','IconChecklist','IconBacteria','IconQuestionnaire'];
   for (const n of candidates) console.log(n, n in I ? 'AVAILABLE' : 'absent');"

ICON_MAP edit surface (src/utils/mii-icons.ts):
  - Add named import to lines 23-46
  - Add ICON_MAP entry between lines 49-85
  - Update inline comment for the affected module (line 72-75 or 81-84)

MII_MODULES edit surface (src/utils/mii-modules.ts):
  - line 258 (mikrobiologie): icon: 'IconVirus' → icon: '<new>'
  - line 315 (pro): icon: 'IconListCheck' → icon: '<new>'

34-06-UAT.md edit surface:
  - §1d: replace placeholder paragraph with §4 contradictions content
  - §2 TTI table: replace `⏳ pending` cells with TTI numbers from tti-snapshot.json
  - §5 sign-off: tick deuteranopia + TTI checkboxes; reference Phase 37 commit SHAs

color-design-audit.md edit surface (Branch C1 only):
  - Append after §4d (line ~218): `## Revision (2026-04-26, Phase 37)\n\nEmpirical capture under Phase 37 found <pair> failed... Replaced <old icon> with <new icon> in MII_MODULES; rationale: ...`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate 37-EMPIRICAL.md §3 (TTI) + §4 (Contradictions) + §5 stub + §6 placeholder + §7</name>
  <files>
    .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
  </files>
  <read_first>
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md (current state from Plan 37-01)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json (Plan 37-02 output)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-CONTEXT.md (D-07 §3-§7 schema; D-08 dual gate; D-09 borderline rule; D-10 LOW-risk rule)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md §Open Questions #3 (Future hardening recommended brief)
    - .planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html (referenced if §3 verdict is FAIL — culprit attribution per CONTEXT D-08)
  </read_first>
  <action>
Step 1: Edit 37-EMPIRICAL.md §3 (TTI). Read tti-snapshot.json, transcribe these fields into the §3 table (replacing all `[populated by Plan 37-02]` placeholders):

| Metric | Value | Source |
|--------|-------|--------|
| baseline_ms (median of 3 runs at 048e99c) | <tti.baseline_ms> | tti-snapshot.json |
| post_phase_ms (median of 3 runs at a7e4544) | <tti.post_phase_ms> | tti-snapshot.json |
| delta_ms | <tti.delta_ms> | tti-snapshot.json |
| delta_pct | <tti.delta_pct>% | tti-snapshot.json |
| dual-gate verdict (D-08) | <tti.verdict> (PASS iff delta_ms ≤ 100 AND delta_pct ≤ 10) | tti-snapshot.json + CONTEXT D-08 |

Then add a 1-2 sentence narrative below the table:
- If verdict = PASS: "Phase 33 D-11 keepMounted={false} on extension Tabs.Panel + Plan 34-05 root keepMounted=false combined to keep extension panels lazy on /patients/:id mount; the empirical delta_ms confirms this invariant held across the Phase-34 14-module rollout."
- If verdict = FAIL: identify the likely culprit by reading `.planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html` chunk treemap (CONTEXT D-08 names this as the post-Phase-36 chunk treemap that represents the actual current state better than Phase 34's). Write a 2-3 sentence culprit-attribution paragraph naming the largest chunk that grew between baseline and post-phase. Mark the failure as either FAIL-WAIVED (feature-intrinsic, no follow-up needed) or FAIL-BLOCKING (regression bug needing a Phase 37+ gap-closure follow-up).

Step 2: Compute and write §4 Contradictions. Re-read §1 + §2 from 37-EMPIRICAL.md. For each row where the Agree? column is `no` (Paper says PASS but Empirical says FAIL, or vice versa), add a row to §4. Format:

```markdown
## §4 Contradictions

The following pairs disagreed between paper prediction and empirical capture:

| § | Pair | Risk | Paper | Empirical | Disposition |
|---|------|------|-------|-----------|-------------|
| §2 #7 | mikrobiologie ↔ molekulargenetik | HIGH | PASS | FAIL | Drives §5 contingency processing (HIGH-risk path) |
| §2 #X | <other pair> | LOW | PASS | FAIL | Informational only per CONTEXT D-10 (LOW-risk failure does NOT trigger contingency) |
```

If 0 disagreements: write "0 contradictions — paper analysis confirmed empirically. The qualitative reasoning in `.planning/research/color-design-audit.md` §4a-§4c held up against Chrome's Machado 2009 deuteranopia simulation."

Note: include a brief paragraph if any disagreement involves a HIGH or MEDIUM-HIGH borderline pair (#7 or #12) — explicitly state that this drives §5 contingency processing. RESEARCH §Pitfall 8 notes the audit reasoned qualitatively across both Brettel and Machado models; if disagreement is only on the Machado-side because Chrome implements Machado not Brettel, document this as a model-mismatch contributing factor (not a paper-falsification).

Step 3: Write §5 stub + §6 cross-ref placeholder.
- If §4 has 0 contradictions OR contradictions only involve LOW-risk pairs (Branch A or B): §5 = "None — no contingency triggered." Skip Tasks 2+3+4 below; jump to Task 5 (UAT closure).
- If §4 has any HIGH or MEDIUM-HIGH borderline pair contradiction (Branch C): §5 = "[populated by Task 2 below — Tabler probe + swap decision]." Continue to Task 2.

§6 placeholder: "[populated by Task 5 — cross-references the commit SHA(s) that updated 34-06-UAT.md]"

Step 4: Write §7 Future hardening (recommended brief per RESEARCH Open Question #3).
```markdown
## §7 Future hardening

Two candidates noted for v1.6+ backlog (no commitment):

- **(a) Headless deuteranopia simulation in Vitest.** A Brettel/Machado JS implementation could re-render the Dashboard tile grid + tab row offscreen, apply the simulation matrix to the rendered RGBA, and assert pairwise icon+color discriminability via OCR or pixel-difference metrics. Would catch icon-or-palette regressions on every PR. Requires a JS canvas-rendering harness in jsdom (or moving to playwright for true Chromium rendering); Phase 37 chose to keep manual Chrome DevTools because the audit-vs-empirical reconciliation is one-shot.
- **(b) ΔE2000 perceptual-distance lint over `MII_MODULES` palette adjacency.** A static lint that fails CI if any two adjacent module palettes (per the final tab order) fall below a perceptual-distance threshold under deuteranopia simulation. Cheap to run, catches palette-tweaking regressions instantly.

Both are flagged for the v1.6 hardening phase if the project decides to invest in CI-side colour-vision regression coverage.
```

Step 5: Commit the doc-only update.
```
git add .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
git commit -m "docs(37-03): populate 37-EMPIRICAL.md §3-§7 (TTI + contradictions + contingency stub + future hardening)"
```
  </action>
  <verify>
    <automated>test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && ! grep -q "\\[populated by Plan 37-02\\]" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && grep -q "^## §3 TTI" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && grep -qE "(0 contradictions|^\\| §[12])" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && grep -q "^## §7 Future hardening" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && (grep -q "None — no contingency triggered" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md || grep -q "\\[populated by Task 2" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md)</automated>
  </verify>
  <acceptance_criteria>
    - §3 has all 5 metric rows populated with numeric values from tti-snapshot.json (no `[populated by Plan 37-02]` placeholders remain)
    - §3 has a 1-2 sentence narrative beneath the table (PASS narrative or FAIL culprit attribution)
    - §4 either says "0 contradictions" OR has a table listing every disagreement
    - §5 either says "None — no contingency triggered" (Branch A/B) OR "[populated by Task 2 below — Tabler probe + swap decision]" (Branch C)
    - §6 says "[populated by Task 5 ...]"
    - §7 exists with the 2-bullet future-hardening list
    - File committed: `git log -1 --pretty=%s | grep -q "docs(37-03):"`
  </acceptance_criteria>
  <done>
    - §3, §4, §5 (stub or final), §6 (placeholder), §7 all populated according to branch decision
    - Single doc-only commit landed
    - Branch decision (A, B, or C) is logged for downstream tasks: print "Branch: A | B | C" to executor session log
  </done>
</task>

<task type="auto">
  <name>Task 2: BRANCH C ONLY — Tabler 3.41.1 alternative-icon probe + swap decision</name>
  <files>
    .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
  </files>
  <read_first>
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md §4 (the contradictions list — drives which pair to address)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md §Pitfall 5 (audit's named swap targets are unfireable as written)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md §Open Question #1 (probe before swap)
    - src/utils/mii-icons.ts (current ICON_MAP — confirms IconVirus + IconListCheck are already shipped)
    - src/utils/mii-modules.ts lines 258 + 315 (current icon assignments for mikrobiologie + pro)
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-UI-SPEC.md §Icons (icon set + fallback options)
    - .planning/research/color-design-audit.md §4d (audit's swap rationale)
  </read_first>
  <action>
**SKIP THIS TASK if Branch A or B (no HIGH/MEDIUM-HIGH contradictions).** Print "SKIP: Branch A or B — no HIGH/MEDIUM-HIGH contradiction; jumping to Task 5" and proceed to Task 5.

**EXECUTE if Branch C** (pair #7 mikrobiologie↔molekulargenetik OR pair #12 pro↔seltene empirically failed):

Step 1: Run the Tabler 3.41.1 availability probe to identify candidate alternative icons.
```
node -e "
const I = require('@tabler/icons-react');
const candidates = [
  // Bacterial / pathogen candidates (for mikrobiologie alternatives if pair #7 fails)
  'IconBiohazard', 'IconParasite', 'IconRadar', 'IconScan', 'IconBug', 'IconShield',
  // Questionnaire / list candidates (for pro alternatives if pair #12 fails)
  'IconClipboardList', 'IconChecklist', 'IconReport', 'IconForms', 'IconNotes',
  // Already-shipped (for confirmation)
  'IconVirus', 'IconListCheck',
  // Audit's named primaries (for confirmation they are still absent in 3.41.1)
  'IconBacteria', 'IconQuestionnaire',
];
for (const n of candidates) {
  console.log(n.padEnd(25), n in I ? 'AVAILABLE' : 'absent');
}
" | tee /tmp/tabler-probe.txt
```

Step 2: For each failed borderline pair, evaluate alternative icons. Selection rule:
- The candidate MUST be AVAILABLE (in the probe output)
- The candidate MUST be unambiguously more shape-distinct from the OTHER icon in the pair than the currently-shipped icon
- If multiple candidates qualify, prefer the one whose silhouette is most opposite to the paired icon (e.g., for mikrobiologie ↔ molekulargenetik where molekulargenetik is IconDna helix: a star/grid silhouette is more distinct from a helix than another globular silhouette)
- If NO candidate is unambiguously superior, do NOT force a swap (CONTEXT D-09 + RESEARCH §Pitfall 5: "If no available candidate is unambiguously more shape-distinct than the current icon, document the failure in 37-EMPIRICAL.md §5 as 'no swap fired — no superior candidate available' instead of forcing a swap")

Step 3: Update 37-EMPIRICAL.md §5 with the decision matrix:

```markdown
## §5 Contingency commits triggered

Tabler @tabler/icons-react@3.41.1 availability probe (Phase 37 — RESEARCH §Pitfall 5):

| Candidate | Available? | Considered for | Outcome |
|-----------|-----------|----------------|---------|
| IconBacteria | absent | mikrobiologie alternative | Cannot use (audit's named primary unavailable; already documented) |
| IconBiohazard | <yes/no> | mikrobiologie alternative | <selected/rejected — reason> |
| ... (one row per probed candidate) | | | |

**Decision for pair #7 (mikrobiologie ↔ molekulargenetik):** <decision>
**Decision for pair #12 (pro ↔ seltene):** <decision>

Where <decision> is one of:
- "Swap fired: <old icon> → <new icon>. Re-capture committed in <commit SHA> (post-swap canonical filename)."
- "No swap fired — no superior candidate available in @tabler/icons-react@3.41.1. The empirical FAIL stands; the borderline pair remains visually ambiguous under Chrome's Machado 2009 deuteranopia simulation. Future hardening (§7) names two candidate paths."
```

Step 4: If decision is "Swap fired" for either pair, log the chosen new icon name and proceed to Task 3. Otherwise (no swap fired), update §5 with the documented failure and proceed to Task 5 (skip Task 3 + Task 4).

Step 5: Commit the §5 update.
```
git add .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
git commit -m "docs(37-03): probe Tabler 3.41.1 alternative icons + record contingency decision in §5"
```
  </action>
  <verify>
    <automated>! grep -q "\\[populated by Task 2 below" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && grep -qE "(Swap fired:|No swap fired|None — no contingency)" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md</automated>
  </verify>
  <acceptance_criteria>
    - If executed (Branch C): §5 has a probe table + decision lines for both borderline pairs (or skipped explicitly if only one failed)
    - If skipped (Branch A/B): §5 retains "None — no contingency triggered" from Task 1 (no edit needed)
    - 37-EMPIRICAL.md no longer carries the "[populated by Task 2 below]" placeholder regardless of branch
    - Probe output committed to /tmp/tabler-probe.txt for audit trail (not committed to git, just session-visible)
  </acceptance_criteria>
  <done>
    - §5 final text written: either swap-decision matrix (Branch C with swap), or no-swap documentation (Branch C without superior candidate), or "None" (Branch A/B)
    - The chosen new icon name (if swap) printed to executor log so Task 3 can consume
    - Single commit landed, OR explicit skip with no commit
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BRANCH C1 ONLY — Apply icon swap to mii-icons.ts + mii-modules.ts and re-capture affected screenshot</name>
  <files>
    src/utils/mii-icons.ts
    src/utils/mii-modules.ts
    .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row-pre-swap.png   (rename from canonical)
    .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-timeline-pre-swap.png (rename from canonical, if pair #7 affected)
    .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row.png           (re-captured with new icon)
    .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-timeline.png         (re-captured if affected)
  </files>
  <read_first>
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md §5 (chosen new icon name(s) from Task 2)
    - src/utils/mii-icons.ts (lines 23-46 import block, 49-85 ICON_MAP)
    - src/utils/mii-modules.ts lines 258 (mikrobiologie icon), 315 (pro icon)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-CONTEXT.md (D-09 — pre-swap PNG renaming pattern)
  </read_first>
  <what-built>
    SKIP UNLESS Task 2 logged a swap decision. Branch C1 = HIGH/MEDIUM-HIGH borderline pair empirically FAILED AND a superior alternative icon was found in @tabler/icons-react@3.41.1.

    This task: (a) preserve the pre-swap PNGs by renaming them with `-pre-swap` suffix; (b) update src/utils/mii-icons.ts to import the new icon and add it to ICON_MAP; (c) update src/utils/mii-modules.ts to reference the new icon in the affected MII_MODULES row; (d) prompt the human to re-capture the affected screenshot under deuteranopia emulation; (e) commit the swap atomically (code + new PNG + audit-doc revision).
  </what-built>
  <how-to-verify>
    SKIP if Task 2 didn't fire a swap. Print "SKIP: no swap from Task 2; jumping to Task 5" and proceed to Task 5.

    EXECUTE if Task 2 logged "Swap fired" for at least one pair:

    Executor preparation steps (auto, before pausing):
    1. Read the chosen new icon name(s) from 37-EMPIRICAL.md §5 (e.g., `IconBiohazard` for mikrobiologie).
    2. Preserve pre-swap PNGs:
       - For pair #7 (mikrobiologie): rename `deuteranopia-tab-row.png` → `deuteranopia-tab-row-pre-swap.png` AND rename `deuteranopia-timeline.png` → `deuteranopia-timeline-pre-swap.png` (timeline shows mikrobiologie entries if data exists).
       - For pair #12 (pro): same rename for tab-row + timeline.
       - For both: same renames (single pre-swap pair preserves the union of affected screenshots).
       Use `git mv` so the rename is tracked.
    3. Edit src/utils/mii-icons.ts:
       - Add the new icon name(s) to the import block (alphabetical-ish, between lines 23-46).
       - Add the new icon name(s) to ICON_MAP (lines 49-85). Add an inline comment indicating the Phase-37 contingency rationale.
       - REMOVE the old icon name (e.g. IconVirus) from the import block AND ICON_MAP **only if** no other module references it (grep `src/utils/mii-modules.ts` to confirm). For mikrobiologie: IconVirus is referenced ONLY by mikrobiologie, so removing is safe. For pro: IconListCheck is referenced ONLY by pro, so removing is safe.
    4. Edit src/utils/mii-modules.ts:
       - Line 258 (mikrobiologie): change `icon: 'IconVirus'` to `icon: '<NewIcon>'`
       - Line 315 (pro): change `icon: 'IconListCheck'` to `icon: '<NewIcon>'`
       - Touch only the line(s) corresponding to the failed pair(s).
    5. Run typecheck + tests to confirm the swap doesn't break anything BEFORE re-capturing:
       - `npx tsc -b --noEmit` must exit 0.
       - `npx vitest run --reporter=dot --no-coverage src/__tests__/mii-icons.test.ts src/__tests__/mii-modules.test.ts` must pass (existing tests cover ICON_MAP coverage + MII_MODULES rows; if a snapshot/fixture references the old icon name, update it as part of this task).
    6. Start the dev server: `npm run dev` (in background) — wait for "Local: http://localhost:5173/" line.
    7. Print resume-block to user (verbatim):

       ```
       === HUMAN RE-CAPTURE REQUIRED — POST-SWAP DEUTERANOPIA SCREENSHOT ===

       The icon swap has been applied to src/utils/mii-icons.ts + src/utils/mii-modules.ts:
         <old-icon> → <new-icon> for module <module-name>

       Vite dev server is up at http://localhost:5173/. Use the SAME deterministic patient as Plan 37-01 (recorded in 37-01-SUMMARY.md).

       Steps (Chrome on macOS, Incognito):
         1. DevTools (Cmd+Option+I) → three-dot menu → More tools → Rendering
         2. Rendering panel → Emulate vision deficiencies → "deuteranopia"
         3. For pair #7 (mikrobiologie ↔ molekulargenetik) re-capture:
            - Navigate to /patients/<patient-id> → click "Show extension modules (14)" toggle
            - Cmd+Shift+P → "Capture full size screenshot"
            - Save as: .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row.png  (overwriting the post-rename canonical path)
            - Click the "Zeitleiste" (Timeline) tab
            - Cmd+Shift+P → "Capture full size screenshot" → save as deuteranopia-timeline.png
         4. (For pair #12 pro ↔ seltene re-capture: same paths — they share the affected screenshots.)
         5. Visually verify the new icon disambiguates from the paired icon under deuteranopia. If still ambiguous, the swap didn't help — STOP and reply with "swap-failed" so this plan rolls back.
         6. Reset DevTools: Rendering panel → "No emulation"
         7. Reply "approved" once both PNGs exist and the post-swap pair appears visually distinct.

       === END HUMAN RE-CAPTURE BLOCK ===
       ```
    8. Pause executor.

    On resume:
    9. Run the post-checkpoint <verify> below — exits non-zero if PNGs missing or unchanged from pre-swap (md5 comparison detects "user forgot to overwrite the file").
    10. Stop the dev server.
    11. Update color-design-audit.md §4d with a `## Revision (2026-04-26, Phase 37)` note describing the empirical failure + the swap (~5-10 lines):
        ```markdown
        ## Revision (2026-04-26, Phase 37)

        Empirical capture under Phase 37 (Plan 37-03) found that <pair-name> failed
        deuteranopia discriminability under Chrome's Machado 2009 simulation. The
        audit's §4d named contingency target for this pair (<old-icon>) is already
        the rendered icon (Plan 34-04 swapped it from the unavailable IconBacteria/
        IconQuestionnaire — see RESEARCH Pitfall 5). Phase 37 selected <new-icon>
        from the available @tabler/icons-react@3.41.1 set on the basis of <reason>.

        Updated MII_MODULES + ICON_MAP entry; pre-swap and post-swap screenshots
        committed under .planning/phases/37-.../ for audit trail.
        ```
    12. Commit ATOMICALLY (one commit covering: pre-swap PNG renames, mii-icons.ts edit, mii-modules.ts edit, color-design-audit.md revision note, post-swap PNG):
        ```
        git add src/utils/mii-icons.ts src/utils/mii-modules.ts .planning/research/color-design-audit.md
        git add .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row*.png
        git add .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-timeline*.png
        git commit -m "fix(37-03): contingency icon swap for <pair-name> deuteranopia FAIL — <old> → <new>"
        ```
  </how-to-verify>
  <action>See <how-to-verify> above. Executor automates the rename + code edit + tsc/vitest sanity checks + dev server + resume-block; the human re-captures the affected screenshot(s) under deuteranopia emulation; the executor stops the dev server, writes the audit-doc revision note, and commits atomically.</action>
  <verify>
    <automated>(! grep -q "Swap fired:" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md) || (test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row-pre-swap.png && test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row.png && grep -q "## Revision (2026-04-26, Phase 37)" .planning/research/color-design-audit.md && npx tsc -b --noEmit && npx vitest run --reporter=dot --no-coverage src/__tests__/mii-icons.test.ts src/__tests__/mii-modules.test.ts)</automated>
  </verify>
  <acceptance_criteria>
    - If skipped (no swap fired in Task 2): no edits made; this section is a no-op
    - If executed (swap fired):
      - Pre-swap PNG exists at `deuteranopia-tab-row-pre-swap.png` (and timeline if affected)
      - Post-swap PNG exists at `deuteranopia-tab-row.png` (canonical filename, content differs from pre-swap by md5)
      - src/utils/mii-icons.ts has new icon imported + in ICON_MAP; old icon removed (if no other module uses it)
      - src/utils/mii-modules.ts has new icon string at line 258 (mikrobiologie) and/or line 315 (pro)
      - color-design-audit.md has `## Revision (2026-04-26, Phase 37)` note appended after §4d
      - `npx tsc -b --noEmit` exits 0
      - `npx vitest run --reporter=dot --no-coverage src/__tests__/mii-icons.test.ts src/__tests__/mii-modules.test.ts` passes (existing tests + any updated snapshots)
      - Atomic commit landed: `git log -1 --pretty=%s | grep -q "fix(37-03): contingency icon swap"`
  </acceptance_criteria>
  <done>
    - If skipped: no work done, executor logs "SKIP: no swap from Task 2"
    - If executed: pre-swap + post-swap PNGs both committed; mii-icons + mii-modules + color-design-audit revision all in one atomic commit; tsc + targeted vitest both green
  </done>
  <resume-signal>Type "approved" if the post-swap re-capture shows the pair is visually distinct under deuteranopia. Type "swap-failed" if the new icon still doesn't disambiguate (Task 4 will roll back). Type "rebuild-deuteranopia" if you forgot to enable deuteranopia emulation before capturing.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: BRANCH C1 ONLY — Full regression sweep after icon swap</name>
  <files>
    (no file edits — sanity-check only)
  </files>
  <read_first>
    - src/utils/mii-icons.ts (post-swap state)
    - src/utils/mii-modules.ts (post-swap state)
  </read_first>
  <action>
SKIP if Task 3 was skipped (no swap fired) OR Task 3 resumed with "swap-failed" (rollback path).

EXECUTE if Task 3 fired a swap and resumed with "approved":

Step 1: Run the full test suite to confirm no test elsewhere depends on the old icon name:
```
npm test 2>&1 | tail -30
```
The tail should show `Tests: ... passed, ... total` with `0 failed`. The Phase-36 baseline is 1059+ passing per ROADMAP §Phase 36. If any failure surfaces:
- Identify the failing test file via the vitest output.
- If the failure is a string-equality assertion against the old icon name (e.g. a snapshot or a row-table test), update the assertion to the new icon name as part of this task.
- If the failure is something else (a real regression), STOP and report — do NOT proceed to Task 5 until the regression is understood.

Step 2: Run a build check:
```
npm run build 2>&1 | tail -10
```
Must exit 0. If build fails, the swap broke tree-shaking or a downstream import — investigate and fix.

Step 3: Run typecheck:
```
npx tsc -b --noEmit
```
Must exit 0.

Step 4: If any test/snapshot updates were needed in Step 1, commit them:
```
git add src/__tests__/mii-icons.test.ts src/__tests__/mii-modules.test.ts <any other affected tests>
git commit -m "test(37-03): update icon-name assertions for contingency swap"
```
If no updates needed: no commit (this task is a verification-only sweep in that branch).
  </action>
  <verify>
    <automated>(! grep -q "Swap fired:" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md) || (npm test --silent 2>&1 | grep -E "Tests:.*passed" | grep -v "failed" && npx tsc -b --noEmit && npm run build --silent 2>&1 | tail -1 | grep -qv "error")</automated>
  </verify>
  <acceptance_criteria>
    - If skipped: no work done
    - If executed: `npm test` passes with 0 failed; `npx tsc -b --noEmit` exits 0; `npm run build` exits 0
    - If any test snapshots/assertions were updated, they are in their own commit titled `test(37-03): update icon-name assertions for contingency swap`
  </acceptance_criteria>
  <done>
    - If skipped: no-op
    - If executed: full regression sweep green; no surprise failures elsewhere from the icon swap
  </done>
</task>

<task type="auto">
  <name>Task 5: Update 34-06-UAT.md §1d + §2 + §5 closure checklist + 37-EMPIRICAL.md §6 cross-ref</name>
  <files>
    .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md
    .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
  </files>
  <read_first>
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md (current placeholders)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md (final state from prior tasks)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-CONTEXT.md (D-11 — Phase 34 closure rules)
  </read_first>
  <action>
Step 1: Edit 34-06-UAT.md §1d — replace the parenthetical placeholder ("(This subsection is populated by the human verifier...)") with §4 from 37-EMPIRICAL.md:

If §4 has 0 contradictions:
```markdown
### 1d. Contradictions (Phase 37 empirical verification)

**0 contradictions** — paper analysis confirmed empirically. Verified 2026-04-26 under
Chrome DevTools deuteranopia emulation (Machado 2009 simulation). See
`.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md` §1+§2
for the full reconciliation matrix.
```

If §4 has contradictions: copy the §4 contradictions table verbatim, prefix with the same "Verified 2026-04-26..." paragraph, and reference the swap commit (if any) per Task 3's commit SHA.

Step 2: Edit 34-06-UAT.md §2 (TTI Regression Check) — replace `⏳ pending` cells in the value column with values from tti-snapshot.json. Also append a "Verified by" line referencing tti-snapshot.json + Plan 37-02.

Specifically the §2 table currently has:
| Baseline TTI (ms) | ⏳ pending | ... |
| Post-phase TTI (ms) | ⏳ pending | ... |
| Delta (ms) | ⏳ pending | ... |
| Regression? | ⏳ pending | ... |
| D-22 gate | ⏳ pending | ... |

Update each with concrete numbers:
| Baseline TTI (ms) | <tti.baseline_ms> | Chrome DevTools Performance panel, baseline at commit 048e99c via /tmp/exploder-tti-baseline worktree (Phase 37 capture, 2026-04-26) |
| Post-phase TTI (ms) | <tti.post_phase_ms> | Chrome DevTools Performance panel, post-phase at commit a7e4544 via /tmp/exploder-tti-postphase worktree (Phase 37 capture, 2026-04-26) |
| Delta (ms) | <tti.delta_ms> | post_phase_ms - baseline_ms |
| Delta (%) | <tti.delta_pct>% | (delta_ms / baseline_ms) * 100 |
| Regression? | <if delta_ms > 0: "yes" else "no"> | delta_ms direction |
| D-22 gate | <tti.verdict> | dual-gate: PASS iff delta_ms ≤ 100 AND delta_pct ≤ 10 |

Append below the table: "Captured by Plan 37-02 (Phase 37). See `.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json` for run-by-run values + audit trail."

Step 3: Edit 34-06-UAT.md §5 sign-off checklist — tick the deuteranopia + TTI checkboxes:

Currently:
```
- [ ] Deuteranopia: paper predictions empirically confirmed (or contradictions documented + resolved) — **⏳ PENDING**
- [ ] TTI: no regression > 100 ms — **⏳ PENDING**
```

After update:
```
- [x] Deuteranopia: paper predictions empirically confirmed (or contradictions documented + resolved) — closed by Phase 37 Plan 37-01 + 37-03 (see `.planning/phases/37-.../37-EMPIRICAL.md` §1+§2+§4)
- [x] TTI: no regression > 100 ms — closed by Phase 37 Plan 37-02 (see `.planning/phases/37-.../tti-snapshot.json`); D-22 verdict: <tti.verdict>
```

(If TTI verdict is FAIL with waiver, mark as `[x] FAIL-WAIVED` and reference §3 culprit-attribution paragraph.)

Step 4: Update the top-of-file Status line in 34-06-UAT.md from "**PARTIAL**" to "**COMPLETE**":
- Old: `**Status:** Phase 34 close — **PARTIAL** (Tasks 3 + 4 complete; Tasks 1 + 2 pending human capture — see §6)`
- New: `**Status:** Phase 34 close — **COMPLETE** (Tasks 3 + 4 complete in original Plan 34-06; Tasks 1 + 2 closed by Phase 37 — see §1d, §2, §5)`

Step 5: Edit 37-EMPIRICAL.md §6 with the actual update commit SHA (this commit is about to land — predict the SHA via `git log --pretty=%H -1` AFTER the commit, OR just reference the commit subject line):
```markdown
## §6 34-06-UAT.md update

The closure update to `.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md`
landed in commit `<SHA>`:

- §1d: filled with §4 Contradictions content (<X contradictions or "0 contradictions">)
- §2 TTI table: replaced 5 `⏳ pending` cells with values from `tti-snapshot.json`
- §5 sign-off: deuteranopia + TTI checkboxes ticked

Phase 34 VERIFICATION status remains `passed` per CONTEXT D-11 — these checkpoint:human-verify
gates were explicitly carried forward, not gaps; Phase 37 closes the gates without re-opening
Phase 34's verification.
```

Step 6: Commit BOTH file edits in one commit:
```
git add .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md
git add .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
git commit -m "docs(37-03): close Phase 34 UAT — 34-06-UAT.md §1d/§2/§5 + 37-EMPIRICAL.md §6 cross-ref"
```

Step 7 (optional, post-commit): If you predicted the SHA in §6 and want it concrete, do an `--amend` of just the §6 line:
```
NEW_SHA=$(git log --pretty=%H -1)
sed -i.bak "s/<SHA>/$NEW_SHA/" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
rm .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md.bak
git add .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
git commit --amend --no-edit
```

(Alternative without amend: leave §6 referencing the commit subject line `docs(37-03): close Phase 34 UAT —...`. That is also acceptable for the audit trail and avoids amending.)
  </action>
  <verify>
    <automated>! grep -q "⏳ pending" .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md && ! grep -q "(This subsection is populated by the human verifier" .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md && grep -q "^- \\[x\\] Deuteranopia" .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md && grep -q "^- \\[x\\] TTI" .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md && grep -q "Phase 34 close — \\*\\*COMPLETE\\*\\*" .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md && grep -q "^## §6 34-06-UAT.md update" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && ! grep -q "\\[populated by Task 5" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md</automated>
  </verify>
  <acceptance_criteria>
    - 34-06-UAT.md has zero `⏳ pending` cells: `! grep -q "⏳ pending" 34-06-UAT.md`
    - 34-06-UAT.md §1d placeholder removed: `! grep -q "(This subsection is populated by the human verifier" 34-06-UAT.md`
    - 34-06-UAT.md §5 deuteranopia + TTI checkboxes ticked: `grep -q "^- \[x\] Deuteranopia" && grep -q "^- \[x\] TTI"`
    - 34-06-UAT.md status line flipped to COMPLETE: `grep -q "Phase 34 close — \*\*COMPLETE\*\*"`
    - 37-EMPIRICAL.md §6 populated (no remaining "[populated by Task 5]" placeholder): `! grep -q "\[populated by Task 5" 37-EMPIRICAL.md`
    - Both file edits committed in one commit: `git log -1 --pretty=%s | grep -q "docs(37-03): close Phase 34 UAT"`
  </acceptance_criteria>
  <done>
    - 34-06-UAT.md fully closed: §1d, §2, §5 all populated; status flipped from PARTIAL to COMPLETE
    - 37-EMPIRICAL.md §6 cross-references the closure commit
    - Single commit landed titled `docs(37-03): close Phase 34 UAT — 34-06-UAT.md §1d/§2/§5 + 37-EMPIRICAL.md §6 cross-ref`
    - Phase 34's `34-VERIFICATION.md` status remains `passed` per CONTEXT D-11 (NOT touched by this plan — those checkpoint:human-verify gates were carried forward, not gaps)
  </done>
</task>

<task type="auto">
  <name>Task 6: Final regression sweep — npm test + tsc + build</name>
  <files>
    (no edits — phase-close gate)
  </files>
  <read_first>
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-VALIDATION.md (validation sign-off list)
  </read_first>
  <action>
Step 1: Full test suite must remain green.
```
npm test 2>&1 | tail -5
```
Required: `Tests: ... passed, ... total` with `0 failed`. The Phase-36 baseline is 1059+ passing; if this plan fired a contingency icon swap (Branches C1) and Task 4 already updated affected snapshots, the count may be the same (snapshot-only updates don't change test count). If the count drops or any test fails, STOP and investigate.

Step 2: TypeScript clean.
```
npx tsc -b --noEmit
```
Must exit 0.

Step 3: Build clean.
```
npm run build 2>&1 | tail -3
```
Must exit 0; no errors in tail output.

Step 4: Validation sign-off — verify each item in 37-VALIDATION.md per-task verification map is satisfied:
```
# Plan 37-01 artifacts
test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-dashboard.png
test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row.png
test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-timeline.png
grep -q "^## §1 Within-family pairs" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
grep -q "^## §2 Cross-family" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md

# Plan 37-02 artifacts
test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json
node -e "const j=require('./.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json'); for (const k of ['baseline_ms','post_phase_ms','delta_ms','delta_pct','patient_id','throttling','commits','runs_per_checkout','verdict']) if (!(k in j)) throw new Error('Missing: '+k)"

# Plan 37-03 artifacts (this plan's own work)
grep -q "^## §3 TTI" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
grep -q "^## §4 Contradictions" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
grep -q "^## §5 Contingency commits triggered" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
grep -q "^## §6 34-06-UAT.md update" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md
! grep -q "⏳ pending" .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md
```
All commands must exit 0.

No commit (this is a verification-only sweep).
  </action>
  <verify>
    <automated>npx tsc -b --noEmit && npm run build --silent 2>&1 | tail -3 | grep -qv "error" && test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-dashboard.png && test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-tab-row.png && test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/deuteranopia-timeline.png && test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json && test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md && ! grep -q "⏳ pending" .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md && grep -q "^## §6" .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-EMPIRICAL.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes with 0 failed (count ≥ 1059 per Phase-36 baseline)
    - `npx tsc -b --noEmit` exits 0
    - `npm run build` exits 0
    - All 7 Phase-37 phase-level artifacts exist + non-empty (3 PNGs + tti-snapshot.json + 37-EMPIRICAL.md, plus 34-06-UAT.md update + optional color-design-audit.md revision)
    - 34-06-UAT.md has zero `⏳ pending` cells in any section
  </acceptance_criteria>
  <done>
    - Final regression sweep green
    - All 37-VALIDATION.md per-task verification map items satisfied
    - Phase 37 ready for `/gsd-verify-work`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new runtime trust boundary introduced beyond what Plan 37-01 + 37-02 already established. If a contingency icon swap fires (Branch C1), the change is a 2-line edit in `src/utils/mii-icons.ts` + `src/utils/mii-modules.ts` to swap one Tabler icon for another — no new trust boundaries, no new fetches, no new authorization paths.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-16 (inherited) | Information Disclosure | Re-captured screenshots may show synthetic PHI | accept | Branch C1 re-capture uses the same Synthea synthetic patient as Plan 37-01 (read from 37-01-SUMMARY.md). No real PHI exposure. Phase 34 already accepted this disposition. |
| T-37-03 (new) | Tampering | Branch C1 icon swap could mask a deuteranopia FAIL by selecting a cosmetically-similar but no-better alternative icon | mitigate | Task 3's resume-signal explicitly requires the human to type "approved" only if the post-swap pair is VISUALLY DISTINCT under deuteranopia; "swap-failed" rolls back. Task 4 then runs `npm test` to confirm no regression elsewhere. The audit-doc revision note in color-design-audit.md §4d records the rationale ("on the basis of <reason>") so a future reviewer can re-evaluate the swap quality. |
| T-37-04 (new) | Repudiation | A swap commit could be confused with the original Plan 34-04 icon-set commit | mitigate | Task 3's atomic commit message explicitly reads `fix(37-03): contingency icon swap for <pair-name> deuteranopia FAIL`. The commit subject line + the §4d revision note + the `<old> → <new>` reference make the audit trail unambiguous. |
</threat_model>

<verification>
- 37-EMPIRICAL.md has all 7 sections (§1-§7) populated; §3-§7 written by this plan
- §3 TTI table populated from tti-snapshot.json with computed verdict
- §4 Contradictions either lists every disagreement or "0 contradictions"
- §5 Contingency either records swap commits or "no swap fired" (with reason) or "None"
- §6 cross-references the closure commit SHA or subject line
- §7 brief future-hardening section authored
- 34-06-UAT.md has zero `⏳ pending` cells; §1d, §2, §5 populated; status flipped to COMPLETE
- If swap fired (Branch C1): pre-swap PNG renamed to `*-pre-swap.png`; post-swap PNG re-captured; mii-icons.ts + mii-modules.ts updated; color-design-audit.md §4d carries `## Revision (2026-04-26, Phase 37)` note; npm test stays green
- If no swap (Branch A/B/C2): src/utils/mii-icons.ts and src/utils/mii-modules.ts UNCHANGED
</verification>

<success_criteria>
Plan 37-03 is closeable when:
- All 7 sections of 37-EMPIRICAL.md final (no [populated by Task X] placeholders remain)
- 34-06-UAT.md status flipped to COMPLETE; §1d + §2 + §5 fully populated
- `npm test` + `npx tsc -b --noEmit` + `npm run build` all green
- Phase 37 closes Phase 34's deferred MII-EXT-11 + D-22 gates per CONTEXT D-11
</success_criteria>

<output>
After completion, create `.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-03-SUMMARY.md` documenting:
- Branch taken (A: no contradictions; B: LOW-only contradictions; C1: swap fired; C2: contradictions but no superior icon)
- If Branch C1: which pair(s), old icon → new icon, rationale (brief), commit SHA(s) for the swap atomic commit + audit-doc revision
- If Branch C2: which pair(s) failed, why no superior icon was found, what was probed
- Final §3 TTI verdict (PASS or FAIL with brief delta_ms summary)
- Final §4 contradiction count
- 34-06-UAT.md closure commit SHA
- Phase 37 sign-off: all human-gated items closed, Phase 34's deferred gates fully satisfied
</output>
