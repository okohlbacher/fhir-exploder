---
phase: 20-v12-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md
  - .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md
autonomous: true
requirements: [DQ-01, DQ-02, DQ-11, DQ-12]
tags: [documentation, verification, retrospective, goal-backward, audit-closure]

must_haves:
  truths:
    - "A file named `15-VERIFICATION.md` exists at `.planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md` with frontmatter `status: passed`"
    - "A file named `18-VERIFICATION.md` exists at `.planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md` with frontmatter `status: passed`"
    - "Each VERIFICATION.md covers every must_have (observable truth + artifact + key link) from its phase's plans with concrete evidence references to existing source files, UAT tests, and VALIDATION entries"
    - "Requirements-coverage tables in both files mark DQ-01, DQ-02 (in 15-) and DQ-11, DQ-12 (in 18-) as SATISFIED with evidence"
  artifacts:
    - path: ".planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md"
      provides: "Retrospective goal-backward verification for Phase 15 (drill-down)"
      contains: "status: passed"
    - path: ".planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md"
      provides: "Retrospective goal-backward verification for Phase 18 (alerting + thresholds)"
      contains: "status: passed"
  key_links:
    - from: "15-VERIFICATION.md"
      to: "15-UAT.md + 15-VALIDATION.md + 15-{01,02,03}-SUMMARY.md + src/components/quality/ResourceIssueTable.tsx"
      via: "Evidence column citations in Observable Truths, Artifacts, Key Links tables"
      pattern: "src/components/quality/ResourceIssueTable.tsx|15-UAT.md"
    - from: "18-VERIFICATION.md"
      to: "18-UAT.md + 18-VALIDATION.md + 18-{01,02,03,04}-SUMMARY.md + src/quality/thresholds.ts + src/components/quality/ThresholdsPage.tsx + src/components/quality/SummaryCard.tsx"
      via: "Evidence column citations"
      pattern: "src/quality/thresholds.ts|src/components/quality/ThresholdsPage.tsx|18-UAT.md"
---

<objective>
Produce the two missing retrospective `VERIFICATION.md` files for Phases 15 and 18 so the v1.2 milestone audit can close its "verification_status: missing" gaps (DQ-01, DQ-02 for Phase 15; DQ-11, DQ-12 for Phase 18).

Purpose: Phase 15 and Phase 18 both shipped with passing UAT (`15-UAT.md`: 9/9 pass, `18-UAT.md`: 8 pass + 1 fixed + 1 waived) and passing VALIDATION artifacts, but neither has the goal-backward `VERIFICATION.md` artifact required by the GSD 3-source cross-reference. The audit (`v1.2-MILESTONE-AUDIT.md`) explicitly flagged these as "missing" — code and wiring are already proven correct; this is a documentation closure task, not a re-verification. Use the Phase 17 `17-VERIFICATION.md` (same format, same author style, same phase family) as the structural template.

Output: Two VERIFICATION.md files, each ~100–170 lines, each with `status: passed`, each cross-referencing existing UAT/VALIDATION/SUMMARY evidence.

Scope: Documentation only. No code changes. No test changes. No spec changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/templates/verification-report.md
</execution_context>

<context>
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/ROADMAP.md

<!-- Template + exemplar -->
@$HOME/.claude/get-shit-done/templates/verification-report.md
@.planning/phases/17-duplicate-detection-relational-integrity/17-VERIFICATION.md

<!-- Phase 15 evidence -->
@.planning/phases/15-quality-check-engine-drill-down/15-UAT.md
@.planning/phases/15-quality-check-engine-drill-down/15-VALIDATION.md
@.planning/phases/15-quality-check-engine-drill-down/15-01-SUMMARY.md
@.planning/phases/15-quality-check-engine-drill-down/15-02-SUMMARY.md
@.planning/phases/15-quality-check-engine-drill-down/15-03-SUMMARY.md
@.planning/phases/15-quality-check-engine-drill-down/15-01-PLAN.md
@.planning/phases/15-quality-check-engine-drill-down/15-02-PLAN.md
@.planning/phases/15-quality-check-engine-drill-down/15-03-PLAN.md

<!-- Phase 18 evidence -->
@.planning/phases/18-quality-alerting-thresholds/18-UAT.md
@.planning/phases/18-quality-alerting-thresholds/18-VALIDATION.md
@.planning/phases/18-quality-alerting-thresholds/18-01-SUMMARY.md
@.planning/phases/18-quality-alerting-thresholds/18-02-SUMMARY.md
@.planning/phases/18-quality-alerting-thresholds/18-03-SUMMARY.md
@.planning/phases/18-quality-alerting-thresholds/18-04-SUMMARY.md
@.planning/phases/18-quality-alerting-thresholds/18-01-PLAN.md
@.planning/phases/18-quality-alerting-thresholds/18-02-PLAN.md
@.planning/phases/18-quality-alerting-thresholds/18-03-PLAN.md
@.planning/phases/18-quality-alerting-thresholds/18-04-PLAN.md

<interfaces>
## Phase 15 must-haves (derived from ROADMAP.md Phase 15 success criteria)

**Phase goal:** "Users can click any quality metric on the dashboard and see exactly which resources and fields are causing that issue"

**Observable truths to verify (from ROADMAP Success Criteria):**
1. User can click a quality metric (completeness, coding coverage, validation) to open a drill-down view listing the specific resources and fields involved
2. Each entry in the drill-down view links to the resource detail view for further inspection
3. Drill-down works for all existing quality panels (completeness, coding coverage, profile validation)

**Requirements covered:** DQ-01, DQ-02

**Key artifacts to verify (from 15-*-SUMMARY.md key-files):**
- `src/quality/types.ts` — NormalizedIssue type + perResource fields on walker reports (Plan 01)
- `src/quality/completenessWalker.ts` — returns perResource data (Plan 01)
- `src/quality/codingCoverageWalker.ts` — returns perResource data (Plan 01)
- `src/hooks/useCompletenessReport.ts` — passes perResource through (Plan 01)
- `src/components/quality/ResourceIssueTable.tsx` — shared table with severity badges, pagination, filters, Link to `/explorer/:type/:id` (Plan 02)
- `src/components/quality/CodingDrillDown.tsx` — Tabs + cross-filter wiring (Plan 03)
- `src/components/quality/CompletenessDrillDown.tsx` — Tabs + cross-filter wiring (Plan 03)
- `src/components/quality/ValidationPanel.tsx` — Tabs + ResourceIssueTable for validation issues (Plan 03)
- `src/__tests__/resource-issue-table.test.tsx` — 11/11 pass (Plan 02)
- `src/__tests__/coding-drilldown.test.tsx`, `src/__tests__/completeness-drilldown.test.tsx` — integration tests (Plan 03)

**Key links to verify:**
- ResourceIssueTable → React Router `Link to='/explorer/:type/:id'` (audit evidence: "ResourceIssueTable.tsx line 193")
- CodingDrillDown Fields-tab click → Resources-tab with initialFieldFilter pre-populated
- CompletenessDrillDown Fields-tab click → Resources-tab with initialFieldFilter pre-populated
- ValidationPanel → ResourceIssueTable rendering normalized validation issues
- Route wired in `src/App.tsx` (audit evidence: "App.tsx line 62")

**UAT evidence:** `15-UAT.md` — 9/9 tests pass, 0 issues, 0 gaps, covering all 9 behavioural points above.

---

## Phase 18 must-haves (derived from ROADMAP.md Phase 18 success criteria)

**Phase goal:** "Users can set quality thresholds and the dashboard visually flags any metrics that breach them"

**Observable truths to verify (from ROADMAP Success Criteria):**
1. User can configure a quality threshold per metric (e.g., "alert if completeness < 80%")
2. Threshold configuration persists across page reloads (browser storage)
3. Dashboard visually highlights metrics that breach their configured threshold (distinct color/icon)
4. Alerting covers both existing metrics (completeness, coding) and new metrics (conformance, plausibility, duplicates, integrity)

**Requirements covered:** DQ-11, DQ-12

**Key artifacts to verify (from 18-*-SUMMARY.md key-files):**
- `src/quality/thresholds.ts` — MetricKey union, METRIC_LABELS, METRIC_ROUTES, DEFAULT_THRESHOLDS, STORAGE_KEY, resolveThreshold, isBreached (Plan 01)
- `src/hooks/useThresholds.ts` — useLocalStorage-backed hook with 8-member API (Plan 01)
- `src/quality/QualityMetricsContext.tsx` — extended from 2 → 7 overall* values + duplicatesBreakdown + setDuplicatesContribution (Plan 02)
- `src/components/quality/SummaryCard.tsx` — breached/threshold/onClick/ariaLabel props, red ring + annotation (Plan 03)
- `src/components/quality/ThresholdsPage.tsx` — /quality/thresholds config page (Plan 03)
- `src/components/quality/OverviewStrip.tsx` — 9-tile expansion (Plan 04)
- `src/components/quality/QualityOverviewPage.tsx` — "Configure thresholds" toolbar button + controlled Tabs bound to `?tab=` (Plan 03 + 04)
- `src/App.tsx` — `/quality/thresholds` route (Plan 03)
- Panels wired to push rollups: ValidationPanel, PlausibilityPanel, LabRangesPanel, ReferencesPanel, DuplicatesPanel (Plan 02)
- Tests: `thresholds.test.ts`, `summary-card.test.tsx`, `thresholds-page.test.tsx`, `lab-ranges-panel.test.tsx`, `references-panel.test.tsx`, `plausibility-panel.test.tsx`, `quality-overview.test.tsx`

**Key links to verify:**
- useThresholds ⇔ `quality.thresholds.v1` localStorage key (D-09 single-profile design)
- SummaryCard breached=true → red ring + `threshold: N%` annotation
- OverviewStrip metric tile click → `useNavigate('/quality?tab=<route>')`
- QualityOverviewPage Tabs → `useSearchParams` read/write with `replace: true` + VALID_TABS fallback
- ThresholdsPage NumberInput blur → `setThreshold(key, number)` → localStorage write
- ThresholdsPage Clear icon → `setThreshold(key, null)` (disabled state)
- ThresholdsPage Reset modal → `resetAll()` → `{}` in localStorage
- DuplicatesPanel `setDuplicatesContribution({ patient, hashType })` + `resourceType` in dep array → per-type average widening

**UAT evidence:** `18-UAT.md` — 8 pass / 1 fixed (SummaryCard vertical layout refactor to un-clip the ring) / 1 waived (?tab=bogus test — pre-existing ConnectionContext scope issue, NOT a Phase 18 regression, logged to deferred-items.md). Total: 10 tests, all resolved or explicitly waived.

**One explicit status note:** Test 10 was waived with a documented rationale. The VERIFICATION.md must record this as `human_needed` OR `passed` with an explicit note in Gaps Summary — but the waiver rationale is preserved in `18-UAT.md`, so `status: passed` is defensible (the waiver is tracked as deferred tech debt, not a Phase 18 gap).
</interfaces>

<writing_style>
Match the tone and structural patterns of `17-VERIFICATION.md` (the existing exemplar in this phase family):
- Frontmatter with `phase`, `verified`, `status`, `score`, plus a `gaps_closed` / `gaps_remaining` sub-block if retrospective
- Intro paragraph stating phase goal, verification timestamp, status
- "Goal Achievement" section with Observable Truths table (#, Truth, Status, Evidence)
- "Required Artifacts" table (Artifact, Expected, Status, Details)
- "Key Link Verification" table (From, To, Via, Status, Details)
- Optional "Data-Flow Trace" and "Behavioral Spot-Checks" if useful
- "Requirements Coverage" table (Requirement, Source Plan, Description, Status, Evidence)
- "Anti-Patterns Found" table (empty OK — note "No blocker or warning anti-patterns")
- "Human Verification Required" section (for Phase 15: cross-filter + link navigation were already manually verified in 15-UAT tests 4/5/7 → note "Already covered by 15-UAT tests 4, 5, 7 — no further human action required"; for Phase 18: breach visualization + keyboard nav + responsive 9-tile layout manually verified in 18-UAT → similar)
- "Gaps Summary" section
- Closing `_Verified: ..._` + `_Verifier: Claude (gsd-verifier, retrospective)_`

Use ✓ VERIFIED / ✗ FAILED / ? UNCERTAIN / WIRED / EXISTS + SUBSTANTIVE status tokens exactly as in `17-VERIFICATION.md`.

Mark this as a RETROSPECTIVE verification in the intro paragraph — make clear the code shipped weeks ago, UAT already passed, and this artifact closes a v1.2-MILESTONE-AUDIT documentation gap.
</writing_style>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write retrospective 15-VERIFICATION.md</name>
  <files>.planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/15-quality-check-engine-drill-down/15-UAT.md (9/9 UAT tests — source of behavioural evidence)
    - .planning/phases/15-quality-check-engine-drill-down/15-VALIDATION.md (per-task validation matrix)
    - .planning/phases/15-quality-check-engine-drill-down/15-01-SUMMARY.md (Walker perResource extension — files created/modified, commit hashes)
    - .planning/phases/15-quality-check-engine-drill-down/15-02-SUMMARY.md (ResourceIssueTable component — 11/11 tests)
    - .planning/phases/15-quality-check-engine-drill-down/15-03-SUMMARY.md (Tabs + cross-filter wiring)
    - .planning/phases/17-duplicate-detection-relational-integrity/17-VERIFICATION.md (structural template + writing style)
    - $HOME/.claude/get-shit-done/templates/verification-report.md (canonical template)
    - .planning/v1.2-MILESTONE-AUDIT.md (audit finding that mandates this file)
  </read_first>
  <action>
    Create the file `.planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md`. Required structure:

    **Frontmatter:**
    ```yaml
    ---
    phase: 15-quality-check-engine-drill-down
    verified: <current-UTC-timestamp-ISO-8601>
    status: passed
    score: 3/3 must-haves verified
    retrospective: true
    closes_audit_finding:
      - id: DQ-01
        source: .planning/v1.2-MILESTONE-AUDIT.md
        prior_status: "partial (verification_status: missing)"
        new_status: "satisfied"
      - id: DQ-02
        source: .planning/v1.2-MILESTONE-AUDIT.md
        prior_status: "partial (verification_status: missing)"
        new_status: "satisfied"
    human_verification: already_covered_by_UAT
    ---
    ```

    **Sections (in order):**

    1. **Title + intro** — "# Phase 15: Quality Check Engine & Drill-Down — Verification Report". State the phase goal verbatim from ROADMAP.md (Phase 15 Goal: "Users can click any quality metric on the dashboard and see exactly which resources and fields are causing that issue"). Mark this as **retrospective** (written 2026-04-14 to close v1.2 audit gap; code shipped 2026-04-13 with passing UAT). Reference `v1.2-MILESTONE-AUDIT.md` as the source of the DQ-01/DQ-02 "partial" → "satisfied" transition.

    2. **Goal Achievement > Observable Truths** (3 rows — derived from ROADMAP.md Phase 15 Success Criteria 1/2/3):
       - Row 1 truth: "User can click a quality metric (completeness, coding coverage, validation) to open a drill-down view listing the specific resources and fields involved" → ✓ VERIFIED → Evidence: `15-UAT.md` tests 1/2/3 all `pass`; files `CodingDrillDown.tsx` / `CompletenessDrillDown.tsx` / `ValidationPanel.tsx` render Tabs with "Fields" + "Resources" tabs and embed `ResourceIssueTable`
       - Row 2 truth: "Each entry in the drill-down view links to the resource detail view for further inspection" → ✓ VERIFIED → Evidence: `ResourceIssueTable.tsx` renders `<Link to='/explorer/:type/:id'>` per row (audit: "line 193"); route wired in `src/App.tsx`; `15-UAT.md` test 7 `pass`; 11/11 tests in `resource-issue-table.test.tsx`
       - Row 3 truth: "Drill-down works for all existing quality panels (completeness, coding coverage, profile validation)" → ✓ VERIFIED → Evidence: All three panels wired in Plan 03 commits; cross-filter from Fields→Resources tab works on each per `15-UAT.md` tests 4/5 + integration tests `coding-drilldown.test.tsx` / `completeness-drilldown.test.tsx`
       - Score line: `**Score:** 3/3 truths verified`

    3. **Required Artifacts** table — enumerate the 7 key files from the Plan SUMMARIES above (types.ts, completenessWalker.ts, codingCoverageWalker.ts, ResourceIssueTable.tsx, CodingDrillDown.tsx, CompletenessDrillDown.tsx, ValidationPanel.tsx + the 3 test files). For each: Expected, Status (✓ EXISTS + SUBSTANTIVE), Details. Reference the Plan NN SUMMARY for provenance.

    4. **Key Link Verification** table — at minimum these 5 links (all ✓ WIRED):
       - ResourceIssueTable row → `/explorer/:type/:id` via `<Link to>`
       - CodingDrillDown Fields tab → Resources tab via `initialFieldFilter` prop
       - CompletenessDrillDown Fields tab → Resources tab via `initialFieldFilter` prop
       - ValidationPanel → ResourceIssueTable via normalized issue array
       - Route `/quality/completeness/:type` / `/quality/coding/:type` wired in App.tsx (integration audit confirmed)

    5. **Requirements Coverage** table — 2 rows (DQ-01, DQ-02) both SATISFIED with plan + evidence file citations.

    6. **Behavioral Spot-Checks** table — 3+ rows:
       - `npx vitest run src/__tests__/resource-issue-table.test.tsx` → 11/11 pass
       - `npx vitest run src/__tests__/coding-drilldown.test.tsx src/__tests__/completeness-drilldown.test.tsx` → pass
       - `grep -c "to='/explorer/" src/components/quality/ResourceIssueTable.tsx` → ≥1 (confirms Link wiring)
       - `grep -cE "Tabs\.Tab.*value=\"resources\"" src/components/quality/CodingDrillDown.tsx` → ≥1 (confirms Resources tab wiring)

    7. **Anti-Patterns Found** — table or "None found." Based on the SUMMARIES, there are none.

    8. **Human Verification Required** — Note: already covered by `15-UAT.md` tests 4, 5, 7 (cross-filter + link navigation manual tests, all `pass`). No outstanding human verification.

    9. **Gaps Summary** — "No gaps found. Phase 15 meets all 3 success criteria. This retrospective VERIFICATION.md closes the DQ-01 and DQ-02 'verification_status: missing' findings from v1.2-MILESTONE-AUDIT.md. Phase 15 is fully verified."

    10. **Verification Metadata** footer — verification approach: goal-backward (retrospective); must-haves source: ROADMAP.md Phase 15 Success Criteria; automated checks: 4 (build + 3 test suites) all green; human checks: 3 (covered by 15-UAT.md).

    11. Closing italic line: `_Verified: <timestamp>_` + `_Verifier: Claude (gsd-planner, retrospective — closes v1.2-MILESTONE-AUDIT.md DQ-01 + DQ-02 gap)_`
  </action>
  <acceptance_criteria>
    - File `.planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md` exists
    - `grep -c "^status: passed$" .planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md` returns `1`
    - `grep -c "^phase: 15-quality-check-engine-drill-down$" .planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md` returns `1`
    - `grep -c "DQ-01" .planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md` returns `>= 2`
    - `grep -c "DQ-02" .planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md` returns `>= 2`
    - `grep -c "ResourceIssueTable" .planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md` returns `>= 3`
    - `grep -c "15-UAT.md" .planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md` returns `>= 2`
    - `grep -cE "## (Observable Truths|Goal Achievement|Required Artifacts|Key Link Verification|Requirements Coverage|Gaps Summary)" .planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md` returns `>= 4`
    - File line count is between 100 and 250 lines (sanity range; 17-VERIFICATION.md is ~170 lines)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md && grep -c "^status: passed$" .planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md</automated>
  </verify>
  <done>15-VERIFICATION.md exists with `status: passed`, covers all 3 ROADMAP success criteria as observable truths, cites concrete evidence from UAT/VALIDATION/SUMMARY, and declares DQ-01 and DQ-02 SATISFIED.</done>
</task>

<task type="auto">
  <name>Task 2: Write retrospective 18-VERIFICATION.md</name>
  <files>.planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/18-quality-alerting-thresholds/18-UAT.md (10 UAT tests: 8 pass, 1 fixed, 1 waived — source of behavioural evidence)
    - .planning/phases/18-quality-alerting-thresholds/18-VALIDATION.md (per-task matrix + wave structure)
    - .planning/phases/18-quality-alerting-thresholds/18-01-SUMMARY.md (thresholds.ts + useThresholds hook)
    - .planning/phases/18-quality-alerting-thresholds/18-02-SUMMARY.md (context extension + 5 panel rollups)
    - .planning/phases/18-quality-alerting-thresholds/18-03-SUMMARY.md (SummaryCard breach + ThresholdsPage)
    - .planning/phases/18-quality-alerting-thresholds/18-04-SUMMARY.md (OverviewStrip 9-tile + ?tab= deep-linking)
    - .planning/phases/17-duplicate-detection-relational-integrity/17-VERIFICATION.md (structural template)
    - $HOME/.claude/get-shit-done/templates/verification-report.md (canonical template)
    - .planning/v1.2-MILESTONE-AUDIT.md (audit finding that mandates this file)
  </read_first>
  <action>
    Create the file `.planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md`. Required structure:

    **Frontmatter:**
    ```yaml
    ---
    phase: 18-quality-alerting-thresholds
    verified: <current-UTC-timestamp-ISO-8601>
    status: passed
    score: 4/4 must-haves verified
    retrospective: true
    closes_audit_finding:
      - id: DQ-11
        source: .planning/v1.2-MILESTONE-AUDIT.md
        prior_status: "partial (verification_status: missing)"
        new_status: "satisfied"
      - id: DQ-12
        source: .planning/v1.2-MILESTONE-AUDIT.md
        prior_status: "partial (verification_status: missing)"
        new_status: "satisfied"
    waived_items:
      - test: "UAT test 10 (?tab=bogus fallback)"
        reason: "Pre-existing ConnectionContext scope limitation — NOT a Phase 18 regression; logged in deferred-items.md"
        source: 18-UAT.md
    ---
    ```

    **Sections (in order):**

    1. **Title + intro** — "# Phase 18: Quality Alerting & Thresholds — Verification Report". State phase goal verbatim from ROADMAP.md Phase 18 ("Users can set quality thresholds and the dashboard visually flags any metrics that breach them"). Mark retrospective (written 2026-04-14 to close v1.2 audit gap; code shipped 2026-04-14 with UAT 8/10 pass + 1 fixed + 1 waived). Reference `v1.2-MILESTONE-AUDIT.md` as the source of the DQ-11/DQ-12 "partial" → "satisfied" transition.

    2. **Goal Achievement > Observable Truths** (4 rows — derived from ROADMAP.md Phase 18 Success Criteria 1/2/3/4):
       - Row 1: "User can configure a quality threshold per metric" → ✓ VERIFIED → Evidence: `18-UAT.md` test 2 (ThresholdsPage renders 7 rows) + test 3 (custom threshold persists on blur); `ThresholdsPage.tsx` NumberInput with save-on-blur; `useThresholds.setThreshold(key, value)` wired
       - Row 2: "Threshold configuration persists across page reloads (browser storage)" → ✓ VERIFIED → Evidence: `18-UAT.md` test 3 (reload preserves value); `useThresholds` wraps `useLocalStorage` against key `quality.thresholds.v1` (D-09 single-profile); `thresholds.test.ts` round-trip tests
       - Row 3: "Dashboard visually highlights metrics that breach their configured threshold (distinct color/icon)" → ✓ VERIFIED (after UAT test 8 fix: SummaryCard vertical-layout refactor un-clipped the ring) → Evidence: `18-UAT.md` test 8 `fixed`; `SummaryCard.tsx` breached→red ring + `threshold: N%` annotation; 8/8 tests in `summary-card.test.tsx`; OverviewStrip.tsx breach gating
       - Row 4: "Alerting covers both existing metrics (completeness, coding) and new metrics (conformance, plausibility, duplicates, integrity)" → ✓ VERIFIED → Evidence: `QualityMetricsContext.tsx` exposes 7 `overall*` values + `duplicatesBreakdown`; 5 panels (Validation, Plausibility, LabRanges, References, Duplicates) push rollups on terminal status; OverviewStrip 9-tile layout in `18-UAT.md` test 7
       - Score line: `**Score:** 4/4 truths verified`

    3. **Required Artifacts** table — enumerate key files from the 4 Plan SUMMARIES:
       - `src/quality/thresholds.ts` (MetricKey, METRIC_LABELS, METRIC_ROUTES, DEFAULT_THRESHOLDS, STORAGE_KEY, resolveThreshold, isBreached)
       - `src/hooks/useThresholds.ts` (8-member API)
       - `src/quality/QualityMetricsContext.tsx` (extended 2→7 overall* + duplicatesBreakdown)
       - `src/components/quality/SummaryCard.tsx` (breached/threshold/onClick/ariaLabel props, vertical layout)
       - `src/components/quality/ThresholdsPage.tsx` (7-row config table + Reset modal)
       - `src/components/quality/OverviewStrip.tsx` (9-tile layout)
       - `src/components/quality/QualityOverviewPage.tsx` ("Configure thresholds" button + controlled Tabs)
       - `src/App.tsx` (route wiring)
       - Test files: `thresholds.test.ts`, `summary-card.test.tsx`, `thresholds-page.test.tsx`, `lab-ranges-panel.test.tsx`, `references-panel.test.tsx`, `plausibility-panel.test.tsx`, `quality-overview.test.tsx`

    4. **Key Link Verification** table — at least these 6 links (all ✓ WIRED):
       - useThresholds ⇔ `quality.thresholds.v1` localStorage key
       - SummaryCard breached=true → red ring + threshold annotation
       - OverviewStrip metric tile onClick → `useNavigate('/quality?tab=<route>')` using METRIC_ROUTES
       - QualityOverviewPage Tabs ↔ `useSearchParams` (read `?tab=`, write via `setSearchParams` with `replace: true`, fallback to Counts on invalid)
       - ThresholdsPage NumberInput blur → `setThreshold(key, number)`; Clear icon → `setThreshold(key, null)`; Reset modal → `resetAll()`
       - DuplicatesPanel `setDuplicatesContribution({ patient, hashType })` + `resourceType` in dep array → per-type averaging widens on widening usage

    5. **Requirements Coverage** table — 2 rows (DQ-11, DQ-12) both SATISFIED with Plan NN + evidence citations.

    6. **Behavioral Spot-Checks** table — at minimum:
       - `npx vitest run src/__tests__/thresholds.test.ts` → 12/12 pass
       - `npx vitest run src/__tests__/summary-card.test.tsx` → 8/8 pass
       - `npx vitest run src/__tests__/thresholds-page.test.tsx` → pass
       - `npx vitest run src/__tests__/quality-overview.test.tsx` → 25/26 pass (1 pre-existing unrelated failure, documented in 18-UAT test 8 resolution notes)
       - `grep -c "quality.thresholds.v1" src/quality/thresholds.ts` → ≥1

    7. **Anti-Patterns Found** — "None found. One UAT test (test 8) initially flagged `Rings are not visible` as a layout bug; fixed in-phase by SummaryCard vertical-layout refactor. No remaining blocker or warning anti-patterns."

    8. **Human Verification Required** — Note: already covered by `18-UAT.md` tests 1–9 (Test 10 was explicitly waived — see frontmatter `waived_items` and below).

    9. **Gaps Summary** — "No critical gaps. One UAT test (test 10: ?tab=bogus fallback) was waived because the failure mode is a pre-existing ConnectionContext scope limitation, NOT a Phase 18 regression — all gated routes (/quality, /explorer, /patients) show 'Not connected' on cold-start URLs because ConnectionContext has no auto-reconnect. The Phase 18 tab-routing logic itself is verified correct by test 9 (which exercises the same useSearchParams → activeTab code path via tile click, `pass`). The waived item is tracked as tech debt in `deferred-items.md`, not a Phase 18 blocker. Phase 18 goal (thresholds + breach visualization) is fully met. This retrospective VERIFICATION.md closes the DQ-11 and DQ-12 'verification_status: missing' findings from v1.2-MILESTONE-AUDIT.md."

    10. **Verification Metadata** footer — goal-backward retrospective; must-haves source: ROADMAP.md Phase 18 Success Criteria; 5+ automated checks green; human checks: 9 UAT passed / 1 fixed / 1 waived.

    11. Closing italic line: `_Verified: <timestamp>_` + `_Verifier: Claude (gsd-planner, retrospective — closes v1.2-MILESTONE-AUDIT.md DQ-11 + DQ-12 gap)_`
  </action>
  <acceptance_criteria>
    - File `.planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md` exists
    - `grep -c "^status: passed$" .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md` returns `1`
    - `grep -c "^phase: 18-quality-alerting-thresholds$" .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md` returns `1`
    - `grep -c "DQ-11" .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md` returns `>= 2`
    - `grep -c "DQ-12" .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md` returns `>= 2`
    - `grep -c "ThresholdsPage" .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md` returns `>= 2`
    - `grep -c "SummaryCard" .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md` returns `>= 2`
    - `grep -c "useThresholds" .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md` returns `>= 2`
    - `grep -c "18-UAT.md" .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md` returns `>= 2`
    - `grep -c "quality.thresholds.v1" .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md` returns `>= 1`
    - `grep -cE "## (Observable Truths|Goal Achievement|Required Artifacts|Key Link Verification|Requirements Coverage|Gaps Summary)" .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md` returns `>= 4`
    - `grep -c "waived" .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md` returns `>= 1` (acknowledges the waived test 10)
    - File line count between 120 and 300 lines
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md && grep -c "^status: passed$" .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md</automated>
  </verify>
  <done>18-VERIFICATION.md exists with `status: passed`, covers all 4 ROADMAP success criteria as observable truths, cites concrete evidence from UAT/VALIDATION/SUMMARY, records the waived UAT test 10 explicitly with rationale, and declares DQ-11 and DQ-12 SATISFIED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. Documentation-only changes to `.planning/` — no code, no config, no runtime artifacts.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-01 | Information Disclosure | .planning/phases/**/15-VERIFICATION.md | accept | No secrets or PII; content is project metadata citing already-committed code paths. |
| T-20-02-02 | Information Disclosure | .planning/phases/**/18-VERIFICATION.md | accept | Same as above. |

Rationale: No new attack surface. Markdown writes inside `.planning/` only. No user input, no auth, no network I/O.
</threat_model>

<verification>
**Documentation gate (closes DQ-01/DQ-02/DQ-11/DQ-12 verification-status gap):**
```bash
test -f .planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md
test -f .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md
grep -l "^status: passed$" .planning/phases/15-quality-check-engine-drill-down/15-VERIFICATION.md
grep -l "^status: passed$" .planning/phases/18-quality-alerting-thresholds/18-VERIFICATION.md
```
All four commands must exit 0.

**Audit re-run (after both files exist):**
`/gsd-audit-milestone v1.2` (or equivalent re-audit) should flip DQ-01/DQ-02/DQ-11/DQ-12 from "partial" → "satisfied". This is an out-of-band check, not part of this plan's automated verify.
</verification>

<success_criteria>
- `15-VERIFICATION.md` exists at the correct path with `status: passed`, covering all 3 Phase 15 ROADMAP success criteria
- `18-VERIFICATION.md` exists at the correct path with `status: passed`, covering all 4 Phase 18 ROADMAP success criteria (with Test 10 waiver explicitly noted)
- Both files cite concrete evidence from existing UAT, VALIDATION, SUMMARY, and source files (no fabricated claims)
- Requirements-coverage tables mark DQ-01, DQ-02 (Phase 15) and DQ-11, DQ-12 (Phase 18) as SATISFIED
- On next `/gsd-audit-milestone v1.2` run, DQ-01/DQ-02/DQ-11/DQ-12 transition from "partial" to "satisfied"
</success_criteria>

<output>
After completion, create `.planning/phases/20-v12-gap-closure/20-02-SUMMARY.md` using `$HOME/.claude/get-shit-done/templates/summary.md`. Frontmatter must list `DQ-01`, `DQ-02`, `DQ-11`, `DQ-12` under `requirements-completed` with provenance "retrospective verification (closes v1.2-MILESTONE-AUDIT gap)".
</output>
