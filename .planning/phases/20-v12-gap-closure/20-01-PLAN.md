---
phase: 20-v12-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quality/profileConformanceChecker.ts
  - src/quality/temporalPlausibilityWalker.ts
autonomous: true
requirements: [DEBT-02]
tags: [tech-debt, typescript, strict-mode, cast-widening, TS2352]

must_haves:
  truths:
    - "Running `npx tsc -b --noEmit` at the repo root exits 0 with zero errors"
    - "Running `npm run build` at the repo root exits 0 with zero errors"
    - "Zero occurrences of the single-cast pattern `as Record<string, unknown>` remain in the two target files (they MUST all be the double-cast form `as unknown as Record<string, unknown>`)"
    - "Runtime behaviour of profileConformanceChecker and temporalPlausibilityWalker is unchanged (same inputs produce same outputs — casts are compile-time only)"
  artifacts:
    - path: "src/quality/profileConformanceChecker.ts"
      provides: "Profile conformance checker with widened casts at lines 139, 142, 143, 144, 147, 296"
      contains: "as unknown as Record<string, unknown>"
    - path: "src/quality/temporalPlausibilityWalker.ts"
      provides: "Temporal plausibility walker with widened cast at line 439"
      contains: "as unknown as Record<string, unknown>"
  key_links:
    - from: "src/quality/profileConformanceChecker.ts"
      to: "tsc strict mode"
      via: "double-cast via unknown satisfies TS2352"
      pattern: "as unknown as Record<string, unknown>"
    - from: "src/quality/temporalPlausibilityWalker.ts"
      to: "tsc strict mode"
      via: "double-cast via unknown satisfies TS2352"
      pattern: "as unknown as Record<string, unknown>"
---

<objective>
Fix all 7 TS2352 errors introduced in Phase 16 so `npx tsc -b --noEmit` and `npm run build` both exit 0. Closes DEBT-02 and unblocks Flow D (Clean Build) from the v1.2 milestone audit.

Purpose: Phase 14 established a zero-error build contract. Phase 16 regressed it by using the single-cast pattern `(x as Record<string, unknown>)` on FHIR union types (`ElementDefinition`, `Resource`) which TypeScript strict mode rejects (the union types have no index signature). Phase 14's own SUMMARY already established the fix pattern: double-cast via `unknown` (`(x as unknown as Record<string, unknown>)`).

Output: 7 cast sites fixed across 2 files; clean build; commit.

Scope: Compile-time only. Runtime behaviour is identical (the double-cast is a no-op at runtime — it only satisfies the type checker).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/phases/15-quality-check-engine-drill-down/15-01-SUMMARY.md
@src/quality/profileConformanceChecker.ts
@src/quality/temporalPlausibilityWalker.ts

<prior_art>
Phase 15 Plan 01 SUMMARY documents the exact same fix being applied successfully:

> **[Rule 1 - Bug] Fixed TS2352 cast errors in walker implementations**
> - **Issue:** `Resource as Record<string, unknown>` fails TypeScript strict checks because FHIR union type `Resource` doesn't overlap with index signature types
> - **Fix:** Changed to `r as unknown as Record<string, unknown>` (double cast via unknown)

This is the established, proven pattern in this codebase. Apply it mechanically — no design questions.
</prior_art>

<interfaces>
The 7 cast sites are **all identical in form** and all cast a FHIR union type to `Record<string, unknown>` to read a property by string key. The change is mechanical:

```typescript
// BEFORE (fails TS2352):
(el as Record<string, unknown>).path
(resource as Record<string, unknown>).id

// AFTER (passes strict mode):
(el as unknown as Record<string, unknown>).path
(resource as unknown as Record<string, unknown>).id
```

### The 7 exact sites (verified via `npx tsc -b --noEmit` on 2026-04-14):

**File 1: `src/quality/profileConformanceChecker.ts`** (6 sites, all inside `checkResource` / `normalizeConformanceIssues`)

| # | Line | Column | Current expression | Fixed expression |
|---|------|--------|--------------------|--------------------------------|
| 1 | 139 | 19 | `(el as Record<string, unknown>).path` | `(el as unknown as Record<string, unknown>).path` |
| 2 | 142 | 18 | `(el as Record<string, unknown>).min` | `(el as unknown as Record<string, unknown>).min` |
| 3 | 143 | 18 | `(el as Record<string, unknown>).max` | `(el as unknown as Record<string, unknown>).max` |
| 4 | 144 | 22 | `(el as Record<string, unknown>).type` | `(el as unknown as Record<string, unknown>).type` |
| 5 | 147 | 22 | `(el as Record<string, unknown>).binding` | `(el as unknown as Record<string, unknown>).binding` |
| 6 | 296 | 46 | `(resource as Record<string, unknown>).id` | `(resource as unknown as Record<string, unknown>).id` |

**File 2: `src/quality/temporalPlausibilityWalker.ts`** (1 site, inside `normalizeTemporalIssues`)

| # | Line | Column | Current expression | Fixed expression |
|---|------|--------|--------------------|--------------------------------|
| 7 | 439 | 46 | `(resource as Record<string, unknown>).id` | `(resource as unknown as Record<string, unknown>).id` |

All 7 errors are `TS2352: Conversion of type '...' to type 'Record<string, unknown>' may be a mistake because neither type sufficiently overlaps with the other. If this was intentional, convert the expression to 'unknown' first.`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen 6 casts in profileConformanceChecker.ts</name>
  <files>src/quality/profileConformanceChecker.ts</files>
  <read_first>
    - src/quality/profileConformanceChecker.ts (read lines 130-160 and 285-303 to see the current code in context before editing)
    - .planning/phases/15-quality-check-engine-drill-down/15-01-SUMMARY.md (confirms the established fix pattern: double-cast via unknown)
  </read_first>
  <action>
    Make exactly 6 one-line edits in `src/quality/profileConformanceChecker.ts`. For each of lines 139, 142, 143, 144, 147, 296: change `(X as Record<string, unknown>)` to `(X as unknown as Record<string, unknown>)` where X is the local variable (`el` for the five ElementDefinition casts at 139/142/143/144/147; `resource` for the Resource cast at 296).

    Keep every surrounding character unchanged — only insert ` unknown as` between `as` and the target type. Do NOT reformat the file, do NOT touch any other lines, do NOT change the property accesses or variable names.

    After the edits, these exact 6 substrings MUST appear in the file:
    - `(el as unknown as Record<string, unknown>).path`
    - `(el as unknown as Record<string, unknown>).min`
    - `(el as unknown as Record<string, unknown>).max`
    - `(el as unknown as Record<string, unknown>).type`
    - `(el as unknown as Record<string, unknown>).binding`
    - `(resource as unknown as Record<string, unknown>).id`

    And this substring MUST NOT appear anywhere in the file (single-cast pattern eradicated):
    - `(el as Record<string, unknown>)`
    - `(resource as Record<string, unknown>)`
  </action>
  <acceptance_criteria>
    - `grep -c "as unknown as Record<string, unknown>" src/quality/profileConformanceChecker.ts` returns `6`
    - `grep -cE "\(el as Record<string, unknown>\)" src/quality/profileConformanceChecker.ts` returns `0`
    - `grep -cE "\(resource as Record<string, unknown>\)" src/quality/profileConformanceChecker.ts` returns `0`
    - `npx tsc -b --noEmit 2>&1 | grep -c "profileConformanceChecker"` returns `0`
    - `npx vitest run src/quality/profileConformanceChecker.test.ts` exits 0 (all existing tests still pass — no runtime behaviour change)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "as unknown as Record<string, unknown>" src/quality/profileConformanceChecker.ts</automated>
  </verify>
  <done>6 casts widened; grep checks above pass; profileConformanceChecker-specific TS2352 errors gone; profileConformanceChecker.test.ts still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Widen 1 cast in temporalPlausibilityWalker.ts</name>
  <files>src/quality/temporalPlausibilityWalker.ts</files>
  <read_first>
    - src/quality/temporalPlausibilityWalker.ts (read lines 430-445 to see the current code in `normalizeTemporalIssues` before editing)
  </read_first>
  <action>
    Make exactly 1 one-line edit in `src/quality/temporalPlausibilityWalker.ts`. On line 439, change `(resource as Record<string, unknown>)` to `(resource as unknown as Record<string, unknown>)`. The surrounding code (the `${resource.resourceType}/${...}` template literal and `?? 'unknown'` fallback) must remain byte-identical.

    After the edit, this exact substring MUST appear in the file:
    - `(resource as unknown as Record<string, unknown>).id`

    And this substring MUST NOT appear:
    - `(resource as Record<string, unknown>)`
  </action>
  <acceptance_criteria>
    - `grep -c "as unknown as Record<string, unknown>" src/quality/temporalPlausibilityWalker.ts` returns `1`
    - `grep -cE "\(resource as Record<string, unknown>\)" src/quality/temporalPlausibilityWalker.ts` returns `0`
    - `npx tsc -b --noEmit 2>&1 | grep -c "temporalPlausibilityWalker"` returns `0`
    - `npx vitest run src/quality/temporalPlausibilityWalker.test.ts` exits 0 (all existing tests still pass)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "as unknown as Record<string, unknown>" src/quality/temporalPlausibilityWalker.ts</automated>
  </verify>
  <done>1 cast widened; grep checks pass; temporalPlausibilityWalker-specific TS2352 errors gone; temporalPlausibilityWalker.test.ts still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Verify clean build end-to-end</name>
  <files>(no files modified; verification only)</files>
  <read_first>
    - .planning/v1.2-MILESTONE-AUDIT.md (confirms the success condition: `npx tsc -b --noEmit` and `npm run build` both exit 0)
  </read_first>
  <action>
    Run the two build commands listed in the Phase 20 success criteria and confirm both exit 0:

    1. `npx tsc -b --noEmit` — must exit 0 with no output on stderr
    2. `npm run build` — must exit 0 (Vite production build)

    Then run the full test suite to confirm no regressions from the cast widening:

    3. `npm test` — must exit 0 (all previously-passing tests still pass)

    If any command fails, stop and report the remaining errors — do NOT patch anything new; re-inspect Task 1 / Task 2 for missed sites.

    When all three pass, the Flow D "Clean Build" gap is closed and DEBT-02 is satisfied.
  </action>
  <acceptance_criteria>
    - `npx tsc -b --noEmit; echo $?` prints `0` on its own line
    - `npm run build 2>&1 | tail -1` contains `built in` (Vite success banner) and `echo $?` is `0`
    - `npx tsc -b --noEmit 2>&1 | grep -c "TS2352"` returns `0` (entire error class eradicated)
    - `npx tsc -b --noEmit 2>&1 | grep -cE "(profileConformanceChecker|temporalPlausibilityWalker)"` returns `0`
    - `npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b --noEmit && npm run build</automated>
  </verify>
  <done>Both `npx tsc -b --noEmit` and `npm run build` exit 0; zero TS2352 errors anywhere in the repo; full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None affected. The change is a pure TypeScript cast widening — no runtime code path changes, no new code, no new inputs.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-01 | Tampering | profileConformanceChecker.ts | accept | Cast widening is a compile-time no-op. The emitted JS is byte-identical to the pre-cast runtime (casts erase). Existing unit test suite (profileConformanceChecker.test.ts) confirms behavioural equivalence. |
| T-20-01-02 | Information Disclosure | temporalPlausibilityWalker.ts | accept | Same rationale — compile-time only. |

Rationale: No new attack surface, no user input, no auth boundaries, no network I/O, no new file writes. Low risk acceptable per `security_enforcement` gate for mechanical tech-debt fixes.
</threat_model>

<verification>
**Build gate (closes DEBT-02 + Flow D):**
```bash
npx tsc -b --noEmit && echo "tsc OK"
npm run build && echo "build OK"
```
Both must exit 0.

**Regression gate:**
```bash
npm test
```
Must exit 0 — cast widening is runtime-inert, so any test failure would indicate an unrelated break and must be investigated separately.

**Pattern eradication check:**
```bash
grep -rE "\(el as Record<string, unknown>\)|\(resource as Record<string, unknown>\)" src/quality/
```
Must return zero lines.
</verification>

<success_criteria>
- `npx tsc -b --noEmit` exits 0
- `npm run build` exits 0
- `npm test` exits 0 (no test regressions)
- DEBT-02 requirement is satisfiable (checkbox flip deferred to Plan 20-03)
- Flow D (Clean Build) in the v1.2 audit flips from broken → complete on next re-audit
</success_criteria>

<output>
After completion, create `.planning/phases/20-v12-gap-closure/20-01-SUMMARY.md` using `$HOME/.claude/get-shit-done/templates/summary.md`. Frontmatter must list `DEBT-02` under `requirements-completed`.
</output>
