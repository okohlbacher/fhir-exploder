---
phase: 23
plan: 7
plan_number: 7
plan_name: "Flip 23-VERIFICATION.md to `passed` and confirm Nyquist flags on 21/22-VALIDATION.md"
type: execute
wave: 3
depends_on: [6]
files_modified:
  - .planning/phases/23-v1.3-close-out/23-VERIFICATION.md
  - .planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md
  - .planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - CLOSE-06
  - CLOSE-07
status: pending

must_haves:
  truths:
    - "23-VERIFICATION.md frontmatter `status:` flips from `human_needed` to `passed`."
    - "23-VERIFICATION.md `human_verification:` block is either removed or moved into a `historical_notes:` / resolved block (so readers do not misread outstanding items)."
    - "23-VERIFICATION.md `score:` line updated from `6/7 must-haves verified (CLOSE-06 partial ...)` to `7/7 must-haves verified` (or similar wording reflecting full pass)."
    - "21-VALIDATION.md frontmatter `nyquist_compliant: true` and `wave_0_complete: true` are CONFIRMED present (if already flipped in commit e1b9a93 per 23-VERIFICATION.md truth #8, this is a verification-only step; only edit if still `false`)."
    - "22-VALIDATION.md frontmatter `nyquist_compliant: true` and `wave_0_complete: true` CONFIRMED present (same as above)."
    - "Commit message explicitly names the trigger: UAT re-run green per Plan 23-06."
    - "REQUIREMENTS.md line 27 `- [ ] **CLOSE-07**` becomes `- [x] **CLOSE-07**` (checkbox flipped to checked). Note: CLOSE-07 IS a `- [ ]` checkbox at REQUIREMENTS.md line 27 — confirmed via Read tool 2026-04-22 — no conditional; this is a deterministic edit, not an if-check."
  artifacts:
    - path: ".planning/phases/23-v1.3-close-out/23-VERIFICATION.md"
      provides: "Frontmatter status: passed; score: 7/7; human_verification block removed or marked resolved"
      contains: "status: passed"
    - path: ".planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md"
      provides: "Frontmatter with nyquist_compliant: true and wave_0_complete: true"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md"
      provides: "Frontmatter with nyquist_compliant: true and wave_0_complete: true"
      contains: "nyquist_compliant: true"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CLOSE-07 checkbox flipped from unchecked to checked on line 27"
      contains: "- [x] **CLOSE-07**"
  key_links:
    - from: "Plan 23-06 green merge"
      to: "23-VERIFICATION.md status flip preconditions"
      via: "depends_on: [6] — UAT MUST pass first"
      pattern: "depends_on"
    - from: "23-VERIFICATION.md status: passed"
      to: "Phase 23 ships-clean signal"
      via: "downstream: /gsd-audit-milestone v1.3 picks up the flip and re-verifies shipped-clean verdict"
      pattern: "shipped-clean"
    - from: "21-VALIDATION.md + 22-VALIDATION.md nyquist_compliant: true"
      to: "CLOSE-07 Nyquist audit sign-off"
      via: "D-08 mechanical YAML edit (if not already committed)"
      pattern: "nyquist_compliant: true"
    - from: "REQUIREMENTS.md line 27 checkbox"
      to: "CLOSE-07 tracking state"
      via: "- [ ] → - [x] edit on CLOSE-07 line"
      pattern: "- \\[x\\] \\*\\*CLOSE-07\\*\\*"
---

<objective>
Close Phase 23 cleanly: flip 23-VERIFICATION.md `status:` from `human_needed` to `passed` now that Plan 23-06 has delivered the live-Blaze pass for T-6.3 A and T-6.3 B. Confirm the Nyquist flags on Phase 21 and Phase 22 VALIDATION.md files are set to `true` — per 23-VERIFICATION.md truth #8, commit e1b9a93 already did this mechanical flip, so this plan is VERIFICATION-FIRST: read and assert they are already true, only edit if they are not. Also flip REQUIREMENTS.md line 27 CLOSE-07 checkbox from `- [ ]` to `- [x]`.

Purpose: provide the final milestone-audit trigger. After this commit lands, `/gsd-audit-milestone v1.3` can flip v1.3's verdict from `tech_debt` to `shipped-clean`, which is the explicit success criterion of Phase 23 per ROADMAP.md line 65.

Output: 23-VERIFICATION.md updated; 21/22-VALIDATION.md confirmed (or flipped if needed); REQUIREMENTS.md CLOSE-07 checkbox flipped; 1 commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-v1.3-close-out/23-CONTEXT.md
@.planning/phases/23-v1.3-close-out/23-VERIFICATION.md
@.planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md
@.planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md
@.planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm 21/22-VALIDATION.md Nyquist flags (verify-first; edit only if needed)</name>
  <files>
    .planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md,
    .planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md (first 15 lines — frontmatter)
    - .planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md (first 15 lines — frontmatter)
    - .planning/phases/23-v1.3-close-out/23-VERIFICATION.md (truth #8 — claims commit e1b9a93 already flipped both; this task is the verification)
    - .planning/phases/23-v1.3-close-out/23-CONTEXT.md §D-08 (flip mechanics: edit YAML frontmatter directly; do NOT re-run /gsd-validate-phase)
  </read_first>
  <action>
    1. Read the first 10 lines of each VALIDATION.md file. Confirm both `nyquist_compliant: true` AND `wave_0_complete: true` are present.
    2. If BOTH are already `true` in BOTH files: make NO edits. Document the no-op in the SUMMARY. Skip to Task 2.
    3. If EITHER flag is `false` in EITHER file: edit the frontmatter to flip it to `true`. Leave all other frontmatter fields untouched. No body edits — D-08 locks the flip as a mechanical YAML-only change.
    4. If the files were edited, run: `git diff --stat .planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md .planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md` — expected stat: 2 files changed, <=4 insertions, <=4 deletions.
    5. Do NOT commit yet — Task 2 bundles all Phase 23 close-out edits into one commit.
  </action>
  <verify>
    <automated>grep -E '^(nyquist_compliant|wave_0_complete): true' .planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md .planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md | wc -l | awk '{if ($1 == 4) exit 0; else exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "nyquist_compliant: true" .planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md` returns 1.
    - `grep -c "wave_0_complete: true" .planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md` returns 1.
    - `grep -c "nyquist_compliant: true" .planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md` returns 1.
    - `grep -c "wave_0_complete: true" .planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md` returns 1.
    - `grep -c "nyquist_compliant: false" .planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md .planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md` returns 0.
  </acceptance_criteria>
  <done>
    Both Phase 21 and Phase 22 VALIDATION.md files have `nyquist_compliant: true` and `wave_0_complete: true`. If they were already set (expected per 23-VERIFICATION.md truth #8), no edit needed — verified-and-left-alone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip 23-VERIFICATION.md status to `passed`, flip REQUIREMENTS.md CLOSE-07 checkbox, and commit milestone close-out</name>
  <files>
    .planning/phases/23-v1.3-close-out/23-VERIFICATION.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/phases/23-v1.3-close-out/23-VERIFICATION.md (full file — especially frontmatter lines 1-14 and Score line at 38)
    - .planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md (post-Plan-23-06 state — Summary `passed: 2, issues: 0`)
    - .planning/REQUIREMENTS.md (line 27 — confirm CLOSE-07 checkbox is currently `- [ ] **CLOSE-07**`)
  </read_first>
  <action>
    **Frontmatter edits (lines 1-14) — ADD-NEW convention (Warning 5 fix; aligned with step 11):**

    1. Line 3 currently reads `verified: 2026-04-17T12:00:00Z`. Do NOT overwrite it. ADD a NEW LINE directly BELOW line 3 reading `re_verified: <today ISO timestamp>` (e.g. `re_verified: 2026-04-22T...`). This preserves the original audit-trail line and records the re-verification separately — mirrors the "keep original, add re-verified" pattern used in step 11 for the body timestamp, resolving the step-1-vs-step-11 ambiguity.
    2. Line 4 `status: human_needed` → `status: passed`. (This IS an overwrite — the old value is now incorrect; no audit-trail value in preserving the stale `human_needed`.)
    3. Line 5 `score: 6/7 must-haves verified (CLOSE-06 partial — 2 UAT items environmental-deferred)` → `score: 7/7 must-haves verified (CLOSE-06 fully resolved via Plan 23-05 + 23-06 live-Blaze re-run)`.
    4. Lines 7-14 (`human_verification:` block): remove the entire block. The two items it contained (T-6.3 A and T-6.3 B) are now resolved and live under `### Deferred Items` historical section if you want to keep a breadcrumb; otherwise simply delete.

    **Body edits:**

    5. Line 20 `Status: human_needed` → `Status: passed`.
    6. Line 21 `Re-verification: No — initial verification` → `Re-verification: Yes — T-6.3 A root-cause fixed by Plan 23-05, re-run passed by Plan 23-06 on <today>`.
    7. Truth table row 7 (CLOSE-06): change `PARTIAL` → `VERIFIED`. Update the Evidence column to reference Plan 23-05 commits (Bug A + Bug B fixes) and Plan 23-06 live-Blaze pass. Keep the history note that initial run had 2 environmental-deferred items — those were resolved by the Plan 23-05 fix + Plan 23-06 re-run.
    8. `### Deferred Items` section: either remove the two rows (T-6.3 A and T-6.3 B now resolved) OR mark them as RESOLVED with a pointer to Plan 23-05 + 23-06 commits. Prefer the "marked resolved" option so the audit trail is preserved.
    9. `### Human Verification Required` section (lines 118-133): add a block-level `**RESOLVED 2026-04-22:**` note at the top, or delete the section if the resolved breadcrumb in frontmatter + score line is sufficient. Prefer resolved-annotation for audit trail.
    10. `### Gaps Summary` section (lines 135-145): append a paragraph: `Resolution: T-6.3 A root cause (OverviewStrip tiles unscoped + 4 report hooks stale on cohort change) was diagnosed in commit 8656431 and fixed in Plan 23-05 (commits <fill-in from git log>). Plan 23-06 delivered the live-Blaze pass on <today>. Phase 23 is now shipped-clean.`
    11. Last line `_Verified: 2026-04-17T12:00:00Z_` → add a new line below: `_Re-verified: <today ISO>_`. Keep the original verification line for audit trail. (This is the mirror of step 1 — both timestamps preserve the original and ADD the new; no overwrites of the original timestamp.)

    **REQUIREMENTS.md CLOSE-07 checkbox flip (Warning 6 fix — deterministic, not conditional):**

    12. Read `.planning/REQUIREMENTS.md` line 27. Confirm it currently reads `- [ ] **CLOSE-07**: ...` (verified via Read tool 2026-04-22 — this IS the current state).
    13. Edit line 27 to change `- [ ]` to `- [x]`. Preserve the `**CLOSE-07**` bold marker and the description text after the colon. Result: `- [x] **CLOSE-07**: nyquist_compliant: true flipped in both Phase 21 and Phase 22 VALIDATION.md once CLOSE-06 UAT is green (N1 audit sign-off).`
    14. Do NOT flip any other checkbox on REQUIREMENTS.md. CLOSE-01..06 may or may not already be checked (separate plans). CLOSE-08 is the newly-added requirement and stays unchecked.

    **Commit (bundles Task 1 changes if any, plus Task 2 changes):**

    ```bash
    git add .planning/phases/23-v1.3-close-out/23-VERIFICATION.md \
            .planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md \
            .planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md \
            .planning/REQUIREMENTS.md

    git commit -m "docs(23): flip VERIFICATION to passed after Plan 23-06 UAT re-run

    CLOSE-06 now fully satisfied — T-6.3 A + B live-Blaze pass recorded in
    Plan 23-06 after Plan 23-05 fixed the two root-cause defects diagnosed
    in 23-HUMAN-UAT.md (Bug A OverviewStrip tiles unscoped; Bug B 4 report
    hooks stale on patientIds change).

    CLOSE-07 Nyquist flags on 21-VALIDATION.md + 22-VALIDATION.md confirmed
    at nyquist_compliant: true (commit e1b9a93 already flipped them; this
    commit verifies-and-locks). REQUIREMENTS.md line 27 CLOSE-07 checkbox
    flipped from - [ ] to - [x].

    Milestone v1.3 is now eligible for shipped-clean audit verdict.
    Trigger /gsd-audit-milestone v1.3 as downstream step."
    ```

    (Note: if Task 1 made no edits, the commit touches 2 files — 23-VERIFICATION.md + REQUIREMENTS.md. If Task 1 needed to flip the Nyquist flags, the commit touches 4 files.)
  </action>
  <verify>
    <automated>grep -c '^status: passed' .planning/phases/23-v1.3-close-out/23-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^status: passed" .planning/phases/23-v1.3-close-out/23-VERIFICATION.md` returns 1.
    - `grep -c "^status: human_needed" .planning/phases/23-v1.3-close-out/23-VERIFICATION.md` returns 0.
    - `grep -c "^verified: 2026-04-17T12:00:00Z" .planning/phases/23-v1.3-close-out/23-VERIFICATION.md` returns 1 (original preserved — Warning 5 fix: no overwrite).
    - `grep -c "^re_verified:" .planning/phases/23-v1.3-close-out/23-VERIFICATION.md` returns 1 (new line added — Warning 5 fix).
    - `grep -c "human_verification:" .planning/phases/23-v1.3-close-out/23-VERIFICATION.md` returns 0 (block removed) OR it appears only inside a resolved/historical context.
    - `grep -E "7/7 must-haves" .planning/phases/23-v1.3-close-out/23-VERIFICATION.md` matches at least once.
    - `grep -E "Plan 23-05|Plan 23-06" .planning/phases/23-v1.3-close-out/23-VERIFICATION.md` matches at least twice (evidence references to the fix plans).
    - `grep -c "^- \\[x\\] \\*\\*CLOSE-07\\*\\*" .planning/REQUIREMENTS.md` returns 1 (Warning 6 fix — CLOSE-07 checkbox flipped to checked).
    - `grep -c "^- \\[ \\] \\*\\*CLOSE-07\\*\\*" .planning/REQUIREMENTS.md` returns 0 (unchecked variant gone).
    - `git log --oneline -1` shows a commit with message matching `^docs\(23\): flip VERIFICATION to passed`.
    - `git diff HEAD~1 HEAD --stat` shows 2-4 files changed (2 if VALIDATION.md files were already correct: VERIFICATION.md + REQUIREMENTS.md; 4 if both VALIDATION.md files needed flipping).
  </acceptance_criteria>
  <done>
    23-VERIFICATION.md reads as a passed verification report with clear audit trail pointing at Plan 23-05 fix + Plan 23-06 re-run (original `verified:` timestamp preserved, `re_verified:` added). REQUIREMENTS.md CLOSE-07 checkbox flipped to `- [x]`. Commit landed. Phase 23 ready for /gsd-audit-milestone v1.3 re-run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning files → git | Files committed to public repo. MUST remain PHI-clean. |
| Audit trail → future reader | A false `passed` status erodes the whole GSD workflow's trust signal. |

## STRIDE Threat Register (ASVS L1 — docs-only plan)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07-01 | Tampering | VERIFICATION.md flipped to `passed` without UAT evidence | mitigate | depends_on: [6] enforces Plan 23-06 green before this runs. Task 2 commit message explicitly cites Plan 23-06 pass. Acceptance criteria require `Plan 23-05\|Plan 23-06` references in the updated file. |
| T-23-07-02 | Repudiation | No audit trail for the flip | mitigate | Task 2 Step 1 ADDS `re_verified:` alongside the original `verified:` line (no overwrite); Step 11 does the same at the body's trailing timestamp; Gaps Summary section gets a resolution paragraph pointing at commits. Warning 5 fix. |
| T-23-07-03 | Information Disclosure | Commit message leaks server URL/patient count | accept | Commit message does not include any server-specific data; patient counts are synthetic (139) and already in prior commits. |
| T-23-07-04 | DoS / Repudiation | Accidentally flip Nyquist on wrong phase | mitigate | Task 1 verify-first: grep existing values before editing. If already true, no edit. If needs flip, edit mechanical YAML only — D-08 locked this scope. |
| T-23-07-05 | Tampering | REQUIREMENTS.md CLOSE-07 checkbox flipped prematurely | mitigate | depends_on: [6] enforces Plan 23-06 green first; Task 2 step 12 explicitly re-verifies line 27 current state via Read before edit. No other checkboxes touched. |
| T-23-07-06 | Spoofing | N/A | accept | No auth flow. |
| T-23-07-07 | Elevation of Privilege | N/A | accept | No privilege boundary. |
</threat_model>

<verification>
1. 23-VERIFICATION.md has `status: passed` in frontmatter (zero instances of `status: human_needed`).
2. 23-VERIFICATION.md has both `verified:` AND `re_verified:` lines in frontmatter (Warning 5: original preserved, new line added).
3. 21-VALIDATION.md + 22-VALIDATION.md have `nyquist_compliant: true` and `wave_0_complete: true`.
4. REQUIREMENTS.md line 27 CLOSE-07 checkbox is `- [x]` (Warning 6 fix: deterministic flip, not conditional).
5. `git log --oneline -5` shows Phase 23 close-out commit chain: test(23) RED → fix(23) Bug A → fix(23) Bug B → docs(23) UAT re-run PASS → docs(23) flip VERIFICATION to passed.
6. STATE.md may need updating in a follow-up — this plan does NOT touch STATE.md; the orchestrator or next session will reconcile.
</verification>

<success_criteria>
- 23-VERIFICATION.md status: passed, score: 7/7, human_verification block resolved, `re_verified:` added alongside original `verified:` timestamp (Warning 5 fix).
- 21/22-VALIDATION.md Nyquist flags confirmed true.
- REQUIREMENTS.md CLOSE-07 checkbox flipped to `- [x]` (Warning 6 fix).
- Single commit on the Phase 23 close-out chain, message explicitly naming Plan 23-05 + 23-06 triggers AND the REQUIREMENTS.md checkbox flip.
- Phase 23 mechanically shipped-clean and ready for v1.3 milestone audit re-run.
</success_criteria>

<output>
After completion, create `.planning/phases/23-v1.3-close-out/23-07-SUMMARY.md`. Include:
- Diff stat (expected: 2-4 files — VERIFICATION + REQUIREMENTS + 0 or 2 VALIDATION files).
- Confirmation that Task 1 was no-op vs. edit.
- Confirmation that `verified:` original line was preserved and `re_verified:` was added (Warning 5 fix).
- Confirmation that REQUIREMENTS.md CLOSE-07 flipped from `- [ ]` to `- [x]` (Warning 6 fix).
- Next step: user runs `/gsd-audit-milestone v1.3` to get the `shipped-clean` verdict.
- Closing note: Phase 23 gap-closure thread complete; v1.4 Phase 24+ work is unblocked (though in this repo Phase 24 is already completed — the unblock is for the broader milestone audit).
</output>
</content>
</invoke>