---
phase: 25
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/quality/types.ts
  - src/quality/codingCoverageWalker.ts
  - src/quality/__tests__/codingCoverageWalker.test.ts
  - src/components/quality/CodingDrillDown.tsx
  - src/__tests__/coding-drilldown.test.tsx
autonomous: true
requirements: [QDDEP-01]
must_haves:
  truths:
    - "aggregateCoverage(sample) returns a report whose perPathExamples[path] is a CodeableConcept picked deterministically (first systemCode wins; textOnly/empty fallback)"
    - "CodingDrillDown reads examples from report.perPathExamples — it never calls sampleResources a second time, so opening a coding drill-down issues zero additional FHIR search requests"
    - "useExamplesByPath no longer exists anywhere in src/"
    - "Existing coding-drilldown tests still pass after mock PerTypeCoverageReport literals are updated to include perPathExamples"
  artifacts:
    - path: "src/quality/types.ts"
      provides: "PerTypeCoverageReport.perPathExamples: Record<string, CodeableConcept>"
      contains: "perPathExamples"
    - path: "src/quality/codingCoverageWalker.ts"
      provides: "aggregateCoverage populates perPathExamples in the existing loop"
      contains: "perPathExamples"
    - path: "src/quality/__tests__/codingCoverageWalker.test.ts"
      provides: "perPathExamples behavior tests (systemCode preference, deterministic first-win, fallback)"
      contains: "perPathExamples"
    - path: "src/components/quality/CodingDrillDown.tsx"
      provides: "reads report.perPathExamples; useExamplesByPath deleted"
      contains: "perPathExamples"
  key_links:
    - from: "src/quality/codingCoverageWalker.ts"
      to: "src/quality/types.ts"
      via: "aggregateCoverage return shape"
      pattern: "perPathExamples"
    - from: "src/components/quality/CodingDrillDown.tsx"
      to: "report.perPathExamples"
      via: "direct property read (replaces useExamplesByPath)"
      pattern: "report\\.perPathExamples|perPathExamples\\["
---

<objective>
Deliver QDDEP-01: populate `perPathExamples: Record<string, CodeableConcept>` inside `codingCoverageWalker.aggregateCoverage` in a single pass, extend `PerTypeCoverageReport` with the field, swap `CodingDrillDown` to read from the report, and delete `useExamplesByPath` wholesale. Halves coding drill-down server calls.

Purpose: Must ship FIRST per ROADMAP intra-phase lock — the DrillDownShell (QDDEP-02, Plan 25-03) can only be designed for truly-symmetric drill-downs once the coding drill-down no longer issues a second sample fetch.

Output:
- `PerTypeCoverageReport.perPathExamples` populated in walker
- `useExamplesByPath` deleted
- `CodingDrillDown` reads from report
- Walker tests cover deterministic selection invariants
- Existing drill-down tests updated for required field
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-quality-module-dedup/25-CONTEXT.md
@.planning/phases/25-quality-module-dedup/25-RESEARCH.md
@.planning/phases/25-quality-module-dedup/25-VALIDATION.md
@src/quality/types.ts
@src/quality/codingCoverageWalker.ts
@src/components/quality/CodingDrillDown.tsx

<interfaces>
<!-- Critical existing shapes the executor must respect. Do NOT re-derive from the codebase. -->

From src/quality/types.ts (existing — ADD perPathExamples to PerTypeCoverageReport):
```typescript
export interface PerTypeCoverageReport {
  systemCode: number;
  textOnly: number;
  empty: number;
  totalCodedFields: number;
  perPath: Record<string, { systemCode: number; textOnly: number; empty: number }>;
  sampleSize: number;
  perResource: Array<{ id: string; fields: ClassifiedCodedField[] }>;
  // NEW (this plan):
  perPathExamples: Record<string, CodeableConcept>;
}
```

From src/quality/codingCoverageWalker.ts (~206 LOC):
- `aggregateCoverage(sample: Resource[]): PerTypeCoverageReport` at lines 144-205
- Existing loop at 158-193 already computes `aggregationPath` (stripped + `[\d+]`→`[*]`) once per `ClassifiedCodedField`
- Each field object already carries `value: CodeableConcept`

From src/components/quality/CodingDrillDown.tsx:
- `useExamplesByPath(client, type, sampleSize)` — local hook (lines 43-90) — ISSUES A SECOND `sampleResources` CALL, then walks the sample again. DELETE entirely after switch.
- Read site at line 112: `const examplesByPath = useExamplesByPath(client, type, sampleSize);` — SWAP to read `report.perPathExamples`.
- Mock in `src/__tests__/coding-drilldown.test.tsx:52-74` (`mockCoverageReport`) — lacks `perPathExamples`; will cause TS errors after type change.

Deterministic selection algorithm (RESEARCH.md Focus Area 1):
```typescript
// Inside the EXISTING `for (const f of fields)` loop, AFTER aggregationPath is computed:
if (!perPathExamples[aggregationPath]) {
  if (f.classification === 'systemCode' && f.value) {
    perPathExamples[aggregationPath] = f.value;
  } else if (f.value && !perPathFallbacks[aggregationPath]) {
    perPathFallbacks[aggregationPath] = f.value;
  }
}

// AFTER outer for-loop:
for (const [k, v] of Object.entries(perPathFallbacks)) {
  if (!perPathExamples[k]) perPathExamples[k] = v;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend PerTypeCoverageReport type + add failing walker tests</name>
  <files>src/quality/types.ts, src/quality/__tests__/codingCoverageWalker.test.ts</files>

  <read_first>
    - src/quality/types.ts (full — note CodeableConcept import at line 15)
    - src/quality/codingCoverageWalker.ts (full — to understand aggregateCoverage return shape)
    - src/quality/__tests__/codingCoverageWalker.test.ts (if exists; otherwise create)
    - .planning/phases/25-quality-module-dedup/25-CONTEXT.md (D-01, D-02, D-04)
    - .planning/phases/25-quality-module-dedup/25-RESEARCH.md (Focus Area 1 insertion algorithm)
  </read_first>

  <behavior>
    - Test 1 ("picks first systemCode example"): sample with 2 Observations whose `code.coding[0]` has `system + code` (systemCode classification). Expect `perPathExamples['code']` to equal the CodeableConcept of the FIRST resource (deterministic first-win).
    - Test 2 ("falls back to textOnly when no systemCode seen"): sample with 1 Observation whose `code` has only `text` (textOnly). Expect `perPathExamples['code']` to equal that textOnly CodeableConcept.
    - Test 3 ("systemCode overrides earlier textOnly"): sample with resource A textOnly-only `code`, resource B systemCode `code`. Expect `perPathExamples['code']` to equal B's CodeableConcept (systemCode preferred even when textOnly was seen first).
    - Test 4 ("empty report has empty perPathExamples"): aggregateCoverage([]) → `report.perPathExamples` exists and equals `{}`.
    - Test 5 ("key format matches existing aggregationPath"): sample with `Observation.component[0].code.coding` → `perPathExamples` key should be `component[*].code` (matching `[\d+]`→`[*]` collapse), identical to `perPath` keys.
  </behavior>

  <action>
    1. Edit `src/quality/types.ts` — add NON-OPTIONAL `perPathExamples: Record<string, CodeableConcept>` field to `PerTypeCoverageReport` (line ~64-79). `CodeableConcept` import already present at line 15; no new imports.

    2. Create (or extend) `src/quality/__tests__/codingCoverageWalker.test.ts`:
       - Import `aggregateCoverage` from `../codingCoverageWalker`.
       - Import `Resource, CodeableConcept` from `@medplum/fhirtypes`.
       - Write 5 tests listed in `<behavior>` above. Use minimal FHIR Resource fixtures (Observation with `code` and `component[0].code`).
       - These tests MUST fail initially (RED): `perPathExamples` field does not yet exist on the return value.

    3. Run `npm test -- src/quality/__tests__/codingCoverageWalker.test.ts --run` — expect RED on Tests 1-3, 5 (compile error on `perPathExamples` OR runtime assertion failure because field doesn't exist yet). Test 4 may pass trivially once the type is added.

    4. Commit RED: `git commit --no-verify -m "test(25-01): add failing perPathExamples tests for codingCoverageWalker (QDDEP-01)"`
  </action>

  <verify>
    <automated>npm test -- src/quality/__tests__/codingCoverageWalker.test.ts --run 2>&1 | tee /tmp/25-01-t1-red.log; grep -E "FAIL|✗|Error" /tmp/25-01-t1-red.log | head -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "perPathExamples" src/quality/types.ts` returns ≥ 1 (field declared)
    - `grep -c "Record<string, CodeableConcept>" src/quality/types.ts` returns ≥ 1
    - `wc -l src/quality/__tests__/codingCoverageWalker.test.ts` returns ≥ 50 (5 tests with fixtures)
    - `grep -c "perPathExamples" src/quality/__tests__/codingCoverageWalker.test.ts` returns ≥ 5 (each test asserts on the field)
    - Test run output contains "FAIL" or "✗" for at least 3 of the 5 new tests (RED state achieved before GREEN commit)
    - `git log -1 --oneline` message matches pattern `test\(25-01\).*perPathExamples.*QDDEP-01`
  </acceptance_criteria>

  <done>
    Type is extended; 5 new tests exist and are RED; RED commit landed. Walker implementation has NOT been touched yet.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement perPathExamples in aggregateCoverage (GREEN) + delete useExamplesByPath + swap CodingDrillDown + update existing mocks</name>
  <files>src/quality/codingCoverageWalker.ts, src/components/quality/CodingDrillDown.tsx, src/__tests__/coding-drilldown.test.tsx</files>

  <read_first>
    - src/quality/codingCoverageWalker.ts (lines 144-205 — full aggregateCoverage body, observe the existing `for (const f of fields)` loop at line 163)
    - src/components/quality/CodingDrillDown.tsx (lines 43-90 for useExamplesByPath body; lines 100-115 for consumption site; full file)
    - src/__tests__/coding-drilldown.test.tsx (lines 52-110 for `mockCoverageReport` literal and any `sampleResources` mocks at :107-109)
    - .planning/phases/25-quality-module-dedup/25-CONTEXT.md (D-01, D-02, D-03)
    - .planning/phases/25-quality-module-dedup/25-RESEARCH.md (Focus Area 1 code example, Pitfalls table entry 2 — TS errors from mock shape drift)
    - .planning/research/PITFALLS.md §7 (autoStart + unmemoized deps — CodingDrillDown uses hooks; verify nothing destabilizes)
  </read_first>

  <behavior>
    - Implementation passes all 5 Task 1 tests (GREEN).
    - `CodingDrillDown` opens without issuing any `sampleResources` call beyond those `useCodingCoverage` already triggers. Existing `coding-drilldown.test.tsx` assertions on `mockCoverageReport` continue to pass after `perPathExamples: {}` is added to the mock literal.
    - A new integration test `it('does not issue a second sample fetch when drill-down opens')` verifies the `sampleResources` mock is invoked ≤ what the parent `useCodingCoverage` triggers (i.e., the previously-doubled fetch is now single).
    - `grep -rn "useExamplesByPath" src/` returns 0 results after this task.
  </behavior>

  <action>
    1. **Modify `src/quality/codingCoverageWalker.ts`** — inside `aggregateCoverage` (lines 144-205), apply the RESEARCH-specified insertion:

       a. Before the outer `for (const r of sample)` loop (around line 157), declare:
          ```typescript
          const perPathExamples: Record<string, CodeableConcept> = {};
          const perPathFallbacks: Record<string, CodeableConcept> = {};
          ```

       b. Inside the existing `for (const f of fields)` loop (line ~163), AFTER `aggregationPath` is computed and BEFORE or ALONGSIDE the existing classification bucket writes, add:
          ```typescript
          if (!perPathExamples[aggregationPath]) {
            if (f.classification === 'systemCode' && f.value) {
              perPathExamples[aggregationPath] = f.value;
            } else if (f.value && !perPathFallbacks[aggregationPath]) {
              perPathFallbacks[aggregationPath] = f.value;
            }
          }
          ```

       c. AFTER the outer `for (const r of sample)` loop, add the fallback merge:
          ```typescript
          for (const [k, v] of Object.entries(perPathFallbacks)) {
            if (!perPathExamples[k]) perPathExamples[k] = v;
          }
          ```

       d. Extend the return object to include `perPathExamples`:
          ```typescript
          return {
            systemCode, textOnly, empty, totalCodedFields,
            perPath,
            perPathExamples,  // NEW
            sampleSize: sample.length,
            perResource,
          };
          ```

       e. Ensure `import type { CodeableConcept } from '@medplum/fhirtypes'` at top of file (add if absent).

    2. **Run Task 1 tests GREEN**: `npm test -- src/quality/__tests__/codingCoverageWalker.test.ts --run` — all 5 tests must pass.

    3. **Modify `src/components/quality/CodingDrillDown.tsx`**:

       a. Delete `function useExamplesByPath(client, type, sampleSize) { ... }` entirely (lines 43-90, approximate).
       b. Delete its consumption site (line ~112: `const examplesByPath = useExamplesByPath(client, type, sampleSize);`).
       c. Replace with reading from the already-loaded report. Concretely: wherever `examplesByPath[path]` or `examplesByPath` is read, use `coverageReport?.perPathExamples?.[path]` instead. The `coverageReport` variable (from `useCodingCoverage`) already exists in this component.
       d. Remove any now-unused imports that `useExamplesByPath` owned (e.g., `sampleResources` from `../../quality/sampling` IF no other code in this file references it — grep before deleting).

    4. **Update `src/__tests__/coding-drilldown.test.tsx`**:

       a. Find `mockCoverageReport` literal (approx lines 52-74) — add `perPathExamples: {}` (or a realistic fixture like `perPathExamples: { code: { coding: [{ system: 'http://loinc.org', code: '1234-5' }] } }`) to satisfy the now-required type field.
       b. Add a new test `it('does not issue a second sample fetch when drill-down opens', async () => { ... })` that:
          - Renders `<CodingDrillDown>` with a `coverageReport` mock containing `perPathExamples`.
          - Spies on `sampleResources` via `vi.mock('../../quality/sampling', ...)` or module-level `vi.fn`.
          - Asserts `sampleResources` call count === whatever `useCodingCoverage` itself invokes (baseline — record before, assert after).
          - Or simpler delta form: `expect(sampleResourcesSpy.mock.calls.length).toBeLessThanOrEqual(initialCalls)` where `initialCalls` is captured before drill-down opens.
       c. Any orphan `vi.mock('../../quality/sampling', ...)` for the deleted `useExamplesByPath` path can stay (harmless) or be removed — executor's choice.

    5. **Run coding-drilldown suite**: `npm test -- src/__tests__/coding-drilldown.test.tsx --run` — all green.

    6. **Post-swap grep gate**: `grep -rn "useExamplesByPath" src/` MUST return 0 matches.

    7. **TypeScript gate**: `npx tsc -b --noEmit` exits 0.

    8. **Full suite smoke**: `npm test -- --run` — 22 pre-existing failures max (baseline per STATE.md); no NEW failures introduced by this plan.

    9. **Commit**: `git commit --no-verify -m "feat(25-01): populate perPathExamples in codingCoverageWalker, delete useExamplesByPath (QDDEP-01)"`
  </action>

  <verify>
    <automated>npm test -- src/quality/__tests__/codingCoverageWalker.test.ts src/__tests__/coding-drilldown.test.tsx --run && npx tsc -b --noEmit && test "$(grep -rn 'useExamplesByPath' src/ | wc -l | tr -d ' ')" = "0" && echo "OK"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn "useExamplesByPath" src/` returns 0 matches (D-03 satisfied)
    - `grep -c "perPathExamples" src/quality/codingCoverageWalker.ts` returns ≥ 3 (declaration + loop write + return)
    - `grep -c "perPathFallbacks" src/quality/codingCoverageWalker.ts` returns ≥ 2 (declaration + backfill loop)
    - `grep -c "report\\.perPathExamples\\|coverageReport.*perPathExamples\\|perPathExamples\\[" src/components/quality/CodingDrillDown.tsx` returns ≥ 1 (read site exists)
    - `grep -c "function useExamplesByPath" src/components/quality/CodingDrillDown.tsx` returns 0
    - `grep -c "perPathExamples" src/__tests__/coding-drilldown.test.tsx` returns ≥ 1 (mock updated)
    - `grep -c "does not issue a second sample fetch\\|does not issue.*additional" src/__tests__/coding-drilldown.test.tsx` returns ≥ 1 (new test exists)
    - `npx tsc -b --noEmit` exits 0
    - All 5 walker tests GREEN
    - Full vitest run: 0 new regressions vs 22-failure baseline
    - `git log -1 --oneline` message matches `feat\(25-01\).*perPathExamples.*QDDEP-01`
  </acceptance_criteria>

  <done>
    QDDEP-01 fully delivered: walker populates perPathExamples, CodingDrillDown reads it, useExamplesByPath deleted, tests GREEN, TS clean, no regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-process data | No new trust boundary — walker operates on already-fetched FHIR resources inside the browser; no new network surface. |

## STRIDE Threat Register

ASVS L1 assessment for a pure refactor (no new PHI paths, no new user input, no new network egress):

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | Tampering | codingCoverageWalker return shape | mitigate | New `perPathExamples` field is non-optional on `PerTypeCoverageReport`; cached blobs lacking it will fail TS-level consumer reads — guard `coverageReport?.perPathExamples?.[path]` on the consumer side (handles pre-upgrade cache hits gracefully by returning undefined). |
| T-25-01-02 | Information Disclosure | CodeableConcept examples in report | accept | Examples are already-rendered clinical codes from the same dataset; `QualityMetricsCache` serialization is unchanged (JSON-safe). No new PHI exposure — the drill-down already displayed these codes before this refactor; they simply ride along on the cached report now instead of being re-fetched. |
| T-25-01-03 | Denial of Service | Increased report payload size | accept | `perPathExamples` adds at most one `CodeableConcept` per distinct aggregation path (~20-40 paths per resource type). Each CodeableConcept is small (~100-200 bytes). Total overhead per cached report < 10KB. `QualityMetricsCache` localStorage capacity already handles full reports. |
| T-25-01-04 | Repudiation | — | accept | No audit/log surface touched. |
| T-25-01-05 | Spoofing | — | accept | No authentication surface touched. |
| T-25-01-06 | Elevation of Privilege | — | accept | No privilege surface touched. |
</threat_model>

<verification>
- `npm test -- --run` — no new regressions vs the 22-failure baseline
- `npx tsc -b --noEmit` exits 0
- `grep -rn "useExamplesByPath" src/` returns 0 matches
- `grep -c "perPathExamples" src/quality/types.ts` returns ≥ 1
- VALIDATION.md truth: "aggregateCoverage(sample).perPathExamples['code'] is the first systemCode CodeableConcept observed" — proven by Task 1 Test 1 + Test 3
- VALIDATION.md truth: "CodingDrillDown fetch-count test — `sampleResources` called ≤ 1 time per drill-down open" — proven by Task 2 new integration test
</verification>

<success_criteria>
- PerTypeCoverageReport has a non-optional `perPathExamples: Record<string, CodeableConcept>` field
- `aggregateCoverage` populates it in the existing single-pass loop (no second traversal)
- `useExamplesByPath` is deleted from the codebase
- `CodingDrillDown` reads examples from `report.perPathExamples`
- 5 new walker tests cover deterministic selection invariants (all GREEN)
- 1 new drill-down integration test proves the second fetch is gone
- All existing `coding-drilldown` tests still pass after mock shape update
- Zero new regressions in the full vitest suite
</success_criteria>

<output>
After completion, create `.planning/phases/25-quality-module-dedup/25-01-SUMMARY.md` following the SUMMARY template. Include:
- `requirements-completed: [QDDEP-01]`
- `requires` — Phase 24 (for useAsyncRun / cache primitives that downstream plans use; this plan itself does not touch them)
- `provides` — "PerTypeCoverageReport.perPathExamples populated in single walker pass; coding drill-down fetch count halved"
- `affects` — Plan 25-03 (DrillDownShell design can now assume symmetric drill-down fetch patterns)
- Commit hashes for RED and GREEN commits
- Walker LOC delta (codingCoverageWalker.ts: +~10 LOC for insertion)
- CodingDrillDown LOC delta (should drop ~50 LOC from useExamplesByPath deletion)
</output>
