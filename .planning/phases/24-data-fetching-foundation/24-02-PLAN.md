---
phase: 24
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useResourceCounts.ts
  - src/hooks/__tests__/useResourceCounts.test.tsx
autonomous: true
requirements:
  - FOUND-01
  - FOUND-04
user_setup: []

must_haves:
  truths:
    - "Switching between Dashboard, Explorer, and Quality pages does NOT re-issue `_summary=count` requests for resource types already counted this session (per same serverUrl)"
    - "Cache is keyed on `${serverUrl}::${resourceType}` (double-colon separator per CONTEXT.md §specifics D-10 note line 111)"
    - "`cancelledRef` is gone from `useResourceCounts.ts` — replaced by closure-scoped `let cancelled = false`"
    - "`resourceTypes.join(',')` in effect deps is replaced by `useMemo`-stabilized `typesKey` (absorbs Phase 27 R12 memo fix)"
    - "`clearQualityCountCache(serverUrl: string): void` is exported from `useResourceCounts.ts` (or sibling module — see §Claude's Discretion D-06)"
    - "`clearAllQualityCountCache(): void` is exported for Settings 'Clear cache' button parity (per Open Question 3 recommendation)"
    - "Cache writes are guarded by `if (cancelled) return` BEFORE `countCache.set(...)` per PITFALLS Pitfall 4"
  artifacts:
    - path: "src/hooks/useResourceCounts.ts"
      provides: "useResourceCounts with module-scope Map cache + closure-scoped cancellation; exports clearQualityCountCache, clearAllQualityCountCache"
      exports: ["useResourceCounts", "clearQualityCountCache", "clearAllQualityCountCache"]
      contains: "let cancelled = false"
    - path: "src/hooks/__tests__/useResourceCounts.test.tsx"
      provides: "Cache hit/miss tests + StrictMode cancellation tests + FOUND-04 regression test"
  key_links:
    - from: "src/hooks/useResourceCounts.ts"
      to: "module-scope `const countCache = new Map<string, number>()`"
      via: "Read on effect seed + write-through on fetchCount success"
      pattern: "countCache\\.get|countCache\\.set"
    - from: "src/hooks/useResourceCounts.ts"
      to: "client.getBaseUrl()"
      via: "Derive `serverUrl` for cache key composition"
      pattern: "client\\.getBaseUrl\\(\\)"
---

<objective>
Refactor `useResourceCounts.ts` to read through a module-scope `Map<${serverUrl}::${type}, number>` cache (FOUND-01), fix the latent `cancelledRef` cancellation bug (FOUND-04) in the same file touch, and absorb the Phase 27 R12 `resourceTypes.join(',')` effect-dep memo fix.

Purpose: Dashboard/Explorer/Quality page switching triggers repeat `_summary=count` fetches today because the hook has no cross-mount cache. This plan makes the cache transparent (same consumer signature) and fixes the latent stale-write bug where `cancelledRef.current = false` at the top of a new effect does not invalidate in-flight promises from a previous effect.

Output: One file modified (`src/hooks/useResourceCounts.ts`), one new test file (`src/hooks/__tests__/useResourceCounts.test.tsx`). Consumer call-sites (`DashboardPage.tsx:77`, `ResourceTypeLanding.tsx:24`, `QualityOverviewPage.tsx:108`, `useResourceCountsMetrics.ts`) are UNCHANGED — cache is transparent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-data-fetching-foundation/24-CONTEXT.md
@.planning/phases/24-data-fetching-foundation/24-RESEARCH.md

@src/hooks/useResourceCounts.ts
@src/hooks/useResourceCountsMetrics.ts
@src/hooks/useCompletenessReport.ts

<interfaces>
<!-- Canonical shapes. Executor implements these verbatim; no design choices. -->

Existing signature (UNCHANGED — backward-compatible):
```typescript
export function useResourceCounts(
  client: MedplumClient | null,
  resourceTypes: string[],
  refetchKey: number = 0,
): Record<string, CountValue>;
```

Where `CountValue = number | 'loading' | 'error'` (existing local type — keep as-is or move to `src/quality/types.ts` if already there; do NOT change the shape).

NEW exports:
```typescript
// Per-server wipe (used by SettingsContext + SettingsPage in Plan 24-03).
export function clearQualityCountCache(serverUrl: string): void;
// Cross-server wipe (used by Settings "Clear metrics cache" button).
export function clearAllQualityCountCache(): void;
```

Cache key format (locked by CONTEXT.md §specifics line 111):
```typescript
function cacheKey(serverUrl: string, type: string): string {
  return `${serverUrl}::${type}`;
}
```

Module-scope cache shape:
```typescript
const countCache = new Map<string, number>(); // key = `${serverUrl}::${type}`
```

Reference implementation (from 24-RESEARCH.md §Pattern 1, lines 176-255) is the authoritative blueprint.

Existing consumers (MUST NOT change):
- `src/hooks/useResourceCountsMetrics.ts:49` — `useResourceCounts(client, types, version)` — signature unchanged
- `src/components/DashboardPage.tsx:77` — calls `useResourceCounts(client, types)`
- `src/components/explorer/ResourceTypeLanding.tsx:24` — calls `useResourceCounts`
- `src/components/quality/QualityOverviewPage.tsx:108` — calls `useResourceCounts`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor `useResourceCounts.ts` with module-scope cache, closure-scoped cancellation, and memoized typesKey</name>
  <files>src/hooks/useResourceCounts.ts</files>
  <read_first>
    - src/hooks/useResourceCounts.ts (current state — lines 1-84; note `cancelledRef` at line 26 + line 29 reset; note line 75 `resourceTypes.join(',')`)
    - src/hooks/useCompletenessReport.ts (lines 71-74 comment — the proven closure-scoped cancellation comment to mirror in this file)
    - src/hooks/useResourceCountsMetrics.ts (lines 49, 62 — confirm `useResourceCounts(client, types, version)` signature is unchanged)
    - .planning/phases/24-data-fetching-foundation/24-RESEARCH.md (§Pattern 1 full code sketch lines 176-255 + §Pitfall 4 write-through guard + §Open Question 3 naming recommendation)
    - .planning/phases/24-data-fetching-foundation/24-CONTEXT.md (D-01 through D-04, D-10, §specifics line 111)
  </read_first>
  <behavior>
    Implementation must match the §Pattern 1 reference in 24-RESEARCH.md exactly, with these specific outcomes:
    - Consumer signature unchanged.
    - First mount with types `['Patient', 'Observation']` → 2 `client.search` calls, cache populated.
    - Second mount (same serverUrl, same types) → 0 `client.search` calls (cache hits).
    - Third mount (different serverUrl) → 2 new `client.search` calls (keys differ).
    - Cancel during pending fetch does NOT write to cache (guard per Pitfall 4).
    - StrictMode double-mount: no stale `setCounts` fires after second-mount cleanup.
    - `clearQualityCountCache('http://a/fhir')` removes only `http://a/fhir::*` keys; leaves `http://b/fhir::*` intact.
  </behavior>
  <action>
    Rewrite `src/hooks/useResourceCounts.ts` with the following structure (implement verbatim against 24-RESEARCH.md §Pattern 1 lines 176-255):

    1. Update imports: add `useMemo` alongside existing `useState`, `useEffect`; DROP `useRef` import (no longer needed).

    2. Keep existing `CONCURRENCY = 4` and `type CountValue = number | 'loading' | 'error'`.

    3. Add module-scope cache BEFORE the `useResourceCounts` function declaration:
       ```typescript
       // Module-scope — lives for the tab's lifetime per D-01 (session-scoped, no TTL).
       // Key format `${serverUrl}::${resourceType}` per CONTEXT.md §specifics line 111.
       // Never automatically refreshed; cleared only by clearQualityCountCache /
       // clearAllQualityCountCache (wired in Plan 24-03 to SettingsPage + setSettings).
       const countCache = new Map<string, number>();

       function cacheKey(serverUrl: string, type: string): string {
         return `${serverUrl}::${type}`;
       }

       export function clearQualityCountCache(serverUrl: string): void {
         const prefix = `${serverUrl}::`;
         for (const k of Array.from(countCache.keys())) {
           if (k.startsWith(prefix)) countCache.delete(k);
         }
       }

       export function clearAllQualityCountCache(): void {
         countCache.clear();
       }
       ```

    4. Inside `useResourceCounts`:
       - DROP `const cancelledRef = useRef(false);` — delete line 26.
       - ADD `const typesKey = useMemo(() => resourceTypes.join(','), [resourceTypes]);` (absorbs Phase 27 R12 memo fix; stable per-deps-change key).
       - REPLACE the `useEffect` body:
         * Line 29 `cancelledRef.current = false` → replace with `let cancelled = false` at the top of the effect body (after the `setCounts({})` early-return). This is FOUND-04.
         * Seed phase: derive `const serverUrl = client.getBaseUrl();` then build `initial` by checking `countCache.get(cacheKey(serverUrl, t))` for each type — if hit, seed with the number; if miss, seed with `'loading'`.
         * Queue the miss list only: `const queue = resourceTypes.filter(t => initial[t] === 'loading');`.
         * Worker pool: in the `processNext` function replace `if (cancelledRef.current) return` with `if (cancelled) return`. In the fetchCount success handler, replace `if (cancelledRef.current) return` with `if (cancelled) return` placed BEFORE `countCache.set(cacheKey(serverUrl, type), count)` (the Pitfall 4 write-through guard). After the cache set, call `setCounts(prev => ({ ...prev, [type]: count }))`.
         * Error handler: `if (cancelled) return` before `setCounts(prev => ({...prev, [type]: 'error'}))` (do NOT write to cache on error).
         * Cleanup: `return () => { cancelled = true; };`.
         * Deps: `[client, typesKey, refetchKey]` (NOT `[client, resourceTypes.join(','), refetchKey]` — the string join in the deps array was the R12 bug).

    5. `fetchCount` helper at the bottom: unchanged.

    6. Add file header JSDoc block citing FOUND-01 + FOUND-04 + R12 (absorbed from Phase 27 per ROADMAP.md Phase 27 depends-on note). Reference `useCompletenessReport.ts:71-74` for the closure-scoped cancellation rationale.

    Constraints:
    - Do NOT change `CountValue` shape. Do NOT change the function's public signature.
    - Do NOT introduce `as` casts.
    - The module-scope `countCache` Map must be at the TOP LEVEL of the module (not inside the function) so it survives unmount/remount.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit && npm test -- src/__tests__/resource-type-landing-counts.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cancelledRef" src/hooks/useResourceCounts.ts` === 0 (variable fully removed per FOUND-04)
    - `grep -c "let cancelled = false" src/hooks/useResourceCounts.ts` ≥ 1
    - `grep -c "useRef" src/hooks/useResourceCounts.ts` === 0 (import no longer needed)
    - `grep -c "useMemo" src/hooks/useResourceCounts.ts` ≥ 1 (new typesKey memo)
    - `grep -c "resourceTypes.join(',')" src/hooks/useResourceCounts.ts` === 1 (only inside useMemo, NOT in deps array)
    - `grep -c "const countCache = new Map" src/hooks/useResourceCounts.ts` ≥ 1 (module-scope cache declared)
    - `grep -c "export function clearQualityCountCache" src/hooks/useResourceCounts.ts` ≥ 1
    - `grep -c "export function clearAllQualityCountCache" src/hooks/useResourceCounts.ts` ≥ 1
    - `grep -c "::" src/hooks/useResourceCounts.ts` ≥ 1 (double-colon key separator used)
    - `grep -c "if (cancelled) return" src/hooks/useResourceCounts.ts` ≥ 3 (guard in processNext, success handler before countCache.set, error handler)
    - `grep -n "countCache.set" src/hooks/useResourceCounts.ts` shows the set call is preceded (within 3 lines above) by `if (cancelled) return;` — verify via `grep -B 3 "countCache.set"`
    - `npx tsc -b --noEmit` exits 0
    - `npm test -- src/__tests__/resource-type-landing-counts.test.tsx --run` exits 0 (existing consumer test still passes)
  </acceptance_criteria>
  <done>useResourceCounts.ts uses module-scope Map cache + closure-scoped `let cancelled`; cancelledRef fully removed; typesKey memoized; clearQualityCountCache + clearAllQualityCountCache exported; tsc clean; existing consumer tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create `useResourceCounts.test.tsx` covering cache hits, cache misses, clear, and FOUND-04 StrictMode cancellation</name>
  <files>src/hooks/__tests__/useResourceCounts.test.tsx</files>
  <read_first>
    - src/hooks/useResourceCounts.ts (from Task 1 — imports and signatures)
    - src/__tests__/completeness-hook.test.tsx (lines 1-80 — renderHook + makeClient stub pattern in this repo)
    - src/__tests__/resource-type-landing-counts.test.tsx (existing consumer test — reference for `_summary=count` mock shape)
    - .planning/phases/24-data-fetching-foundation/24-RESEARCH.md (§Example 3 lines 822-876 — use as starting template)
    - .planning/phases/24-data-fetching-foundation/24-RESEARCH.md (§Pitfall 8 — beforeEach cache wipe requirement for test isolation)
  </read_first>
  <behavior>
    - Test 1 (cache hit): First mount with `client(baseUrl='http://test/fhir')` + `types=['Patient']` fires 1 `client.search` call. Unmount. Remount with same client + same types. Assert 0 additional `client.search` calls AND `result.current.Patient === 42`.
    - Test 2 (cache miss after clear): First mount populates cache. Unmount. `clearQualityCountCache('http://test/fhir')`. Remount. Assert `client.search` was called a SECOND time.
    - Test 3 (server isolation): `clearQualityCountCache('http://a/fhir')` does NOT remove entries keyed on `http://b/fhir::`. Populate both; clear a; assert b's entry still returns cached.
    - Test 4 (typesKey stability): Parent re-renders that pass an identity-different-but-equal `resourceTypes` array (e.g., `['Patient']` fresh each render) do NOT re-fire the effect. Assert `client.search` call count stays at 1 across 3 parent re-renders.
    - Test 5 (FOUND-04 StrictMode cancellation): Render the hook inside `<StrictMode>` with a delayed `search` mock (e.g., 50ms resolution). Immediately change `resourceTypes` from `['Patient']` to `['Observation']` before the first promise resolves. Assert no late `setCounts` writes from the first effect run (verify `result.current` has only Observation, not Patient — or Patient is absent from the final state, proving the first effect's in-flight promise's state update was properly cancelled).
    - Test 6 (write-through guard per Pitfall 4): Start a fetch, cancel during flight (by unmounting), then mount fresh and check the module-scope cache did NOT persist the cancelled-type's value. Use a `client.search` stub that resolves AFTER the unmount; verify the second mount re-fetches (cache did not capture the cancelled value).
    - Test 7 (error state, no cache write): `client.search` rejects for Patient. First mount → `result.current.Patient === 'error'`. Unmount. Remount. `client.search` is called AGAIN for Patient (error was NOT cached).
    - Test 8 (`clearAllQualityCountCache`): Populate cache with 2 servers (A, B). Call `clearAllQualityCountCache()`. Next mount for server A re-fetches; same for B.
  </behavior>
  <action>
    Create `src/hooks/__tests__/useResourceCounts.test.tsx` using vitest + `@testing-library/react`.

    Structure (mirror `src/__tests__/completeness-hook.test.tsx` lines 1-80 conventions):

    1. Imports: `describe, it, expect, vi, beforeEach, afterEach` from vitest; `renderHook, act, waitFor` from `@testing-library/react`; `StrictMode` from react; `useResourceCounts, clearQualityCountCache, clearAllQualityCountCache` from `../useResourceCounts`; type `MedplumClient` from `@medplum/core`.

    2. `makeClient(baseUrl: string, counts: Record<string, number>, options?: { delayMs?: number; errorTypes?: string[] }): MedplumClient` helper. `search` returns `Promise<{ resourceType: 'Bundle', total: number }>`. If `options.delayMs`, awaits `setTimeout`. If `options.errorTypes.includes(type)`, rejects with `new Error('search failed')`.

    3. `beforeEach(() => { clearAllQualityCountCache(); })` — per PITFALLS §Pitfall 8.

    4. Write 8 tests per the `<behavior>` list above. For StrictMode test 5, use a strict-mode wrapper:
       ```typescript
       const wrapper = ({ children }: { children: ReactNode }) => <StrictMode>{children}</StrictMode>;
       renderHook(() => useResourceCounts(client, types), { wrapper });
       ```

    5. For timing-sensitive tests (5, 6), use real timers with `await new Promise(r => setTimeout(r, N))` for N slightly larger than the mock's `delayMs`, then `await waitFor(...)`.

    6. Assertion style: use `expect(client.search).toHaveBeenCalledTimes(N)` via `vi.fn()`. Use `expect(result.current.Patient).toBe(42)`.

    Constraints:
    - Do NOT import from `@medplum/core` outside types — always mock via `as unknown as MedplumClient` (one-time test-mock cast IS allowed; this is a test file, not consumer panel code — PITFALLS Pitfall 1 concerns consumer panels, not test fixtures).
    - Use `unmount()` from `renderHook` to trigger cleanup.
    - Do NOT manipulate the module-scope `countCache` directly from tests; only through exported `clearQualityCountCache` / `clearAllQualityCountCache`.
  </action>
  <verify>
    <automated>npm test -- src/hooks/__tests__/useResourceCounts.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - File `src/hooks/__tests__/useResourceCounts.test.tsx` exists
    - `grep -c "^  it(" src/hooks/__tests__/useResourceCounts.test.tsx` ≥ 8
    - `grep -c "clearQualityCountCache" src/hooks/__tests__/useResourceCounts.test.tsx` ≥ 2 (imported + used in at least one test)
    - `grep -c "clearAllQualityCountCache" src/hooks/__tests__/useResourceCounts.test.tsx` ≥ 2 (imported + used in beforeEach + used in Test 8)
    - `grep -c "StrictMode" src/hooks/__tests__/useResourceCounts.test.tsx` ≥ 1
    - `grep -c "beforeEach" src/hooks/__tests__/useResourceCounts.test.tsx` ≥ 1 (Pitfall 8 guard)
    - `grep -c "toHaveBeenCalledTimes" src/hooks/__tests__/useResourceCounts.test.tsx` ≥ 3 (cache-hit assertions)
    - `npm test -- src/hooks/__tests__/useResourceCounts.test.tsx --run` exits 0
    - `npm test --run` (full suite) exits 0 (no regressions)
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>8 passing tests covering cache hit, cache miss after clear, server isolation, typesKey stability, StrictMode cancellation (FOUND-04 regression), write-through guard, error non-caching, clearAllQualityCountCache wipe. Full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module-scope `countCache` ← useEffect fetchCount success | Write-through from network response to Map. Must be guarded by `cancelled` check to prevent stale writes after cancel. |
| module-scope `countCache` ← `setSettings()` / Settings button | Read path for `clearQualityCountCache(serverUrl)` and `clearAllQualityCountCache()`. Plan 24-03 wires the callers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Tampering | Stale cache write after cancellation (Pitfall 4) | mitigate | `if (cancelled) return;` placed BEFORE `countCache.set(...)` in the fetchCount success path. Tested in Test 6. |
| T-24-02-02 | Tampering | Cross-server data leak via shared cache key | mitigate | Key format `${serverUrl}::${type}` per D-10 note; `clearQualityCountCache(serverUrl)` uses `startsWith(`${serverUrl}::`)` prefix match. Tested in Test 3 (server isolation). |
| T-24-02-03 | Info Disclosure | Error states polluting cache | mitigate | Error path does NOT call `countCache.set` — only success path writes. Tested in Test 7. |
| T-24-02-04 | DoS (memory) | Unbounded count cache growth | accept | Count cache is bounded by `resource types × distinct serverUrls` — typical ≤ 100 × 2 = 200 entries (each a small number). Phase 24 does NOT add LRU eviction for the count cache (only the metrics registry has LRU per FOUND-02). If future profiling shows growth, add eviction in a later milestone. |
| T-24-02-05 | Tampering | `cancelledRef` stale reset after rapid deps change (FOUND-04) | mitigate | Closure-scoped `let cancelled = false` replaces the shared ref; each effect run gets its own flag. Tested in Test 5 (StrictMode + rapid deps change). |
</threat_model>

<verification>
- `cancelledRef` variable removed from `useResourceCounts.ts` (grep guard)
- Closure-scoped `let cancelled = false` present (grep guard)
- Module-scope `countCache = new Map` present at module top level
- `resourceTypes.join(',')` appears exactly once (inside `useMemo`, NOT in deps array) — grep count === 1
- `if (cancelled) return` appears ≥ 3 times (processNext, success before cache.set, error handler)
- 8 new tests in `src/hooks/__tests__/useResourceCounts.test.tsx` all pass
- Existing consumer tests pass: `src/__tests__/resource-type-landing-counts.test.tsx`, `src/__tests__/quality-overview.test.tsx` (if it exercises counts)
- Full `npm test` suite green
- `npx tsc -b --noEmit` clean
</verification>

<success_criteria>
- [ ] `useResourceCounts` uses module-scope `Map<string, number>` cache keyed on `${serverUrl}::${type}`
- [ ] FOUND-04: `cancelledRef` fully removed; closure-scoped `let cancelled` in effect body
- [ ] R12 absorption: `typesKey = useMemo(() => resourceTypes.join(','), [resourceTypes])`; deps array uses `[client, typesKey, refetchKey]`
- [ ] `clearQualityCountCache(serverUrl)` + `clearAllQualityCountCache()` exported
- [ ] Cache write guarded by `if (cancelled) return` BEFORE `countCache.set`
- [ ] Error path does NOT write to cache
- [ ] 8 new tests pass including StrictMode + rapid deps change (FOUND-04 regression gate)
- [ ] Full suite green + tsc clean
- [ ] Zero new `as` casts in `useResourceCounts.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/24-data-fetching-foundation/24-02-SUMMARY.md` documenting:
- The `${serverUrl}::${type}` cache key format
- How FOUND-04 was fixed (cancelledRef removal + closure-scoped let cancelled)
- How R12 was absorbed (`typesKey` memo) — flag to ROADMAP that EFF-01's Phase 27 scope is reduced by the R12 absorption
- Cache size invariants (unbounded in Phase 24; future LRU deferred)
- Downstream wiring: Plan 24-03 wires `clearQualityCountCache` into SettingsContext + SettingsPage
</output>
