---
phase: 24
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quality/metricsCache.ts
  - src/quality/__tests__/metricsCache.test.ts
  - src/hooks/useCompletenessReport.ts
  - src/hooks/useCodingCoverage.ts
  - src/contexts/SettingsContext.tsx
  - src/components/settings/SettingsPage.tsx
  - src/__tests__/settings-clear-cache.test.tsx
autonomous: true
requirements:
  - FOUND-02
user_setup: []

must_haves:
  truths:
    - "`getQualityMetricsCache(serverUrl)` returns the SAME `QualityMetricsCache` instance on repeat calls for the same serverUrl"
    - "After 3 distinct serverUrls are requested, `cachesByServer.size === 2` (2-entry LRU eviction enforced)"
    - "MRU touch on `get` prevents eviction of the most-recently-touched entry"
    - "`clearQualityMetricsCache(serverUrl)` removes the entry from the registry AND calls `cache.clear()` (wipes localStorage namespace)"
    - "`useCompletenessReport` and `useCodingCoverage` both import `getQualityMetricsCache` instead of the local `cacheInstance`/`getCache` helpers"
    - "`setSettings()` in `SettingsContext` calls both `clearQualityCountCache(serverUrl)` AND `clearQualityMetricsCache(serverUrl)` for the current serverUrl (D-04, D-05)"
    - "Settings 'Clear metrics cache' button calls both `clearAllQualityCountCache()` AND `clearAllQualityMetrics()` (D-03)"
  artifacts:
    - path: "src/quality/metricsCache.ts"
      provides: "Registry functions getQualityMetricsCache, clearQualityMetricsCache; class QualityMetricsCache unchanged"
      exports: ["QualityMetricsCache", "getQualityMetricsCache", "clearQualityMetricsCache", "clearAllQualityMetrics"]
    - path: "src/quality/__tests__/metricsCache.test.ts"
      provides: "Unit tests for registry LRU + clear + MRU touch"
    - path: "src/hooks/useCompletenessReport.ts"
      provides: "Uses getQualityMetricsCache(serverUrl) — no local cacheInstance"
    - path: "src/hooks/useCodingCoverage.ts"
      provides: "Uses getQualityMetricsCache(serverUrl) — no local cacheInstance"
    - path: "src/contexts/SettingsContext.tsx"
      provides: "setSettings calls clearQualityCountCache + clearQualityMetricsCache for current serverUrl"
    - path: "src/components/settings/SettingsPage.tsx"
      provides: "handleClearMetricsCache additionally calls clearAllQualityCountCache"
  key_links:
    - from: "src/hooks/useCompletenessReport.ts"
      to: "src/quality/metricsCache.ts"
      via: "import { getQualityMetricsCache } from '../quality/metricsCache'"
      pattern: "getQualityMetricsCache"
    - from: "src/hooks/useCodingCoverage.ts"
      to: "src/quality/metricsCache.ts"
      via: "import { getQualityMetricsCache } from '../quality/metricsCache'"
      pattern: "getQualityMetricsCache"
    - from: "src/contexts/SettingsContext.tsx"
      to: "clearQualityCountCache + clearQualityMetricsCache"
      via: "setSettings function body invokes both per current settings.fhir.serverUrl"
      pattern: "clearQuality(Count|Metrics)Cache"
    - from: "src/components/settings/SettingsPage.tsx"
      to: "clearAllQualityCountCache"
      via: "handleClearMetricsCache additional call"
      pattern: "clearAllQualityCountCache"
---

<objective>
Deliver FOUND-02: replace the two rotating `cacheInstance` singletons in `useCompletenessReport` + `useCodingCoverage` with a single `Map<serverUrl, QualityMetricsCache>` registry in `metricsCache.ts`, enforce 2-entry LRU eviction on insert, and wire `setSettings()` + the Settings "Clear metrics cache" button to clear both the new metrics registry and the count cache from Plan 24-02.

Purpose: Today, switching server URL discards the previous server's metrics cache (known limitation documented at `useCompletenessReport.ts:40-42`). The registry preserves the last 2 servers' caches (A/B toggle is instant) while bounding memory (PITFALLS Pitfall 3). The `setSettings()` wiring implements D-04/D-05: any settings save clears the current server's caches, preventing stale reports from silently accumulating after a sampleSize change or auth rotation.

Output: `metricsCache.ts` gains 2 new exported functions; 2 hook files replace their local helpers with the registry call; `SettingsContext.tsx` gains a direct-call cache-clear wiring at save time (no watcher per D-04); `SettingsPage.tsx` "Clear metrics cache" button also clears the count cache; new unit test file + extended existing integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-data-fetching-foundation/24-CONTEXT.md
@.planning/phases/24-data-fetching-foundation/24-RESEARCH.md

@src/quality/metricsCache.ts
@src/hooks/useCompletenessReport.ts
@src/hooks/useCodingCoverage.ts
@src/contexts/SettingsContext.tsx
@src/components/settings/SettingsPage.tsx
@src/__tests__/settings-clear-cache.test.tsx

<interfaces>
<!-- Canonical shapes. Executor implements these verbatim; no design choices. -->

NEW exports added to `src/quality/metricsCache.ts`:
```typescript
const LRU_LIMIT = 2;
const cachesByServer = new Map<string, QualityMetricsCache>();

export function getQualityMetricsCache(serverUrl: string): QualityMetricsCache;
export function clearQualityMetricsCache(serverUrl: string): void;
```

Reference implementation (from 24-RESEARCH.md §Pattern 1 lines 263-304) is the authoritative blueprint:
```typescript
export function getQualityMetricsCache(serverUrl: string): QualityMetricsCache {
  const existing = cachesByServer.get(serverUrl);
  if (existing) {
    cachesByServer.delete(serverUrl);       // MRU touch: delete + reinsert
    cachesByServer.set(serverUrl, existing);
    return existing;
  }
  const cache = new QualityMetricsCache({ serverUrl });
  cachesByServer.set(serverUrl, cache);
  if (cachesByServer.size > LRU_LIMIT) {
    const oldestKey = cachesByServer.keys().next().value;
    if (oldestKey !== undefined) cachesByServer.delete(oldestKey);
  }
  return cache;
}

export function clearQualityMetricsCache(serverUrl: string): void {
  const cache = cachesByServer.get(serverUrl);
  if (!cache) return;
  cache.clear();                            // existing method wipes memory + localStorage namespace
  cachesByServer.delete(serverUrl);
}
```

Existing `QualityMetricsCache` class body MUST remain UNCHANGED — only the registry is new.

Existing `clearAllQualityMetrics()` function MUST be extended to also clear the registry:
```typescript
export function clearAllQualityMetrics(): number {
  cachesByServer.clear();                   // NEW LINE: drop all in-memory references
  // ... existing localStorage iteration unchanged ...
  return removed;
}
```

Downstream consumer changes:

`src/hooks/useCompletenessReport.ts`:
- DELETE lines 35-51 (the `cacheInstance`, `cacheServerUrl`, `getCache` helper + its header comment about "Known limitation: switching server URLs...")
- UPDATE import at line 28: replace `import { QualityMetricsCache } from '../quality/metricsCache';` with `import { getQualityMetricsCache } from '../quality/metricsCache';`
- UPDATE line 81 body: replace `const cache = getCache(serverUrl);` with `const cache = getQualityMetricsCache(serverUrl);`

`src/hooks/useCodingCoverage.ts`:
- Identical change: delete lines 31-43 (cacheInstance + getCache); replace import at line 23; replace `getCache(serverUrl)` call at line 72.

`src/contexts/SettingsContext.tsx`:
- Import at top: `import { clearQualityCountCache } from '../hooks/useResourceCounts';` and `import { clearQualityMetricsCache } from '../quality/metricsCache';`
- In `setSettings` function body (line 27-30), BEFORE `setSettingsState(next)`:
  ```typescript
  const prevUrl = settings?.fhir?.serverUrl;
  const nextUrl = next.fhir?.serverUrl;
  // D-05: any settings save clears caches for BOTH prior and new serverUrl
  // (covers auth rotation, sampleSize changes, etc.)
  if (prevUrl) {
    clearQualityCountCache(prevUrl);
    clearQualityMetricsCache(prevUrl);
  }
  if (nextUrl && nextUrl !== prevUrl) {
    clearQualityCountCache(nextUrl);
    clearQualityMetricsCache(nextUrl);
  }
  ```
  Note: the prevUrl clear is the current-server wipe per D-04. If URL also changed, clear the new URL too (defensive; cheap; prevents any stale-from-a-prior-tab-session data from surfacing).

`src/components/settings/SettingsPage.tsx`:
- Import at top: add `clearAllQualityCountCache` from `'../../hooks/useResourceCounts'`
- In `handleClearMetricsCache` (line 41), add `clearAllQualityCountCache();` immediately before the existing `const removed = clearAllQualityMetrics();` line. No message/toast change (the existing toast already says "cached metrics" — a single wipe button clearing both caches is user-transparent per D-03).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add registry to `metricsCache.ts` + create `metricsCache.test.ts`</name>
  <files>src/quality/metricsCache.ts, src/quality/__tests__/metricsCache.test.ts</files>
  <read_first>
    - src/quality/metricsCache.ts (entire file — confirm class body stays untouched; line 163 `clearAllQualityMetrics` needs one extension line)
    - .planning/phases/24-data-fetching-foundation/24-RESEARCH.md (§Pattern 1 registry lines 263-304 + §Example 2 tests lines 761-817 + §Pitfall 3 unbounded-Map risk)
    - .planning/phases/24-data-fetching-foundation/24-CONTEXT.md (D-06 registry signature + §Claude's Discretion note on eviction using `keys().next().value`)
    - src/quality/keys.ts (LOCAL_STORAGE_PREFIX — already imported in existing metricsCache; registry tests will hit it)
  </read_first>
  <behavior>
    - Test 1: `getQualityMetricsCache('http://a/fhir')` called twice returns the SAME instance (`toBe`).
    - Test 2: Adding 3 distinct serverUrls `a`, `b`, `c` in order → `a` evicted. Re-calling `getQualityMetricsCache('http://a/fhir')` returns a NEW instance (`not.toBe(a)`).
    - Test 3: MRU touch — add `a`, `b`, touch `a` via `getQualityMetricsCache(a)`, add `c`. `b` is evicted, `a` is retained. Re-getting `a` returns same instance; re-getting `b` returns new instance.
    - Test 4: `clearQualityMetricsCache('http://a/fhir')` removes entry; next `getQualityMetricsCache('http://a/fhir')` returns a NEW instance with `size() === 0`.
    - Test 5: `clearQualityMetricsCache(serverUrl)` calls `cache.clear()` — verify by pre-populating the cache with an entry, then asserting `localStorage` has no `quality-metrics:v1:<serverUrl>|` keys after clear.
    - Test 6: `clearAllQualityMetrics()` drops registry entries too — after call, `getQualityMetricsCache('http://a/fhir')` returns NEW instance (different reference from what existed before).
    - Test 7: Registry is isolated per test via `beforeEach(clearAllQualityMetrics)` — populate registry in one test, assert empty state at the start of the next test.
  </behavior>
  <action>
    1. Edit `src/quality/metricsCache.ts`:
       a. At the TOP of the module (after imports, before the `MEMORY_LIMIT` constant), add:
          ```typescript
          const LRU_LIMIT = 2;
          const cachesByServer = new Map<string, QualityMetricsCache>();
          ```
       b. After the `QualityMetricsCache` class definition (after line 151, before the existing `clearAllQualityMetrics`), add the two new exported functions `getQualityMetricsCache` and `clearQualityMetricsCache` per the reference code in `<interfaces>` above.
       c. Inside the existing `clearAllQualityMetrics()` function body at line 163, add `cachesByServer.clear();` as the FIRST line of the function body (before the `if (typeof localStorage === 'undefined') return 0;` guard).
       d. Do NOT modify the `QualityMetricsCache` class body.
       e. Add a JSDoc block above each new exported function explaining the LRU limit (2 entries), MRU-touch-on-get semantics, and cross-referencing FOUND-02 + PITFALLS Pitfall 3.

    2. Create `src/quality/__tests__/metricsCache.test.ts`:
       - Use vitest; import `describe, it, expect, beforeEach` + `getQualityMetricsCache, clearQualityMetricsCache, clearAllQualityMetrics, QualityMetricsCache` from `'../metricsCache'`.
       - `beforeEach(() => { clearAllQualityMetrics(); if (typeof localStorage !== 'undefined') localStorage.clear(); })`.
       - 7 tests per the `<behavior>` list above, using the §Example 2 pattern from 24-RESEARCH.md lines 761-817 as starting template.
       - For Test 2 + 3: rely on the insertion-order semantic of `Map` to derive eviction order. Use `cache.size()` (class method) where instance-level inspection is needed.
       - For Test 5: pre-populate via `cache.set('k1', { value: 'v1', computedAt: Date.now(), serverUrl: 'http://a/fhir', resourceType: 'Patient', sampleSize: 100 })`. After clear, iterate `localStorage` and assert no keys with the `quality-metrics:v1:http://a/fhir|` prefix remain.

    Constraints:
    - Do NOT export `cachesByServer` directly (internal module state only).
    - Do NOT re-export `QualityMetricsCache` from any new module — consumers always go through `getQualityMetricsCache`.
    - Keep `LRU_LIMIT` as a module-private const (do not export).
  </action>
  <verify>
    <automated>npx tsc -b --noEmit && npm test -- src/quality/__tests__/metricsCache.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const LRU_LIMIT = 2" src/quality/metricsCache.ts` === 1
    - `grep -c "const cachesByServer = new Map" src/quality/metricsCache.ts` === 1
    - `grep -c "export function getQualityMetricsCache" src/quality/metricsCache.ts` === 1
    - `grep -c "export function clearQualityMetricsCache" src/quality/metricsCache.ts` === 1
    - `grep -c "cachesByServer.clear()" src/quality/metricsCache.ts` === 1 (inside `clearAllQualityMetrics`)
    - `grep -c "cachesByServer.keys().next().value" src/quality/metricsCache.ts` === 1 (eviction logic)
    - `grep -c "class QualityMetricsCache" src/quality/metricsCache.ts` === 1 (class unchanged)
    - File `src/quality/__tests__/metricsCache.test.ts` exists
    - `grep -c "^  it(" src/quality/__tests__/metricsCache.test.ts` ≥ 7
    - `grep -c "beforeEach" src/quality/__tests__/metricsCache.test.ts` ≥ 1
    - `npm test -- src/quality/__tests__/metricsCache.test.ts --run` exits 0
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>Registry added with 2-entry LRU eviction; `clearAllQualityMetrics` extended to drop registry; 7 unit tests green; class body unchanged; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate `useCompletenessReport` + `useCodingCoverage` to use `getQualityMetricsCache`</name>
  <files>src/hooks/useCompletenessReport.ts, src/hooks/useCodingCoverage.ts</files>
  <read_first>
    - src/hooks/useCompletenessReport.ts (lines 35-51 — the local cacheInstance helper being deleted; line 81 `getCache(serverUrl)` call site)
    - src/hooks/useCodingCoverage.ts (lines 31-43 — same pattern, second site; line 72 `getCache(serverUrl)` call site)
    - src/quality/metricsCache.ts (from Task 1 — new `getQualityMetricsCache` export)
    - src/__tests__/completeness-hook.test.tsx (existing test suite — MUST continue to pass)
    - src/__tests__/coding-coverage-panel.test.tsx (existing test — MUST continue to pass)
  </read_first>
  <behavior>
    - No public-API change to either hook — existing consumer tests in `src/__tests__/completeness-hook.test.tsx` and `src/__tests__/coding-coverage-panel.test.tsx` pass WITHOUT modification.
    - After migration, the two hooks no longer have local `cacheInstance` / `cacheServerUrl` / `getCache` symbols.
    - Switching between two servers A and B interleaves cache hits for both (verified by running both hooks against A, then B, then back to A — the second A call resolves from cache per FOUND-02).
  </behavior>
  <action>
    1. `src/hooks/useCompletenessReport.ts`:
       a. DELETE lines 35-51 entirely (the block starting with `// Module-scoped cache, one per server URL.` through `function getCache(serverUrl: string): QualityMetricsCache { ... }`).
       b. UPDATE the import statement at line 28: change from `import { QualityMetricsCache } from '../quality/metricsCache';` to `import { getQualityMetricsCache } from '../quality/metricsCache';`.
       c. In the `useEffect` body at line 81, change `const cache = getCache(serverUrl);` to `const cache = getQualityMetricsCache(serverUrl);`.
       d. Update the header JSDoc block (lines 1-20): replace the `Known limitation: switching server URLs discards the previous server's cache` paragraph with a sentence citing FOUND-02 migration: "Cross-server cache preserved via `getQualityMetricsCache(serverUrl)` registry in `metricsCache.ts` (2-entry LRU — Plan 24-03)."

    2. `src/hooks/useCodingCoverage.ts`:
       a. DELETE lines 31-43 entirely (same block — `cacheInstance`, `cacheServerUrl`, `getCache` helper).
       b. UPDATE the import statement at line 23: change from `import { QualityMetricsCache } from '../quality/metricsCache';` to `import { getQualityMetricsCache } from '../quality/metricsCache';`.
       c. In the `useEffect` body at line 72, change `const cache = getCache(serverUrl);` to `const cache = getQualityMetricsCache(serverUrl);`.
       d. Update the header JSDoc comment at lines 11-16: replace the `Why a separate singleton from the completeness hook` paragraph with `Uses the shared metrics-cache registry via getQualityMetricsCache(serverUrl) — see Plan 24-03. buildMetricsKey's metric namespace ('completeness' vs 'coverage') guarantees no key collision within a single QualityMetricsCache instance.`

    Constraints:
    - Do NOT change either hook's public signature or return shape.
    - Do NOT modify any behavior other than the cache-instance resolution.
    - `buildMetricsKey` usage is unchanged.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit && npm test -- src/__tests__/completeness-hook.test.tsx src/__tests__/coding-coverage-panel.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cacheInstance" src/hooks/useCompletenessReport.ts` === 0
    - `grep -c "cacheServerUrl" src/hooks/useCompletenessReport.ts` === 0
    - `grep -c "function getCache" src/hooks/useCompletenessReport.ts` === 0
    - `grep -c "getQualityMetricsCache" src/hooks/useCompletenessReport.ts` ≥ 2 (import + call)
    - `grep -c "cacheInstance" src/hooks/useCodingCoverage.ts` === 0
    - `grep -c "cacheServerUrl" src/hooks/useCodingCoverage.ts` === 0
    - `grep -c "function getCache" src/hooks/useCodingCoverage.ts` === 0
    - `grep -c "getQualityMetricsCache" src/hooks/useCodingCoverage.ts` ≥ 2
    - `npm test -- src/__tests__/completeness-hook.test.tsx src/__tests__/coding-coverage-panel.test.tsx --run` exits 0 (existing consumer tests unchanged + still green)
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>Both hooks import `getQualityMetricsCache` from `metricsCache`; local cacheInstance helpers fully removed; existing consumer tests pass unchanged; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire `setSettings` + SettingsPage to clear both caches; extend `settings-clear-cache.test.tsx`</name>
  <files>src/contexts/SettingsContext.tsx, src/components/settings/SettingsPage.tsx, src/__tests__/settings-clear-cache.test.tsx</files>
  <read_first>
    - src/contexts/SettingsContext.tsx (entire file, 45 lines — setSettings at line 27)
    - src/components/settings/SettingsPage.tsx (lines 1-55 — handleClearMetricsCache at line 41; imports at line 1-8)
    - src/__tests__/settings-clear-cache.test.tsx (entire file — existing tests for "Clear terminology cache" only; Phase 24 extends to "Clear metrics cache" coverage)
    - src/hooks/useResourceCounts.ts (Plan 24-02 output — `clearQualityCountCache` + `clearAllQualityCountCache` exports)
    - src/quality/metricsCache.ts (Plan 24-03 Task 1 output — `clearQualityMetricsCache` export)
    - .planning/phases/24-data-fetching-foundation/24-CONTEXT.md (D-03, D-04, D-05 — invalidation triggers)
  </read_first>
  <behavior>
    - Clicking the existing "Clear metrics cache" button in SettingsPage ALSO clears the count cache (verified: count cache populated before click, empty after click).
    - Calling `setSettings(nextSettings)` with a sampleSize change (serverUrl unchanged) clears BOTH the metrics registry entry AND the count cache for the current serverUrl (D-05: any settings save triggers wipe).
    - Calling `setSettings(nextSettings)` with a changed serverUrl clears BOTH caches for BOTH the prior and new serverUrl.
    - Calling `setSettings` on initial load (prevSettings === null) is a no-op for the clear paths (no prior URL to clear).
    - No new toast added; existing toast message preserved verbatim (per UI-SPEC notifications contract line 202-208).
  </behavior>
  <action>
    1. Edit `src/contexts/SettingsContext.tsx`:
       a. Add imports at top (after existing imports):
          ```typescript
          import { clearQualityCountCache } from '../hooks/useResourceCounts';
          import { clearQualityMetricsCache } from '../quality/metricsCache';
          ```
       b. Replace the existing `setSettings` function (current lines 27-30) with:
          ```typescript
          const setSettings = (next: AppSettings, options?: { usingDefaults?: boolean }) => {
            // D-04 / D-05: direct-call cache invalidation at save time (no watcher).
            // Any settings save (even sampleSize-only) wipes the current server's
            // metrics + count caches to prevent stale reports from silently
            // accumulating. If the serverUrl also changed, wipe the new URL too.
            const prevUrl = settings?.fhir?.serverUrl;
            const nextUrl = next.fhir?.serverUrl;
            if (prevUrl) {
              clearQualityCountCache(prevUrl);
              clearQualityMetricsCache(prevUrl);
            }
            if (nextUrl && nextUrl !== prevUrl) {
              clearQualityCountCache(nextUrl);
              clearQualityMetricsCache(nextUrl);
            }
            setSettingsState(next);
            setUsingDefaults(options?.usingDefaults ?? false);
          };
          ```

    2. Edit `src/components/settings/SettingsPage.tsx`:
       a. Update the import at line 8: change from `import { clearAllQualityMetrics } from '../../quality/metricsCache';` to add the count-cache import alongside:
          ```typescript
          import { clearAllQualityMetrics } from '../../quality/metricsCache';
          import { clearAllQualityCountCache } from '../../hooks/useResourceCounts';
          ```
       b. In `handleClearMetricsCache` at line 41, add `clearAllQualityCountCache();` as the FIRST line of the function body (before `const removed = clearAllQualityMetrics();`). Do NOT change the toast copy (the existing message "cached metrics removed from local storage" covers both caches transparently per D-03 + UI-SPEC line 202-208).

    3. Edit `src/__tests__/settings-clear-cache.test.tsx`:
       a. Add an import at the top (alongside existing ones):
          ```typescript
          import {
            getQualityMetricsCache,
            clearAllQualityMetrics,
          } from '../quality/metricsCache';
          import {
            clearAllQualityCountCache,
          } from '../hooks/useResourceCounts';
          ```
       b. In the existing `beforeEach` (line 77), add: `clearAllQualityMetrics(); clearAllQualityCountCache();` to prevent inter-test bleed.
       c. Add a NEW `describe('SettingsPage — Clear metrics cache (FOUND-02)', () => { ... })` block AFTER the existing terminology-cache describe. Inside it, write 2 new tests:
          - Test A: "Clear metrics cache button clears BOTH quality metrics registry AND count cache". Populate via `getQualityMetricsCache('https://fhir.example/fhir').set('k1', {value: 'v1', computedAt: Date.now(), serverUrl: 'https://fhir.example/fhir', resourceType: 'Patient', sampleSize: 100})`. Render `SettingsPage`, click the "Clear metrics cache" button (`screen.getByRole('button', { name: /Clear metrics cache/i })`). Assert: `getQualityMetricsCache('https://fhir.example/fhir')` returns a NEW instance with `size() === 0` (the registry was dropped + recreated fresh).
          - Test B (optional but recommended): The toast copy is byte-identical to the pre-Phase-24 version (grep the call `notifications.show({... title: 'Cache cleared' ...})` — verified by asserting `screen.getByText(/Cache cleared/)` still appears after click).

    Constraints:
    - Do NOT add a new toast. Do NOT change the button label. Do NOT modify the existing "Clear terminology cache" tests in any way.
    - The `setSettings` wiring MUST be a direct call at save time (D-04) — NOT a `useEffect` watching settings identity.
    - Preserve the existing `useMemo([settings, usingDefaults, loading])` deps array and its `eslint-disable-next-line` at `SettingsContext.tsx:34` — Phase 26 SHELL-05 is responsible for wrapping `setSettings` in `useCallback`; Phase 24 is out of scope for that change.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit && npm test -- src/__tests__/settings-clear-cache.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "clearQualityCountCache" src/contexts/SettingsContext.tsx` ≥ 2 (import + ≥ 1 call site in setSettings)
    - `grep -c "clearQualityMetricsCache" src/contexts/SettingsContext.tsx` ≥ 2
    - `grep -c "const prevUrl = settings?.fhir?.serverUrl" src/contexts/SettingsContext.tsx` === 1
    - `grep -c "clearAllQualityCountCache" src/components/settings/SettingsPage.tsx` ≥ 2 (import + call in handleClearMetricsCache)
    - `grep -c "clearAllQualityMetrics" src/components/settings/SettingsPage.tsx` === 2 (unchanged — 1 import + 1 call)
    - `grep -c "Clear metrics cache" src/components/settings/SettingsPage.tsx` === 1 (button label UNCHANGED per UI-SPEC)
    - `grep -c "describe('SettingsPage — Clear metrics cache (FOUND-02)" src/__tests__/settings-clear-cache.test.tsx` === 1
    - `grep -c "^    it(" src/__tests__/settings-clear-cache.test.tsx` ≥ 3 (original 2 + new ≥ 1)
    - `npm test -- src/__tests__/settings-clear-cache.test.tsx --run` exits 0 (all tests pass)
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>setSettings clears both caches for prev + new serverUrl; SettingsPage "Clear metrics cache" button clears both caches; button label + toast copy unchanged; existing test file extended with ≥ 1 new FOUND-02 test; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SettingsContext `setSettings` → cache-clear | Direct call at save time invalidates both metrics registry entry and count cache entries for the affected serverUrl(s). |
| SettingsPage `handleClearMetricsCache` → dual cache wipe | User-initiated cross-server wipe of both caches behind a single button. |
| `getQualityMetricsCache(serverUrl)` registry creation | Creates a fresh `QualityMetricsCache` instance per unique serverUrl; eviction bounds at 2 instances. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-01 | DoS (memory) | Unbounded `cachesByServer` growth across server switches | mitigate | 2-entry LRU eviction on insert via `cachesByServer.keys().next().value`. Tested in Task 1 Test 2. |
| T-24-03-02 | Tampering | Stale reports after settings change (auth rotation, sampleSize) | mitigate | D-04/D-05 direct-call wipe in `setSettings` clears the current serverUrl's entry from BOTH caches. Tested in Task 3 Test A. |
| T-24-03-03 | Info Disclosure | Cross-server data leak via shared cache instance | mitigate | Each `QualityMetricsCache` is serverUrl-namespaced internally (existing behavior); registry scopes by serverUrl key; `clearQualityMetricsCache(serverUrl)` only wipes the specified serverUrl. |
| T-24-03-04 | Tampering | Race: settings save clears cache, in-flight fetch then re-populates stale | accept | D-02 tolerates brief staleness for a local tool. Worst case is 1 stale value until next refresh. Documented in 24-RESEARCH.md Pitfall 5. |
</threat_model>

<verification>
- `metricsCache.ts` has `LRU_LIMIT = 2`, `cachesByServer = new Map`, `getQualityMetricsCache`, `clearQualityMetricsCache`
- `QualityMetricsCache` class body unchanged (grep line count or structural check)
- Both `useCompletenessReport` and `useCodingCoverage` no longer contain `cacheInstance` / `cacheServerUrl` / `function getCache`
- Both hooks import `getQualityMetricsCache` from `../quality/metricsCache`
- `SettingsContext.setSettings` calls `clearQualityCountCache` + `clearQualityMetricsCache` for prevUrl + (nextUrl if different)
- `SettingsPage.handleClearMetricsCache` calls `clearAllQualityCountCache` before `clearAllQualityMetrics`
- Button label "Clear metrics cache" unchanged (grep)
- 7 new metricsCache tests pass
- Existing `completeness-hook.test.tsx` + `coding-coverage-panel.test.tsx` + `settings-clear-cache.test.tsx` (with new describe) all pass
- Full `npm test` suite green
- `npx tsc -b --noEmit` clean
</verification>

<success_criteria>
- [ ] `getQualityMetricsCache` + `clearQualityMetricsCache` exported from `metricsCache.ts`
- [ ] 2-entry LRU eviction enforced (verified by unit test)
- [ ] MRU touch on `get` verified
- [ ] `clearAllQualityMetrics` drops registry entries in addition to its existing localStorage wipe
- [ ] `useCompletenessReport` and `useCodingCoverage` migrated (no local `cacheInstance`)
- [ ] `SettingsContext.setSettings` wipes both caches for prev + new serverUrl (direct-call, not watcher)
- [ ] `SettingsPage.handleClearMetricsCache` clears both caches under the existing button
- [ ] No UI-visible change (button label, toast copy unchanged)
- [ ] Full suite green + tsc clean + zero new `as` casts in consumer hooks
</success_criteria>

<output>
After completion, create `.planning/phases/24-data-fetching-foundation/24-03-SUMMARY.md` documenting:
- Registry + LRU eviction policy (2 entries, MRU-touch-on-get)
- Settings-save wiring contract (D-04/D-05 direct call)
- Two hooks migrated (completeness, coding coverage)
- Consumer-panel contract unchanged (tests stayed green without modification)
- Downstream: Plan 24-04 migrates 4 report hooks; SHELL-05 (Phase 26) will wrap `setSettings` in `useCallback` (deferred)
</output>
