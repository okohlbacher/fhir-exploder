---
phase: 38.1-fix-sort-date-blaze-incompatibility
plan: 01
status: complete
closed_at: 2026-04-28T15:00:00Z
tasks_completed: 6/6
test_baseline_before: 1054
test_baseline_after: 1060
test_baseline_delta: "+6 (added 4 expect calls in MiiModuleTab.test.tsx + 2 expect calls in ClinicalTimeline.test.tsx; vitest counts these as separate tests under the existing describe blocks)"
files_modified: 7
commits: 6
re_walk: passed
---

# Plan 38.1-01 SUMMARY — `_sort=-date` Blaze incompatibility fix

## Per-task verdict (6/6 complete)

| Task | Type | Action | Commit | Verify |
|------|------|--------|--------|--------|
| T1 | auto | Swap line 81 + comment line 100 in `MiiModuleTab.tsx` | `7abe68f` | grep + 8/8 vitest pass |
| T2 | auto | Swap line 72 in `ClinicalTimeline.tsx` | `e3488dc` | grep + 3/3 vitest pass |
| T3 | auto | Swap line 122 in `PatientTimeline.tsx` | `4605972` | grep + tsc clean (no dedicated test file) |
| T4 | auto | Swap line 132 in `FhirResourcesView.tsx` | `b9cf977` | grep + 7/7 vitest pass |
| T5 | auto | Regression-locking assertions in 2 colocated test files | `bc427d1` | grep + full suite 1060/0 + tsc + build |
| T6 | checkpoint:human-verify | Live-Blaze re-walk Phase 33 Tests 1, 2, 6 | `76f2183` | 3 fix-commit notes appended; `result: fail` preserved |

## Per-file commit shas

| File | Type | Commit | Notes |
|------|------|--------|-------|
| `src/components/patients/MiiModuleTab.tsx` | source | `7abe68f` | Line 81 (URL) + comment around line 100 (4-line rephrase to avoid the literal substring) |
| `src/components/patients/ClinicalTimeline.tsx` | source | `e3488dc` | Line 72 |
| `src/components/patients/PatientTimeline.tsx` | source | `4605972` | Line 122 |
| `src/components/patients/FhirResourcesView.tsx` | source | `b9cf977` | Line 132 |
| `src/components/patients/__tests__/MiiModuleTab.test.tsx` | test | `bc427d1` | Lines 102+ — positive `_sort=-_lastUpdated` + negative `_sort=-date` assertions |
| `src/components/patients/__tests__/ClinicalTimeline.test.tsx` | test | `bc427d1` | Lines 99+ — same shape, asserts every `searchResources` call |
| `.planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md` | docs | `76f2183` | 3 evidence appends; `result: fail` rows untouched per D-03 |

## Vitest baseline

- **Before (Phase 38 close):** 1054 passing / 0 failing / 22 todo / 3 skipped
- **After (Plan 38.1-01 close):** **1060 passing / 0 failing / 22 todo / 3 skipped**
- Delta: +6 passes from the new regression-locking assertions in T5
- TypeScript: `tsc -b --noEmit` clean (exit 0)
- Vite build: clean (exit 0)

## Smoke probe (live Blaze, before re-walk)

After T1-T5 commits, direct curl confirmed Blaze accepts the fixed URL:

```
curl -i "http://localhost:8080/fhir/Observation?patient=Patient/DHOT622BDE5AAY4S&_count=5&_sort=-_lastUpdated&category=laboratory"
→ HTTP 200, total=5
```

Same patient that returned HTTP 400 from Blaze during the Phase 38 walk. The fix is provable at the API layer before the human re-walk.

## Re-walk evidence (T6 — checkpoint:human-verify, approved)

Verifier walked Phase 33 HUMAN-UAT Tests 1, 2, 6 against the same Blaze + Synthea fingerprint pinned in `38-SESSION.md` (Blaze 1.6.2, Patient=500, container `blaze-blaze-1`). All 3 walks passed:

| Phase 33 Test | Re-walk outcome | Evidence appended to `33-HUMAN-UAT.md` |
|----|-----------------|------------------|
| 1 (Laborbefund extraQuery) | PASS | `→ fixed in Phase 38.1, commit 7abe68f.` Laborbefund tab populated with lab Observations; `&category=laboratory` filter applies cleanly |
| 2 (Timeline color + German label) | PASS | `→ fixed in Phase 38.1, commit e3488dc.` Zeitleiste renders 4-color timeline (Encounter→Fall, Condition→Diagnose, Procedure→Prozedur, Observation→Laborbefund); 4 per-type requests all HTTP 200 |
| 6 (Per-patient Laborbefund populated) | PASS | `→ fixed in Phase 38.1, commit 7abe68f.` UAT-FU-06 closure verified end-to-end against live Blaze |

`result: fail` preserved on all 3 rows per Phase 38 D-03 (historical record of what the v1.5 walk surfaced; fix-commit append documents the closure separately).

## Decision-coverage matrix (5/5 REQ-IDs closed at full fidelity)

| REQ-ID | Closes via | Status |
|--------|------------|--------|
| MII-EXT-04 | T1 (MiiModuleTab.tsx swap) + T6 (Test 1 re-walk pass) | Full closure |
| MII-EXT-06 | T1 (multi-type fan-out reaches Blaze with 200) + T6 | Full closure |
| MII-EXT-07 | T1 (UAT-FU-06 extraQuery actually returns data) + T6 (Test 6 pass) | Full closure |
| MII-EXT-08 | T2 (ClinicalTimeline.tsx swap) + T6 (Test 2 re-walk pass) | Full closure |
| UAT-FU-06 | T1 + T6 (Test 6 re-walk pass; Laborbefund populated end-to-end) | Full closure |

## Verification (global gates)

All 7 plan-level invariants from `<verification>` block confirmed:

1. ✓ `grep -rn "_sort=-date" src/ | grep -v "__tests__"` returns **0 lines** (production-vs-test split clean)
2. ✓ `grep -rn "_sort=-_lastUpdated" src/` returns **7 lines** (≥6 required: 4 production sites + 2 test files + 1 comment)
3. ✓ `npm test` 1060 passing / 0 failing
4. ✓ `npx tsc -b --noEmit` exits 0
5. ✓ `npm run build` exits 0
6. ✓ `grep -c "fixed in Phase 38.1" 33-HUMAN-UAT.md` returns **3** (one append per Tests 1, 2, 6)
7. ✓ `33-VERIFICATION.md` byte-identical to its Plan 38-03 close state (last commit `f619a6c`)

## Scope discipline check

This plan touched **only** the 4 listed source files + 2 colocated test files + the HUMAN-UAT writeback. Verified:

- ✓ NO audit of other `_sort=` usages elsewhere in `src/`
- ✓ NO refactor into a shared sort-helper utility
- ✓ NO edits to Phase 33's verification narrative beyond the per-row append (33-VERIFICATION.md untouched)
- ✓ NO new dependencies introduced
- ✓ NO test infrastructure changes (no new test runner, no new wiring for PatientTimeline.tsx)

## Cosmetic note (carried out per acceptance criteria)

Plan T1 acceptance required `grep -c "_sort=-date" src/components/patients/MiiModuleTab.tsx` to return **0**. The initial code-comment update I drafted included the literal phrase `swapped from \`_sort=-date\` because Blaze...`, which would have left a single grep hit. Rephrased to `swapped from the previous \`date\` sort clause because Blaze...` so the grep is clean while preserving the historical context for future readers. Same comment still references `38-SUMMARY.md` for the full root-cause writeup.

## Hand-off

Plan 38.1-01 closes. The phase-level orchestrator should now:

1. Run phase verification (gsd-verifier) against the 4 ROADMAP success criteria.
2. Mark Phase 38.1 complete in ROADMAP + STATE.md.
3. Commit closure artifacts.
4. Surface next-step routing (likely: v1.5 milestone audit → archive → v1.6 cycle, since 38.1 was the last outstanding item in v1.5).
