---
phase: 38.2-fix-valuequantity-render-show-value-followed-by-unit-on-obse
verified: 2026-04-29T11:10:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 4/5
  gaps_closed:
    - "Re-walk of Phase 33 HUMAN-UAT Test 1 + Test 6 against the same Blaze + Synthea fingerprint (38-SESSION.md) confirms inline values render correctly"
  gaps_remaining: []
  regressions: []
---

# Phase 38.2: Fix valueQuantity Render Verification Report

**Phase Goal:** Make Observation rows on the patient detail Laborbefund tab render `valueQuantity.value` followed by `valueQuantity.unit` instead of just the unit. Today the cell shows `mg/dL` where it should show `98 mg/dL`. The bug was masked by the `_sort=-date` regression closed in Phase 38.1; once the Laborbefund tab populated, the value-rendering defect became visible during the 38.1 re-walk on 2026-04-28.

**Verified:** 2026-04-29T11:10:00Z
**Status:** passed
**Re-verification:** Yes — after live-Blaze re-walk closure (previous status: `human_needed`)

## Re-Verification Summary

The prior VERIFICATION.md (timestamp 2026-04-29T11:05:00Z) returned `human_needed` with score 4/5: truths #1-#4 passed but truth #5 (the Phase 33 HUMAN-UAT Test 1 + Test 6 live-Blaze re-walk) was routed to human verification because the SUMMARY (lines 135-137) admitted the doc-level 33-HUMAN-UAT.md append landed without an actual live-Blaze visual walk.

**Closure evidence (2026-04-29):** User confirmed via human-verify checkpoint reply "Passed" that the live-Blaze + Synthea re-walk against the 38-SESSION.md fingerprint visually verified inline `<value> <unit>` rendering on Observation rows. The appended evidence in `33-HUMAN-UAT.md` (commit `9dd7fde`, present on Test 1 line 63 and Test 6 line 192) is now backed by an actual user-executed walk, satisfying SC #4 and closing the only outstanding gap.

No regressions introduced. All previously-passing checks still pass: full Vitest suite at 1064/0, TypeScript clean, branch-order invariant intact, append-only HUMAN-UAT chain intact.

## Goal Achievement

### Observable Truths (Roadmap Success Criteria #1-4)

| #   | Truth                                                                                                                                                                            | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Lab Observation rows render `<value> <unit>` (e.g. `98 mg/dL`) when both `valueQuantity.value` and `valueQuantity.unit` are present                                              | ✓ VERIFIED | `src/components/explorer/ResourcePropertyTable.tsx:134` returns `<Text size="sm">{obj.value as number} {obj.unit as string}</Text>` when `hasValue && hasUnit`. Branch positioned at line 130 (Quantity-shape detection) BEFORE the Coding branch at line 145 (`awk` line-order check: `Quantity_line=132 Coding_line=145 quantity_first=1`). Test 1 (`ResourcePropertyTable.test.tsx:127`) asserts `getByText(/98\s*mg\/dL/)`. |
| 2   | Missing-field cases handled: only `value` → render `value`; only `unit` → render empty / dash (NOT bare unit); neither → empty cell                                              | ✓ VERIFIED | `ResourcePropertyTable.tsx:131-141` enumerates all 4 shapes — `hasValue && hasUnit` → `value unit`; `hasValue` alone → `value`; otherwise (unit-only or neither) → em-dash via `<Text c="dimmed">—</Text>`. Tests 2-4 (`ResourcePropertyTable.test.tsx:140-188`) lock all three remaining shapes including the anti-regression that asserts no bare `mg/dL` appears in the row when `valueQuantity.value` is absent.       |
| 3   | Existing test suite stays green; new regression test asserts that for an Observation fixture with `valueQuantity: { value: 98, unit: 'mg/dL' }`, rendered DOM contains BOTH `98` AND `mg/dL` | ✓ VERIFIED | `src/__tests__/ResourcePropertyTable.test.tsx:126-189` contains the `Phase 38.2: valueQuantity rendering on Observation` describe block with 4 tests. Test 1 (line 127) renders the headline fixture with `valueQuantity: { value: 98, unit: 'mg/dL', system: '...', code: 'mg/dL' }` and asserts `screen.getByText(/98\s*mg\/dL/)`. `npm test` exits 0 with `1064 passed` / 22 todo / 0 failed (exceeds 1059 baseline by 5). |
| 4   | Re-walk of Phase 33 HUMAN-UAT Test 1 / Test 6 (Laborbefund populated) confirms values render correctly inline against live Blaze + Synthea (same fingerprint as `38-SESSION.md`) | ✓ VERIFIED | `33-HUMAN-UAT.md` lines 63-76 (Test 1) and lines 192-203 (Test 6) carry the appended evidence `→ valueQuantity render fixed in Phase 38.2, commit 9dd7fde`. User confirmed via human-verify checkpoint reply "Passed" on 2026-04-29 that the live-Blaze + Synthea re-walk visually verified inline `<value> <unit>` rendering. Original `result: fail` markers preserved per D-03 (`grep -c "result: fail"` = 8 — unchanged). |

**Score:** 4/4 roadmap success criteria verified (and 5/5 plan-frontmatter must-have truths)

### Required Artifacts

| Artifact                                                                                              | Expected                                                                                          | Status     | Details                                                                                                                                                                                                                                                                                            |
| ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/components/explorer/ResourcePropertyTable.tsx`                                                   | Quantity branch in `RenderValue` handling all 4 input shapes; positioned before Coding branch     | ✓ VERIFIED | Quantity-shape branch at lines 120-142; Coding branch at line 145; `awk` line-order check confirms Quantity precedes Coding. Old single-condition `if (obj.value !== undefined && obj.unit)` branch deleted (`grep -c` returns 0). New comment block tags the fix with `Phase 38.2:` marker.       |
| `src/__tests__/ResourcePropertyTable.test.tsx`                                                        | Regression-test fixture with `valueQuantity: { value: 98, unit: 'mg/dL' }`; assert both fragments | ✓ VERIFIED | `Phase 38.2: valueQuantity rendering on Observation` describe block at line 126; 4 test cases covering all 4 input shapes; headline test asserts `getByText(/98\s*mg\/dL/)`; all 4 pass when running `npm test -- src/__tests__/ResourcePropertyTable.test.tsx` (8/8 in this file).                  |
| `.planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md`             | Append `→ fixed in Phase 38.2, commit <sha>` evidence on Test 1 + Test 6 rows                     | ✓ VERIFIED | `grep -c "fixed in Phase 38.2"` returns 2 (Test 1 line 63, Test 6 line 192). Phase 38.1 evidence preserved (`grep -c "fixed in Phase 38.1"` returns 3 — Tests 1, 2, 6). `result: fail` preserved unchanged (`grep -c "result: fail"` returns 8). Commit SHA `9dd7fde` matches `git log` `fix(38.2)` commit. User-confirmed live-Blaze re-walk now backs the doc-level append. |

### Key Link Verification

| From                                                | To                                  | Via                                                                                            | Status   | Details                                                                                                                                                                                                                                              |
| --------------------------------------------------- | ----------------------------------- | ---------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `MiiModuleTab.tsx`                                  | `ResourceDetailPage.tsx`            | `navigate('/patients/${patientId}/${r.resourceType}/${r.id}')` on Laborbefund row click       | ✓ WIRED  | Pre-existing wiring; live re-walk confirmed clicking lab Observation rows opens the resource detail page.                                                                                                                                          |
| `ResourceDetailPage.tsx`                            | `HumanReadableView.tsx`             | Default `activeTab='human-readable'` renders `<HumanReadableView resource={resource} />`      | ✓ WIRED  | Pre-existing wiring; live re-walk confirmed Human-readable tab is the default and renders `valueQuantity` row.                                                                                                                                    |
| `HumanReadableView.tsx`                             | `ResourcePropertyTable.tsx`         | `<ResourcePropertyTable resource={display} />` wraps each FHIR field with `RenderValue`       | ✓ WIRED  | Pre-existing wiring; the fix flows through this path because each Observation field (including `valueQuantity`) is iterated in `ResourcePropertyTable` and passed to `RenderValue`.                                                               |
| `ResourcePropertyTable.tsx` (RenderValue, Quantity) | DOM Text element                    | Render `<Text>{obj.value} {obj.unit}</Text>` for Quantity-shaped objects                       | ✓ WIRED  | Line 134: `<Text size="sm">{obj.value as number} {obj.unit as string}</Text>`. Branch is reachable (positioned before Coding); regression tests + live-Blaze re-walk both confirm the rendered DOM contains both fragments.                       |

### Data-Flow Trace (Level 4)

| Artifact                                            | Data Variable     | Source                                                                                          | Produces Real Data | Status      |
| --------------------------------------------------- | ----------------- | ----------------------------------------------------------------------------------------------- | ------------------ | ----------- |
| `ResourcePropertyTable.tsx` (RenderValue Quantity)  | `obj` (Quantity)  | FHIR `Observation.valueQuantity` field, fetched by `MedplumClient.search('Observation', ...)` from live Blaze 1.6.2 | ✓ FLOWING          | ✓ VERIFIED  |

Live re-walk evidence (user reply "Passed" 2026-04-29) confirms real Synthea Observations flow through MedplumClient → MiiModuleTab → ResourceDetailPage → HumanReadableView → ResourcePropertyTable → RenderValue and render `<value> <unit>` correctly against the 38-SESSION.md fingerprint.

### Behavioral Spot-Checks

| Behavior                                                                  | Command                                              | Result                                                                                | Status |
| ------------------------------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------- | ------ |
| Full Vitest suite passes with no failures                                 | `npm test`                                           | `Test Files 114 passed | 3 skipped (117); Tests 1064 passed | 22 todo (1086)`        | ✓ PASS |
| Phase 38.2 colocated regression tests pass (4 new + 4 existing UAT-FU-02) | `npm test -- src/__tests__/ResourcePropertyTable.test.tsx` | `Test Files 1 passed (1); Tests 8 passed (8)`                                          | ✓ PASS |
| TypeScript compiles clean                                                 | `npx tsc -b --noEmit`                                | `EXITCODE=0`                                                                           | ✓ PASS |
| Quantity branch precedes Coding branch in source                          | `awk` line-order check on `ResourcePropertyTable.tsx` | `Quantity_line=132 Coding_line=145 quantity_first=1`                                   | ✓ PASS |
| Old single-condition Quantity branch deleted                              | `grep -c "if (obj.value !== undefined && obj.unit)"` | `0`                                                                                   | ✓ PASS |
| HUMAN-UAT append-only chain present                                       | `grep -c "fixed in Phase 38.2" 33-HUMAN-UAT.md`      | `2` (≥ 2 required); Phase 38.1 markers preserved (`grep -c "fixed in Phase 38.1"` = 3) | ✓ PASS |
| Phase 38.2 commits exist in git log                                       | `git log --oneline | grep "(38\.2)"`                 | `3a8a1cf test(38.2)`, `9dd7fde fix(38.2)`, `2ece6a9 docs(38.2)` all present           | ✓ PASS |
| Live-Blaze visual re-walk Test 1 + Test 6                                 | (user-executed via `npm run dev` + browser)          | User reply "Passed" 2026-04-29                                                        | ✓ PASS |

### Requirements Coverage

Per phase preamble + plan frontmatter, Phase 38.2 declares no new REQ-IDs (`requirements: []`). It is an implementation-level follow-up to UAT-FU-06 (the per-patient Laborbefund populated case), which was previously satisfied via Phase 33 + closed by Phase 38.1. The empty `requirements:` field is intentional and is NOT flagged as a gap per the verifier inputs.

### Anti-Patterns Found

Code review (`38.2-REVIEW.md`) returned 3 Info-level findings (IN-01: Quantity-with-code-no-unit could fall back to `obj.code`; IN-02: `as number` cast could be type-gated; IN-03: `!obj.coding` truthy-check would mismatch a defensively-empty `coding: []`), 0 Warning, 0 Critical. Per verifier inputs, Info-level findings do NOT count as gaps and are not blocking. No TODO/FIXME/PLACEHOLDER strings introduced; no empty implementations; no hardcoded empty data; no console.log-only handlers in the modified files.

### Human Verification Required

None remaining. The blocking checkpoint (Task 3 — live Blaze + Synthea re-walk of Phase 33 HUMAN-UAT Tests 1 + 6) was completed by the user; the reply "Passed" on 2026-04-29 is the audit-trail evidence, and `33-HUMAN-UAT.md` carries the append-only `fixed in Phase 38.2, commit 9dd7fde` annotations on both Test 1 (line 63) and Test 6 (line 192).

### Gaps Summary

No gaps. All 4 roadmap success criteria are verified by direct code/test/doc evidence + user-executed live-Blaze re-walk:

1. **SC #1 (both fields render `<value> <unit>`):** `ResourcePropertyTable.tsx:134` + Test 1 + live re-walk visual confirmation.
2. **SC #2 (missing-field cases handled):** `ResourcePropertyTable.tsx:131-141` + Tests 2-4 (anti-regression for unit-only).
3. **SC #3 (regression test asserts BOTH `98` AND `mg/dL`):** Test 1 in the new `Phase 38.2:` describe block; full suite green at 1064/0.
4. **SC #4 (live Blaze re-walk):** `33-HUMAN-UAT.md` lines 63-76 + 192-203 carry append-only evidence pinned to commit `9dd7fde`; user confirmed re-walk PASSED on 2026-04-29; original `result: fail` markers preserved per D-03.

The Quantity-first dispatch fix is regression-locked by 4 colocated tests, TypeScript compiles clean, the HUMAN-UAT audit chain extends the Phase 38.1 → Phase 38.2 fix lineage on the two affected tests, and the live-Blaze visual confirmation closes the previously-open human-verify gap.

---

_Verified: 2026-04-29T11:10:00Z (re-verification after human-verify closure)_
_Verifier: Claude (gsd-verifier)_
