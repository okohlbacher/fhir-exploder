---
phase: 37
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json
autonomous: false
gap_closure: false
requirements:
  - D-22
tags: [uat, tti, performance, worktree, mii-extension]
must_haves:
  truths:
    - "tti-snapshot.json exists at .planning/phases/37-.../ with all required fields per CONTEXT D-07 (`baseline_ms`, `post_phase_ms`, `delta_ms`, `delta_pct`, `patient_id`, `throttling`, `commits`, `runs_per_checkout`, `verdict`)"
    - "baseline_ms is the median of 3 runs captured against commit 048e99c (Phase 33 tail) — NOT current main HEAD"
    - "post_phase_ms is the median of 3 runs captured against commit a7e4544 (Phase 34 HEAD) — NOT current main HEAD (main includes Phase 36 lazy-load mitigation per RESEARCH §Pitfall 6)"
    - "patient_id field records the deterministic-first patient (alphabetical by name[0].family) computed client-side at capture time (Blaze rejects _sort=family per RESEARCH §Don't Hand-Roll)"
    - "throttling field documents 'no throttling' (CPU 1×, network unrestricted) per CONTEXT D-04 so a future re-capture is reproducible"
    - "verdict computed via D-08 dual gate: PASS iff delta_ms ≤ 100 AND delta_pct ≤ 10; FAIL otherwise"
    - "Both /tmp/exploder-tti-baseline and /tmp/exploder-tti-postphase worktrees removed at end of capture (no lingering /tmp pollution per CONTEXT D-06 + RESEARCH §Runtime State)"
    - "Vite preview --strictPort used on ports 4173 (baseline) and 4174 (post-phase) so port collisions error loudly (RESEARCH §Pitfall 2)"
  artifacts:
    - path: ".planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json"
      provides: "Empirical TTI capture record: baseline_ms (at 048e99c), post_phase_ms (at a7e4544), delta_ms, delta_pct, dual-gate verdict, deterministic-patient id, throttling profile, captured commits, runs-per-checkout"
      min_lines: 20
      contains: "baseline_ms"
  key_links:
    - from: "git worktree at /tmp/exploder-tti-baseline (048e99c)"
      to: "tti-snapshot.json.baseline_ms"
      via: "Chrome DevTools Performance panel TTI marker, median of 3 runs"
      pattern: "baseline_ms.*[0-9]+"
    - from: "git worktree at /tmp/exploder-tti-postphase (a7e4544)"
      to: "tti-snapshot.json.post_phase_ms"
      via: "Chrome DevTools Performance panel TTI marker, median of 3 runs (same patient route as baseline)"
      pattern: "post_phase_ms.*[0-9]+"
    - from: "tti-snapshot.json.delta_ms + tti-snapshot.json.delta_pct"
      to: "tti-snapshot.json.verdict.gate_pass"
      via: "D-08 dual-gate: delta_ms ≤ 100 AND delta_pct ≤ 10"
      pattern: "verdict"
---

<objective>
Capture before/after Time-to-Interactive on `/patients/<deterministic-id>` at commit `048e99c` (baseline = Phase 33 tail) and commit `a7e4544` (post-phase = Phase 34 HEAD), via Chrome DevTools Performance panel under twin git worktrees. Write `tti-snapshot.json` per CONTEXT D-07 schema. Closes the empirical leg of Phase 34 D-22.

Purpose: Phase 34 added 14 extension modules to /patients/:id, with the enabling invariant being Phase 33 D-11 (`keepMounted={false}` on extension Tabs.Panel — extension panels lazy-mount on click). Plan 34-06 was supposed to verify TTI didn't regress, but ran under `--auto` mode and never produced `tti-snapshot.json` (Pitfall 1 — auto-mode silently approved the human-verify gate). Phase 36 then lazy-loaded the 225 KB profiles chunk, which means measuring current `main` HEAD would conflate Phase 34's regression with Phase 36's mitigation (RESEARCH §Pitfall 6). This plan uses twin git worktrees pinned to `048e99c` and `a7e4544` to isolate the Phase-34-only delta the D-22 gate was originally designed to measure.

Output: One `tti-snapshot.json` file with the dual-gate verdict computed; cleaned-up worktrees; ports freed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-CONTEXT.md
@.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md
@.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-VALIDATION.md
@.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-PLAN.md
@.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-06-UAT.md

<interfaces>
<!-- Authoritative commits and worktree commands. Executor uses these verbatim. -->
<!-- DO NOT substitute commit refs — RESEARCH §Pitfall 6 explicitly warns against capturing against current main HEAD. -->

Baseline commit: `048e99c` (Phase 33 tail — "docs(phase-33): evolve PROJECT.md after phase completion")
Post-phase commit: `a7e4544` (Phase 34 HEAD — "docs(phase-34): complete + evolve PROJECT.md")

Worktree commands:
  git worktree add /tmp/exploder-tti-baseline 048e99c
  git worktree add /tmp/exploder-tti-postphase a7e4544
  git worktree remove /tmp/exploder-tti-baseline   (cleanup)
  git worktree remove /tmp/exploder-tti-postphase  (cleanup)

Preview commands (each in its own worktree):
  npm install                                        (per worktree — lockfiles diverge per RESEARCH §Pitfall 4)
  npm run build
  npm run preview -- --port 4173 --strictPort &      (baseline)
  npm run preview -- --port 4174 --strictPort &      (post-phase)

Deterministic-first patient (client-side, since Blaze rejects _sort=family per RESEARCH):
  curl -s "http://localhost:8080/fhir/Patient?_summary=true&_count=1000&_elements=id,name" \
    | python3 -c "
  import json, sys
  data = json.load(sys.stdin)
  def famof(p):
      n = p.get('name') or [{}]
      return n[0].get('family') or ''
  items = [(famof(e['resource']), e['resource'].get('id'))
           for e in data.get('entry', [])]
  items_with_family = [t for t in items if t[0]]
  items_with_family.sort(key=lambda t: t[0].lower())
  fam, pid = items_with_family[0]
  print(f'patient_id={pid} family={fam}')"

tti-snapshot.json schema (CONTEXT D-07 required + RESEARCH §tti-snapshot.json Schema suggested):
  {
    "schema_version": 1,
    "captured_on": "2026-04-26",
    "captured_by": "<human verifier>",
    "method": "Chrome DevTools Performance panel — TTI metric in Timings lane",
    "throttling": { "cpu": "1x (off — no throttling)", "network": "no throttling" },
    "patient_id": "<resolved at capture time>",
    "patient_family_name": "<resolved at capture time>",
    "route": "/patients/<patient_id>",
    "commits": { "baseline": "048e99c", "post_phase": "a7e4544" },
    "preview_ports": { "baseline": 4173, "post_phase": 4174 },
    "runs_per_checkout": 3,
    "baseline_runs_ms": [<run1>, <run2>, <run3>],
    "baseline_ms": <median of baseline_runs_ms>,
    "post_phase_runs_ms": [<run1>, <run2>, <run3>],
    "post_phase_ms": <median of post_phase_runs_ms>,
    "delta_ms": <post_phase_ms - baseline_ms>,
    "delta_pct": <(delta_ms / baseline_ms) * 100, 2 dp>,
    "verdict": "PASS" | "FAIL",
    "gate": {
      "delta_ms_threshold": 100,
      "delta_pct_threshold": 10,
      "gate_pass": <delta_ms <= 100 AND delta_pct <= 10>
    },
    "notes": "<free text — e.g. unusual run-to-run variance, environment notes>"
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up twin worktrees with isolated npm installs and resolve deterministic patient ID</name>
  <files>
    /tmp/exploder-tti-baseline/      (created via git worktree)
    /tmp/exploder-tti-postphase/     (created via git worktree)
  </files>
  <read_first>
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-CONTEXT.md (D-04, D-05, D-06)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md §Architecture Pattern 1 (Twin-Worktree)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md §Pitfall 4 (lockfile divergence — npm install per worktree mandatory)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md §Pitfall 6 (commit ref ambiguity — must use a7e4544 not main)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md §Code Examples (Deterministic-First Patient Selection)
  </read_first>
  <action>
Step 1: Verify pre-conditions.
- `git status` — main working tree must be clean (no uncommitted changes that could pollute the worktrees).
- `git worktree list` — record current worktrees so cleanup at end of plan can verify the count returns to baseline.
- `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/fhir/metadata` — must return 200 (Blaze must be up; if not, tell the user to start Blaze and abort this task).

Step 2: Create the baseline worktree at commit 048e99c (Phase 33 tail).
```
git worktree add /tmp/exploder-tti-baseline 048e99c
cd /tmp/exploder-tti-baseline
npm install
```
Expect ~470 packages, 1-3 minutes. Verify by checking that `node_modules/fhir-package-loader/` does NOT exist (Phase 33 tail predates the Phase 34 devDep — RESEARCH §Pitfall 4):
```
test ! -d /tmp/exploder-tti-baseline/node_modules/fhir-package-loader && echo "OK: baseline lockfile does not include fhir-package-loader" || echo "WARN: baseline appears to have post-Phase-34 lockfile"
```

Step 3: Build the baseline production bundle.
```
cd /tmp/exploder-tti-baseline
npm run build
```
Expect `dist/` to be populated. If build fails, halt and report (something is wrong with the baseline checkout).

Step 4: Create the post-phase worktree at commit a7e4544 (Phase 34 HEAD).
```
cd /Users/kohlbach/Claude/Exploder
git worktree add /tmp/exploder-tti-postphase a7e4544
cd /tmp/exploder-tti-postphase
npm install
```
Verify Phase 34's devDep is present:
```
test -d /tmp/exploder-tti-postphase/node_modules/fhir-package-loader && echo "OK: postphase lockfile includes fhir-package-loader" || echo "FAIL: postphase missing Phase 34 devDep"
```

Step 5: Build the post-phase production bundle.
```
cd /tmp/exploder-tti-postphase
npm run build
```

Step 6: Resolve the deterministic-first patient (alphabetical by `name[0].family`, client-side).
Run the curl + python3 pipeline from <interfaces> above against `http://localhost:8080`. Record the output `patient_id=<X> family=<Y>`. Save these two values to a temp file `/tmp/tti-deterministic-patient.txt` (single line `<X>|<Y>`) so Task 2 + Task 3 can re-read without re-running the curl (Blaze data could in theory shift between captures — pinning the result here keeps both captures on the same patient per RESEARCH §Pitfall 7).

Step 7: cd back to main working tree.
```
cd /Users/kohlbach/Claude/Exploder
```
  </action>
  <verify>
    <automated>test -d /tmp/exploder-tti-baseline/dist && test -d /tmp/exploder-tti-postphase/dist && test -s /tmp/tti-deterministic-patient.txt && test ! -d /tmp/exploder-tti-baseline/node_modules/fhir-package-loader && test -d /tmp/exploder-tti-postphase/node_modules/fhir-package-loader && git worktree list | grep -q "/tmp/exploder-tti-baseline" && git worktree list | grep -q "/tmp/exploder-tti-postphase"</automated>
  </verify>
  <acceptance_criteria>
    - Both worktrees exist: `git worktree list | grep -c "/tmp/exploder-tti-"` returns 2
    - Both worktrees built: `test -d /tmp/exploder-tti-baseline/dist && test -d /tmp/exploder-tti-postphase/dist`
    - Lockfile divergence verified: baseline does NOT have `node_modules/fhir-package-loader/`, post-phase DOES (Pitfall 4 catches stale-cache build mistakes)
    - Deterministic patient pinned: `/tmp/tti-deterministic-patient.txt` exists, non-empty, contains a `|` separator
    - Main working tree unchanged: `git status` (in /Users/kohlbach/Claude/Exploder) is clean
  </acceptance_criteria>
  <done>
    - Twin worktrees set up at commits 048e99c and a7e4544
    - Each worktree has its own `node_modules/` (npm install run independently per Pitfall 4) and `dist/` (npm run build successful)
    - Deterministic-first patient ID + family name resolved client-side (Blaze rejects _sort=family) and pinned to /tmp/tti-deterministic-patient.txt
    - No commits made (this task is build-only setup; Task 3 commits the JSON artifact)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture TTI on both worktrees via Chrome DevTools Performance panel (3 runs each)</name>
  <files>
    /tmp/exploder-tti-baseline-runs.txt    (3 runs, one per line, baseline)
    /tmp/exploder-tti-postphase-runs.txt   (3 runs, one per line, post-phase)
  </files>
  <read_first>
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-CONTEXT.md (D-04 — 3 runs, take median, no throttling; D-08 — dual gate)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md §Capture Procedure (TTI) lines 322-370 (full Chrome DevTools recipe)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md §Pitfall 2 (vite preview port collision — use --strictPort)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md §Pitfall 3 (Chrome page cache — use Incognito + Hard Reload)
    - /tmp/tti-deterministic-patient.txt (the patient ID resolved in Task 1)
  </read_first>
  <what-built>
    Task 1 set up two git worktrees at the canonical commits, built each, and pinned the deterministic-first Synthea patient. Now: capture 3 Chrome DevTools Performance-panel TTI readings per checkout against `/patients/<deterministic-id>`.

    The Performance panel TTI marker is human-read from a flame chart — there is no Performance API call this project uses that returns the same Lighthouse-equivalent number. Median of 3 runs (CONTEXT D-04) suppresses run-to-run noise (10-30 ms floor per Plan 34-05 narrative).
  </what-built>
  <how-to-verify>
    Executor preparation steps (auto, before pausing):
    1. Read `/tmp/tti-deterministic-patient.txt`. Parse out patient_id (left of `|`) and family (right of `|`). Print to user.
    2. Start the baseline preview server in the background (must be `--strictPort` per RESEARCH Pitfall 2):
       ```
       cd /tmp/exploder-tti-baseline
       npm run preview -- --port 4173 --strictPort &
       ```
       Wait 2-3 seconds; verify with `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:4173/` returning 200.
    3. Start the post-phase preview server in the background:
       ```
       cd /tmp/exploder-tti-postphase
       npm run preview -- --port 4174 --strictPort &
       ```
       Verify port 4174 with curl.
    4. Print this resume-block to the user (verbatim):

       ```
       === HUMAN CAPTURE REQUIRED — TTI BEFORE/AFTER ===

       Two preview servers are up:
         BASELINE  (commit 048e99c, Phase 33 tail) → http://localhost:4173/
         POSTPHASE (commit a7e4544, Phase 34 HEAD) → http://localhost:4174/

       Deterministic patient: <patient_id> (family <family>)

       Steps (Chrome on macOS, Incognito recommended — no extensions, isolates cache):

       === BASELINE CAPTURE (3 runs) ===
         1. Open Incognito window
         2. Navigate to http://localhost:4173/patients/<patient_id>
         3. DevTools (Cmd+Option+I) → Network tab → CHECK "Disable cache"  (Pitfall 3 mitigation)
         4. Performance tab → Click ● (Record) → Cmd+Shift+R (Hard Reload)
         5. Wait until page is fully settled (timeline visible, no spinners) → click Stop
         6. Look at the Timings lane in the flame chart → find the "Time to Interactive" marker → READ the ms value
         7. Repeat steps 4-6 two more times (total 3 runs)
         8. Write the 3 ms values, one per line, to /tmp/exploder-tti-baseline-runs.txt:
              echo "$RUN1\\n$RUN2\\n$RUN3" > /tmp/exploder-tti-baseline-runs.txt
            (or just open a terminal and write the file directly with your numbers)

       === POSTPHASE CAPTURE (3 runs) ===
         9. In the same Incognito window, navigate to http://localhost:4174/patients/<patient_id>
            (note: /patients route is the same; only the port differs)
         10. Repeat steps 4-7 (3 runs against port 4174)
         11. Write 3 ms values to /tmp/exploder-tti-postphase-runs.txt

       Anti-patterns to AVOID:
         - DO NOT switch tabs between runs (let the cache state stay consistent within a checkout)
         - DO NOT close + reopen DevTools between runs (keep panel open for all 3 runs per checkout)
         - If RUN1 << RUN2 (e.g. 1850 ms vs 420 ms), Pitfall 3 cache effect — close the tab, open a fresh Incognito window, and retake all 3 runs. Document the retake in the json `notes` field.

       Reply "approved" once both /tmp files exist with 3 numeric lines each.

       === END HUMAN CAPTURE BLOCK ===
       ```
    5. Pause executor.

    On resume:
    6. Run the post-checkpoint <verify> below — exits non-zero if either file is missing or doesn't have exactly 3 numeric lines (defeats Pitfall 1).
    7. Tear down both preview servers:
       ```
       pkill -f "vite.*preview.*4173" 2>/dev/null
       pkill -f "vite.*preview.*4174" 2>/dev/null
       sleep 1
       lsof -i :4173 -t 2>/dev/null && echo "WARN: port 4173 still bound" || echo "OK: 4173 freed"
       lsof -i :4174 -t 2>/dev/null && echo "WARN: port 4174 still bound" || echo "OK: 4174 freed"
       ```
    8. Print the captured runs to the executor log so they are visible in the session for Task 3 to consume.

    No commits in this task — Task 3 produces the single JSON artifact + commit.
  </how-to-verify>
  <action>See <how-to-verify> above. Executor brings up the two preview servers, prints the resume-block, pauses for the human to operate Chrome DevTools Performance panel for 6 total recordings, then on resume tears down both preview servers and verifies the two `/tmp/*-runs.txt` files are well-formed.</action>
  <verify>
    <automated>test -s /tmp/exploder-tti-baseline-runs.txt && test -s /tmp/exploder-tti-postphase-runs.txt && [ $(grep -c '^[0-9]\+\(\.[0-9]\+\)\?$' /tmp/exploder-tti-baseline-runs.txt) -eq 3 ] && [ $(grep -c '^[0-9]\+\(\.[0-9]\+\)\?$' /tmp/exploder-tti-postphase-runs.txt) -eq 3 ] && ! lsof -i :4173 -t 2>/dev/null && ! lsof -i :4174 -t 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/exploder-tti-baseline-runs.txt` exists, has exactly 3 lines, each line is a numeric value: `[ $(grep -c '^[0-9]\+\(\.[0-9]\+\)\?$' /tmp/exploder-tti-baseline-runs.txt) -eq 3 ]`
    - `/tmp/exploder-tti-postphase-runs.txt` exists, has exactly 3 lines, each line is a numeric value
    - Both preview servers stopped (ports 4173 and 4174 not bound at end of task): `! lsof -i :4173 -t && ! lsof -i :4174 -t`
    - Per-run values are reasonable for `/patients/:id` TTI (between 100 ms and 60000 ms — sanity bounds; outside this range suggests a measurement error): `awk 'NR>0 && ($1 < 100 || $1 > 60000) {exit 1}' /tmp/exploder-tti-baseline-runs.txt && awk 'NR>0 && ($1 < 100 || $1 > 60000) {exit 1}' /tmp/exploder-tti-postphase-runs.txt`
  </acceptance_criteria>
  <done>
    - Both `/tmp/*-runs.txt` files exist with 3 numeric lines each (one per run)
    - Both preview servers stopped (no listeners on 4173 or 4174 at end of task)
    - User has reported (in resume-signal text) any anomalies (Pitfall 3 cache effect, retakes, browser glitches) for inclusion in `tti-snapshot.json` notes field
  </done>
  <resume-signal>Type "approved" once /tmp/exploder-tti-baseline-runs.txt and /tmp/exploder-tti-postphase-runs.txt each contain exactly 3 numeric lines (one TTI value per line). If any retake was needed for cache effect or other reason, briefly note it in the resume message so it can be added to `tti-snapshot.json` notes.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Compute median + delta + dual-gate verdict and write tti-snapshot.json + cleanup worktrees</name>
  <files>
    .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json
  </files>
  <read_first>
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-CONTEXT.md (D-07 §3 schema; D-08 dual gate)
    - .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-RESEARCH.md §tti-snapshot.json Schema (lines 396-433)
    - /tmp/exploder-tti-baseline-runs.txt (Task 2 output)
    - /tmp/exploder-tti-postphase-runs.txt (Task 2 output)
    - /tmp/tti-deterministic-patient.txt (Task 1 output)
  </read_first>
  <action>
Step 1: Compute medians and write the JSON.

Use a single `node -e ...` invocation that reads both `/tmp/*-runs.txt`, sorts, takes the middle element of 3 (= sorted index 1, which is `Math.median` for n=3), reads the patient ID, computes delta_ms + delta_pct, applies CONTEXT D-08 dual-gate, and writes the JSON to the canonical path:

```
node -e "
const fs = require('fs');
const path = require('path');

const baselineRuns = fs.readFileSync('/tmp/exploder-tti-baseline-runs.txt', 'utf8')
  .split('\n').map(l => l.trim()).filter(l => l).map(Number);
const postRuns = fs.readFileSync('/tmp/exploder-tti-postphase-runs.txt', 'utf8')
  .split('\n').map(l => l.trim()).filter(l => l).map(Number);
if (baselineRuns.length !== 3) throw new Error('baseline must have 3 runs');
if (postRuns.length !== 3) throw new Error('postphase must have 3 runs');

const median3 = (xs) => [...xs].sort((a,b) => a-b)[1];
const baseline_ms = median3(baselineRuns);
const post_phase_ms = median3(postRuns);
const delta_ms = post_phase_ms - baseline_ms;
const delta_pct = Math.round((delta_ms / baseline_ms) * 10000) / 100;  // 2 dp

const gate_pass = delta_ms <= 100 && delta_pct <= 10;

const patientLine = fs.readFileSync('/tmp/tti-deterministic-patient.txt', 'utf8').trim();
const [patient_id, patient_family_name] = patientLine.split('|');

const snapshot = {
  schema_version: 1,
  captured_on: new Date().toISOString().slice(0, 10),
  captured_by: process.env.USER || 'unknown',
  method: 'Chrome DevTools Performance panel — TTI metric in Timings lane (Machado 2009 simulation NOT applied during TTI; capture is normal-vision)',
  throttling: { cpu: '1x (off — no throttling)', network: 'no throttling' },
  patient_id: patient_id,
  patient_family_name: patient_family_name,
  route: '/patients/' + patient_id,
  commits: { baseline: '048e99c', post_phase: 'a7e4544' },
  preview_ports: { baseline: 4173, post_phase: 4174 },
  runs_per_checkout: 3,
  baseline_runs_ms: baselineRuns,
  baseline_ms: baseline_ms,
  post_phase_runs_ms: postRuns,
  post_phase_ms: post_phase_ms,
  delta_ms: delta_ms,
  delta_pct: delta_pct,
  verdict: gate_pass ? 'PASS' : 'FAIL',
  gate: { delta_ms_threshold: 100, delta_pct_threshold: 10, gate_pass: gate_pass },
  notes: 'Baseline at Phase 33 tail (048e99c); post_phase at Phase 34 HEAD (a7e4544). Twin-worktree pattern per RESEARCH §Pattern 1 — explicitly NOT current main HEAD because Phase 36 lazy-load mitigates Phase 34 bundle growth (RESEARCH §Pitfall 6). Phase 33 D-11 keepMounted={false} on extension Tabs.Panel + Plan 34-05 root keepMounted=false combine to keep extension panels lazy on /patients/:id mount.'
};

const outPath = '.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json';
fs.writeFileSync(outPath, JSON.stringify(snapshot, null, 2) + '\n');
console.log('Wrote', outPath, '— verdict:', snapshot.verdict, '(delta_ms=' + delta_ms + ', delta_pct=' + delta_pct + '%)');
"
```

Step 2: Validate the JSON shape (this re-runs the same predicate as the post-checkpoint verify so we know it's well-formed before committing):
```
node -e "
const j = require('./.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json');
for (const k of ['baseline_ms','post_phase_ms','delta_ms','delta_pct','patient_id','throttling','commits','runs_per_checkout','verdict']) {
  if (!(k in j)) { console.error('Missing key:', k); process.exit(1); }
}
const ok = j.delta_ms <= 100 && j.delta_pct <= 10;
if (j.verdict !== (ok ? 'PASS' : 'FAIL')) {
  console.error('Verdict inconsistent with thresholds — verdict=' + j.verdict + ', expected=' + (ok ? 'PASS' : 'FAIL'));
  process.exit(2);
}
console.log('OK — schema valid, verdict consistent with dual-gate thresholds');
"
```

Step 3: Cleanup worktrees (CONTEXT D-06 mandates cleanup).
```
cd /Users/kohlbach/Claude/Exploder
git worktree remove /tmp/exploder-tti-baseline
git worktree remove /tmp/exploder-tti-postphase
rm -f /tmp/tti-deterministic-patient.txt /tmp/exploder-tti-baseline-runs.txt /tmp/exploder-tti-postphase-runs.txt
```
Verify cleanup:
```
git worktree list   # should NOT show the two /tmp/exploder-tti-* paths
```

Step 4: Commit.
```
git add .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json
git commit -m "perf(37-02): capture TTI snapshot — 048e99c → a7e4544 (delta_ms=$(node -e 'console.log(require(\"./.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json\").delta_ms)'))

Captured via Chrome DevTools Performance panel under twin git worktrees:
- Baseline (Phase 33 tail, commit 048e99c) measured on port 4173
- Post-phase (Phase 34 HEAD, commit a7e4544) measured on port 4174
3 runs per checkout, median taken; no throttling per CONTEXT D-04.
Dual-gate verdict computed per CONTEXT D-08."
```
  </action>
  <verify>
    <automated>test -s .planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json && node -e "const j = require('./.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/tti-snapshot.json'); for (const k of ['baseline_ms','post_phase_ms','delta_ms','delta_pct','patient_id','throttling','commits','runs_per_checkout','verdict']) if (!(k in j)) { console.error('Missing:',k); process.exit(1); } const ok = j.delta_ms <= 100 && j.delta_pct <= 10; if (j.verdict !== (ok ? 'PASS' : 'FAIL')) { console.error('Verdict inconsistent'); process.exit(2); } if (j.commits.baseline !== '048e99c') { console.error('baseline commit must be 048e99c'); process.exit(3); } if (j.commits.post_phase !== 'a7e4544') { console.error('post_phase commit must be a7e4544'); process.exit(4); } if (j.runs_per_checkout !== 3) { console.error('runs_per_checkout must be 3'); process.exit(5); } if (!Array.isArray(j.baseline_runs_ms) || j.baseline_runs_ms.length !== 3) { console.error('baseline_runs_ms must be array of 3'); process.exit(6); } if (!Array.isArray(j.post_phase_runs_ms) || j.post_phase_runs_ms.length !== 3) { console.error('post_phase_runs_ms must be array of 3'); process.exit(7); }" && ! git worktree list | grep -q "/tmp/exploder-tti-"</automated>
  </verify>
  <acceptance_criteria>
    - tti-snapshot.json exists, non-empty, parses as JSON: `node -e 'JSON.parse(require("fs").readFileSync(...))'`
    - All required CONTEXT D-07 fields present: baseline_ms, post_phase_ms, delta_ms, delta_pct, patient_id, throttling, commits, runs_per_checkout, verdict
    - Commits frozen to the canonical refs: `commits.baseline === "048e99c"` AND `commits.post_phase === "a7e4544"` (RESEARCH §Pitfall 6 lock)
    - Dual-gate consistent: `(delta_ms ≤ 100 && delta_pct ≤ 10) === (verdict === "PASS")`
    - 3 runs per checkout pinned in arrays: `baseline_runs_ms.length === 3 && post_phase_runs_ms.length === 3`
    - Median is computed correctly: `baseline_ms === sortedBaselineRuns[1] && post_phase_ms === sortedPostRuns[1]` (verifiable by re-deriving from arrays)
    - Worktrees cleaned up: `git worktree list | grep -c "/tmp/exploder-tti-"` returns 0
    - Temp files cleaned up: `! test -e /tmp/tti-deterministic-patient.txt && ! test -e /tmp/exploder-tti-baseline-runs.txt && ! test -e /tmp/exploder-tti-postphase-runs.txt`
    - Commit landed: `git log -1 --pretty=%s | grep -q "perf(37-02): capture TTI snapshot"`
  </acceptance_criteria>
  <done>
    - tti-snapshot.json written with full CONTEXT D-07 schema + computed dual-gate verdict
    - Both worktrees removed (`git worktree list` shows pre-phase count)
    - All `/tmp/exploder-tti-*` and `/tmp/tti-*` temp files removed
    - Commit landed with informative message including the captured delta_ms
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new runtime trust boundary introduced. Performance probing runs against `localhost:4173` and `localhost:4174` (two vite preview instances against twin git worktrees). The Blaze server at `localhost:8080` provides Synthea synthetic patient data. No production servers, no real PHI in TTI measurements.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-17 (inherited) | Tampering | tti-snapshot.json could be hand-edited to fake a PASS verdict | accept | The post-checkpoint verify re-derives `verdict === (delta_ms ≤ 100 && delta_pct ≤ 10)`. A hand-edit that changes only `verdict` without changing the underlying numbers fails the verify. A hand-edit that also changes `baseline_ms`/`post_phase_ms` is detectable by inspecting `baseline_runs_ms`/`post_phase_runs_ms` (the median of 3 must equal the recorded *_ms field). UAT reviewer can re-run if figures look suspicious. Phase 34 already accepted this disposition. |
| T-37-02 (new) | Information Disclosure | The /tmp worktrees contain the entire repo source (~600 MB each including node_modules + dist) — accidental long-term retention could expose source code beyond the GSD workflow boundary | mitigate | Task 3 explicitly runs `git worktree remove /tmp/exploder-tti-baseline` and `git worktree remove /tmp/exploder-tti-postphase` as MANDATORY cleanup steps. Post-checkpoint verify exits non-zero if `git worktree list` still shows either path. As a belt-and-suspenders, /tmp is auto-cleared by macOS on reboot. |
</threat_model>

<verification>
- tti-snapshot.json exists with all CONTEXT D-07 fields populated
- Commits frozen to 048e99c (baseline) and a7e4544 (post-phase) — NOT current main HEAD
- Median computed correctly from 3-run arrays
- Dual-gate verdict (D-08) consistent with thresholds
- Both /tmp worktrees removed at end of plan
- Both preview ports (4173, 4174) freed
- Commit landed under `perf(37-02): ...`
</verification>

<success_criteria>
Plan 37-02 is closeable when:
- tti-snapshot.json schema is valid + verdict is consistent with dual-gate thresholds
- Both worktrees + all temp files cleaned up
- Both preview ports freed
- `npm test` baseline preserved (no test changes in this plan; smoke-run optional)
</success_criteria>

<output>
After completion, create `.planning/phases/37-phase-34-uat-empirical-capture-deuteranopia-tti/37-02-SUMMARY.md` documenting:
- baseline_ms, post_phase_ms, delta_ms, delta_pct (one-line summary)
- The dual-gate verdict (PASS or FAIL)
- The deterministic-patient ID + family used
- Whether any retakes were necessary (Pitfall 3 cache effect, etc.)
- Confirmation of worktree cleanup (`git worktree list` count delta)
- If verdict is FAIL: a brief culprit-attribution paragraph referencing chunks in `.planning/phases/36-phase-34-profile-lazy-load-bundle-size-waiver-follow-up/36-visualizer-after.html` (CONTEXT D-08 names this as the post-Phase-36 chunk treemap)
</output>
