---
phase: 34
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/color-design-audit.md
autonomous: true
requirements:
  - MII-EXT-09
  - MII-EXT-10
  - MII-EXT-11
must_haves:
  truths:
    - "color-design-audit.md exists with 14 module rows and 7 palette contrast-audit tables"
    - "Every extension module has a confirmed package version, canonical URL, PRIMARY fhirResourceType, secondary_types column (explicit 'none' or a JSON-array list — NEVER blank), patientSearchParamOverrides map (possibly empty), badgeColor family assignment, Tabler icon name, German + FHIR label"
    - "Every new palette family has shade-6 hex + contrast ratios ≥ 4.5:1 (text-on-fill) and ≥ 3:1 (UI chrome) — failing palettes tweaked and re-measured"
    - "Paper-only deuteranopia discriminability rationale committed in §4 (icon shape distinctiveness + color contrast theory); empirical screenshots deferred to Plan 34-06"
    - "Plan 34-02 can read seed hex strings from this doc and paste them into src/theme.ts with zero further research"
    - "Plan 34-04 can read per-module fhirResourceType ARRAY (primary + secondary_types) from §1 verbatim — no string-vs-array ambiguity"
  artifacts:
    - path: ".planning/research/color-design-audit.md"
      provides: "Per-module spec table + WCAG AA audit + paper-only deuteranopia discriminability rationale + icon picks with rationale"
      min_lines: 200
  key_links:
    - from: ".planning/research/color-design-audit.md §palette"
      to: "src/theme.ts (Plan 34-02 MantineColorsTuple seed hexes)"
      via: "planner copies 7 × 10 hex arrays verbatim into theme.ts"
      pattern: "const (oncology|imaging|genetics|pathology|bioanalysis|administration|patientReported): MantineColorsTuple"
    - from: ".planning/research/color-design-audit.md §per-module spec"
      to: "src/utils/mii-modules.ts (Plan 34-04 14 module entries)"
      via: "per-module row → one MII_MODULES entry with fhirResourceType (array or string per secondary_types column), badgeColor + icon + patientSearchParamOverrides copied verbatim"
      pattern: "key: '(bildgebung|biobank|dokument|intensivmedizin|kardiologie|molekulargenetik|mtb|onkologie|pathologie|pro|seltene|studie|symptom|mikrobiologie)'"
---

<objective>
Produce the per-module research + palette + icon + WCAG + paper-only deuteranopia audit at `.planning/research/color-design-audit.md` that unblocks Plans 34-02 through 34-05.

Purpose: Phase 34 ships 14 MII extension module entries with palette + icons + patientSearchParamOverrides. The data for these entries (canonical URLs, current package versions, per-type search params, Mantine palette hexes, Tabler icon names, WCAG contrast ratios, deuteranopia discriminability rationale) is NOT trivially derivable at plan-execution time — it must be researched against live MII IG pages + `packages.fhir.org` + Mantine Colors Generator BEFORE any code lands. This plan produces the audit doc that every downstream plan references.

**Scope note (Plan 34-01 vs Plan 34-06):** This plan performs a *paper* deuteranopia discriminability assessment (reasoning about icon shape distinctiveness + color contrast theory — no running app required). Empirical post-implementation deuteranopia screenshots against the fully landed 14 modules + palette are captured in Plan 34-06 (Wave 5 UAT). The circular dependency pointed out by the checker (this plan can't screenshot code that doesn't exist yet) is resolved by this split.

Output: `.planning/research/color-design-audit.md` (committed; ROADMAP-locked path per CONTEXT D-02). Zero code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-CONTEXT.md
@.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-RESEARCH.md
@.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-UI-SPEC.md
@.planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-01-INVESTIGATION.md
@src/utils/mii-modules.ts
@src/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create color-design-audit.md skeleton + per-module spec table (14 rows) + paper-only deuteranopia assessment</name>
  <files>.planning/research/color-design-audit.md</files>
  <read_first>
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-CONTEXT.md (D-01..D-09 — module ordering, palette family mapping, icon picks)
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-RESEARCH.md §Canonical Refs (14-package table with versions + canonical URLs from STACK.md 2026-04-23 probe)
    - .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-01-INVESTIGATION.md (Phase 33 live-Blaze patient probe pattern for `patient=` vs `subject=` verification)
    - src/utils/mii-modules.ts (current MiiModule interface + 7 base module shape — reference for 14 extension rows)
  </read_first>
  <action>
Create `.planning/research/color-design-audit.md` with this exact top-level structure (sections in order):

```markdown
# Phase 34 — Color, Icon, and Per-Module Spec Audit

**Authored:** 2026-04-24 (Plan 34-01)
**Status:** Required reading for Plans 34-02 (theme + schema) through 34-06 (UAT). Frozen for Phase 34; revisions require a new audit committed under a versioned filename.
**Scope:** 14 MII extension modules — per-module IG + search-param verification; 7 palette families with WCAG AA audit; 21 Tabler icon assignments; paper-only deuteranopia discriminability assessment (empirical screenshots = Plan 34-06).

## 1. Per-Module Spec Table (14 rows × 10 columns)

**Column contract:**
- `secondary_types` — MUST be either the literal string `none` OR a JSON-array literal like `["DiagnosticReport"]`. A blank/empty cell FAILS the plan. If the MII IG lists ≥ 2 profile families for a module, `secondary_types` MUST be an array (not `none`). Plan 34-04 Task 5 enforces this as a blocking verify.
- `patientSearchParamOverrides` — JSON-object literal; `{}` (empty object) is valid.

**Multi-profile MII modules — secondary_types MUST be a non-empty array** (known multi-profile IGs per MII spec): onkologie, mtb, bildgebung, pathologie, kardiologie, intensivmedizin. A value of `none` for any of these 6 is a PLAN-BLOCKING ERROR.

| # | Module key (D-01 alpha) | German label | Canonical IG URL | Package name | Version (verified YYYY-MM-DD) | Pre-GA? | Primary FHIR type | secondary_types | patientSearchParamOverrides |
|---|------------------------|--------------|------------------|---------------|------------------------------|---------|-------------------|-----------------|------------------------------|
| 1 | bildgebung | Bildgebung | https://www.medizininformatik-initiative.de/fhir/ext/modul-bildgebung | de.medizininformatikinitiative.kerndatensatz.bildgebung | 2026.0.0 | no | ImagingStudy | ["DiagnosticReport"] | {} |
| 2 | biobank | Biobank | ... | de.medizininformatikinitiative.kerndatensatz.biobank | 2026.0.1 | no | Specimen | none | { "Specimen": "subject" } |
| 3 | dokument | Dokument | ... | ... | ... | no | DocumentReference | none | {} |
| 4 | intensivmedizin | Intensivmedizin | ... | ... | ... | no | Observation | ["Encounter", "Procedure"] | {} |
| 5 | kardiologie | Kardiologie | ... | ... | 2026.0.0-alpha.2 | yes | Observation | ["Procedure", "Condition"] | {} |
| 6 | mikrobiologie | Mikrobiologie | ... | ... | ... | no | Observation | none | {} |
| 7 | molekulargenetik | Molekulargenetik | ... | ... | ... | no | Observation | none | {} |
| 8 | mtb | MTB | ... | ... | ... | no | Observation | ["Condition", "MedicationStatement"] | {} |
| 9 | onkologie | Onkologie | ... | ... | ... | no | Condition | ["Observation", "Procedure", "MedicationStatement"] | {} |
| 10 | pathologie | Pathologie | ... | ... | ... | no | Observation | ["DiagnosticReport", "Specimen"] | {} |
| 11 | pro | PRO | ... | ... | ... | no | Observation | none | {} |
| 12 | seltene | Seltene Erkrankungen | ... | ... | ... | no | Condition | none | {} |
| 13 | studie | Studie | ... | ... | ... | no | ResearchStudy | none | { "ResearchStudy": "enrollment" } |
| 14 | symptom | Symptom | ... | ... | 2024.0.0-ballot | yes | Observation | none | {} |

(14 rows total — alphabetical-by-German-label order per D-01: bildgebung, biobank, dokument, intensivmedizin, kardiologie, mikrobiologie, molekulargenetik, mtb, onkologie, pathologie, pro, seltene, studie, symptom)

## 2. Palette Assignment + Icon Picks (21 total)

### 2a. Base 7 (LOCKED — D-08)

| Module key | badgeColor | Icon (Tabler) |
|------------|-----------|----------------|
| person | blue | IconUser |
| fall | indigo | IconBedFlat |
| diagnose | teal | IconStethoscope |
| prozedur | violet | IconMedicalCross |
| consent | pink | IconFileSignature |
| laborbefund | cyan | IconFlask |
| medikation | orange | IconPill |

### 2b. Extension 14 (D-05 clinical-semantic grouping)

| Module key | badgeColor (palette family) | Icon (Tabler) | Icon rationale (1 sentence) |
|------------|-----------------------------|----------------|------------------------------|
| onkologie | oncology | IconRadioactive | Cancer / radiation therapy association |
| mtb | oncology | IconUsersGroup | Tumor board = multidisciplinary meeting |
| bildgebung | imaging | IconPhoto | Imaging = photo capture |
| studie | imaging | IconClipboardData | Study = data collection clipboard |
| molekulargenetik | genetics | IconDna | Genetics = DNA strand |
| seltene | genetics | IconPuzzle | Rare disease = "puzzle" diagnostic challenge |
| pathologie | pathology | IconMicroscope | Histology = microscope |
| mikrobiologie | pathology | IconBacteria | Microbe = bacteria |
| biobank | bioanalysis | IconTestPipe | Biobank = specimen test tubes |
| intensivmedizin | bioanalysis | IconBedFilled | ICU = filled bed |
| kardiologie | administration | IconHeartbeat | Cardio = heartbeat waveform |
| dokument | administration | IconFileDescription | Document = annotated file |
| symptom | patient-reported | IconMoodSmile | Symptom = self-reported mood marker |
| pro | patient-reported | IconQuestionnaire | PRO = patient questionnaire |

## 3. WCAG AA Contrast Audit (§wcag)

### 3a. Seed hexes (from Mantine Colors Generator)

**Verification requirement:** Every cell in columns "Shade 6 (rendered)" and "Shade 1 (hover wash)" MUST be a concrete 7-char hex like `#c92a2a`. The strings `{measured hex}`, `{shade...}`, and `...` (ellipsis) are forbidden template placeholders — verify output rejects them.

| Palette family | Seed hex (input to generator) | Shade 6 (rendered) | Shade 1 (hover wash) |
|----------------|-------------------------------|---------------------|------------------------|
| oncology | #e03131 | (measured) | (measured) |
| imaging | #0c8599 | (measured) | (measured) |
| genetics | #8e4ec6 | (measured) | (measured) |
| pathology | #6741d9 | (measured) | (measured) |
| bioanalysis | #099268 | (measured) | (measured) |
| administration | #364fc7 | (measured) | (measured) |
| patient-reported | #c2255c | (measured) | (measured) |

### 3b. Contrast ratios

| Palette family | White text on shade-6 fill (≥ 4.5:1 required) | Shade-6 fill on #ffffff card (≥ 3:1 required) | Shade-6 fill on #e7ecff active-pill bg (≥ 3:1 required) | PASS/FAIL |
|----------------|----------------------------------------------|-----------------------------------------------|---------------------------------------------------------|-----------|
| oncology | (measured) | (measured) | (measured) | PASS/FAIL |
| imaging | (measured) | (measured) | (measured) | PASS/FAIL |
| genetics | (measured) | (measured) | (measured) | PASS/FAIL |
| pathology | (measured) | (measured) | (measured) | PASS/FAIL |
| bioanalysis | (measured) | (measured) | (measured) | PASS/FAIL |
| administration | (measured) | (measured) | (measured) | PASS/FAIL |
| patient-reported | (measured) | (measured) | (measured) | PASS/FAIL |

### 3c. Failing palette tweaks (if any)

(Document any palette whose seed was darkened/lightened to pass. Format: "bioanalysis: initial seed #099268 → shade 6 contrast 4.2:1 (FAIL on white-on-fill); re-seeded to #087050 → shade 6 contrast 4.8:1 (PASS).")

## 4. Deuteranopia Discriminability — Paper Assessment (§deuteranopia-paper)

**Why paper-only in this plan:** No code lives yet (14 modules, palette, icons not implemented until Plans 34-02/04). Empirical browser screenshots require the feature to be live. Plan 34-06 captures the post-implementation deuteranopia screenshots against the fully landed UI for sign-off. This section is a *theoretical* discriminability analysis that:
  - Drives any pre-emptive icon swaps BEFORE implementation (cheaper than post-hoc rework)
  - Gives Plan 34-06 a concrete hypothesis to verify (pass/fail predictions per pair)

### 4a. Method

Reason about each adjacent-pair discriminability using three orthogonal channels:
  1. **Color channel under deuteranopia simulation**: Protanopia/deuteranopia roughly collapses the red-green axis; colors shift along a blue-yellow axis. Palettes that differ mostly in red-vs-green (e.g., oncology red vs bioanalysis teal) may appear closer under deuteranopia. Use the Brettel 1997 or Machado 2009 simulation matrices as mental models (no code required — this is a reasoning exercise grounded in color theory).
  2. **Icon shape distinctiveness**: Rank each icon's silhouette on an abstract shape-family axis — e.g., IconRadioactive (radial trefoil) vs IconUsersGroup (clustered circles) are shape-distinct; IconBedFilled vs IconBedFlat would NOT be (both beds).
  3. **Combined channel**: A pair passes paper-discriminability if EITHER the color shift under deuteranopia remains visible OR the icons are shape-distinct enough to disambiguate on their own.

### 4b. Within-family adjacent-pair assessment

| Palette family | Module pair | Icon A | Icon B | Color-under-deuteranopia | Icon shape distinct? | Predicted paper-pass? |
|----------------|-------------|---------|---------|---------------------------|----------------------|-------------------------|
| oncology | onkologie + mtb | IconRadioactive | IconUsersGroup | same palette → no color help | YES (trefoil vs clustered circles) | ✅ |
| imaging | bildgebung + studie | IconPhoto | IconClipboardData | same palette | YES (frame+lens vs clipboard) | ✅ |
| genetics | molekulargenetik + seltene | IconDna | IconPuzzle | same palette | YES (helix vs interlocking tiles) | ✅ |
| pathology | pathologie + mikrobiologie | IconMicroscope | IconBacteria | same palette | YES (tall apparatus vs blob cluster) | ✅ |
| bioanalysis | biobank + intensivmedizin | IconTestPipe | IconBedFilled | same palette | YES (vertical cylinder vs rectangle) | ✅ |
| administration | kardiologie + dokument | IconHeartbeat | IconFileDescription | same palette | YES (waveform vs rectangle-with-lines) | ✅ |
| patient-reported | symptom + pro | IconMoodSmile | IconQuestionnaire | same palette | YES (circle-face vs list-grid) | ✅ |

### 4c. Cross-family adjacent-tab assessment (21-tab row)

Under deuteranopia, palettes that differ primarily along the red-green axis may collapse. Pre-assess risky adjacent pairings in the final tab order (base 7 then extension 14 alphabetical):

| Left module | Right module | Palette pair | Color-collapse risk under deuteranopia | Icon shape disambig? | Predicted pass |
|-------------|--------------|--------------|----------------------------------------|----------------------|------------------|
| medikation (orange) | bildgebung (imaging-cyan) | orange vs cyan | LOW (orange-cyan stays distinct on blue-yellow axis) | pill vs photo = distinct | ✅ |
| biobank (bioanalysis-teal) | dokument (administration-indigo) | teal vs indigo | MEDIUM (both shift bluish) | test-pipe vs file-lines = distinct | ✅ via icon |
| intensivmedizin (bioanalysis) | kardiologie (administration) | teal vs indigo | MEDIUM | bed vs heartbeat = distinct | ✅ via icon |
| mikrobiologie (pathology-violet) | molekulargenetik (genetics-grape) | violet vs grape | HIGH (both bluish, similar hue) | bacteria vs DNA = distinct | ✅ via icon (color fails) |
| mtb (oncology-red) | onkologie (oncology-red) | same family | N/A within-family — see 4b | distinct icons | ✅ |

### 4d. Preemptive icon swap recommendations (if any paper-pair is borderline)

If any §4b or §4c row is predicted FAIL, swap to the UI-SPEC fallback candidate BEFORE Plan 34-04 lands. Record any swap here as "icon X → fallback Y (rationale)". Plan 34-06's empirical capture is expected to confirm these preemptive choices.

### 4e. Empirical capture deferred to Plan 34-06

Plan 34-06 captures the three empirical deuteranopia screenshots:
  - `.planning/phases/34-.../deuteranopia-dashboard.png` — /dashboard with 21 MII tiles
  - `.planning/phases/34-.../deuteranopia-tab-row.png` — /patients/:id tab row with 7 base + 14 extension tabs
  - `.planning/phases/34-.../deuteranopia-timeline.png` — /patients/:id timeline with one entry per module

Pass criterion for 34-06: every paper prediction in §4b / §4c confirmed empirically. If any empirical failure contradicts the paper analysis, Plan 34-06 opens a revision to this doc + Plan 34-04 MII_MODULES icon entries.

## 5. Provenance
- IG URLs + package versions probed: `npm view <package> version` (run 2026-04-24)
- Mantine Colors Generator: https://mantine.dev/colors-generator/
- Contrast ratios measured via: `npx wcag-contrast <fg> <bg>` (one-off; NOT a runtime dep)
- Color-vision simulation models referenced: Brettel 1997 / Machado 2009 (paper-only reasoning — no code)
- Downstream plans: 34-02 (theme), 34-04 (module entries + fhirResourceType arrays — secondary_types column enforced), 34-05 (empty-state UX), 34-06 (UAT — empirical deuteranopia + TTI + bundle-size)
```

Fill in actual values for:

**Section 1 (per-module table):** Use the exact 14 canonical URLs from 34-RESEARCH.md §Canonical Refs (lines 1001-1014 of that file — Onkologie = https://www.medizininformatik-initiative.de/fhir/ext/modul-onko, etc.). Run `npm view <package-name> version` for each of the 14 packages to probe current (2026-04-24) versions — flag `PRE-GA` in the Pre-GA column when the version string matches `PRE_GA_PATTERN = /-(alpha|beta|rc|ballot|draft)/i`. Populate the `secondary_types` column for every row — use `none` when the module has a single primary IG profile family, or a JSON-array literal like `["DiagnosticReport", "Specimen"]` when the IG ships multiple profile families. The 6 known multi-profile modules listed in the column contract above MUST have a non-empty array. For each `(module, type)` pair, decide `patientSearchParamOverrides` based on R4 spec defaults (per 34-RESEARCH.md Pitfall P-02 — Specimen has no `patient` param, use `subject`; ResearchStudy has no per-patient param, use `enrollment`; DocumentReference uses `patient`). If you can probe a live Blaze server during audit, record empirical results; otherwise record R4-spec-derived defaults with a NOTE. Known exceptions from 34-RESEARCH.md Plan 34-04 §Per-type search param research: Biobank→Specimen `subject`, Studie→ResearchStudy `enrollment`, ImagingStudy both work (prefer `patient`), DocumentReference both work (prefer `patient`).

**Section 2b (extension icon picks):** Use the 14 recommendations from 34-UI-SPEC.md Icons table (lines 251-264 of that file) — these are the locked picks. Fallback candidates are noted in the UI-SPEC; use them only if a primary icon proves non-existent when verified against https://tabler.io/icons/icon/{name} (e.g., if `IconHeartbeat` doesn't exist use `IconHeart`).

**Section 3a + 3b (WCAG):** Use the K-02 PLACEHOLDER seed hexes from 34-RESEARCH.md (lines 1065-1071 of that file): oncology=#e03131, imaging=#0c8599, genetics=#8e4ec6, pathology=#6741d9, bioanalysis=#099268, administration=#364fc7, patient-reported=#c2255c. Paste each seed into https://mantine.dev/colors-generator/ and capture the 10-shade output. Record shade 6 (used at all three render sites per D-07) and shade 1 (hover wash). Measure 3 contrast ratios per palette (21 ratios total) using `npx wcag-contrast <fg> <bg>` — a 4.5:1 floor for text-on-fill and 3:1 floor for UI chrome per D-06. If any palette fails, darken the seed (`#e03131` → `#c92a2a` family) until shade-6 passes and document the tweak in section 3c.

**CRITICAL — NO TEMPLATE PLACEHOLDERS:** The verify block for this task greps for literal strings `{measured hex}`, `{shade`, and `...` (ellipsis continuation) — if any remain, the plan fails. Every measurement cell MUST contain a concrete hex or numeric value.

**Section 4 (deuteranopia paper assessment):** Fill in §4a method, §4b within-family 7-pair table, §4c cross-family risky-pair table, §4d any preemptive icon swaps, and §4e deferral note. NO screenshot capture in this plan — that's Plan 34-06.

Commit the audit doc. No screenshots attached.
  </action>
  <verify>
    <automated>test -f .planning/research/color-design-audit.md && grep -c '^## ' .planning/research/color-design-audit.md | grep -qE '^[4-9]$|^[0-9]{2,}$' && grep -qE '^\| [0-9]+ \| (bildgebung|biobank|dokument|intensivmedizin|kardiologie|mikrobiologie|molekulargenetik|mtb|onkologie|pathologie|pro|seltene|studie|symptom) \|' .planning/research/color-design-audit.md && [ $(grep -cE '^\| [0-9]+ \|' .planning/research/color-design-audit.md) -ge 14 ] && ! grep -q '{measured hex}' .planning/research/color-design-audit.md && ! grep -q '{shade' .planning/research/color-design-audit.md && ! grep -qE '^\| \.\.\. \|' .planning/research/color-design-audit.md && grep -q 'secondary_types' .planning/research/color-design-audit.md && grep -q 'paper-pass' .planning/research/color-design-audit.md</automated>
  </verify>
  <done>
    - File `.planning/research/color-design-audit.md` exists with ≥ 4 top-level `##` headings
    - Section 1 table has ≥ 14 rows, each with all 10 columns populated (pipe-delimited row count ≥ 14)
    - Every one of the 14 extension module keys (bildgebung, biobank, ... symptom) appears in a row at column 2
    - `secondary_types` column present in §1 header; every row populated with `none` or a JSON-array literal
    - 6 known multi-profile modules (onkologie, mtb, bildgebung, pathologie, kardiologie, intensivmedizin) all have NON-"none" `secondary_types`
    - Section 2a (base 7) + 2b (extension 14) icon tables populated
    - Section 3a seed-hex table has 7 palette rows with concrete shade-6 hex + shade-1 hex (NO `{measured hex}`, `{shade...`, or `...` continuation placeholders)
    - Section 3b contrast ratios has 7 palette rows with 3 concrete ratios each (21 numbers total); any FAIL has a 3c tweak documented
    - Section 4 populated with §4a method, §4b within-family 7-pair paper table, §4c cross-family risky-pair table, §4d any preemptive swaps, §4e deferral-to-34-06 note
    - NO screenshot file references as deliverables of THIS plan (they land in 34-06)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No network egress from the Phase 34 browser bundle. The audit doc is a local artifact only. `npm view` runs against npm's public registry (no PHI, no auth).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02 | Tampering | `npm view` output (package version probe) | accept | `npm view` hits public npm registry over HTTPS; no registry substitution likely at audit time; pinned versions in Plan 34-03 script re-verify. |
</threat_model>

<verification>
- `.planning/research/color-design-audit.md` is committed and contains 14 module rows with every column populated (including the new `secondary_types` column)
- Every palette family's WCAG contrast table PASSes (or tweaks documented in §3c) with concrete hexes/ratios — no `{measured hex}`, `{shade...`, or `...` placeholders remaining
- Every paper-pair in §4b + §4c has a predicted PASS or a preemptive swap in §4d
- Empirical deuteranopia capture deferred to Plan 34-06 (§4e documents the hand-off)
</verification>

<success_criteria>
Plan 34-02 can read shade-6 hex arrays from section 3 of this doc; plan 34-04 can read per-module `(fhirResourceType-primary, secondary_types, patientSearchParamOverrides, badgeColor, icon)` tuples from sections 1 + 2b with zero ambiguity about string-vs-array; Plan 34-06 has a concrete paper hypothesis to empirically verify via deuteranopia screenshots.
</success_criteria>

<output>
After completion, create `.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-01-SUMMARY.md` documenting: audit doc location, WCAG pass/fail counts, paper-deuteranopia predicted-pass count, any preemptive icon swaps recommended for Plan 34-04.
</output>
</output>
