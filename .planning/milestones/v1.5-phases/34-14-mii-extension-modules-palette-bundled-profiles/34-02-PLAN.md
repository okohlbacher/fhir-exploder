---
phase: 34
plan: 02
type: execute
wave: 2
depends_on:
  - "34-01"
files_modified:
  - src/theme.ts
  - src/utils/mii-modules.ts
  - src/__tests__/mii-modules.test.ts
  - src/__tests__/theme.test.ts
autonomous: true
requirements:
  - MII-EXT-10
  - MII-EXT-11
must_haves:
  truths:
    - "theme.colors contains 7 new MantineColorsTuple keys: oncology, imaging, genetics, pathology, bioanalysis, administration, patient-reported"
    - "Each new palette is exactly 10 shades (MantineColorsTuple contract)"
    - "Base 7 Mantine colors (blue, indigo, teal, violet, pink, cyan, orange) are UNCHANGED — no renames, no overrides in theme.colors"
    - "MiiModule.icon?: string is declared in the interface (optional — Phase 34-04 populates the values for all 21 modules)"
    - "Zero visible UI change yet (no consumer reads theme.colors['oncology'] or module.icon in this plan)"
    - "npm test shows 836+ passing / 0 failing; npx tsc -b --noEmit exits 0"
  artifacts:
    - path: "src/theme.ts"
      provides: "createTheme() with 7 new MantineColorsTuple entries + base 7 preserved"
      contains: "oncology"
    - path: "src/utils/mii-modules.ts"
      provides: "MiiModule interface with optional icon?: string field"
      contains: "icon?:"
    - path: "src/__tests__/theme.test.ts"
      provides: "Palette snapshot test locking the 7 new keys + 10-shade contract"
      exports: []
  key_links:
    - from: ".planning/research/color-design-audit.md §3a"
      to: "src/theme.ts palette constants"
      via: "seed hex strings pasted verbatim as 10-element MantineColorsTuple arrays"
      pattern: "const (oncology|imaging|genetics|pathology|bioanalysis|administration|patientReported)\\s*:\\s*MantineColorsTuple"
    - from: "src/utils/mii-modules.ts MiiModule.icon?: string"
      to: "src/__tests__/mii-modules.test.ts type-accepts-icon test"
      via: "type-level assertion that the MiiModule interface accepts an icon field"
      pattern: "icon\\??:\\s*string"
---

<objective>
Land the theme palette + MiiModule.icon schema field in two minimal commits that don't yet alter any visible UI. Plan 34-02 is the schema-landing plan; Plan 34-04 populates the values; the split preserves D-26 "no broken intermediate states".

Purpose: By landing the palette keys and schema field in isolation (no data-drop yet), (1) the palette snapshot test locks the 7 new keys as a contract before 34-04 fans them out across 21 modules; (2) the `icon` field is optional so Phase 33's 7 base modules without `icon` remain valid; (3) Plan 34-04 can append 14 extension modules and add icons to the base 7 as a pure data-append commit without simultaneously widening types (= easier review, smaller diff).

Output: `src/theme.ts` with 7 new `MantineColorsTuple`s, `src/utils/mii-modules.ts` with `icon?: string` added to the `MiiModule` interface, `src/__tests__/theme.test.ts` (new) with palette snapshot test, `src/__tests__/mii-modules.test.ts` extended with icon-field type acceptance test.

This plan runs in parallel with Plan 34-03 (fetch script + prepare hook + registry). No file-overlap between the two.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/color-design-audit.md
@.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-CONTEXT.md
@.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-RESEARCH.md
@src/theme.ts
@src/utils/mii-modules.ts
@src/__tests__/mii-modules.test.ts

<interfaces>
<!-- Current src/theme.ts shape (Phase 30 redesign): -->
```typescript
import { createTheme } from '@mantine/core';

export const theme = createTheme({
  fontFamily: '"IBM Plex Sans", ui-sans-serif, system-ui, sans-serif',
  fontFamilyMonospace: '"IBM Plex Mono", ui-monospace, monospace',
  primaryColor: 'indigo',
  primaryShade: 6,
  defaultRadius: 'md',
  radius: { sm: '4px', md: '6px', lg: '10px' },
  colors: {
    gray: [/* 10 warm-neutral shades — DO NOT CHANGE */],
  },
  components: {
    Card: { defaultProps: { withBorder: true, radius: 'lg', padding: 'lg' } },
    Table: { defaultProps: { verticalSpacing: 'xs', horizontalSpacing: 'md' } },
    Button: { defaultProps: { size: 'sm', radius: 'md' } },
    Tabs: { defaultProps: { variant: 'pills' } },
  },
});
```

<!-- Mantine 8 MantineColorsTuple contract (from @mantine/core type decl): -->
```typescript
// MantineColorsTuple = readonly [string, string, string, string, string, string, string, string, string, string];
// Exactly 10 elements. Mantine accepts arbitrary string keys in theme.colors.
```

<!-- Current src/utils/mii-modules.ts MiiModule interface (Phase 33): -->
```typescript
export interface MiiModule {
  key: string;
  germanLabel: string;
  fhirResourceType: string | string[];
  category: 'base' | 'extension';
  badgeColor: string;
  patientSearchParam: string;
  patientSearchParamOverrides?: Record<string, string>;
  extraQuery?: string;
  extraQueryByType?: Record<string, string>;
  // Phase 34-02 adds: icon?: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add palette snapshot test (Wave 0 — test-first)</name>
  <files>src/__tests__/theme.test.ts</files>
  <read_first>
    - src/theme.ts (current shape — lines 16-45)
    - src/__tests__/mii-modules.test.ts (existing test-file style — import pattern, describe/it idiom, expect.arrayContaining usage)
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-RESEARCH.md §K-09 (test-for-palette strategy: keys + shade count, NOT hex values)
  </read_first>
  <behavior>
    - Test 1: `theme.colors` contains the 8 expected keys: `gray` (preserved), `oncology`, `imaging`, `genetics`, `pathology`, `bioanalysis`, `administration`, `patient-reported`
    - Test 2: Each of the 7 new palette keys resolves to a tuple of exactly 10 shades
    - Test 3: The shade at index 6 is a valid 7-character hex string (`#` + 6 hex digits) — primaryShade=6 invariant check
    - Test 4: Base 7 Mantine colors (blue, indigo, teal, violet, pink, cyan, orange) are NOT overridden in theme.colors (Mantine falls back to built-in defaults for these)
  </behavior>
  <action>
Create `src/__tests__/theme.test.ts` with this exact structure:

```typescript
import { describe, it, expect } from 'vitest';
import { theme } from '../theme';

describe('theme palette (Phase 34 MII-EXT-10)', () => {
  const NEW_PALETTE_KEYS = [
    'oncology',
    'imaging',
    'genetics',
    'pathology',
    'bioanalysis',
    'administration',
    'patient-reported',
  ] as const;

  const BASE_MANTINE_COLORS = [
    'blue', 'indigo', 'teal', 'violet', 'pink', 'cyan', 'orange',
  ] as const;

  it('theme.colors contains the 7 new MII extension palette keys', () => {
    const keys = Object.keys(theme.colors ?? {});
    expect(keys).toEqual(
      expect.arrayContaining(['gray', ...NEW_PALETTE_KEYS]),
    );
  });

  it.each(NEW_PALETTE_KEYS)('palette "%s" has exactly 10 shades (MantineColorsTuple contract)', (key) => {
    const tuple = theme.colors?.[key];
    expect(tuple).toBeDefined();
    expect(Array.isArray(tuple)).toBe(true);
    expect(tuple).toHaveLength(10);
  });

  it.each(NEW_PALETTE_KEYS)('palette "%s" shade-6 is a valid 7-char hex string', (key) => {
    const tuple = theme.colors?.[key];
    expect(tuple).toBeDefined();
    const shade6 = tuple![6];
    expect(shade6).toMatch(/^#[0-9a-fA-F]{6}$/);
  });

  it.each(BASE_MANTINE_COLORS)('base Mantine color "%s" is NOT overridden in theme.colors (UNCHANGED invariant)', (color) => {
    // Per CONTEXT D-04: base 7 colors stay as Mantine defaults — not renamed,
    // not overridden. `theme.colors.blue` should be undefined (Mantine fills
    // from its built-in palette at runtime).
    expect(theme.colors?.[color]).toBeUndefined();
  });
});
```

Run `npx vitest run src/__tests__/theme.test.ts` — it MUST fail (RED) because theme.ts does not yet have the 7 new keys. Commit the failing test with message: `test(34-02): add palette snapshot test for 7 new MantineColorsTuple keys`.
  </action>
  <verify>
    <automated>test -f src/__tests__/theme.test.ts && grep -q "oncology" src/__tests__/theme.test.ts && grep -q "patient-reported" src/__tests__/theme.test.ts && grep -q "MantineColorsTuple contract" src/__tests__/theme.test.ts</automated>
  </verify>
  <done>
    - File `src/__tests__/theme.test.ts` exists with 4 `it`/`it.each` blocks
    - Test file references all 7 palette keys: oncology, imaging, genetics, pathology, bioanalysis, administration, patient-reported
    - Test file references all 7 base colors: blue, indigo, teal, violet, pink, cyan, orange
    - `npx vitest run src/__tests__/theme.test.ts` FAILS with descriptive errors (keys undefined) — RED state confirmed
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 7 MantineColorsTuple constants to theme.ts (GREEN)</name>
  <files>src/theme.ts</files>
  <read_first>
    - src/theme.ts (current shape — preserve ALL existing fields verbatim)
    - .planning/research/color-design-audit.md §3a (seed hex table — 7 rows with measured shade arrays)
    - .planning/research/color-design-audit.md §3c (any palette tweaks — use tweaked hexes if documented)
    - src/__tests__/theme.test.ts (the Task 1 test that must flip GREEN)
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-CONTEXT.md (D-04 — ROADMAP-locked palette names, DO NOT rename)
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-RESEARCH.md §K-01 (key-naming convention — camelCase consts + kebab-case theme.colors key for patient-reported)
  </read_first>
  <behavior>
    - theme.colors gains 7 new keys (each mapped to a 10-shade tuple)
    - gray palette preserved verbatim (no whitespace drift)
    - All other createTheme fields (fontFamily, primaryColor, primaryShade, defaultRadius, radius, components) preserved verbatim
    - npx vitest run src/__tests__/theme.test.ts flips GREEN (all 4 tests pass)
  </behavior>
  <action>
Edit `src/theme.ts` and apply ONLY the changes below. Do NOT touch any other field, whitespace, or comment.

Step 1: Change the import to pull in `MantineColorsTuple`:

```typescript
import { createTheme, type MantineColorsTuple } from '@mantine/core';
```

Step 2: Between the import block and `export const theme = createTheme(...)`, insert 7 const declarations. Copy the 10-shade arrays VERBATIM from `.planning/research/color-design-audit.md` §3a (the measured shade-by-shade columns from Mantine Colors Generator). Use the tweaked hexes from §3c if any palette failed contrast. Add a JSDoc comment block citing the audit doc:

```typescript
// MII extension module palette (Phase 34, MII-EXT-10, CONTEXT D-04).
// Names locked by ROADMAP — DO NOT rename (oncology, imaging, genetics,
// pathology, bioanalysis, administration, patient-reported).
// Seed hexes chosen + contrast-audited in
// .planning/research/color-design-audit.md §palette. Shade 6 is the primary
// render shade (primaryShade: 6); shade 1 is reserved for hover washes.
// Base 7 Mantine color tokens (blue, indigo, teal, violet, pink, cyan, orange)
// are UNCHANGED — they resolve to Mantine's stock palette at runtime.
const oncology: MantineColorsTuple = [
  '#{shade0}', '#{shade1}', '#{shade2}', '#{shade3}', '#{shade4}',
  '#{shade5}', '#{shade6}', '#{shade7}', '#{shade8}', '#{shade9}',
];
const imaging: MantineColorsTuple = [/* 10 shades from audit */];
const genetics: MantineColorsTuple = [/* 10 shades from audit */];
const pathology: MantineColorsTuple = [/* 10 shades from audit */];
const bioanalysis: MantineColorsTuple = [/* 10 shades from audit */];
const administration: MantineColorsTuple = [/* 10 shades from audit */];
const patientReported: MantineColorsTuple = [/* 10 shades from audit */];
```

Step 3: Inside `createTheme({ colors: { gray: [...] } })`, ADD the 7 new keys AFTER the existing `gray` tuple. DO NOT modify `gray`. The kebab-case key `'patient-reported'` uses the camelCase const `patientReported` (per RESEARCH K-01):

```typescript
colors: {
  gray: [
    '#fafaf8',
    '#f5f4f1',
    '#ecebe7',
    '#dedcd6',
    '#c5c3bc',
    '#9a9791',
    '#6e6b66',
    '#504d48',
    '#35332f',
    '#1c1b18',
  ],
  oncology,
  imaging,
  genetics,
  pathology,
  bioanalysis,
  administration,
  'patient-reported': patientReported,
},
```

Grep verification to run after edit:
- `grep -c "MantineColorsTuple" src/theme.ts` returns ≥ 8 (1 import + 7 const declarations)
- `grep -c "'patient-reported': patientReported" src/theme.ts` returns 1 (exactly)
- `grep -cE "^\s*(blue|indigo|teal|violet|pink|cyan|orange):" src/theme.ts` returns 0 (base 7 untouched)

Run `npx vitest run src/__tests__/theme.test.ts` — MUST pass (GREEN). Commit with message: `feat(34-02): add 7 MII extension MantineColorsTuple palettes per CONTEXT D-04`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/theme.test.ts && grep -q "const oncology: MantineColorsTuple" src/theme.ts && grep -q "const patientReported: MantineColorsTuple" src/theme.ts && grep -q "'patient-reported': patientReported" src/theme.ts && [ $(grep -cE "const (oncology|imaging|genetics|pathology|bioanalysis|administration|patientReported):\s*MantineColorsTuple" src/theme.ts) -eq 7 ] && npx tsc -b --noEmit</automated>
  </verify>
  <done>
    - `src/theme.ts` declares all 7 MantineColorsTuple consts (grep shows all 7 regex matches)
    - Each const has exactly 10 hex strings (MantineColorsTuple type-enforced by TS)
    - `'patient-reported': patientReported` appears literally in theme.colors block
    - Base 7 Mantine color tokens (blue/indigo/teal/violet/pink/cyan/orange) are NOT declared as keys in theme.colors
    - `npx vitest run src/__tests__/theme.test.ts` shows 4/4 passing (GREEN)
    - `npx tsc -b --noEmit` exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add optional icon?: string field to MiiModule interface + type-acceptance test</name>
  <files>src/utils/mii-modules.ts, src/__tests__/mii-modules.test.ts</files>
  <read_first>
    - src/utils/mii-modules.ts (current MiiModule interface lines 15-72 — preserve all existing fields verbatim, add only the `icon` field)
    - src/__tests__/mii-modules.test.ts (existing `MiiModule type shape accepts array fhirResourceType + extension category` test at lines 127-142 — model the new test after this idiom)
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-RESEARCH.md §K-03 (icon-as-string rationale: JSON-serializable, no cyclic imports, tree-shaking preserved)
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-CONTEXT.md (D-07 — 3 render sites + Drawer; string storage)
  </read_first>
  <behavior>
    - MiiModule interface gains `icon?: string` after the `extraQueryByType` field
    - JSDoc on `icon` explains: optional in Phase 34-02 so base 7 existing rows remain valid; Plan 34-04 fills values for all 21; storing as string not component reference per K-03
    - New mii-modules.test.ts case: `MiiModule type shape accepts optional icon field` — constructs a module object with `icon: 'IconRadioactive'` and asserts it compiles + roundtrips
    - Existing tests continue to pass unchanged (icon is optional, so no base-7 test breaks)
  </behavior>
  <action>
Step 1: Edit `src/utils/mii-modules.ts`. After the existing `extraQueryByType?: Record<string, string>;` field (line 71 of the current file, inside the `MiiModule` interface closing at line 72), add this BEFORE the `}` that closes the interface:

```typescript
  /**
   * Tabler icon component name (e.g. 'IconUser', 'IconRadioactive').
   *
   * Rendered at three sites per CONTEXT D-07:
   *   - ClinicalTimeline dot marker (14px)
   *   - MiiModuleTabs tab subtitle leading position (14px)
   *   - DashboardPage MII tile swatch (32px), Drawer header (20px)
   *
   * Stored as a string — not a React component — to keep `MII_MODULES`
   * JSON-serializable (useful for test fixtures and debugging) and to
   * avoid cyclic import concerns. Consumers use a local `ICON_MAP` object
   * keyed by this string to resolve to the Tabler component; missing key
   * or missing map entry renders no icon (defensive). See Plan 34-04 for
   * the 21-entry ICON_MAP definition.
   *
   * OPTIONAL in Phase 34-02 (schema-landing plan). Plan 34-04 populates
   * `icon` on all 21 modules (base 7 + extension 14) in a single data
   * commit; this split preserves D-26 no-broken-intermediate-states.
   */
  icon?: string;
```

Do NOT modify any other field. The trailing `}` of the interface stays intact.

Step 2: Edit `src/__tests__/mii-modules.test.ts`. Immediately after the existing `it('MiiModule type shape accepts array fhirResourceType + extension category', ...)` block (ends around line 142), ADD this new test inside the same `describe('MII_MODULES configuration', ...)` block (so it sits as a sibling of the existing type-shape tests, BEFORE the closing `});` of that describe around line 143):

```typescript
  it('MiiModule type shape accepts optional icon field (Phase 34-02 schema landing)', () => {
    const moduleWithIcon: MiiModule = {
      key: 'x-icon',
      germanLabel: 'X-Icon',
      fhirResourceType: 'Resource',
      category: 'extension',
      badgeColor: 'oncology',
      patientSearchParam: 'patient',
      icon: 'IconRadioactive',
    };
    expect(moduleWithIcon.icon).toBe('IconRadioactive');

    // Icon is optional — module without icon still compiles
    const moduleWithoutIcon: MiiModule = {
      key: 'x-no-icon',
      germanLabel: 'X-No-Icon',
      fhirResourceType: 'Resource',
      category: 'base',
      badgeColor: 'gray',
      patientSearchParam: 'patient',
    };
    expect(moduleWithoutIcon.icon).toBeUndefined();
  });
```

Commit with message: `feat(34-02): add optional icon?: string field to MiiModule interface per CONTEXT D-07`.
  </action>
  <verify>
    <automated>grep -q "icon?: string" src/utils/mii-modules.ts && grep -q "MiiModule type shape accepts optional icon field" src/__tests__/mii-modules.test.ts && npx vitest run src/__tests__/mii-modules.test.ts && npx tsc -b --noEmit</automated>
  </verify>
  <done>
    - `src/utils/mii-modules.ts` contains literal `icon?: string` on its own line
    - JSDoc comment on `icon` references CONTEXT D-07 and "ICON_MAP"
    - `src/__tests__/mii-modules.test.ts` contains new `it(...'accepts optional icon field'...)` test
    - `npx vitest run src/__tests__/mii-modules.test.ts` shows all tests passing (the new one + all existing)
    - `npx tsc -b --noEmit` exits 0 (type widen is additive; existing Phase 33 base-7 entries compile unchanged because icon is optional)
  </done>
</task>

<task type="auto">
  <name>Task 4: Full-suite regression gate</name>
  <files>src/theme.ts, src/utils/mii-modules.ts, src/__tests__/theme.test.ts, src/__tests__/mii-modules.test.ts</files>
  <read_first>
    - src/theme.ts (post-edit)
    - src/utils/mii-modules.ts (post-edit)
    - .planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-CONTEXT.md (D-26 — each plan ends green)
  </read_first>
  <action>
Run the full test suite + TypeScript type-check. Both must exit 0. If either fails, fix the underlying issue (do NOT suppress with `.skip` or `@ts-ignore`) and re-run.

Commands to run in order:

1. `npm test` — expect 836+ passing / 0 failing (Phase 33 baseline + the 4 new theme tests + 1 new icon-field test = 841+ passing)
2. `npx tsc -b --noEmit` — expect exit 0
3. `grep -cE "const (oncology|imaging|genetics|pathology|bioanalysis|administration|patientReported):\s*MantineColorsTuple" src/theme.ts` — expect exactly 7
4. `grep -c "icon?: string" src/utils/mii-modules.ts` — expect exactly 1

No code changes in this task — it is a gate task that verifies Tasks 1-3 land cleanly. If any check fails, diagnose the specific file + line and fix before proceeding to commit.

Commit any gate-fix changes (if needed) with message: `fix(34-02): resolve regression from palette + icon schema landing`.
  </action>
  <verify>
    <automated>npm test && npx tsc -b --noEmit && [ $(grep -cE "const (oncology|imaging|genetics|pathology|bioanalysis|administration|patientReported):\s*MantineColorsTuple" src/theme.ts) -eq 7 ] && [ $(grep -c "icon?: string" src/utils/mii-modules.ts) -eq 1 ]</automated>
  </verify>
  <done>
    - `npm test` exits 0 with ≥ 836 passing, 0 failing
    - `npx tsc -b --noEmit` exits 0
    - 7 `MantineColorsTuple` const declarations present in theme.ts
    - 1 `icon?: string` field present in mii-modules.ts
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary introduced. `theme.colors` is consumed only by Mantine component rendering at paint time; shade hex values are static assets, no user input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Tampering | theme.colors palette values | accept | Hexes are compile-time constants in source; no runtime mutation path; build pipeline is trusted. |
| T-34-04 | Information Disclosure | MiiModule.icon | accept | Icon field stores a string identifier; no PHI; serializable in test fixtures. |
</threat_model>

<verification>
- All 4 palette snapshot tests pass (palette keys + shade count + shade-6 hex validity + base-color-not-overridden)
- All existing Phase 33 mii-modules tests still pass (icon optional → no baseline shift)
- TypeScript build (`npx tsc -b --noEmit`) exits 0
- Git diff shows ONLY: theme.ts (palette constants + colors block extension), mii-modules.ts (+icon field), theme.test.ts (new file), mii-modules.test.ts (+1 test)
</verification>

<success_criteria>
- Plan 34-03 can land in parallel (no file overlap)
- Plan 34-04 can reference `theme.colors['oncology']` (Mantine resolves at render time) and assign `icon: 'IconRadioactive'` to MII_MODULES entries without any further type widening
- Zero visible UI regression on `/dashboard` or `/patients/:id` — the palette + schema are declared but not yet consumed
</success_criteria>

<output>
After completion, create `.planning/phases/34-14-mii-extension-modules-palette-bundled-profiles/34-02-SUMMARY.md` documenting: 7 palette hex values committed, icon field declaration, test-count delta (from baseline to post-plan), any WCAG palette tweaks that were needed during Task 2.
</output>
