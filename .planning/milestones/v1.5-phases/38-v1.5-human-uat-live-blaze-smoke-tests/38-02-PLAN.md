---
phase: 38-v1.5-human-uat-live-blaze-smoke-tests
plan: 02
type: execute
wave: 2
depends_on:
  - "38-01"
files_modified:
  - .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md
autonomous: false
requirements:
  - UAT-FU-01
  - UAT-FU-02
  - UAT-FU-05
must_haves:
  truths:
    - "All 6 tests in 35-HUMAN-UAT.md have result: pass OR result: fail (not result: pending) with non-empty evidence: |"
    - "35-HUMAN-UAT.md top-frontmatter status field flipped from `shelved` → `complete` on commit"
    - "Single browser session preserved from Plan 38-01: same Blaze, same Synthea, same dev server, same browser instance (D-09)"
    - "Counts table from 38-SESSION.md NOT re-captured (D-13 single-source-of-truth honored)"
  artifacts:
    - path: ".planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md"
      provides: "6 tests updated with result: pass | fail + evidence: | block + frontmatter status flipped to complete"
      contains: "result: pass"
  key_links:
    - from: "Plan 38-01 38-SESSION.md (Synthea fingerprint)"
      to: "Plan 38-02 walk context (verifier consults counts when grounding evidence)"
      via: "verifier reads 38-SESSION.md before walk; reuses fingerprint in evidence prose"
      pattern: "38-SESSION.md"
    - from: "35-HUMAN-UAT.md test 1..6 entries"
      to: "browser interactions on http://localhost:5173 in the SAME browser session"
      via: "verifier walks each test in numbered order, records result + evidence"
      pattern: "result: (pass|fail)"
    - from: "35-HUMAN-UAT.md frontmatter status: shelved"
      to: "35-HUMAN-UAT.md frontmatter status: complete"
      via: "in-place frontmatter flip on commit"
      pattern: "^status: complete"
---

<objective>
Walk Phase 35's 6 live-Blaze HUMAN-UAT tests in the SAME browser session inherited from Plan 38-01 (D-09 single-session invariant). Records pass/fail per test inline in `35-HUMAN-UAT.md` using the Phase 23 `result: pass | fail` + `evidence: |` YAML shape (D-04). Also flips the file's top-frontmatter `status: shelved` → `status: complete` on commit (D-08).

Purpose: Closes Phase 35 SC5 (live-Blaze UAT clause) by walking the 6 observational tests that programmatic verification cannot exercise: real Synthea Date/Status column population for 6 resource types, Mantine Tooltip hover portal rendering, Modal Transition behavior, bottom-Extensions section visual layout, per-type quality matrix card with REAL metric data populated by user-driven panel runs, and PHI gate behavior on chevron click. These tests are why `35-VERIFICATION.md` is `status: human_needed`.

Output: 6 updated test entries + flipped `status:` frontmatter in `35-HUMAN-UAT.md`. NO `src/` edits. NO re-capture of `38-SESSION.md` counts (D-13 — Plan 38-01 is the single source of truth). Counts may be referenced in evidence prose but are not re-queried.

This plan runs Wave 2 after Plan 38-01. Not autonomous — Task 0 (the walk) has a `checkpoint:human-verify` gate covering all 6 tests in one human-verifier hand-off (D-11). Browser session, dev server, and Blaze instance are inherited from Plan 38-01 — do not restart any of them or `38-SESSION.md`'s fingerprint becomes invalid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-CONTEXT.md
@.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md
@.planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md
@.planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VERIFICATION.md
@.planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md

<interfaces>
<!-- Authoritative file shape the executor must preserve. -->

From `.planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md` (current shape):

```yaml
---
status: shelved                  # <-- FLIP TO `complete` ON COMMIT (D-08)
phase: 35-phase-30-uat-follow-ups-per-type-quality-matrix
source: [35-VERIFICATION.md]
shelved-to: Phase 999.4
shelved-at: 2026-04-25T09:00:00Z
acceptance-basis: <unchanged historical narrative>
---

### N. <test name>
expected: <unchanged>
result: [pending]                # <-- replace with `pass` or `fail`
                                  # <-- ADD an `evidence: |` block immediately below
```

After the walk, also UPDATE the `## Summary` block at the bottom: `pending: 6 (all shelved...)` becomes `pending: 0`, `passed: <N>`, `issues: <M>`. Append a one-line note: `walked: 2026-04-28 by phase-38-02` (or today's date) so the in-place narrative still tells the right story.

**DO NOT remove** the historical `## Acceptance Basis` narrative — that is the Phase 35 close record and downstream readers need it. Just flip the frontmatter `status` field and update per-test results.

Phase 23 evidence pattern reference (`23-HUMAN-UAT.md`):

```yaml
result: pass
evidence: |
  Re-run 2026-04-22 against live Blaze (localhost:8080/fhir) ...
  <multi-line prose narrative; counts, observed values, sentence(s) per D-02 (free-text)>
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Walk Phase 35's 6 HUMAN-UAT tests in numbered order; record result + evidence; flip status frontmatter</name>
  <files>
    .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md
  </files>
  <read_first>
    - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md (Synthea fingerprint from Plan 38-01 — DO NOT re-capture; reference for evidence grounding only per D-13)
    - .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md (the 6 tests + their `expected:` clauses + `## Acceptance Basis` historical narrative — preserve all of it; ONLY edit per-test result + evidence + frontmatter status)
    - .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VERIFICATION.md (broader context for what each test verifies; e.g., SC4 per-type matrix details, P-04/P-05/P-08 invariants)
    - .planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md (Phase 23 `result: pass` + `evidence: |` shape — this is the YAML shape to mirror)
    - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-CONTEXT.md (D-01 severity-tier protocol, D-04 evidence shape, D-05 net-tab judgment, D-08 status flip mandate, D-09 single-session invariant, D-10 numbered order, D-11 single checkpoint)
  </read_first>
  <what-built>
    Phase 35 shipped: `getResourceDateByType` + `getResourceStatusByType` extractors in `SearchResultsPage.tsx` (UAT-FU-01); identifier-system Tooltip + DeepJsonModal + bottom Extensions section in `HumanReadableView.tsx` + `ResourcePropertyTable.tsx` (UAT-FU-02); `ClinicalRawView.tsx` deleted, Developer→JSON rename in `ResourceDetailPage.tsx` (UAT-FU-03); `QualityByTypeMatrix.tsx` with 8-column sortable table consuming Phase 32's per-metric byType slots (UAT-FU-05). All 6 must-haves are programmatically VERIFIED in `35-VERIFICATION.md`. The 6 remaining items in `35-HUMAN-UAT.md` were `status: shelved` to backlog 999.4 with assumed-approved acceptance — Phase 38 is the formal walk that flips them to recorded outcomes.

    Plan 38-01 already passed pre-flight against the same Blaze the verifier is using now, captured the Synthea fingerprint in `38-SESSION.md`, and walked Phase 33's 6 tests. The browser session is still open at http://localhost:5173/ — do not close it, do not restart Blaze, do not re-import Synthea (D-09).
  </what-built>
  <how-to-verify>
    Continue in the SAME browser session inherited from Plan 38-01. If for some reason the session was lost (browser closed, dev server stopped, Blaze restarted), STOP and re-run Plan 38-01 — counts in `38-SESSION.md` would no longer be valid (D-09 invariant).

    Walk the 6 tests in **numbered order 1 → 2 → 3 → 4 → 5 → 6** (D-10). For each test, record `result: pass` or `result: fail` plus a multi-line `evidence: |` block describing what you observed. Free-text per D-02 + D-04; length scales with the test.

    **Test 1 — Date/Status column population on real Synthea data.** Visit each of these in turn:
    - `/explorer/Patient` — Date column should show `birthDate`; Status column should show `'active'` / `'inactive'` (boolean→label per RESEARCH P-01).
    - `/explorer/Condition` — Date should show `onsetDateTime`; Status should walk `clinicalStatus.coding[0].code` or `clinicalStatus.text` (CodeableConcept walk per RESEARCH P-02).
    - `/explorer/Observation` — Date should show `effectiveDateTime`; Status should show `status` enum.
    - `/explorer/MedicationStatement` — Date FLAT `effectiveDateTime` (NOT effectivePeriod per RESEARCH note); Status enum.
    - `/explorer/Encounter` — Date should show `period.start.slice(0, 10)` (per RESEARCH P-03); Status enum.
    - `/explorer/Procedure` — Date `performedDateTime`; Status enum.
    Empty cells must render as empty strings, **NEVER** literal `undefined` or `null`. Reference `38-SESSION.md` counts to confirm there are records for each type. Evidence: per-type spot check (e.g., "Patient: 5/5 rows show birthDate + active/inactive correctly; Condition: 12/15 rows show onsetDateTime, 3 empty cells render as empty strings; ..."). Note: zero-count types (e.g., if Consent count is 0 in `38-SESSION.md`) cannot exercise that branch — note this as a data-coverage observation per D-01.

    **Test 2 — Identifier-system Tooltip hover behavior.** Open any Patient detail page (`/explorer/Patient/<id>` for an id from a row in `/explorer/Patient`). Find the identifier rows. Hover the value cell of an identifier row. Expected: a Mantine **Tooltip** portal appears showing the system URL; the cursor changes to `help`; the system URL does NOT appear in the main row text (only in the Tooltip). Evidence: which identifier you hovered + the system URL you saw + a sentence on cursor change + confirmation the URL is hidden from the main row.

    **Test 3 — Modal transition + close behavior.** On the same Patient detail page, find a deeply-nested address-extension or any bottom-Extensions row. Click `[View]`. Expected: a Mantine **Modal** opens showing the JSON in a Code block. Verify ALL THREE close paths: (a) X button (must have `aria-label="Close"`), (b) backdrop click, (c) `Escape` key. Evidence: which row you opened + a sentence per close-path confirming it closed the Modal.

    **Test 4 — Bottom Extensions section visual layout.** On the same Patient detail page (or any resource with multiple `Resource.extension[]` entries), scroll to the bottom of the HumanReadableView. Expected: a section titled `Extensions` renders with one row per **unique URL** (deduplicated — no duplicate rows for same `url`). Each row shows: URL fragment + value summary + `[View]` button. The section MUST be hidden when the resource has no resource-level extensions. Evidence: count of unique URL rows + whether dedup worked correctly + a sentence on whether the section is hidden on a resource without extensions (test on a Condition or Observation if Patient has no extensions).

    **Test 5 — Per-type quality matrix card with real metric data.** Navigate to `/quality?tab=counts`. Below the existing counts table, a card titled "Quality by resource type" should render. Initial state: cells show em-dash (`—`), NEVER `0%`. Now run each per-metric panel **once** on a few resource types so producer hooks populate the byType maps:
    - Run **Completeness** panel on Patient + Condition.
    - Run **Coverage** panel on Patient.
    - Run **Validation** panel on Observation (this populates `validationIssuesByType` per Q-01 — see `35-VERIFICATION.md` SC4 evidence).
    - Run **References** panel on Encounter.
    Return to `/quality?tab=counts`. Expected: corresponding cells populate with %s; sparse cells (combinations not run) still render `—`. Threshold-breach cells (e.g., Completeness < 80%) render in **red**. Sortable via column headers (`SortableTh`). Evidence: list which cells populated + which stayed em-dash + a sentence on threshold-breach color rendering + confirmation that 0% never appears as a literal display for unrun cells.

    **Test 6 — PHI gate behavior on chevron click.** From the matrix, identify a row whose validation cell is em-dash (so navigation triggers panel use). Open DevTools → Network tab → clear current entries. Click the row's chevron. Expected: navigation to `/quality?tab=<metric>&type=<resourceType>` (URL pre-selection — see `35-VERIFICATION.md` SC4 wiring). ValidationPanel and ReferencesPanel must pre-select the resource type from the URL. CRITICAL: the Network tab must show **zero outbound `fetch()`** before the user clicks the PHI acknowledgment button (Phase 7 PHI gate preserved per Pitfall P-08). User still has to click "Run" / "Acknowledge PHI" — no auto-fire. Evidence: which chevron you clicked + the resulting URL + a sentence on the Network tab observation (D-05 — trust verifier judgment, no mandatory phrasing) + confirmation that the panel pre-selected the type but did not auto-fetch.

    **For each test, write into `35-HUMAN-UAT.md` directly under the test heading.** Replace `result: [pending]` with `result: pass` or `result: fail`. Immediately below, add an `evidence: |` block (Phase 23 shape — see `23-HUMAN-UAT.md`).

    **Failure routing (D-01) — record severity in evidence; sub-phase fires from Plan 38-03 closure, NOT here:**
    - **Critical/major UX defect** (e.g., Tooltip never renders in Test 2; Modal won't close in Test 3; chevron auto-fires fetch in Test 6) → `result: fail` with explicit "blocker" / "critical" / "regression" language so Plan 38-03 closure can detect and route to `/gsd-insert-phase` for sub-phase 38.1.
    - **Minor/cosmetic** → `result: fail` + override rationale "Accepted as cosmetic; no fix in v1.5".
    - **Data-coverage gap** (e.g., Test 1 cannot exercise a type because `38-SESSION.md` shows 0 records for it) → `result: fail` + rationale citing the count from `38-SESSION.md` + "Accepted as data-coverage reality".

    **Update the file's frontmatter** in the same edit:
    1. **`status: shelved` → `status: complete`** (D-08 mandates this flip in Plan 38-02).
    2. Add `walked_at: <today's ISO-8601 UTC timestamp>` immediately after `shelved-at:` (preserves shelved-at as historical record per D-07 / Phase 23 precedent).
    3. Add `walked_by: phase-38-02` immediately after `walked_at:`.
    4. **DO NOT remove** the `shelved-to:`, `shelved-at:`, or `acceptance-basis:` fields — those are the historical close record. Append, don't replace.

    **Update the `## Summary` block** at the bottom: `pending: 6 (all shelved...)` becomes `pending: 0`, `passed: <N>`, `issues: <M>` reflects fails. **DO NOT remove** the `## Acceptance Basis` narrative — historical close record stays for audit trail.

    DO NOT change `35-VERIFICATION.md` here — Plan 38-03 owns that re-flip per D-06.

    When all 6 tests are recorded and frontmatter is flipped, type `approved` to resume the executor; the executor commits the file with message: `docs(38-02): record 6 Phase-35 HUMAN-UAT results + flip status to complete (pass: <N>, fail: <M>)`.

    DO NOT close the browser, restart Blaze, or re-import Synthea after `approved` — Plan 38-03 will run as autonomous closure but you may want to leave the session up briefly in case the closure agent flags an inconsistency that requires re-checking a test.
  </how-to-verify>
  <action>
See `<how-to-verify>` above. Plan 38-01 has finished; browser + Blaze + Synthea + dev server are still up; `38-SESSION.md` is committed and represents the unchanged Synthea bundle. The user runs the 6 browser tests in order, edits `35-HUMAN-UAT.md` per-test, flips the frontmatter `status:` field, types `approved`, then Claude commits the file.
  </action>
  <verify>
    <automated>test -f .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md && ! grep -qE "^result: \[pending\]$" .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md && [ $(grep -cE "^result: (pass|fail)$" .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md) -eq 6 ] && [ $(grep -cE "^evidence: \|$" .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md) -eq 6 ] && grep -qE "^status: complete$" .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md && ! grep -qE "^status: shelved$" .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md && grep -q "shelved-at:" .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - All 6 test entries in `35-HUMAN-UAT.md` have `result: pass` OR `result: fail` (NOT `result: [pending]`).
    - All 6 test entries have an `evidence: |` block immediately following the `result:` line, with non-empty multi-line content.
    - The numbered ordering 1 → 6 matches the source file (D-10).
    - Top-frontmatter `status:` field is now `complete` (was `shelved`); the flip is in the same commit as the per-test updates.
    - Top-frontmatter `walked_at:` field added with today's ISO-8601 UTC timestamp.
    - Top-frontmatter `walked_by: phase-38-02` field added.
    - Top-frontmatter `shelved-to:`, `shelved-at:`, `acceptance-basis:` fields preserved (historical record).
    - `## Summary` block updated: `pending: 0`, `passed: <N>`, `issues: <M>`.
    - `## Acceptance Basis` historical narrative preserved (audit trail).
    - `38-SESSION.md` (from Plan 38-01) NOT modified, NOT re-captured (D-13 single-source-of-truth).
    - File committed to git.
  </acceptance_criteria>
  <done>
    - `35-HUMAN-UAT.md` updated with 6 results + 6 evidence blocks + flipped frontmatter status.
    - File committed.
    - Single browser session still alive (Plan 38-03 may consult Plan 38-02's findings during closure).
  </done>
  <resume-signal>Type "approved" once all 6 tests in 35-HUMAN-UAT.md show result: pass | fail with non-empty evidence: | blocks AND the frontmatter status field has been flipped from `shelved` to `complete`. If any critical/major UX defect was observed, also call it out in your reply (so Plan 38-03 closure can route to sub-phase 38.1 via /gsd-insert-phase per D-01).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ localhost Blaze | Inherited from Plan 38-01; same Synthea bundle, same trust assumptions |
| verifier ↔ HUMAN-UAT.md | Verifier is the trust root for evidence claims |
| Plan 38-01 ↔ Plan 38-02 session | Single browser session invariant (D-09) — breakage invalidates `38-SESSION.md` fingerprint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05 | Tampering | `35-HUMAN-UAT.md` evidence could be hand-edited to fake a pass | accept | Same as Plan 38-01 T-38-01 — solo-developer GSD trust model |
| T-38-06 | Repudiation | Frontmatter status flip could be made silently without verifier walking the tests | mitigate | `walked_at` + `walked_by` frontmatter fields + commit message reference + 6 evidence blocks make the flip auditable |
| T-38-07 | Tampering | Synthea bundle could change between Plans 38-01 and 38-02 (Blaze re-import) | mitigate | D-09 single-session invariant; if invariant breaks, verifier re-runs Plan 38-01 (counts table re-validates) |
</threat_model>

<verification>
- `35-HUMAN-UAT.md` has 6 entries each carrying `result: pass | fail` (zero `pending`) plus a non-empty `evidence: |` block.
- Frontmatter `status: complete` (flipped from `shelved`); `walked_at:` + `walked_by:` fields added; historical `shelved-to:` / `shelved-at:` / `acceptance-basis:` preserved.
- `## Summary` block shows `pending: 0`.
- `## Acceptance Basis` historical narrative preserved.
- `38-SESSION.md` UNCHANGED (D-13 — Plan 38-01 is the single source of truth).
- Single browser session is still alive for Plan 38-03 reference checks (D-09 invariant preserved).
</verification>

<success_criteria>
Plan 38-02 closes when:
- All 6 Phase-35 tests have a recorded outcome with evidence.
- `35-HUMAN-UAT.md` frontmatter `status` is `complete` (D-08 mandate satisfied).
- `38-SESSION.md` from Plan 38-01 is untouched (single-source-of-truth honored).
- Plan 38-03 has all the inputs it needs (both walks complete) to write the SUMMARY and re-flip both VERIFICATION files.
</success_criteria>

<output>
After completion, create `.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-02-SUMMARY.md` documenting: per-test pass/fail tally for Phase 35's 6 tests, the frontmatter status flip (shelved → complete), and any critical-severity flags raised for Plan 38-03 sub-phase routing. Reference `38-SESSION.md` for the Blaze + Synthea fingerprint rather than re-listing counts.
</output>
