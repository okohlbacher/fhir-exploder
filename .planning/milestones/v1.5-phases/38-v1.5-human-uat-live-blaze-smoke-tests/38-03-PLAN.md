---
phase: 38-v1.5-human-uat-live-blaze-smoke-tests
plan: 03
type: execute
wave: 3
depends_on:
  - "38-01"
  - "38-02"
files_modified:
  - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md
  - .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-VERIFICATION.md
  - .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VERIFICATION.md
  - .planning/STATE.md
autonomous: true
requirements:
  - MII-EXT-04
  - MII-EXT-06
  - MII-EXT-07
  - MII-EXT-08
  - UAT-FU-01
  - UAT-FU-02
  - UAT-FU-05
must_haves:
  truths:
    - "38-SUMMARY.md exists with Blaze URL header, the 11-row Synthea counts table inlined from 38-SESSION.md, and a 12-row per-test pass/fail digest (6 from Phase 33 + 6 from Phase 35)"
    - "33-VERIFICATION.md frontmatter `status: human_needed` flipped to `status: passed`; `re_verified:` ISO-8601 field added; `re_verified_by: phase-38` field added; `## Re-verification (Phase 38, <sha>)` section appended"
    - "35-VERIFICATION.md frontmatter `status: human_needed` flipped to `status: passed`; `re_verified:` ISO-8601 field added; `re_verified_by: phase-38` field added; `## Re-verification (Phase 38, <sha>)` section appended"
    - "Original verification narratives in both files preserved untouched (D-06 historical-record invariant)"
    - "STATE.md decisions log appended with one-line entry: `v1.5 HUMAN-UAT closure landed Phase 38`"
    - "If any HUMAN-UAT row carries critical-severity language, 38-SUMMARY.md surfaces a `## Sub-phase 38.1 trigger` section listing the failing test(s) + the literal `/gsd-insert-phase` invocation the user should run; if zero critical fails, the section reads `None — Phase 38 closes cleanly.`"
  artifacts:
    - path: ".planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md"
      provides: "Phase 38 closure record: Blaze URL + counts table + 12-test digest + sub-phase 38.1 trigger summary"
      min_lines: 60
      contains: "| Resource type | Count |"
    - path: ".planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-VERIFICATION.md"
      provides: "Phase 33 verification re-flipped to passed with appended Re-verification section"
      contains: "status: passed"
    - path: ".planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VERIFICATION.md"
      provides: "Phase 35 verification re-flipped to passed with appended Re-verification section"
      contains: "status: passed"
  key_links:
    - from: "38-SESSION.md (Plan 38-01)"
      to: "38-SUMMARY.md Blaze URL + counts table"
      via: "closure inlines fingerprint into summary"
      pattern: "fhir.serverUrl|_summary=count"
    - from: "33-HUMAN-UAT.md + 35-HUMAN-UAT.md test results"
      to: "38-SUMMARY.md 12-test digest + verification re-flip stanzas"
      via: "closure aggregates pass/fail counts and appends per-test outcome lines"
      pattern: "result: (pass|fail)"
    - from: "Critical-severity language in walk evidence"
      to: "38-SUMMARY.md `## Sub-phase 38.1 trigger` section"
      via: "closure scans evidence for blocker/critical/regression markers and prints the /gsd-insert-phase invocation"
      pattern: "(critical|blocker|regression)"
---

<objective>
Close Phase 38. Aggregate the 12 walk results from Plans 38-01 + 38-02 into `38-SUMMARY.md` (Blaze URL + counts table + 12-test digest + any sub-phase 38.1 trigger). Re-flip both `33-VERIFICATION.md` and `35-VERIFICATION.md` from `status: human_needed` to `status: passed` per D-06 procedure (top-frontmatter update plus appended `## Re-verification (Phase 38, <sha>)` section; original narrative untouched). Append a one-line closure entry to `STATE.md` decisions log.

Purpose: This plan is the formal closure of Phase 38 and, by extension, the live-Blaze UAT clauses (Phase 33 SC5 + Phase 35 SC5). After this commits, both source phases' verification reports are `passed` and the v1.5 HUMAN-UAT debt is retired. If any walk surfaced a critical UX defect, the SUMMARY documents the sub-phase 38.1 trigger (D-01 critical path) but does NOT itself spawn 38.1 — the user runs `/gsd-insert-phase` from the surfaced invocation.

Output: 1 new SUMMARY file; 2 in-place verification re-flips; 1 STATE.md log entry. NO `src/` edits. Autonomous: this plan is mechanical aggregation + frontmatter flips + log append; no human-verify gate is required because Plans 38-01 + 38-02 already gathered the human evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-CONTEXT.md
@.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md
@.planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md
@.planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-VERIFICATION.md
@.planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md
@.planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VERIFICATION.md
@.planning/phases/23-v1.3-close-out/23-VERIFICATION.md

<interfaces>
<!-- Authoritative file shapes the executor must produce. -->

`38-SUMMARY.md` shape:

```markdown
---
phase: 38-v1.5-human-uat-live-blaze-smoke-tests
status: complete
closed_at: <ISO-8601 UTC>
closed_by: plan-38-03
walks:
  - plan: 38-01
    tests: 6
    passed: <n>
    failed: <m>
  - plan: 38-02
    tests: 6
    passed: <n>
    failed: <m>
totals:
  tests: 12
  passed: <N>
  failed: <M>
sub_phase_trigger: <none | 38.1>
---

# Phase 38: v1.5 HUMAN-UAT live-Blaze smoke tests — SUMMARY

## Session fingerprint

- **Blaze URL:** `<from 38-SESSION.md>`
- **Blaze software:** `<software.name + version, or "unavailable">`
- **Pre-flight captured:** `<ISO-8601 from 38-SESSION.md>`

## Synthea bundle counts (single capture, both walks)

| Resource type        | Count |
| -------------------- | ----- |
| Patient              | <n>   |
| Condition            | <n>   |
| Observation          | <n>   |
| Encounter            | <n>   |
| Procedure            | <n>   |
| MedicationStatement  | <n>   |
| AllergyIntolerance   | <n>   |
| Consent              | <n>   |
| Immunization         | <n>   |
| DiagnosticReport     | <n>   |
| ServiceRequest       | <n>   |

## 12-test digest

### Phase 33 walk (Plan 38-01)

| # | Test | Result | Notes |
|---|------|--------|-------|
| 1 | Synthea Laborbefund extraQuery (no leakage) | pass / fail | <one-line evidence summary> |
| 2 | Timeline color + German label (4 types) | pass / fail | ... |
| 3 | Dashboard MII heading "Server-wide totals" | pass / fail | ... |
| 4 | Dashboard tile click → Drawer (no fetch) | pass / fail | ... |
| 5 | MiiModuleTabs deep-link forward-compat | pass / fail | ... |
| 6 | Per-patient Laborbefund populated | pass / fail | ... |

### Phase 35 walk (Plan 38-02)

| # | Test | Result | Notes |
|---|------|--------|-------|
| 1 | Date/Status real-data population (6 types) | pass / fail | ... |
| 2 | Identifier-system Tooltip hover behavior | pass / fail | ... |
| 3 | Modal transition + 3 close paths | pass / fail | ... |
| 4 | Bottom Extensions section dedup | pass / fail | ... |
| 5 | Per-type quality matrix populated | pass / fail | ... |
| 6 | PHI gate no-fetch on chevron click | pass / fail | ... |

## Sub-phase 38.1 trigger

<EITHER>

None — Phase 38 closes cleanly.

<OR, if any HUMAN-UAT row carried critical/blocker/regression language:>

The following test(s) recorded critical-severity defects per D-01:
- **Phase XX, Test N — <test name>** — <one-line summary of what failed>

Run the following to spawn sub-phase 38.1:

```
/gsd-insert-phase 38.1 "fix <one-line summary>"
```

After 38.1 lands, append `→ fixed in Phase 38.1, commit <sha>` to the failing HUMAN-UAT row (the row keeps `result: fail` per D-03).

## Verification re-flips

- `33-VERIFICATION.md` — `status: human_needed` → `status: passed`. Appended `## Re-verification (Phase 38, <sha>)` section enumerates the 6 tests' final outcomes.
- `35-VERIFICATION.md` — `status: human_needed` → `status: passed`. Appended `## Re-verification (Phase 38, <sha>)` section enumerates the 6 tests' final outcomes.

## ROADMAP success criteria

- [x] All 12 tests in `33-HUMAN-UAT.md` + `35-HUMAN-UAT.md` updated with `result: pass | fail` + evidence.
- [x] `33-VERIFICATION.md` `status: human_needed` → `passed` with re-verification stanza.
- [x] `35-VERIFICATION.md` `status: human_needed` → `passed` with re-verification stanza.
- [x] Phase 38 SUMMARY records the Blaze URL + Synthea counts fingerprint for reproducibility.
```

`33-VERIFICATION.md` re-flip — top-frontmatter shape after edit:

```yaml
---
phase: 33-mii-schema-foundation-extension-modules-collapse-ui
verified: 2026-04-24T13:42:00Z          # UNCHANGED — original verification timestamp preserved
status: passed                           # was: human_needed
re_verified: <ISO-8601 UTC>              # NEW
re_verified_by: phase-38                 # NEW
score: 8/8 must-haves verified
overrides_applied: 0                     # bump if any test was overridden as accepted-fail (data-coverage / cosmetic)
human_verification:                      # UNCHANGED — historical record of WHY humans were needed
  - <existing list of 6 tests preserved verbatim>
---
```

`33-VERIFICATION.md` appended section (after the existing report body, at end of file):

```markdown

## Re-verification (Phase 38, <commit-sha-of-walk-plan-38-01>)

Phase 38 walked the 6 live-Blaze HUMAN-UAT items in a single browser session against
`<Blaze URL from 38-SESSION.md>` (Synthea fingerprint pinned in `38-SESSION.md`).

| # | Test | Outcome | Rationale |
|---|------|---------|-----------|
| 1 | Synthea Laborbefund extraQuery | pass / fail-overridden / fail-fixed-in-38.1 | <one line> |
| 2 | Timeline color + German label | ... | ... |
| 3 | Dashboard MII heading scope | ... | ... |
| 4 | Dashboard tile → Drawer (no fetch) | ... | ... |
| 5 | MiiModuleTabs deep-link forward-compat | ... | ... |
| 6 | Per-patient Laborbefund populated | ... | ... |

**Walk plan:** `.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-01-PLAN.md`
**Recorded results:** `.planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md`
**Closure summary:** `.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md`
```

`35-VERIFICATION.md` re-flip uses the same shape, swapping the test list to Phase 35's 6 tests and the walk plan reference to `38-02-PLAN.md` / `35-HUMAN-UAT.md`.

`STATE.md` append shape (one line under whatever decisions / activity log section the file currently uses — read the file to find the right anchor):

```markdown
- 2026-04-28: v1.5 HUMAN-UAT closure landed Phase 38 (12/12 tests walked; 33 + 35 verifications re-flipped to passed).
```

(Adjust date to today's ISO date; the line text is fixed by D-disc-from-CONTEXT recommendation.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Aggregate walk results into 38-SUMMARY.md</name>
  <files>
    .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md (Blaze URL, software version, ISO timestamp, 11-row counts table — inline these verbatim into 38-SUMMARY.md)
    - .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md (extract 6 `result:` values + a one-line summary per evidence block; scan evidence for `critical|blocker|regression` markers)
    - .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md (extract 6 `result:` values + a one-line summary per evidence block; scan evidence for `critical|blocker|regression` markers)
    - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-CONTEXT.md (D-01 severity-tier protocol; D-03 sub-phase numbering; D-06 re-verification format; failure-routing decisions)
    - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-01-PLAN.md (walk plan reference for the digest)
    - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-02-PLAN.md (walk plan reference for the digest)
  </read_first>
  <action>
1. **Read `38-SESSION.md`.** Extract: Blaze URL (from `## Blaze instance` section), `metadata.software` line, `Capture timestamp (UTC)`, all 11 rows of the counts table. Confirm `status: pre_flight_passed` in frontmatter — if `pre_flight_halted`, abort this plan with a clear error: "Plan 38-01 halted at pre-flight; closure not applicable. Re-run Plan 38-01 first."

2. **Parse `33-HUMAN-UAT.md`.** For each of the 6 tests (numbered 1..6 per D-10):
   - Extract the `result:` value (must be `pass` or `fail`; if `[pending]` survives, abort with: "Test 33-N is still pending; Plan 38-01 did not complete cleanly. Re-run Plan 38-01.").
   - Extract the test name from the `### N. <name>` heading.
   - Read the `evidence: |` block; produce a single-line summary (≤ 90 chars). Heuristic: take the first non-empty line of evidence and trim. If the evidence contains the words `critical`, `blocker`, or `regression` (case-insensitive), record this test as a critical-severity hit.

3. **Parse `35-HUMAN-UAT.md`.** Same procedure as step 2, for the 6 Phase-35 tests. Confirm frontmatter `status: complete` (Plan 38-02 D-08 mandate); if still `shelved`, abort with: "35-HUMAN-UAT.md status is still 'shelved'; Plan 38-02 did not complete cleanly. Re-run Plan 38-02."

4. **Compute totals.** `passed_33 = count of result: pass in Phase 33 walk`; `failed_33 = 6 - passed_33`. Same for Phase 35. `total_passed = passed_33 + passed_35`; `total_failed = 12 - total_passed`. Compute `overrides_applied` = number of `result: fail` rows whose evidence does NOT contain critical-severity language (i.e., minor/cosmetic + data-coverage overrides per D-01).

5. **Determine sub-phase 38.1 trigger.** If `critical-severity hit count > 0`, set `sub_phase_trigger: 38.1` in the SUMMARY frontmatter and emit the trigger section listing each critical hit + the literal `/gsd-insert-phase 38.1 "<one-line summary>"` invocation. Otherwise set `sub_phase_trigger: none` and emit the section with text `None — Phase 38 closes cleanly.`

6. **Write `.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md`** following the shape in `<interfaces>`. Specifically:
   - Frontmatter: `phase`, `status: complete`, `closed_at` (today's ISO-8601 UTC), `closed_by: plan-38-03`, `walks` array (38-01 + 38-02 with tests/passed/failed counts), `totals`, `sub_phase_trigger`.
   - `## Session fingerprint` section: Blaze URL, software, capture timestamp from step 1.
   - `## Synthea bundle counts` section: inline the 11-row counts table verbatim from `38-SESSION.md`.
   - `## 12-test digest` section: two sub-tables (one per walk) with columns `# | Test | Result | Notes`.
   - `## Sub-phase 38.1 trigger` section per step 5.
   - `## Verification re-flips` section: bullets describing the two re-flips Task 1 + Task 2 will perform.
   - `## ROADMAP success criteria` section: 4 checkboxes mapped to ROADMAP §Phase 38 success criteria, all `[x]`.

7. **Commit:** `docs(38-03): write Phase 38 SUMMARY (12/12 walked; pass: <N>, fail: <M>; sub-phase 38.1 trigger: <none | yes>)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md && grep -q "^status: complete$" .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md && grep -q "closed_by: plan-38-03" .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md && grep -q "## Synthea bundle counts" .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md && grep -q "| Patient " .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md && grep -q "| ServiceRequest " .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md && grep -q "## 12-test digest" .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md && grep -q "## Sub-phase 38.1 trigger" .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `38-SUMMARY.md` exists with frontmatter `phase: 38-v1.5-human-uat-live-blaze-smoke-tests`, `status: complete`, `closed_by: plan-38-03`, ISO-8601 `closed_at`, `walks` array with 38-01 + 38-02 entries, `totals.tests: 12`, `sub_phase_trigger: none` OR `sub_phase_trigger: 38.1`.
    - Body contains `## Session fingerprint` with Blaze URL line + software line + capture timestamp.
    - Body contains `## Synthea bundle counts` with the 11-row table including `Patient` + `Observation` + `ServiceRequest` rows.
    - Body contains `## 12-test digest` with two sub-headings (`### Phase 33 walk (Plan 38-01)` and `### Phase 35 walk (Plan 38-02)`), each followed by a 6-row table (`# | Test | Result | Notes`).
    - Body contains `## Sub-phase 38.1 trigger` section. If zero critical-severity rows: section text reads `None — Phase 38 closes cleanly.` If ≥ 1 critical row: section lists each + the literal `/gsd-insert-phase 38.1` invocation string.
    - Body contains `## Verification re-flips` and `## ROADMAP success criteria` sections.
    - Aborts cleanly (no SUMMARY written) if pre-conditions fail (38-SESSION.md halted, any test still `[pending]`, or 35-HUMAN-UAT.md still `shelved`).
    - File committed.
  </acceptance_criteria>
  <done>
    - `38-SUMMARY.md` written and committed.
  </done>
</task>

<task type="auto">
  <name>Task 1: Re-flip 33-VERIFICATION.md to status: passed and append Re-verification (Phase 38, sha) section</name>
  <files>
    .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-VERIFICATION.md (current frontmatter + entire body — preserve verbatim except for the frontmatter status flip + appended section)
    - .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md (per-test results to enumerate in the appended Re-verification section)
    - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md (Blaze URL for the appended section)
    - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-CONTEXT.md (D-06 procedure: 3 frontmatter changes + appended section format; original narrative untouched)
    - .planning/phases/23-v1.3-close-out/23-VERIFICATION.md (Phase 23 re-verification narrative shape — referenced by D-07 as the format precedent)
  </read_first>
  <action>
1. **Read the current `33-VERIFICATION.md`.** Capture the entire file contents into memory.

2. **Determine the walk-plan commit SHA.** Run `git log -1 --format=%h .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md` to get the short SHA of the most recent commit that touched the Phase 33 HUMAN-UAT file (this is Plan 38-01's walk commit). Persist as `WALK_SHA_33`. If the command returns nothing, abort with: "33-HUMAN-UAT.md has no git history — Plan 38-01 walk commit missing. Re-run Plan 38-01."

3. **Compute timestamp.** `RE_VERIFIED_AT=<today's ISO-8601 UTC, e.g. 2026-04-28T11:00:00Z>`.

4. **Edit frontmatter (in place):**
   - Change `status: human_needed` → `status: passed`.
   - Insert `re_verified: <RE_VERIFIED_AT>` immediately after the `status:` line.
   - Insert `re_verified_by: phase-38` immediately after the `re_verified:` line.
   - **DO NOT TOUCH** the existing `verified:`, `score:`, `overrides_applied:`, `human_verification:` fields — those are the historical record per D-06.

5. **Append the Re-verification section at the very end of the file** (after whatever existing terminal section the file already has). Use this exact shape:

```markdown

## Re-verification (Phase 38, <WALK_SHA_33>)

Phase 38 walked the 6 live-Blaze HUMAN-UAT items in a single browser session against
`<Blaze URL from 38-SESSION.md>` (Synthea fingerprint pinned in `38-SESSION.md`).

| # | Test | Outcome | Rationale |
|---|------|---------|-----------|
| 1 | Synthea Laborbefund extraQuery (no leakage) | <pass / fail-overridden / fail-fixed-in-38.1> | <one-line evidence summary or override basis> |
| 2 | Timeline color + German label (4 types) | ... | ... |
| 3 | Dashboard MII heading "Server-wide totals" | ... | ... |
| 4 | Dashboard tile click → Drawer (no fetch) | ... | ... |
| 5 | MiiModuleTabs deep-link forward-compat | ... | ... |
| 6 | Per-patient Laborbefund populated | ... | ... |

**Walk plan:** `.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-01-PLAN.md`
**Recorded results:** `.planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-HUMAN-UAT.md`
**Closure summary:** `.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md`
```

For the `Outcome` column:
- If the test recorded `result: pass` → `pass`.
- If `result: fail` AND evidence has critical-severity language → `fail-fixed-in-38.1` (assumes 38.1 will land; if 38.1 hasn't landed yet, use `fail-pending-38.1`).
- If `result: fail` AND evidence is override (cosmetic / data-coverage) → `fail-overridden`.

For the `Rationale` column: take the first non-empty line of the evidence block, trim to ≤ 90 chars.

6. **Commit:** `docs(33): re-verify Phase 33 — flip human_needed → passed (Phase 38 walk, <WALK_SHA_33>)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-VERIFICATION.md && grep -qE "^status: passed$" .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-VERIFICATION.md && ! grep -qE "^status: human_needed$" .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-VERIFICATION.md && grep -qE "^re_verified: " .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-VERIFICATION.md && grep -qE "^re_verified_by: phase-38$" .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-VERIFICATION.md && grep -q "^## Re-verification (Phase 38," .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-VERIFICATION.md && grep -q "^human_verification:" .planning/phases/33-mii-schema-foundation-extension-modules-collapse-ui/33-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `status: passed` (was `human_needed`).
    - Frontmatter `re_verified:` field present with an ISO-8601 timestamp.
    - Frontmatter `re_verified_by: phase-38` field present.
    - Frontmatter `verified:`, `score:`, `overrides_applied:`, `human_verification:` fields preserved verbatim (historical record per D-06).
    - File body has an appended `## Re-verification (Phase 38, <SHA>)` section with a 6-row table (one row per Phase-33 test).
    - Original report body (sections above the appended one) byte-identical except for the frontmatter changes.
    - `<SHA>` placeholder replaced with the actual short SHA of Plan 38-01's walk commit.
    - File committed.
  </acceptance_criteria>
  <done>
    - `33-VERIFICATION.md` re-flipped + appended.
    - File committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-flip 35-VERIFICATION.md to status: passed and append Re-verification (Phase 38, sha) section</name>
  <files>
    .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VERIFICATION.md (current frontmatter + entire body — preserve verbatim except for the frontmatter status flip + appended section)
    - .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md (per-test results to enumerate in the appended Re-verification section)
    - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SESSION.md (Blaze URL for the appended section)
    - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-CONTEXT.md (D-06 procedure)
  </read_first>
  <action>
1. **Read the current `35-VERIFICATION.md`.** Capture entire file contents.

2. **Determine the walk-plan commit SHA.** Run `git log -1 --format=%h .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md` to get the SHA of Plan 38-02's walk commit. Persist as `WALK_SHA_35`. If empty, abort with: "35-HUMAN-UAT.md has no git history — Plan 38-02 walk commit missing. Re-run Plan 38-02."

3. **Compute timestamp.** `RE_VERIFIED_AT=<today's ISO-8601 UTC>`.

4. **Edit frontmatter (in place):**
   - Change `status: human_needed` → `status: passed`.
   - Insert `re_verified: <RE_VERIFIED_AT>` after `status:`.
   - Insert `re_verified_by: phase-38` after `re_verified:`.
   - **DO NOT TOUCH** `verified:`, `score:`, `overrides_applied:`, `human_verification:` fields.

5. **Append the Re-verification section at the very end of the file:**

```markdown

## Re-verification (Phase 38, <WALK_SHA_35>)

Phase 38 walked the 6 live-Blaze HUMAN-UAT items in a single browser session against
`<Blaze URL from 38-SESSION.md>` (Synthea fingerprint pinned in `38-SESSION.md`).

| # | Test | Outcome | Rationale |
|---|------|---------|-----------|
| 1 | Date/Status real-data population (6 types) | <pass / fail-overridden / fail-fixed-in-38.1> | <one-line summary> |
| 2 | Identifier-system Tooltip hover behavior | ... | ... |
| 3 | Modal transition + 3 close paths | ... | ... |
| 4 | Bottom Extensions section dedup | ... | ... |
| 5 | Per-type quality matrix populated | ... | ... |
| 6 | PHI gate no-fetch on chevron click | ... | ... |

**Walk plan:** `.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-02-PLAN.md`
**Recorded results:** `.planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-HUMAN-UAT.md`
**Closure summary:** `.planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md`
```

Outcome / Rationale columns follow the same rules as Task 1 step 5.

6. **Commit:** `docs(35): re-verify Phase 35 — flip human_needed → passed (Phase 38 walk, <WALK_SHA_35>)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VERIFICATION.md && grep -qE "^status: passed$" .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VERIFICATION.md && ! grep -qE "^status: human_needed$" .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VERIFICATION.md && grep -qE "^re_verified: " .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VERIFICATION.md && grep -qE "^re_verified_by: phase-38$" .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VERIFICATION.md && grep -q "^## Re-verification (Phase 38," .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VERIFICATION.md && grep -q "^human_verification:" .planning/phases/35-phase-30-uat-follow-ups-per-type-quality-matrix/35-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `status: passed` (was `human_needed`).
    - Frontmatter `re_verified:` field present with an ISO-8601 timestamp.
    - Frontmatter `re_verified_by: phase-38` field present.
    - Frontmatter `verified:`, `score:`, `overrides_applied:`, `human_verification:` fields preserved verbatim.
    - File body has an appended `## Re-verification (Phase 38, <SHA>)` section with a 6-row table (one row per Phase-35 test).
    - Original report body (sections above appended one) byte-identical except for frontmatter changes.
    - `<SHA>` placeholder replaced with actual short SHA of Plan 38-02's walk commit.
    - File committed.
  </acceptance_criteria>
  <done>
    - `35-VERIFICATION.md` re-flipped + appended.
    - File committed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append closure entry to STATE.md decisions log</name>
  <files>
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/STATE.md (find the existing decisions log / activity log section; append at the bottom of that section without disturbing other content)
    - .planning/phases/38-v1.5-human-uat-live-blaze-smoke-tests/38-SUMMARY.md (to confirm closure values + sub-phase trigger status before appending)
  </read_first>
  <action>
1. **Read `.planning/STATE.md`.** Locate the decisions log / activity log section (heading typically `## Decisions` or `## Decision Log` or similar — read the file to confirm). If the file has no decisions section, locate the most recent activity-log section instead.

2. **Compose the closure entry.** Use today's ISO date and a one-line entry per CONTEXT.md "Claude's Discretion" recommendation:

   ```
   - <YYYY-MM-DD>: v1.5 HUMAN-UAT closure landed Phase 38 (12/12 tests walked; 33 + 35 verifications re-flipped to passed; sub-phase 38.1 trigger: <none | yes>).
   ```

   - `<YYYY-MM-DD>` = today's date.
   - `<none | yes>` mirrors `38-SUMMARY.md` frontmatter `sub_phase_trigger`. If `none`, write `none`. If `38.1`, write `yes — see 38-SUMMARY.md`.

3. **Append the entry** at the very end of the decisions / activity log section (immediately before the next `##` heading or end-of-file). Preserve all existing entries verbatim.

4. **Commit:** `docs(state): record Phase 38 v1.5 HUMAN-UAT closure`.
  </action>
  <verify>
    <automated>grep -q "v1.5 HUMAN-UAT closure landed Phase 38" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `STATE.md` contains the literal substring `v1.5 HUMAN-UAT closure landed Phase 38`.
    - The entry sits inside the decisions/activity log section (not at end-of-file orphaned, unless that section IS at end-of-file).
    - Pre-existing STATE.md content unchanged except for the new line.
    - File committed.
  </acceptance_criteria>
  <done>
    - `STATE.md` updated and committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 38-01/02 walks ↔ Plan 38-03 closure | Closure trusts the per-test `result:` + `evidence:` content recorded by the verifier; no re-verification of the walk itself |
| Verification narrative ↔ Re-verification append | Original narrative is the historical-record contract per D-06; appended section is the new contribution. Closure must not modify the original |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-08 | Tampering | Closure could silently overwrite the original verification narrative | mitigate | Tasks 1 + 2 explicitly DO-NOT-TOUCH the existing `verified:` / `score:` / `human_verification:` fields and original sections; verify automation greps for `^human_verification:` to confirm preservation |
| T-38-09 | Repudiation | Re-verification could be backdated or attributed to a different phase | mitigate | `re_verified:` (ISO-8601 timestamp) + `re_verified_by: phase-38` + `<SHA>` in the appended section header all anchor the re-flip to a specific commit + phase + time |
| T-38-10 | Elevation of Privilege | Closure could mark a critical-severity defect as `passed` to retire the phase | mitigate | Task 0 step 2 + 3 require evidence-text scanning for `critical|blocker|regression` language and force a `sub_phase_trigger: 38.1` SUMMARY entry; reviewer can grep summaries for the trigger status |
| T-38-11 | Information Disclosure | SUMMARY counts table could leak deployment scale | accept | Same as Plan 38-01 T-38-02; Synthea is synthetic; `.planning/` is not externally published |
</threat_model>

<verification>
- `38-SUMMARY.md` exists with status `complete`, embeds Blaze URL + 11-row counts table + 12-row digest.
- `38-SUMMARY.md` `sub_phase_trigger` field is `none` (zero critical-severity rows) OR `38.1` with the literal `/gsd-insert-phase 38.1` invocation in the trigger section.
- `33-VERIFICATION.md` `status: passed` + `re_verified:` + `re_verified_by: phase-38` + appended `## Re-verification (Phase 38, <sha>)` section with 6-row outcome table.
- `35-VERIFICATION.md` same pattern with Phase 35's 6 tests.
- `STATE.md` decisions log carries the closure entry.
- All four files committed; commit messages follow `docs(38-03):`, `docs(33):`, `docs(35):`, `docs(state):` patterns.
- Original verification narratives (sections preceding the appended `## Re-verification` block) byte-identical to pre-edit state.
</verification>

<success_criteria>
Plan 38-03 closes when:
- `38-SUMMARY.md` written with all required sections + sub-phase 38.1 trigger evaluated.
- Both `33-VERIFICATION.md` and `35-VERIFICATION.md` re-flipped to `status: passed` + appended Re-verification sections; original narratives preserved.
- `STATE.md` carries the closure log entry.
- Phase 38 itself can now be marked complete by the GSD orchestrator.
- If `sub_phase_trigger: 38.1` was set, the user has clear instructions in `38-SUMMARY.md` to run `/gsd-insert-phase` — this plan does NOT spawn 38.1 itself (D-03: sub-phase fires AFTER closure surfaces the trigger; user-initiated).
</success_criteria>

<output>
After completion, the orchestrator may run `/gsd-verify-phase 38` to confirm Phase 38's own ROADMAP success criteria are satisfied. If `38-SUMMARY.md` `sub_phase_trigger: 38.1`, the user runs the surfaced `/gsd-insert-phase` command to spawn the fix sub-phase; Phase 38 closure does NOT wait on 38.1 (D-01 — Phase 38 stays observational; the fix-phase has its own PLAN/EXECUTE/VERIFY cycle).
</output>
