---
phase: 23
plan: 4
plan_number: 4
plan_name: "Nyquist frontmatter flip + milestone audit re-run"
type: execute
wave: 3
depends_on: [1, 2, 3]
files_modified:
  - .planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md
  - .planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md
autonomous: true
requirements:
  - CLOSE-07
status: pending

must_haves:
  truths:
    - "Phase 21's 21-VALIDATION.md frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`."
    - "Phase 22's 22-VALIDATION.md frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`."
    - "The frontmatter flip only happens AFTER Plans 23-01, 23-02, 23-03 have committed green (depends_on enforced)."
    - "A milestone audit re-run is triggered as a downstream signal so v1.3-MILESTONE-AUDIT.md verdict can flip from `tech_debt` to `shipped-clean`."
  artifacts:
    - path: ".planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md"
      provides: "Frontmatter with nyquist_compliant: true and wave_0_complete: true"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md"
      provides: "Frontmatter with nyquist_compliant: true and wave_0_complete: true"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "Plans 23-01 / 23-02 / 23-03 green"
      to: "Nyquist flip preconditions satisfied"
      via: "depends_on: [1, 2, 3] in this plan's frontmatter"
      pattern: "depends_on"
    - from: "Nyquist flip commit"
      to: "/gsd-audit-milestone v1.3 re-run"
      via: "downstream audit signal noted in SUMMARY"
      pattern: "shipped-clean"
---

<objective>
Flip the Nyquist audit gate (N1/CLOSE-07) after all other Phase 23 work is green. Plan is gated by `depends_on: [1, 2, 3]` on Plans 23-01 (code warnings), 23-02 (cosmetic notes), and 23-03 (human UAT).

Purpose: Phase 21 and Phase 22 VALIDATION.md frontmatter currently have `nyquist_compliant: false` and `wave_0_complete: false` — per the milestone audit at `.planning/milestones/v1.3-MILESTONE-AUDIT.md` (lines 144-150), both phases have complete per-task verification maps and all 219+ tests green; only the formal sign-off was never executed. Per D-08, the flip is a mechanical YAML edit, not a full `/gsd-validate-phase` re-run.

Output: 2 frontmatter edits (Phase 21 + Phase 22 VALIDATION.md), 1 git commit, and a downstream signal to the user that `/gsd-audit-milestone v1.3` can now flip the verdict from `tech_debt` to `shipped-clean`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-v1.3-close-out/23-CONTEXT.md
@.planning/milestones/v1.3-MILESTONE-AUDIT.md
@.planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md
@.planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md

<interfaces>
<!-- Current frontmatter state of both VALIDATION.md files — extracted verbatim -->

`.planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md` current frontmatter (lines 1-8):
```
(frontmatter delimited by triple-dash on lines 1 and 8)
phase: 21
slug: interactive-cohort-builder-rename
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-15
```

`.planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md` current frontmatter (lines 1-8):
```
(frontmatter delimited by triple-dash on lines 1 and 8)
phase: 22
slug: programmatic-cohort-definition-fhirpath-fdpg
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-16
```

Target frontmatter (both files, after flip):
- `nyquist_compliant: true`
- `wave_0_complete: true`
- (All other frontmatter fields unchanged; do NOT touch the body of either file.)

Per D-08 mechanics: "edit YAML frontmatter directly on both 21-VALIDATION.md and 22-VALIDATION.md to flip `nyquist_compliant: false` → `true` and `wave_0_complete: false` → `true` AFTER CLOSE-01..06 are all green. Do NOT re-run `/gsd-validate-phase 21` / `/gsd-validate-phase 22` unless the milestone audit explicitly requires a fresh audit artifact."
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify preconditions (Plans 23-01, 23-02, 23-03 green)</name>
  <files>(verification only — no edits)</files>

  <read_first>
    - .planning/phases/23-v1.3-close-out/23-01-SUMMARY.md (must exist; CLOSE-01/02/03 closed)
    - .planning/phases/23-v1.3-close-out/23-02-SUMMARY.md (must exist; CLOSE-04/05 closed)
    - .planning/phases/23-v1.3-close-out/23-03-SUMMARY.md (must exist; CLOSE-06 closed per D-10 policy)
  </read_first>

  <action>
    Before flipping anything, verify the three preceding plans are genuinely complete:

    1. Confirm each SUMMARY file exists (`ls .planning/phases/23-v1.3-close-out/23-0{1,2,3}-SUMMARY.md`).

    2. Confirm via `git log --oneline --grep="CLOSE-0"` that commits exist for `CLOSE-01`, `CLOSE-02`, `CLOSE-03`, `CLOSE-04`, `CLOSE-05`, AND `CLOSE-06` (or equivalent references like `W1`/`W2`/`W3`/`I1`/`I2`/`UAT`).

    3. Read `23-03-SUMMARY.md`'s "Final recommendation" line. It MUST say "proceed". If it says "block", STOP — the Nyquist flip is NOT authorized. In that case, fail this task with a clear error message: `"23-03 recommended block; flip not authorized"` and exit.

    4. Re-run `npm test` one final time. Must exit 0 (or with only the 22 pre-existing failures from STATE.md baseline; NO new failures).

    5. Run `npx tsc -b --noEmit`. Must exit 0.

    If ANY of 1-5 fail, do NOT proceed to Task 2. Surface the blocker to the user.
  </action>

  <verify>
    <automated>npm test &amp;&amp; npx tsc -b --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - Files `.planning/phases/23-v1.3-close-out/23-01-SUMMARY.md`, `23-02-SUMMARY.md`, `23-03-SUMMARY.md` all exist.
    - `git log --oneline --grep="CLOSE-0"` returns at least 3 commit entries (one per plan 23-01..03).
    - Grep `.planning/phases/23-v1.3-close-out/23-03-SUMMARY.md` contains `proceed` AND does NOT contain `recommendation: block`.
    - `npm test` exits 0 OR with exactly the pre-existing 22 failures (STATE.md baseline).
    - `npx tsc -b --noEmit` exits 0.
  </acceptance_criteria>

  <done>
    Preconditions verified; Plans 23-01..03 are green; Plan 23-03 recommends "proceed". Authorized to flip.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip Phase 21 VALIDATION.md frontmatter</name>
  <files>.planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md</files>

  <read_first>
    - .planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md (lines 1-10 — confirm current frontmatter matches the interface block above)
  </read_first>

  <action>
    Edit the YAML frontmatter at the top of `.planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md`:

    - Change line 5 from `nyquist_compliant: false` to `nyquist_compliant: true`.
    - Change line 6 from `wave_0_complete: false` to `wave_0_complete: true`.

    Do NOT touch any other line. Do NOT modify the body of the file. Do NOT add new frontmatter fields. The `status: draft` field at line 4 stays as `draft` — per D-08, this plan does not re-run `/gsd-validate-phase 21`, which would be the only authority to flip `status`. The Nyquist flip is a separate, mechanical signal.

    After the edit, the first 8 lines of the file must read exactly:
    ```yaml
    ---
    phase: 21
    slug: interactive-cohort-builder-rename
    status: draft
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-15
    ---
    ```
  </action>

  <verify>
    <automated>head -10 .planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md | grep -E "nyquist_compliant: true|wave_0_complete: true" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - Grep `.planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md` contains the exact string `nyquist_compliant: true` on line 5.
    - Grep `.planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md` contains the exact string `wave_0_complete: true` on line 6.
    - The same file does NOT contain `nyquist_compliant: false` anywhere.
    - The same file does NOT contain `wave_0_complete: false` anywhere.
    - `git diff .planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md` shows exactly 2 line changes (both in the frontmatter).
  </acceptance_criteria>

  <done>
    Phase 21 Nyquist flipped.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip Phase 22 VALIDATION.md frontmatter</name>
  <files>.planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md</files>

  <read_first>
    - .planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md (lines 1-10 — confirm current frontmatter matches the interface block above)
  </read_first>

  <action>
    Edit the YAML frontmatter at the top of `.planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md`:

    - Change line 5 from `nyquist_compliant: false` to `nyquist_compliant: true`.
    - Change line 6 from `wave_0_complete: false` to `wave_0_complete: true`.

    Same rules as Task 2: do NOT touch anything else. `status: draft` stays as `draft`.

    After the edit, the first 8 lines of the file must read exactly:
    ```yaml
    ---
    phase: 22
    slug: programmatic-cohort-definition-fhirpath-fdpg
    status: draft
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-16
    ---
    ```
  </action>

  <verify>
    <automated>head -10 .planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md | grep -E "nyquist_compliant: true|wave_0_complete: true" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - Grep `.planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md` contains the exact string `nyquist_compliant: true` on line 5.
    - Grep `.planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md` contains the exact string `wave_0_complete: true` on line 6.
    - The same file does NOT contain `nyquist_compliant: false` anywhere.
    - The same file does NOT contain `wave_0_complete: false` anywhere.
    - `git diff .planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md` shows exactly 2 line changes (both in the frontmatter).
  </acceptance_criteria>

  <done>
    Phase 22 Nyquist flipped.
  </done>
</task>

<task type="auto">
  <name>Task 4: Commit the Nyquist flip with explicit trigger-condition message</name>
  <files>(commit step — stages the two VALIDATION.md edits)</files>

  <read_first>
    - Outcome of Tasks 1, 2, 3 (preconditions confirmed; both files edited; diffs show 2 lines each)
  </read_first>

  <action>
    Stage and commit the two VALIDATION.md edits together with a commit message that makes the trigger condition explicit (per 23-CONTEXT.md §Integration Points note: "the commit message should make the trigger condition explicit — post-UAT-green").

    ```
    git add .planning/phases/21-interactive-cohort-builder-rename/21-VALIDATION.md
    git add .planning/phases/22-programmatic-cohort-definition-fhirpath-fdpg/22-VALIDATION.md
    git commit -m "docs(23-04): flip Nyquist compliance flags on Phase 21 + 22 VALIDATION.md (CLOSE-07)

    All preconditions satisfied per D-08:
      - Plan 23-01 green (CLOSE-01/02/03 — code review warnings W1/W2/W3)
      - Plan 23-02 green (CLOSE-04/05 — cosmetic integration notes I1/I2)
      - Plan 23-03 green (CLOSE-06 — 8 live-Blaze UAT items, D-10 policy applied)

    nyquist_compliant: false -> true and wave_0_complete: false -> true
    on both VALIDATION.md files. This is the mechanical sign-off per D-08;
    the per-task verification maps in both files were already complete
    (Phase 21: 12/12, Phase 22: 4/4) per the milestone audit at
    .planning/milestones/v1.3-MILESTONE-AUDIT.md lines 144-150."
    ```

    After commit, run `git log -1 --stat` to confirm the commit touches exactly the 2 VALIDATION.md files.
  </action>

  <verify>
    <automated>git log -1 --oneline</automated>
  </verify>

  <acceptance_criteria>
    - `git log -1 --oneline` shows a commit subject containing `CLOSE-07` AND `Nyquist` (case-insensitive).
    - `git show HEAD --stat` shows exactly 2 files changed: `21-VALIDATION.md` and `22-VALIDATION.md`.
    - `git show HEAD --stat` shows ≤ 4 insertions + 4 deletions total (2 lines per file).
    - `git status` is clean post-commit.
  </acceptance_criteria>

  <done>
    CLOSE-07 closed. Nyquist flipped on both phases with explicit trigger-condition commit message.
  </done>
</task>

<task type="auto">
  <name>Task 5: Produce SUMMARY and surface downstream milestone-audit signal</name>
  <files>.planning/phases/23-v1.3-close-out/23-04-SUMMARY.md</files>

  <read_first>
    - Task 1-4 outcomes
    - .planning/milestones/v1.3-MILESTONE-AUDIT.md (frontmatter — the `status: tech_debt` field that the user will flip separately via `/gsd-audit-milestone v1.3`)
  </read_first>

  <action>
    Create `.planning/phases/23-v1.3-close-out/23-04-SUMMARY.md` documenting:

    1. **Preconditions satisfied** (Task 1 outcome): CLOSE-01..06 closure commits, npm test green, tsc clean.
    2. **Flip executed** (Tasks 2-4 outcome): both VALIDATION.md files' frontmatter flipped; commit hash.
    3. **Downstream signal to user** — explicit recommendation:

       > Phase 23 is now complete. All 7 CLOSE-0X requirements are closed. The v1.3 milestone is ready for the audit verdict to flip from `tech_debt` to `shipped-clean`. Next step (not part of this plan's scope):
       >
       > Run `/gsd-audit-milestone v1.3` — the audit will re-score all phases and inspect the Nyquist compliance matrix. With this plan's commit, the matrix should read:
       > - Phase 21: `nyquist_compliant: true`, `wave_0_complete: true`
       > - Phase 22: `nyquist_compliant: true`, `wave_0_complete: true`
       >
       > The audit should then flip `.planning/milestones/v1.3-MILESTONE-AUDIT.md` frontmatter `status: tech_debt` → `status: shipped-clean`. After that, `/gsd-complete-milestone v1.3` finalizes the milestone and unblocks the v1.4 refactor thread (Phase 24+).

    4. **Any deferred items carried forward**: list any environmental gaps accepted per Task 5 of Plan 23-03 (if any), so the next milestone-audit run sees them.

    Do NOT run `/gsd-audit-milestone v1.3` from this plan — that's a user-initiated step per D-08 ("do not re-run /gsd-validate-phase unless the milestone audit explicitly requires a fresh audit artifact"). The SUMMARY surfaces the recommendation; the user drives the invocation.

    Commit the SUMMARY: `git add .planning/phases/23-v1.3-close-out/23-04-SUMMARY.md && git commit -m "docs(23-04): summarize Phase 23 close-out completion"`.
  </action>

  <verify>
    <automated>ls .planning/phases/23-v1.3-close-out/23-04-SUMMARY.md &amp;&amp; git log -1 --oneline</automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/phases/23-v1.3-close-out/23-04-SUMMARY.md` exists.
    - Grep the SUMMARY contains the string `shipped-clean`.
    - Grep the SUMMARY contains the string `/gsd-audit-milestone v1.3`.
    - Grep the SUMMARY lists each of CLOSE-01 through CLOSE-07 with a closure note.
    - `git log -2 --oneline` shows both the Nyquist flip commit (Task 4) and the SUMMARY commit in sequence.
  </acceptance_criteria>

  <done>
    Phase 23 complete. SUMMARY committed with explicit next-step recommendation for the user to run `/gsd-audit-milestone v1.3`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new code surface. Frontmatter-only edits to planning documents.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Repudiation | Nyquist flip (trust-boundary signal for downstream milestone audit) | mitigate | Task 1 explicitly verifies that Plans 23-01, 23-02, 23-03 are green (SUMMARY files present + CLOSE-0X commits in log + Plan 23-03 recommendation says "proceed") BEFORE Task 2 flips anything. `depends_on: [1, 2, 3]` in this plan's frontmatter enforces the gate at the orchestrator level. Task 4's commit message records the exact preconditions satisfied, so any future auditor can reproduce the flip's justification from git log alone. |
</threat_model>

<verification>
- Plans 23-01, 23-02, 23-03 all have committed SUMMARY files referencing their respective CLOSE-0X closures.
- 21-VALIDATION.md frontmatter flipped (verified by grep).
- 22-VALIDATION.md frontmatter flipped (verified by grep).
- Git history contains a single commit for both flips with an explicit trigger-condition message.
- SUMMARY created and committed with the user-facing `/gsd-audit-milestone v1.3` recommendation.
</verification>

<success_criteria>
- CLOSE-07 closed: both Nyquist flags flipped to `true` on Phase 21 + 22 VALIDATION.md.
- Preconditions gate satisfied (depends_on [1, 2, 3] enforced).
- Downstream signal clear: user knows `/gsd-audit-milestone v1.3` is the next step.
- Phase 23 fully complete — all 7 CLOSE-0X requirements closed.
</success_criteria>

<output>
After completion, `.planning/phases/23-v1.3-close-out/23-04-SUMMARY.md` exists and documents all of the above. The v1.3 milestone is ready for audit re-run; Phase 24 (v1.4 Data-Fetching Foundation) is unblocked on user confirmation.
</output>
