---
phase: 23
plan: 6
plan_number: 6
plan_name: "Re-run T-6.3 A + T-6.3 B UAT against live Blaze with 139-patient cohort"
type: execute
wave: 2
depends_on: [5]
files_modified:
  - .planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md
  - .planning/phases/21-interactive-cohort-builder-rename/21-UAT.md
autonomous: false
gap_closure: true
requirements:
  - CLOSE-06
status: pending

must_haves:
  truths:
    - "T-6.3 A re-run against a live Blaze instance hosting a dataset with at least one cohort matching >= 1 patient (target: the 139-patient cohort from the original UAT failure) flips the HUMAN-UAT result from `issue` to `pass`."
    - "T-6.3 A network evidence recorded: browser DevTools Network tab shows OverviewStrip count requests carrying `patient=Patient/{id}` for non-Patient types (and `_id=` for Patient type) with <=40 IDs, or POST `/_search` for >40 IDs."
    - "T-6.3 A per-metric network evidence recorded: for EACH of the 4 previously-stale report hooks (Plausibility, Lab Ranges, Duplicates, References) AND for Validation (regression-guard), the Network tab shows at least one new sampleResources-issued request (either `?patient=...` GET or POST `_search` with `patient=` body) on cohort activation. A silent-regression channel where tile-value equality happens to match between scoped and unscoped MUST NOT false-flag a pass."
    - "T-6.3 A panel evidence recorded: quality dashboard OverviewStrip tile values (Total resources, Resource types) AND the 7 metric tiles (Completeness, Coding Coverage, Validation, Plausibility, Lab ranges, Duplicates, References) recompute to values strictly lower than the unscoped baseline when the cohort activates — including the 4 previously-stale metrics (Plausibility, Lab ranges, Duplicates, References) which now re-fire on cohort change per Plan 23-05 Bug B fix. Tile values are SECONDARY evidence — network traffic is PRIMARY."
    - "T-6.3 B re-run against the same cohort: Capture snapshot writes to `quality.trends.v1` with `cohortId`, `cohortName`, `cohortPatientCount` populated; PDF export cover page shows both `Resource types:` and `Cohort: ...` lines."
    - "Recording is PHI-safe: no patient IDs, auth tokens, or tenant-identifiable paths are written to the UAT files. Cohort name (synthetic / user-defined) is the only identifier allowed."
    - "D-10 policy reapplied: if a regression surfaces during re-run, classify as code-bug (file CLOSE-09+, fix inline) | environmental (append to `## Gaps`, escalate to user) | cosmetic (v1.5+ backlog). The only acceptable `issue` result after this plan is a NEW defect, not the one fixed by Plan 23-05."
    - "Summary block in 23-HUMAN-UAT.md reflects the re-run: `passed: 2`, `issues: 0` (assuming clean re-run)."
  artifacts:
    - path: ".planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md"
      provides: "Test 1 result updated from `issue` to `pass` with fresh timestamp + evidence bullets (including per-metric Network-tab lines); Test 2 re-confirmed pass against same cohort"
      contains: "result: pass"
    - path: ".planning/phases/21-interactive-cohort-builder-rename/21-UAT.md"
      provides: "## Gaps section updated — T-6.3 A environmental entry flipped to PASS with re-run date + fix reference (Plan 23-05)"
      contains: "Plan 23-05"
  key_links:
    - from: "Plan 23-05 green merge"
      to: "UAT re-run preconditions"
      via: "depends_on: [5] — fix MUST land before UAT"
      pattern: "depends_on"
    - from: "Live Blaze instance with 139-patient cohort"
      to: "OverviewStrip + 7 metric tiles"
      via: "browser DevTools visual + Network observation"
      pattern: "Network: patient=|_id=|_search"
    - from: "UAT results recorded"
      to: "23-HUMAN-UAT.md Summary"
      via: "YAML-like result: block + Summary counts"
      pattern: "result: pass|passed: [0-9]"
---

<objective>
Human-in-the-loop re-run of T-6.3 A (panel scoping) and T-6.3 B (snapshot/PDF cohort metadata) against a live Blaze instance with a 139-patient cohort (the exact scenario that failed in the original UAT at commit 8482720). Plan 23-05 has fixed the two root-cause defects diagnosed in 23-HUMAN-UAT.md's `root_cause` block; this plan provides the live-server evidence that the fixes actually work.

Purpose: unblock Plan 23-07 (VERIFICATION flip + Nyquist audit) by providing the missing live-Blaze pass that Phase 23 has been stalled on since 2026-04-17.

Output: 23-HUMAN-UAT.md Test 1 flips from `issue` to `pass` with evidence (including per-metric Network-tab bullets); Test 2 re-confirmed; 21-UAT.md `## Gaps` note updated to reference the Plan 23-05 fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-v1.3-close-out/23-CONTEXT.md
@.planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md
@.planning/phases/23-v1.3-close-out/23-05-PLAN.md
@.planning/phases/21-interactive-cohort-builder-rename/21-UAT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Human runs dev server, activates 139-patient cohort, captures evidence</name>
  <files>
    .planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md,
    .planning/phases/21-interactive-cohort-builder-rename/21-UAT.md
  </files>
  <read_first>
    - .planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md (current `issue` result on test 1 + root_cause block — the exact claim we need to falsify post-fix)
    - .planning/phases/21-interactive-cohort-builder-rename/21-UAT.md (prior run notes + `## Gaps` environmental entry for T-6.3 A / T-6.3 B)
    - .planning/phases/23-v1.3-close-out/23-CONTEXT.md (D-07 recording format; D-10 blocker escalation policy)
    - .planning/phases/23-v1.3-close-out/23-05-PLAN.md (so the human knows which code change is under test)
  </read_first>
  <what-built>
    Plan 23-05 delivered two code fixes:
    - Bug A: OverviewStrip tiles 1-2 (`Total resources`, `Resource types`) now recompute when a cohort is active, because `useResourceCounts` issues scoped FHIR searches with `patient=Patient/{id}` (<=40 IDs) or POST `/_search` (>40 IDs).
    - Bug B: `usePlausibilityReport`, `useLabRangesReport`, `useDuplicateReport`, `useReferenceReport` now auto-restart when `patientIds` changes (`autoStart: true` + memoized `patientIdsKey` deps), so Plausibility/Lab Ranges/Duplicates/References panels no longer display stale unscoped results after cohort activation.

    Completeness, Coding Coverage, and Validation were already correct and continue to re-scope. A regression-guard unit test was added for Validation in Plan 23-05 Task 1 (`src/hooks/__tests__/useValidationRun.test.tsx`); this live-UAT plan adds a Network-tab assertion as the companion runtime evidence.
  </what-built>
  <action>
    **This is a human-action checkpoint.** The executor does NOT perform the UAT itself — the human operator does. The executor's job on resume is to read the updated UAT files, sanity-check the format, and proceed to Task 2 for the commit.

    Human operator workflow (see `<how-to-verify>` below for the detailed procedure):
    1. Boot dev server against a live Blaze with a 139-patient cohort.
    2. Run T-6.3 A and T-6.3 B with DevTools Network tab open.
    3. Record results directly in `.planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md` (flip Test 1 from `issue` to `pass`; update Summary).
    4. Append a resolution line to `.planning/phases/21-interactive-cohort-builder-rename/21-UAT.md` `## Gaps` section.
    5. Reply with the resume signal.
  </action>
  <how-to-verify>
    **Prerequisites (environment):**

    1. Live Blaze FHIR server reachable at the URL configured in `~/.fhir-exploder/settings.yaml` or the app's Settings page. The server MUST contain a dataset where at least one saved cohort resolves to exactly 139 patients (the scenario that originally failed). If the MII Synthea seed is loaded on `http://localhost:8080/fhir`, the condition-code cohort from the original UAT session should match 139 patients — if not, create a cohort with criteria that resolves to a non-zero subset and document the actual patient count.
    2. Dev server: `npm run dev` → open `http://localhost:5173/`.
    3. Browser DevTools open with the Network tab selected and XHR filter active. Before cohort activation, CLEAR the Network log (trash-can icon) so the post-activation traffic is isolated.

    **T-6.3 A — Panel scoping re-run:**

    1. Navigate to `/quality`. Note the UNSCOPED baseline values of ALL 9 tiles (Total resources, Resource types, Completeness, Coding Coverage, Validation, Plausibility, Lab ranges, Duplicates, References) — write them down or screenshot.
    2. Open the "Active cohort" Select; activate the 139-patient cohort.
    3. Watch the Network tab while the dashboard recomputes. Expected (per Bug A fix — count tiles):
       - Count requests for each resource type MUST now include either `patient=Patient/{id},Patient/{id},...` (non-Patient types) or `_id={id},{id},...` (Patient type) as query params, OR — if the cohort resolves to >40 IDs (likely with 139 patients) — you'll see POST `/_search` requests with `application/x-www-form-urlencoded` bodies containing `_summary=count&patient=...`.
       - If NO requests carry cohort scoping, Bug A is NOT fixed — STOP and file a CLOSE-09 code-bug per D-10.
    4. **Per-metric Network assertion (Warning 4 + Blocker 1 companion — REQUIRED evidence, not optional):** For EACH of the 5 report hooks that issue sampleResources calls (Plausibility, Lab Ranges, Duplicates, References, AND Validation-regression-guard), observe the Network tab and confirm a NEW request (GET with `?patient=` / `?_id=` OR POST `_search` with `patient=` in body) was issued during cohort activation. This is PRIMARY EVIDENCE — tile value deltas alone can false-pass when a scoped value coincidentally equals the unscoped value. Record one bullet line per metric in the UAT file, e.g.:
       - `Plausibility: POST /Observation/_search issued at <timestamp> with body containing patient=Patient/...` ✓
       - `Lab Ranges: POST /Observation/_search issued at <timestamp>` ✓
       - `Duplicates: POST /Patient/_search issued at <timestamp>` ✓
       - `References: POST /Observation/_search issued at <timestamp>` ✓
       - `Validation (regression guard): POST /_search or ?patient= GET issued at <timestamp>` ✓ (Validation tile recomputes on Cohort activation only if user has already clicked "Run"; if Validation is `idle`, note `not-applicable (Validation is user-triggered; no new request expected unless Run was already active)`. If Validation WAS running / complete and shows NO new request on cohort change → silent regression → classify per D-10 as code-bug CLOSE-09.)
       - If ANY of the 4 Bug B metrics (Plausibility/Lab Ranges/Duplicates/References) show NO new network call on cohort activation, Bug B is NOT fixed — STOP and file CLOSE-09 per D-10.
    5. Observe the OverviewStrip tiles. Every tile should recompute. Expected:
       - **Tiles 1-2 (Total resources, Resource types):** values should be STRICTLY LOWER than baseline (139 patients cannot possibly have more resources than the entire dataset).
       - **Tiles 3-9 (the 7 metric tiles):** Completeness/Coding/Validation already worked pre-fix; focus attention on Plausibility/Lab Ranges/Duplicates/References — these should now ALSO recompute (they were stale pre-fix). A changed value OR an `–` em-dash (if the cohort has no observations/specimens of the relevant type) is acceptable evidence of a re-run; a value UNCHANGED from baseline, COMBINED with a missing network call from step 4, is a Bug B regression.
    6. Repeat activation/deactivation 2-3 times to confirm tiles track cohort state AND that Network traffic (step 4) fires each time cohort activates.

    **T-6.3 B — Snapshot + PDF cohort metadata:**

    1. With the cohort still active, click "Capture snapshot". Confirm the blue toast fires.
    2. DevTools → Application → Local Storage → `quality.trends.v1`. Find the latest entry. Confirm it contains `cohortId`, `cohortName`, `cohortPatientCount: 139` (or whatever the actual matched patient count is).
    3. Click "Export PDF". Open the PDF. The cover page MUST show BOTH lines:
       - `Resource types: ...`
       - `Cohort: "<name>" (139 patients)` (or actual count).
    4. Deactivate the cohort. Click "Export PDF" again. The cover page should show only `Resource types: ...` — NO `Cohort:` line.

    **Recording:**

    Update `.planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md`:
    - Flip Test 1 `result:` from `issue` to `pass`.
    - Remove the `reported:` and `severity:` fields (or convert them into a historical note).
    - Add fresh evidence bullets under a new `evidence:` multiline block (timestamp, server URL sanitized to host only — `http://localhost:8080/fhir` or `http://<host>:<port>/fhir` with no auth/tenant path, observed tile deltas, AND the 5 per-metric Network-tab bullets from step 4 above).
    - Re-confirm Test 2 — either leave its `result: pass` and add a re-run note, or (if the original pass was against jsdom only, not against live Blaze with matching patients) replace the pass with fresh live-Blaze evidence.
    - Update `Summary` block: `total: 2`, `passed: 2`, `issues: 0`, `pending: 0`, `skipped: 0`, `blocked: 0`.
    - Remove/update the `## Gaps` block to reflect resolution — keep the historical root_cause block but add a `resolved:` line pointing at Plan 23-05 commits.
    - Bump frontmatter `updated: <today ISO timestamp>`.

    Update `.planning/phases/21-interactive-cohort-builder-rename/21-UAT.md`:
    - In the `## Gaps` section, append a line noting T-6.3 A and T-6.3 B re-ran successfully after Plan 23-05 fix; reference commit hashes.

    **PHI sanitization check (mandatory before commit):**
    - grep the two updated files for obvious PHI patterns. Acceptable tokens: synthetic cohort names, `localhost`, `8080`, `Patient` (type name), numeric patient counts. Unacceptable: any actual patient ID, MRN, name, DOB, or identifier that looks like it came from the server. If unsure, replace with `<sanitized>`.
  </how-to-verify>
  <verify>
    Human confirms procedure completed by updating the two target files AND replying with the resume-signal (see below). The schema-level verification (`result: pass` present, Summary counts correct) is performed automatically in Task 2 before commit.
  </verify>
  <done>
    Both UAT files edited by the human with live-Blaze evidence (including the 5 per-metric Network-tab bullets from step 4); human has replied with `approved` (or a classified non-approval per D-10). Plan advances to Task 2 or halts per the classification.
  </done>
  <resume-signal>
    Reply with one of:
    - `approved` — both tests pass on live Blaze, files updated (including per-metric Network-tab evidence), PHI check clean. Proceed to Plan 23-07.
    - `code-bug: <description>` — a new defect surfaced (including: any metric's Network tab showed no new request on cohort activation). Per D-10, file as CLOSE-09+ and request a new plan (do NOT flip nyquist).
    - `environmental: <description>` — server missing capability or test data still absent. Escalate for accept-and-defer decision; Plan 23-07 Nyquist flip BLOCKED until resolved.
    - `cosmetic: <description>` — minor UX-only issue. Record as v1.5+ backlog todo, continue with approval.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Commit UAT updates after human approval</name>
  <files>
    .planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md,
    .planning/phases/21-interactive-cohort-builder-rename/21-UAT.md
  </files>
  <read_first>
    - .planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md (post-human-edit — Test 1 should now be `pass`)
    - .planning/phases/21-interactive-cohort-builder-rename/21-UAT.md (post-human-edit — `## Gaps` updated)
  </read_first>
  <action>
    1. Diff the two files and confirm the changes match the expected shape described in Task 1's `<how-to-verify>` recording section (including the per-metric Network-tab bullets for Plausibility / Lab Ranges / Duplicates / References / Validation).
    2. PHI grep: `grep -E '\\b[0-9]{10,}\\b|\\bPatient/[a-zA-Z0-9-]{10,}\\b|Bearer [A-Za-z0-9._-]{20,}' .planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md .planning/phases/21-interactive-cohort-builder-rename/21-UAT.md`. If any match: STOP, surface to user, do NOT commit.
    3. If PHI grep is clean AND Test 1 `result: pass` is present AND Summary block shows `passed: 2, issues: 0`, commit.
    4. Commit message:
       ```
       docs(23): T-6.3 A + B UAT re-run PASS on live Blaze after Plan 23-05 fix

       - Bug A (OverviewStrip tiles scoped to cohort) verified with network evidence
       - Bug B (4 report hooks re-fire on patientIds change) verified via per-metric
         Network-tab bullets (Plausibility, Lab Ranges, Duplicates, References)
       - Validation regression-guard Network evidence recorded
       - Closes the gap that stalled Phase 23 at `human_needed` since 2026-04-17
       ```
    5. Note: this plan does NOT flip 23-VERIFICATION.md — that's Plan 23-07's job.
  </action>
  <verify>
    <automated>grep -E '^result: pass' .planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md | wc -l | awk '{if ($1 >= 2) exit 0; else exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md` contains `result: pass` at least twice (one per test) and zero instances of `result: issue`.
    - `grep -c "passed: 2" .planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md` returns 1.
    - `grep -c "issues: 0" .planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md` returns 1.
    - `grep -Ei "Plausibility|Lab Ranges|Duplicates|References" .planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md | wc -l` >= 4 (per-metric evidence bullets present — Warning 4 fix).
    - `grep -Ei "Network|_search|patient=" .planning/phases/23-v1.3-close-out/23-HUMAN-UAT.md | wc -l` >= 1 (runtime network evidence present).
    - `.planning/phases/21-interactive-cohort-builder-rename/21-UAT.md` `## Gaps` section mentions Plan 23-05 or a similar marker.
    - PHI grep pattern `Bearer [A-Za-z0-9._-]{20,}` returns 0 matches across both files.
    - Git log shows a commit matching `^docs\(23\): T-6\.3 A.*UAT re-run PASS`.
  </acceptance_criteria>
  <done>
    Both UAT files committed with live-Blaze pass evidence (including per-metric Network-tab bullets). PHI-safe. Ready for Plan 23-07 to flip 23-VERIFICATION.md to `passed`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Live Blaze server | Human operates browser; PHI flows in server responses but NOT into UAT.md files. |
| Human → UAT.md recording | Risk: human pastes raw response fragments into UAT notes. Mitigated by Task 2 PHI grep + D-07 sanitization rule. |
| UAT.md → git | Committed files are public-in-repo. MUST be PHI-clean. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06-01 | Information Disclosure | UAT.md recording includes patient IDs or tokens | mitigate | Task 2 runs PHI regex grep before commit; D-07 documented rule; human is instructed to replace real IDs with `<sanitized>` or synthetic patient counts only. Fail-closed: if grep matches, Task 2 aborts commit. |
| T-23-06-02 | Information Disclosure | Server URL in UAT.md reveals tenant/auth | mitigate | D-07 rule: sanitized = host:port only (e.g. `http://localhost:8080/fhir`); no `Bearer` tokens, no tenant paths, no basic-auth URLs. |
| T-23-06-03 | Tampering | Human falsifies UAT pass without running | accept | Solo-developer workflow; trust user to run the live UAT honestly. Task 2 does not re-verify via automation (by design — this IS the human UAT gate). |
| T-23-06-04 | Repudiation | Timestamp missing from UAT evidence | mitigate | Recording format requires timestamp per D-07; D-10 classification requires a recorded decision. |
| T-23-06-05 | DoS | 139-patient cohort OverviewStrip fetch exceeds URL length on Blaze | mitigate | Plan 23-05 Task 2 Step A.5 already includes POST `/_search` fallback for >40 IDs; 139 > 40 will trigger POST. If the live Blaze rejects POST `_search`, this surfaces as a code-bug (CLOSE-09) per D-10. |
| T-23-06-06 | Spoofing | N/A | accept | No auth flow changed. |
| T-23-06-07 | Elevation of Privilege | N/A | accept | No privilege boundary. |
| T-23-06-08 | Information Disclosure | Screenshots or evidence artifacts committed with PHI | mitigate | Evidence is TEXTUAL (tile deltas described, Network tab described) — NO screenshots committed. Enforced via Task 2 acceptance criteria (no image files in the commit). |
| T-23-06-09 | Tampering | Tile-value coincidental equality false-passes Bug B | mitigate | Warning 4 fix: step 4 of <how-to-verify> REQUIRES per-metric Network-tab assertion (not just tile-delta observation). Task 2 acceptance criteria `grep -Ei "Plausibility\|Lab Ranges\|Duplicates\|References"` >= 4 enforces per-metric evidence on commit. |
</threat_model>

<verification>
1. 23-HUMAN-UAT.md Test 1 `result:` is `pass` (was `issue` at the start of this plan).
2. 23-HUMAN-UAT.md Summary: `passed: 2, issues: 0`.
3. 23-HUMAN-UAT.md evidence block contains per-metric Network-tab bullets for Plausibility, Lab Ranges, Duplicates, References (Warning 4).
4. 21-UAT.md `## Gaps` references Plan 23-05.
5. No PHI tokens in either file (synthetic cohort name + localhost server URL only).
6. One commit landed: `docs(23): T-6.3 A + B UAT re-run PASS on live Blaze after Plan 23-05 fix`.
</verification>

<success_criteria>
- Both T-6.3 A and T-6.3 B visibly PASS on a live Blaze instance with a 139-patient cohort (or whatever positive-match cohort is available).
- OverviewStrip tiles 1-2 recompute; 7 metric tiles recompute (including the previously-stale 4).
- Network traffic visibly carries cohort-scoped params or POST /_search body — PRIMARY evidence.
- Per-metric Network-tab bullets present in the UAT recording for each of Plausibility / Lab Ranges / Duplicates / References / Validation (Warning 4 + Blocker 1).
- Snapshot + PDF cohort metadata confirmed.
- UAT files updated with evidence, PHI-clean, committed.
</success_criteria>

<output>
After completion, create `.planning/phases/23-v1.3-close-out/23-06-SUMMARY.md`. Include:
- Cohort description (criteria, matched patient count).
- Server URL sanitized.
- Human's approval signal (the exact string received).
- Evidence narrative (tile deltas, per-metric network observations) — TEXT ONLY, no raw server data.
- If any non-blocking cosmetic issues surfaced: list them with a todo pointer.
- D-10 classification of any new finding (code-bug | environmental | cosmetic).
</output>
</content>
</invoke>