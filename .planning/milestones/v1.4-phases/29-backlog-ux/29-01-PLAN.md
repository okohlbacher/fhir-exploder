---
phase: 29-backlog-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/quality/OverviewStrip.tsx
  - src/components/quality/QualityOverviewPage.tsx
  - src/components/quality/PdfReportLayout.tsx
  - src/utils/relativeTime.ts
  - src/utils/__tests__/relativeTime.test.ts
  - src/components/quality/__tests__/OverviewStrip.test.tsx
  - src/__tests__/pdf-report-layout.test.tsx
  - .planning/milestones/v1.2-phases/18-quality-alerting-thresholds/18-UI-SPEC.md
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - "OverviewStrip renders exactly 7 metric ring tiles (Total resources + Resource types no longer appear inside the grid)."
    - "A status line above the tile strip reads `N resources · M types · Last computed {relative-time}` using Mantine Text size='sm' c='dimmed' with interpunct separators."
    - "Relative-time formatter handles <60s → 'just now', <60m → 'Nm ago', <24h → 'Nh ago', ≥24h → absolute ISO date."
    - "PDF export overview page renders 7 tiles (not 9); existing 'Total resources on server' + 'Distinct resource types' text lines below the grid remain."
    - "18-UI-SPEC.md reflects the 7-tile layout + status line and carries a trailing changelog note attributing the change to Phase 29 UX-02."
    - "Cardinality entries (Total resources, Resource types in the status line) remain non-clickable."
  artifacts:
    - path: "src/utils/relativeTime.ts"
      provides: "Shared relative-time formatter reused by QualityOverviewPage header and OverviewStrip status line"
      exports: ["formatRelative"]
    - path: "src/utils/__tests__/relativeTime.test.ts"
      provides: "Unit tests covering the 4 thresholds"
    - path: "src/components/quality/OverviewStrip.tsx"
      provides: "7-tile strip + status line header"
      contains: "Last computed"
    - path: "src/components/quality/__tests__/OverviewStrip.test.tsx"
      provides: "Tile count + status line render tests"
    - path: "src/components/quality/PdfReportLayout.tsx"
      provides: "7-tile overview page grid (matches dashboard)"
    - path: ".planning/milestones/v1.2-phases/18-quality-alerting-thresholds/18-UI-SPEC.md"
      provides: "Updated design spec with D-36 (9→7 reduction) and trailing changelog note"
      contains: "Phase 29 UX-02"
  key_links:
    - from: "src/components/quality/OverviewStrip.tsx"
      to: "src/utils/relativeTime.ts"
      via: "import { formatRelative }"
      pattern: "from '\\.\\./\\.\\./utils/relativeTime'"
    - from: "src/components/quality/QualityOverviewPage.tsx"
      to: "src/utils/relativeTime.ts"
      via: "import { formatRelative } (replaces inline helper at lines 70-78)"
      pattern: "from '\\.\\./\\.\\./utils/relativeTime'"
    - from: "src/components/quality/PdfReportLayout.tsx"
      to: "METRIC_ORDER"
      via: "Only the 7 metric SummaryCards are rendered inside the grid; the 2 informational cards are removed"
      pattern: "METRIC_ORDER\\.map"
---

<objective>
UX-02: Reduce the Quality dashboard OverviewStrip from 9 tiles to 7 metric rings and move the two informational cards (Total resources, Resource types) into a status-line header that also shows a relative-time "Last computed" stamp. Propagate the layout change into the PDF export and update the canonical UI-SPEC (`18-UI-SPEC.md`) with a dated decision note (D-36).

Purpose: The two informational tiles crowd the strip without conveying any breach signal, and the relative-time context for the metrics was previously hidden inside the page toolbar (`formatRelative` at `QualityOverviewPage.tsx:70-78`). Consolidating both facts into a single status line above the rings is the agreed UX (per CONTEXT D-01/D-02/D-03) and matches the tile layout already shipped in `PdfReportLayout.tsx` at lines 253-292 once the two info tiles are dropped from the grid.

Output: 7-tile `OverviewStrip` with interpunct status line; shared `formatRelative` utility in `src/utils/relativeTime.ts`; PDF regression tests (structural, not snapshots) asserting 7 tiles + status line; in-place `18-UI-SPEC.md` update with changelog entry attributing the change to Phase 29 UX-02.

Implements: UX-02 per user decisions D-01 (drop tiles 1-2), D-02 (status line format + inline formatter), D-03 (Mantine Text size='sm' c='dimmed' + interpunct), D-04 (cardinality non-clickable), D-05 (18-UI-SPEC.md in-place update), D-06 (PDF export updated with structural regression tests per research §2.6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-backlog-ux/29-CONTEXT.md
@.planning/phases/29-backlog-ux/29-RESEARCH.md

@src/components/quality/OverviewStrip.tsx
@src/components/quality/QualityOverviewPage.tsx
@src/components/quality/PdfReportLayout.tsx
@src/__tests__/pdf-report-layout.test.tsx
@.planning/milestones/v1.2-phases/18-quality-alerting-thresholds/18-UI-SPEC.md

<interfaces>
<!-- Key types + shapes extracted from the codebase so the executor does not need to explore. -->

From src/components/quality/OverviewStrip.tsx (current, 150 LOC):
```typescript
export interface OverviewStripProps {
  summary: CountSummary;   // { total: number, typeCount: number, ... }
  isLoading: boolean;
}
// GRID_COLS currently: { base: 1, xs: 2, sm: 3, md: 4, lg: 5, xl: 9 }
// Tiles 1-2 hardcoded at lines 103-112 (Total resources, Resource types)
// Tiles 3-9 derived from METRIC_ORDER at lines 118-147
```

From src/quality/counts.ts (CountSummary):
```typescript
export interface CountSummary {
  total: number;
  typeCount: number;
  // … other fields used by Counts tab, not needed for this plan
}
```

From src/components/quality/QualityOverviewPage.tsx:70-78 (inline helper — to extract):
```typescript
function formatRelative(d: Date | null): string {
  if (!d) return 'Never';
  const secs = Math.floor((Date.now() - d.getTime()) / 1000);
  if (secs < 60) return `${secs}s ago`;
  const mins = Math.floor(secs / 60);
  if (mins < 60) return `${mins}min ago`;
  const hrs = Math.floor(mins / 60);
  return `${hrs}h ago`;
}
```
Note: CONTEXT D-02 adds a 4th threshold: ≥24h → absolute ISO date (e.g., `d.toISOString().slice(0,10)`); also the <60s case becomes `'just now'` per D-02 wording.

From src/components/quality/PdfReportLayout.tsx (current, relevant lines):
- Line 253: `gridTemplateColumns: 'repeat(3, 232px)'` → must become a layout that fits 7 tiles cleanly (recommended: `repeat(4, 176px)` mirrors Trends grid at line 317, wrapping 4+3).
- Lines 258-267: 2 hardcoded informational SummaryCards (Total resources + Resource types) — REMOVE.
- Lines 269-286: `METRIC_ORDER.map(k => …)` renders 7 metric SummaryCards — KEEP.
- Lines 288-292: separate `<Text>` lines "Total resources on server: 1234" + "Distinct resource types: 15" — KEEP (they are not tiles, they are the text below the grid, and the PDF tests at `src/__tests__/pdf-report-layout.test.tsx:151-152` already assert them).

From src/__tests__/pdf-report-layout.test.tsx:
- Existing structural-assertion style (`screen.getByText`, `queryByText`); NO snapshot files.
- Mantine + html-to-image polyfills at lines 22-42.
- 11 test cases total; add 3 for UX-02 (do NOT rewrite existing tests).

18-UI-SPEC.md decision numbering convention:
- File uses `D-01` through `D-19` + `UI-SPEC-03` style IDs for prior decisions.
- Per CONTEXT D-05 and RESEARCH §2.5 recommendation: add a new decision `D-36` dated `2026-04-23` noting the 9→7 reduction.
- Add a trailing changelog note at the very end of the file: `*v1.4 Phase 29 UX-02 (2026-04-23): reduced OverviewStrip from 9 tiles to 7; Total resources + Resource types moved to status-line header above the strip. Cardinality entries remain non-clickable.*`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract formatRelative → src/utils/relativeTime.ts + unit tests</name>
  <files>
    src/utils/relativeTime.ts (NEW)
    src/utils/__tests__/relativeTime.test.ts (NEW)
    src/components/quality/QualityOverviewPage.tsx (replace inline helper at lines 70-78)
  </files>
  <read_first>
    - `src/components/quality/QualityOverviewPage.tsx` lines 70-78 (existing inline helper)
    - CONTEXT D-02 (the 4 thresholds locked by the user)
    - Any existing `src/utils/` file to confirm project convention for barrel exports (likely none — simple named exports)
  </read_first>
  <behavior>
    - Test 1: `formatRelative(null)` → `'Never'`.
    - Test 2: `formatRelative(new Date(Date.now() - 30_000))` → `'just now'` (under 60s per D-02 wording, replacing the prior `'{N}s ago'`).
    - Test 3: `formatRelative(new Date(Date.now() - 5 * 60_000))` → `'5m ago'` (CONTEXT D-02 says `'Nm ago'`; match that exact wording).
    - Test 4: `formatRelative(new Date(Date.now() - 3 * 3600_000))` → `'3h ago'`.
    - Test 5: `formatRelative(new Date(Date.now() - 25 * 3600_000))` → a string matching `/^\d{4}-\d{2}-\d{2}$/` (ISO date, `toISOString().slice(0, 10)`), per D-02 `>24h` fallback.
    - Test 6: boundary — exactly 60s ago returns `'1m ago'` (not `'just now'`); exactly 60m ago returns `'1h ago'`; exactly 24h ago returns the ISO date.
  </behavior>
  <action>
    1. Create `src/utils/relativeTime.ts`:
       ```typescript
       /**
        * Relative-time formatter shared between the Quality dashboard header
        * (QualityOverviewPage toolbar) and the OverviewStrip status line.
        *
        * Thresholds locked by Phase 29 CONTEXT D-02:
        *   - null             → "Never"
        *   - <60 s            → "just now"
        *   - <60 min          → "Nm ago"
        *   - <24 h            → "Nh ago"
        *   - ≥24 h            → ISO date YYYY-MM-DD
        */
       export function formatRelative(d: Date | null): string {
         if (!d) return 'Never';
         const secs = Math.floor((Date.now() - d.getTime()) / 1000);
         if (secs < 60) return 'just now';
         const mins = Math.floor(secs / 60);
         if (mins < 60) return `${mins}m ago`;
         const hrs = Math.floor(mins / 60);
         if (hrs < 24) return `${hrs}h ago`;
         return d.toISOString().slice(0, 10);
       }
       ```
    2. Create `src/utils/__tests__/relativeTime.test.ts` with the 6 cases from `<behavior>`. Use `vi.useFakeTimers()` + `vi.setSystemTime(new Date('2026-04-23T12:00:00Z'))` at `beforeEach` so `Date.now()` is stable; call `vi.useRealTimers()` at `afterEach`.
    3. Edit `src/components/quality/QualityOverviewPage.tsx`: delete the inline `formatRelative` function at lines 70-78; add `import { formatRelative } from '../../utils/relativeTime';` near the other imports (around line 29). Do not change any call sites — signature is identical.
    4. NOTE: The old helper returned `'Ns ago'` for <60s and `'Nmin ago'` for minutes. CONTEXT D-02 changes the wording to `'just now'` / `'Nm ago'`. Update any page-level tests in `QualityOverviewPage.test.tsx` if they match those strings (grep to check; likely none since overview page tests focus on toolbar buttons).
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/relativeTime.test.ts</automated>
    <automated>npx tsc -b --noEmit</automated>
    <automated>grep -n "formatRelative" src/components/quality/QualityOverviewPage.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/utils/relativeTime.ts` exists and exports `formatRelative`.
    - `grep -rn "function formatRelative" src/components/quality/` returns zero matches (inline helper removed).
    - `grep -n "from '../../utils/relativeTime'" src/components/quality/QualityOverviewPage.tsx` returns exactly one match.
    - All 6 tests in the new file pass.
    - `tsc -b --noEmit` clean.
  </acceptance_criteria>
  <done>
    Shared `formatRelative` available to both `QualityOverviewPage` (toolbar header) and `OverviewStrip` (status line). No duplication. All new tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drop tiles 1-2 from OverviewStrip + add status-line header</name>
  <files>
    src/components/quality/OverviewStrip.tsx (modify)
    src/components/quality/__tests__/OverviewStrip.test.tsx (NEW)
    src/components/quality/QualityOverviewPage.tsx (pass lastComputed timestamp to OverviewStrip)
  </files>
  <read_first>
    - `src/components/quality/OverviewStrip.tsx` (full file, 150 LOC) — current 9-tile layout
    - `src/components/quality/QualityOverviewPage.tsx` — to identify the "last computed" timestamp source (look for `useResourceCountsMetrics` return value; it exposes a computedAt/lastRefreshed field used by the toolbar header). Confirm by reading `src/hooks/useResourceCountsMetrics.ts`.
    - CONTEXT D-01/D-02/D-03/D-04 (locked decisions)
  </read_first>
  <behavior>
    - Test 1: `render(<OverviewStrip summary={{ total: 1234, typeCount: 15, ... }} lastComputed={new Date(Date.now() - 30_000)} isLoading={false} />)` inside a `MantineProvider` + `MemoryRouter` + `QualityMetricsProvider` — `screen.queryByText('Total resources')` returns null inside the SimpleGrid (informational tile gone).
    - Test 2: same render — status line text matches `/1,234 resources · 15 types · Last computed just now/` (Mantine default `toLocaleString()` produces `1,234`).
    - Test 3: same render with `lastComputed={null}` — status line text matches `/0 resources? ·|Last computed Never/`; fallback wording must stay consistent.
    - Test 4: loading state — `render(<OverviewStrip summary={...} isLoading={true} />)` renders exactly 7 `<Skeleton>` rings (not 9) and no status line.
    - Test 5: tile count — after load, the strip contains exactly 7 tiles with ring visuals. Assert via `screen.getAllByRole('button')` (metric tiles are clickable) returning length 7, OR by counting icons from METRIC_ICONS.
    - Test 6: cardinality entries (numbers in status line) are NOT wrapped in any `<a>` / `onClick` handler — non-clickable per D-04. Assert no `role="button"` on the status-line container.
  </behavior>
  <action>
    1. Update `OverviewStripProps` interface in `src/components/quality/OverviewStrip.tsx`:
       ```typescript
       export interface OverviewStripProps {
         summary: CountSummary;
         lastComputed: Date | null;   // NEW — drives the "Last computed {rel}" suffix
         isLoading: boolean;
       }
       ```
    2. Change `GRID_COLS` from `{ base: 1, xs: 2, sm: 3, md: 4, lg: 5, xl: 9 }` to `{ base: 1, xs: 2, sm: 3, md: 4, lg: 5, xl: 7 }` (matches RESEARCH §2.5 recommendation; keeps small-screen wrapping intact).
    3. Delete the two hardcoded informational `<SummaryCard>` blocks at lines 103-112 (Total resources + Resource types). The `METRIC_ORDER.map` block at lines 118-147 is unchanged.
    4. Update the Skeleton fallback at lines 72-78 from `{ length: 9 }` to `{ length: 7 }`.
    5. Remove unused icon imports: `IconDatabase` and `IconListDetails` are no longer referenced (grep after edit to confirm).
    6. Add a `<Stack gap="xs">` wrapper around the existing `<SimpleGrid>` so the status line sits directly above. The status line:
       ```tsx
       import { Stack, Text } from '@mantine/core';
       import { formatRelative } from '../../utils/relativeTime';
       // …
       <Stack gap="xs">
         <Text size="sm" c="dimmed">
           {`${summary.total.toLocaleString()} resources · ${summary.typeCount} types · Last computed ${formatRelative(lastComputed)}`}
         </Text>
         <SimpleGrid cols={GRID_COLS} spacing="sm">
           {/* … existing METRIC_ORDER.map … */}
         </SimpleGrid>
       </Stack>
       ```
       Per D-03 Mantine `Text size='sm' c='dimmed'` + interpunct (`·`, U+00B7) separators; do NOT add icons; single line on desktop, wraps on narrow viewports (default Text behavior).
    7. Edit `src/components/quality/QualityOverviewPage.tsx` to pass `lastComputed` to `<OverviewStrip>`. The timestamp comes from `useResourceCountsMetrics` (already threaded through QualityOverviewPage for the toolbar); check its return shape and wire the equivalent prop. If it exposes `lastRefreshed` or similar, pass that; otherwise use the existing toolbar source.
    8. Create `src/components/quality/__tests__/OverviewStrip.test.tsx` with the 6 tests from `<behavior>`. Mirror the test setup in `src/components/quality/__tests__/RunProgress.test.tsx` (or `SortableTh.test.tsx`) for MantineProvider wrapping conventions; wrap in `MemoryRouter` for `useNavigate` and in `QualityMetricsProvider` for the context reads at line 67.
  </action>
  <verify>
    <automated>npx vitest run src/components/quality/__tests__/OverviewStrip.test.tsx</automated>
    <automated>npx tsc -b --noEmit</automated>
    <automated>grep -n "Total resources\|Resource types" src/components/quality/OverviewStrip.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SummaryCard" src/components/quality/OverviewStrip.tsx` returns 1 (single occurrence inside the `METRIC_ORDER.map` block, not the two deleted informational tiles).
    - `grep -n "IconDatabase\|IconListDetails" src/components/quality/OverviewStrip.tsx` returns zero matches.
    - `grep -n "'just now'\|Last computed" src/components/quality/OverviewStrip.tsx` returns at least one match in the status-line JSX.
    - `grep -n "{ length: 7 }" src/components/quality/OverviewStrip.tsx` returns one match (skeleton count updated).
    - All 6 OverviewStrip tests pass.
    - `tsc -b --noEmit` clean.
  </acceptance_criteria>
  <done>
    OverviewStrip renders 7 metric tiles + a status line per D-02. QualityOverviewPage passes `lastComputed` through. Cardinality entries remain non-clickable (D-04).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PDF layout 9→7 + structural regression tests + 18-UI-SPEC.md update</name>
  <files>
    src/components/quality/PdfReportLayout.tsx (modify)
    src/__tests__/pdf-report-layout.test.tsx (add tests)
    .planning/milestones/v1.2-phases/18-quality-alerting-thresholds/18-UI-SPEC.md (modify in place)
  </files>
  <read_first>
    - `src/components/quality/PdfReportLayout.tsx` lines 240-295 (overview grid region)
    - `src/__tests__/pdf-report-layout.test.tsx` (full file, 226 LOC) — note that it uses structural `screen.getByText` / `queryByText` assertions, NOT snapshots (per RESEARCH §2.6)
    - `.planning/milestones/v1.2-phases/18-quality-alerting-thresholds/18-UI-SPEC.md` — locate the "OverviewStrip" section and the "Component Inventory > Modified Components" row
    - CONTEXT D-05 + RESEARCH §2.5 (UI-SPEC update mechanics)
  </read_first>
  <behavior>
    - New PDF test 1: `'renders 7 metric tiles in overview grid (was 9 pre-Phase-29)'` — count `SummaryCard` descendants inside the overview grid by locating the container with `data-testid` or by querying specific tile labels (Completeness, Coverage, Validation, Plausibility, Lab ranges, Duplicates, References). Assert all 7 present.
    - New PDF test 2: `'does not render informational tiles inside overview grid after 9→7 reduction'` — `queryByText(/^Total resources$/)` scoped inside the overview `Paper` container returns null (the label text below the grid is still present but not as a tile label).
    - New PDF test 3: `'renders status line above overview grid with last-computed timestamp'` — `screen.getByText(/\d+ resources · \d+ types · Last computed/)`.
    - Existing PDF tests that assert `Total resources on server: 1234` and `Distinct resource types: 15` at `pdf-report-layout.test.tsx` ~line 151-152 must continue to pass unchanged (those are the text lines BELOW the grid per RESEARCH §2.5).
  </behavior>
  <action>
    1. Edit `src/components/quality/PdfReportLayout.tsx`:
       - Remove the 2 informational `<SummaryCard>` blocks at lines 258-267 (Total resources + Resource types).
       - Change the grid template at line 253 from `gridTemplateColumns: 'repeat(3, 232px)'` to `gridTemplateColumns: 'repeat(4, 176px)'` (matches the Trends page grid at line 317 that already handles 7 tiles cleanly as 4+3).
       - Keep `METRIC_ORDER.map` block at lines 269-286 unchanged (still renders 7 metric cards).
       - Keep the `<Text>` lines at 288-292 ("Total resources on server: ..." + "Distinct resource types: ...") unchanged — these are NOT tiles; they are the status text below the grid. Add a third `<Text>` above the grid mirroring the dashboard status line: `{total.toLocaleString()} resources · {typeCount} types · Last computed {formatRelative(lastComputed)}` using the shared util from Task 1. Feed `lastComputed` via the `CountSummary` parent prop or add a new optional prop to `PdfReportLayout` (check the existing prop signature and match).
       - Remove unused `IconDatabase` / `IconListDetails` imports if they're still referenced only by the now-deleted tiles (grep after edit).
       - Add a data-testid to the overview grid container for scoped querying in tests: `<Paper … data-testid="pdf-overview-grid">`.
       - Update the JSDoc at line 12 from `3×3 grid of 9 SummaryCards (Total + Types + 7 metric` to `4-col grid of 7 metric SummaryCards; informational totals moved to status line per Phase 29 UX-02`.

    2. Edit `src/__tests__/pdf-report-layout.test.tsx`:
       - Add the 3 new test cases from `<behavior>`. Use `within(screen.getByTestId('pdf-overview-grid'))` to scope the "no informational tile inside grid" assertion.
       - Verify the existing line 151-152 assertions (`Total resources on server: 1234`, `Distinct resource types: 15`) still match — these targets are the unchanged `<Text>` elements below the grid.

    3. Edit `.planning/milestones/v1.2-phases/18-quality-alerting-thresholds/18-UI-SPEC.md` IN PLACE (per CONTEXT D-05 + RESEARCH §2.5):
       - Find the "Component Inventory > Modified Components" section; update the `OverviewStrip` row to note "7 metric tiles + status-line header (Phase 29 UX-02, 2026-04-23); informational totals moved to status line; cardinality entries remain non-clickable."
       - Find the "Layout Contract > OverviewStrip" section heading — change "9 tiles" → "7 tiles". Grid cols: `{ base: 1, xs: 2, sm: 3, md: 4, lg: 5, xl: 7 }`.
       - Renumber tile order: drop original items 1-2 (Total resources, Resource types); original items 3-9 (metric tiles) become new items 1-7.
       - Add a new decision under the "Decisions" section: `**D-36 (2026-04-23 / Phase 29 UX-02):** OverviewStrip reduced 9→7. Total resources + Resource types moved to a status-line header reading "N resources · M types · Last computed {relative-time}" per D-02. Cardinality entries remain non-clickable (preserves UI-SPEC-03 intent). PDF export reflects the same layout (`PdfReportLayout.tsx`).`
       - Append a trailing changelog line at the very end of the file: `*v1.4 Phase 29 UX-02 (2026-04-23): reduced OverviewStrip from 9 tiles to 7; Total resources + Resource types moved to status-line header above the strip.*`
       - Do NOT touch any other decisions (UI-SPEC-01, D-01..D-35 preserved).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/pdf-report-layout.test.tsx</automated>
    <automated>npx tsc -b --noEmit</automated>
    <automated>grep -n "D-36\|Phase 29 UX-02" .planning/milestones/v1.2-phases/18-quality-alerting-thresholds/18-UI-SPEC.md</automated>
    <automated>grep -n "repeat(4, 176px)\|gridTemplateColumns" src/components/quality/PdfReportLayout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SummaryCard" src/components/quality/PdfReportLayout.tsx` drops by 2 (the two info cards removed); only the `METRIC_ORDER.map` block still renders SummaryCards in the overview.
    - All 3 new PDF tests pass; all pre-existing PDF tests still pass.
    - `grep -n "D-36" .planning/milestones/v1.2-phases/18-quality-alerting-thresholds/18-UI-SPEC.md` returns at least one match.
    - `grep -n "Phase 29 UX-02" .planning/milestones/v1.2-phases/18-quality-alerting-thresholds/18-UI-SPEC.md` returns at least 2 matches (D-36 body + trailing changelog).
    - `tsc -b --noEmit` clean.
  </acceptance_criteria>
  <done>
    PDF overview grid matches the dashboard (7 tiles + status line). Structural regression tests lock the layout (no snapshots per RESEARCH §2.6). 18-UI-SPEC.md updated in place with dated D-36 and trailing changelog line — traceable in future diffs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | UX-02 is display-only; no new network calls, no new untrusted inputs, no new auth surface. The dropped informational tiles were pure in-browser rendering of already-trusted data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | I (Information Disclosure) | Status line in OverviewStrip + PDF | accept | The status line only shows aggregate counts (`summary.total`, `summary.typeCount`) already visible in the dashboard toolbar and PDF cover page. No PHI, no identifiers. Same trust class as the tiles being dropped. |
| T-29-01-02 | T (Tampering) | 18-UI-SPEC.md update | accept | Spec file is a planning artifact in `.planning/`, not runtime code. Changes are git-tracked with the PR; D-36 carries a dated trail. No runtime impact. |
| T-29-01-03 | D (Denial of Service) | PDF export with new grid | mitigate | Grid template change (`repeat(3, 232px)` → `repeat(4, 176px)`) keeps the same total tile area; PDF generation time unaffected. Existing html-to-image mock in tests catches regressions. |
</threat_model>

<verification>
1. `npx vitest run src/utils/__tests__/relativeTime.test.ts src/components/quality/__tests__/OverviewStrip.test.tsx src/__tests__/pdf-report-layout.test.tsx` — all new + existing tests green.
2. `npx vitest run` — full suite baseline unchanged or better vs Phase 28 post-commit (22 pre-existing failures, 814+ passing).
3. `npx tsc -b --noEmit` — clean.
4. `grep -rn "function formatRelative" src/` → only `src/utils/relativeTime.ts` (no duplicates in QualityOverviewPage or elsewhere).
5. Manual inspection: `.planning/milestones/v1.2-phases/18-quality-alerting-thresholds/18-UI-SPEC.md` contains `D-36` + trailing `Phase 29 UX-02` changelog line.
</verification>

<success_criteria>
- [ ] `OverviewStrip` renders 7 metric tiles + status line in the live dashboard (verified by OverviewStrip.test.tsx).
- [ ] PDF overview page renders 7 tiles + status line; "Total resources on server" + "Distinct resource types" text lines below the grid unchanged.
- [ ] `formatRelative` extracted to `src/utils/relativeTime.ts`; 6 threshold cases covered by unit tests.
- [ ] `18-UI-SPEC.md` carries `D-36` decision entry AND trailing changelog note attributing the change to Phase 29 UX-02.
- [ ] No TypeScript errors (`tsc -b --noEmit` clean).
- [ ] Full test suite shows no new regressions vs Phase 28 baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/29-backlog-ux/29-01-SUMMARY.md` capturing:
- Files modified (with line-count delta)
- Test cases added (new + updated counts)
- Grep/tsc verification output snippets
- Any divergences from the locked decisions (there should be none — if there are, flag for replanning)
- Handoff note for Plan 29-02: "OverviewStrip + PDF + 18-UI-SPEC stable; `ValidationPanel.tsx:167-178` inline OperationOutcome mapper and `ValidationPanel.tsx:72,100-107,235,270-294` PHI gate are UNCHANGED — ready for Plan 29-02 extraction."
</output>
