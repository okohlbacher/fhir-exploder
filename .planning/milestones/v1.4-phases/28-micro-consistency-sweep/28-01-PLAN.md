---
phase: 28-micro-consistency-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useReferenceReport.ts
  - src/hooks/useConformanceRun.ts
  - src/quality/contentHasher.ts
  - src/quality/temporalPlausibilityWalker.ts
  - src/quality/temporalPlausibilityWalker.test.ts
  - src/quality/orphanDetector.ts
  - src/quality/profileConformanceChecker.ts
  - src/quality/codingCoverageWalker.ts
  - src/quality/completenessWalker.ts
  - src/__tests__/coding-coverage-walker.test.ts
  - src/components/patients/PatientListPage.tsx
  - src/components/quality/PlausibilityDrillDown.tsx
  - src/components/quality/ResourceIssueTable.tsx
autonomous: true
requirements:
  - SWEEP-01
  - SWEEP-02

must_haves:
  truths:
    - "Every `as unknown as Record<string, unknown>` cast in src/ (except the single site inside the toRecord helper's own body) is replaced with a call to toRecord(x)."
    - "ASCII double-dash `--` used as a separator or numeric-range glyph in JSX text and user-facing comment in PlausibilityDrillDown.tsx and ResourceIssueTable.tsx is replaced with the correct unicode dash (em-dash `—` for separators, en-dash `–` for numeric ranges)."
    - "No new eslint-disable comments are introduced; no behavior change; no test additions beyond cast-site migrations in existing test files."
  artifacts:
    - path: "src/utils/fhir-helpers.ts"
      provides: "toRecord helper (unchanged signature OR extended with a generic-widening overload if the sweep requires it)"
      contains: "export function toRecord"
    - path: "src/components/quality/PlausibilityDrillDown.tsx"
      provides: "Em-dash separator in title + header comment"
      contains: "—"
    - path: "src/components/quality/ResourceIssueTable.tsx"
      provides: "En-dash numeric range in pagination status"
      contains: "–"
  key_links:
    - from: "src/hooks/useReferenceReport.ts (and 10 other cast-site files)"
      to: "src/utils/fhir-helpers.ts"
      via: "import { toRecord } from '../utils/fhir-helpers' (path relative per file)"
      pattern: "toRecord\\("
---

<objective>
Mechanical sweep of 23 `as unknown as Record<string, unknown>` cast sites across 12 files to use the existing `toRecord` helper at `src/utils/fhir-helpers.ts:9`, plus a narrow fix of 3-4 ASCII double-dash (`--`) sites in JSX text / user-facing strings in `PlausibilityDrillDown.tsx` and `ResourceIssueTable.tsx` to the correct unicode dash glyphs.

Purpose: Close SWEEP-01 (goal grep: `grep -rn 'as unknown as Record<string, unknown>' src/ --include='*.ts*' | wc -l` returns ≤1 — the single site inside the helper's own body) and the narrow, acceptance-shaped part of SWEEP-02 (ASCII `--` in JSX text content, NOT the 635 idiomatic unicode em-dashes already in src/).

Output: 11 files touched with `toRecord(...)` replacements + 2 files with ASCII `--` replaced by the correct unicode glyph. No behavior change. `npx tsc -b --noEmit` + `npx eslint src/` pass.

Scope boundary — SWEEP-02 in this plan covers ONLY:
- `src/components/quality/PlausibilityDrillDown.tsx` (header comment + JSX title)
- `src/components/quality/ResourceIssueTable.tsx` (pagination numeric range `{start}--{end}`)

DO NOT attempt a mass migration of the 635 unicode em-dashes already in the repo — those are idiomatic and consistent per RESEARCH.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-micro-consistency-sweep/28-CONTEXT.md
@.planning/phases/28-micro-consistency-sweep/28-RESEARCH.md

Source files to read for interface/exports before editing:
@src/utils/fhir-helpers.ts

<interfaces>
<!-- From src/utils/fhir-helpers.ts (verified 2026-04-23): -->
```typescript
export function toRecord(resource: Resource): Record<string, unknown> {
  return resource as unknown as Record<string, unknown>;
}
```

The helper accepts `Resource` (from `@medplum/fhirtypes`). All 22 non-helper cast sites either:
(a) cast a value whose TypeScript type is already `Resource` (most hooks, most walkers) — straight swap `(x as unknown as Record<string, unknown>)` → `toRecord(x)`;
(b) cast a value of a broader/narrower type (e.g., `unknown`, `ElementDefinition`, `ValueSet.compose.include[i]`) — requires a helper extension decision (see Task 1 action).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 22 cast sites with toRecord (SWEEP-01)</name>
  <files>
    src/hooks/useReferenceReport.ts,
    src/hooks/useConformanceRun.ts,
    src/quality/contentHasher.ts,
    src/quality/temporalPlausibilityWalker.ts,
    src/quality/temporalPlausibilityWalker.test.ts,
    src/quality/orphanDetector.ts,
    src/quality/profileConformanceChecker.ts,
    src/quality/codingCoverageWalker.ts,
    src/quality/completenessWalker.ts,
    src/__tests__/coding-coverage-walker.test.ts,
    src/components/patients/PatientListPage.tsx
  </files>
  <read_first>
    - `src/utils/fhir-helpers.ts` — confirm the exported signature `toRecord(resource: Resource): Record<string, unknown>`.
    - Run `grep -rn 'as unknown as Record<string, unknown>' src/ --include='*.ts*'` to enumerate the live site list (23 occurrences confirmed by scout 2026-04-23: 22 to migrate + 1 inside the helper's own body at `src/utils/fhir-helpers.ts:10` — DO NOT edit that site).
    - For each cast-site file: read the file header to determine the correct relative import path for `toRecord` (e.g., `../utils/fhir-helpers`, `../../utils/fhir-helpers`).
  </read_first>
  <action>
    Per D-01, D-02, D-03 of 28-CONTEXT.md and SWEEP-01 of REQUIREMENTS.md:

    1. For each of the 11 files listed in `<files>` above (all 22 non-helper cast sites live here), grep-find each `as unknown as Record<string, unknown>` occurrence and replace with `toRecord(X)` where X is the source expression.

       Examples (from verified 2026-04-23 grep):
       - `src/hooks/useReferenceReport.ts:79`
         - Before: `(r as unknown as Record<string, unknown>).id as string | undefined;`
         - After: `toRecord(r).id as string | undefined;`
       - `src/quality/orphanDetector.ts:109`
         - Before: `const value = (r as unknown as Record<string, unknown>)[fieldName];`
         - After: `const value = toRecord(r)[fieldName];`
       - `src/quality/contentHasher.ts:54`
         - Before: `...(resource as unknown as Record<string, unknown>),`
         - After: `...toRecord(resource),`

    2. Add the import to each touched file if not already present:
       - From `src/hooks/*` files: `import { toRecord } from '../utils/fhir-helpers';`
       - From `src/quality/*` files: `import { toRecord } from '../utils/fhir-helpers';`
       - From `src/__tests__/*` files: `import { toRecord } from '../utils/fhir-helpers';`
       - From `src/components/patients/PatientListPage.tsx`: `import { toRecord } from '../../utils/fhir-helpers';`
       Insert next to existing imports from `../utils/*` or `../../utils/*`; preserve alphabetical/group ordering.

    3. **EXCEPTION — DO NOT edit `src/utils/fhir-helpers.ts`.** The cast on line 10 is inside the helper's own body (`return resource as unknown as Record<string, unknown>;`). Self-refactoring would create infinite recursion. The line-5 comment mentioning the pattern is also fine and should be left alone (it documents the helper).

    4. **Signature-mismatch handling (per D-02):** For each cast site, verify the source expression's TypeScript type is assignable to `Resource` (the helper's parameter type). Expected matches (safe straight swap):
       - `r: Resource`, `resource: Resource`, `batch[j]: Resource`, `r: Condition`, `r: Observation`, etc. (FHIR resource types) — all safe.
       - Any value typed as `Resource | undefined` — wrap in a null-check or keep the existing narrowing that was already there pre-refactor.

       If ANY site's source is NOT assignable to `Resource` (e.g., `ElementDefinition` inside `profileConformanceChecker.ts`, which is a FHIR *type* but not a `Resource`), you have two options per D-02:

       **Option A (preferred if ≥2 sites need it):** Extend the helper with a generic overload in `src/utils/fhir-helpers.ts`:
       ```typescript
       export function toRecord(resource: Resource): Record<string, unknown>;
       export function toRecord<T extends object>(value: T): Record<string, unknown>;
       export function toRecord(value: object): Record<string, unknown> {
         return value as unknown as Record<string, unknown>;
       }
       ```
       Add a comment: `/* Generic overload added in Phase 28 SWEEP-01 for non-Resource call sites (ElementDefinition, etc.). */`

       **Option B (preferred if 0-1 sites need it):** Leave those specific sites untouched with an inline comment `// Intentional: toRecord expects Resource; this is an ElementDefinition sub-object.` and note in the summary which sites were skipped and why.

       The profileConformanceChecker.ts sites at lines 139/142/143/144/147 are the most likely to need this — inspect the type of `el` at those lines before choosing Option A vs B.

       **Pick ONE approach for the whole plan and stick to it.** Do not mix.

    5. Post-edit verification (run after all 11 files are edited):
       - `grep -rn 'as unknown as Record<string, unknown>' src/ --include='*.ts*' | wc -l` must return either `1` (if the helper body is the only remaining site and Option A was chosen) or `2` (if Option A added a duplicate inside the overload body — that is also acceptable). Any number >2 is a regression.
       - `npx tsc -b --noEmit` must pass with zero new errors.

    6. Commit: one atomic commit titled `refactor(28-01): migrate 22 cast sites to toRecord helper (SWEEP-01)`. Mechanical replacement, blast radius zero.
  </action>
  <verify>
    <automated>bash -c 'set -e; COUNT=$(grep -rn "as unknown as Record&lt;string, unknown&gt;" src/ --include="*.ts*" | wc -l | tr -d " "); echo "Remaining cast sites: $COUNT"; if [ "$COUNT" -gt 2 ]; then echo "FAIL: expected ≤2 (helper body and optional overload body); got $COUNT"; exit 1; fi; npx tsc -b --noEmit'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'as unknown as Record<string, unknown>' src/ --include='*.ts*' | wc -l` returns ≤2 (1 for helper body; +1 if generic overload body added).
    - The only remaining occurrence(s) are in `src/utils/fhir-helpers.ts`.
    - `npx tsc -b --noEmit` passes.
    - Every modified file has `import { toRecord } from '.../utils/fhir-helpers';` at the top (path relative).
    - No new eslint-disable comments introduced (`git diff src/ | grep -c 'eslint-disable' | ...` no increase).
  </acceptance_criteria>
  <done>
    - 22 cast sites replaced with `toRecord(...)` calls in 11 files.
    - 1 site remains in `src/utils/fhir-helpers.ts:10` (helper body) + optionally 1 in the generic overload body.
    - TypeScript compiles; no new lint errors; no test failures beyond the pre-existing 22 pinned failures (post-Phase 27 baseline).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix ASCII double-dash in JSX text (SWEEP-02 narrow scope)</name>
  <files>
    src/components/quality/PlausibilityDrillDown.tsx,
    src/components/quality/ResourceIssueTable.tsx
  </files>
  <read_first>
    - `src/components/quality/PlausibilityDrillDown.tsx` lines 1-50 — confirm the header comment at line 2 contains `PlausibilityDrillDown -- /quality/plausibility/:type sub-page.` and JSX at line 38 contains `${type} -- Plausibility drill-down`.
    - `src/components/quality/ResourceIssueTable.tsx` lines 155-210 — confirm line 160 contains `Showing {start}--{end} of {filtered.length} issues` and line 202 contains `<Code>--</Code>` (note: line 202 is DISPLAYED INSIDE A `<Code>` element as a literal example of the CLI-style dash — DO NOT edit that one; it is intentional code-font rendering).
    - Run `grep -n ' -- ' src/components/quality/PlausibilityDrillDown.tsx src/components/quality/ResourceIssueTable.tsx` to verify exact line numbers before editing.
  </read_first>
  <action>
    Per D-04, D-05, D-06 of 28-CONTEXT.md and the narrow SWEEP-02 scope confirmed in 28-RESEARCH.md:

    Convention (D-04): em-dash `—` (U+2014) for separators, en-dash `–` (U+2013) for numeric ranges. ASCII `--` is the glyph being eliminated in this plan.

    1. **`src/components/quality/PlausibilityDrillDown.tsx`:**
       - Line 2 (header comment): change `PlausibilityDrillDown -- /quality/plausibility/:type sub-page.` to `PlausibilityDrillDown — /quality/plausibility/:type sub-page.` (em-dash separator).
       - Line 38 (JSX title attribute): change `${type} -- Plausibility drill-down` to `${type} — Plausibility drill-down` (em-dash separator).

    2. **`src/components/quality/ResourceIssueTable.tsx`:**
       - Line 160 (JSX pagination status): change `Showing {start}--{end} of {filtered.length} issues` to `Showing {start}–{end} of {filtered.length} issues` (en-dash, U+2013, numeric range).
       - Line 202 (`<Code>--</Code>`): **DO NOT EDIT.** This renders the literal two-hyphen string inside a code-font element as a visual example (likely a "null value" or "missing data" indicator). Leave it.
       - Line 2 (header comment `ResourceIssueTable -- Phase 15, Plan 02.`): **OPTIONAL** — change to em-dash `—` for internal consistency with PlausibilityDrillDown. Not acceptance-required, but do it for consistency.

    3. **Do NOT grep-sweep other files.** This plan's SWEEP-02 scope is limited to the two files above per 28-RESEARCH.md §SWEEP-02. The 635 existing unicode em-dashes are idiomatic; do not touch them. PlausibilityDrillDown is in this plan (not 28-02) so 28-02 can touch that file for SWEEP-03 sequentially without conflict — **wait, actually there IS overlap: this task touches PlausibilityDrillDown.tsx and 28-02 also touches it for SWEEP-03.** The plan-level `depends_on: [28-01]` in 28-02 enforces sequential execution, so no parallel-write conflict.

    4. Verification greps (run post-edit):
       - `grep -n ' -- ' src/components/quality/PlausibilityDrillDown.tsx` returns 0 lines (besides the intentional `<Code>--</Code>` which is in ResourceIssueTable, not here).
       - `grep -n '{start}--{end}' src/components/quality/ResourceIssueTable.tsx` returns 0 lines.
       - `npx tsc -b --noEmit` passes (dash glyph change is string-level, should not affect types).
       - Existing UI snapshots for these components should still pass — if they contain the old ASCII `--` literally, they will fail and need the snapshot updated (run `npx vitest --run --update` scoped to the affected test files only).

    5. Commit: one atomic commit titled `refactor(28-01): replace ASCII -- with unicode em/en-dash in JSX text (SWEEP-02 narrow)`.
  </action>
  <verify>
    <automated>bash -c 'set -e; P_MATCHES=$(grep -cE "\-\-" src/components/quality/PlausibilityDrillDown.tsx || true); R_RANGE=$(grep -c "{start}--{end}" src/components/quality/ResourceIssueTable.tsx || true); echo "PlausibilityDrillDown -- count: $P_MATCHES (expect 0)"; echo "ResourceIssueTable {start}--{end} count: $R_RANGE (expect 0)"; if [ "$P_MATCHES" -gt 0 ]; then echo "FAIL: PlausibilityDrillDown still has --"; exit 1; fi; if [ "$R_RANGE" -gt 0 ]; then echo "FAIL: ResourceIssueTable still has --"; exit 1; fi; npx tsc -b --noEmit'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n ' -- ' src/components/quality/PlausibilityDrillDown.tsx` returns 0 matches.
    - `grep -n '{start}--{end}' src/components/quality/ResourceIssueTable.tsx` returns 0 matches.
    - The intentional `<Code>--</Code>` on ResourceIssueTable line 202 remains unchanged (grep-check: `grep -c '<Code>--</Code>' src/components/quality/ResourceIssueTable.tsx` returns 1).
    - TypeScript compiles.
    - No snapshot regressions OR snapshots updated deliberately via `--update`.
  </acceptance_criteria>
  <done>
    - 3 ASCII `--` sites replaced with the correct unicode glyph (em-dash at Plausibility:2 and Plausibility:38; en-dash at ResourceIssueTable:160).
    - Optional: ResourceIssueTable:2 header comment upgraded to em-dash for consistency.
    - `<Code>--</Code>` preserved at ResourceIssueTable:202.
    - Compiles and tests pass at the post-Phase-27 baseline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches 12 files and performs ONLY type-level refactors (cast → helper call) and string-literal glyph swaps (`--` → `—`/`–`). No new inputs, no new sinks, no changes to auth/PHI/data fetch paths. ASVS L1 scope: no applicable trust boundaries change.

| Boundary | Description |
|----------|-------------|
| (none crossed) | This is a pure refactor — same data, same types (widened to `Record<string, unknown>` at exactly the same places), same rendered UI strings (different glyph, same visual intent). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Tampering | `toRecord` helper | accept | Helper returns the same widened view it did at each call site pre-refactor. No new input surface; no new exported APIs consumed at trust boundaries. |
| T-28-01-02 | Information Disclosure | JSX dash glyph swap | accept | Changing `--` to `—`/`–` is a rendering change only; no data included or excluded from any rendered field. The two affected strings (`${type} — Plausibility drill-down`, `Showing {start}–{end} of {filtered.length} issues`) still render the same FHIR resource type and pagination counts they did before. |
| T-28-01-03 | Elevation of Privilege | Generic `toRecord` overload (if Option A is chosen) | accept | Adding a generic `<T extends object>` overload widens *compile-time* type acceptance only. At runtime it executes the identical `as unknown as Record<string, unknown>` cast that each call site performed inline pre-refactor. No new execution paths; no new sinks. |
</threat_model>

<verification>
Overall phase checks after Task 1 + Task 2:

1. **SWEEP-01 grep check:** `grep -rn 'as unknown as Record<string, unknown>' src/ --include='*.ts*' | wc -l` returns ≤2.
2. **SWEEP-02 grep check:** `grep -n ' -- ' src/components/quality/PlausibilityDrillDown.tsx src/components/quality/ResourceIssueTable.tsx` returns 0 matches (excluding the preserved `<Code>--</Code>`).
3. **Typecheck:** `npx tsc -b --noEmit` passes with zero new errors.
4. **Lint:** `npx eslint src/ --max-warnings=0` holds at the post-Phase-27 baseline (no new warnings introduced by the refactor).
5. **Test suite:** `npx vitest --run` passes with no regressions beyond the 22 pre-existing pinned failures.
</verification>

<success_criteria>
- SWEEP-01 acceptance: 22 cast sites migrated to `toRecord(...)`; only the helper body (±1 generic overload body) retains the raw cast.
- SWEEP-02 acceptance (narrow scope): 3 JSX-text ASCII `--` sites converted to the correct unicode dash glyph; intentional `<Code>--</Code>` preserved.
- No behavior change: build green, tests at baseline, no new lint warnings.
- Both requirements appear on this plan's frontmatter `requirements` field: `[SWEEP-01, SWEEP-02]`.
- Plan 28-02 may start after this plan's summary file exists.
</success_criteria>

<output>
After completion, create `.planning/phases/28-micro-consistency-sweep/28-01-SUMMARY.md` covering:
- Which of the 22 cast sites needed the generic overload (if Option A was chosen) vs straight swap.
- The exact SWEEP-02 edits landed (line numbers pre/post).
- Final grep counts for both acceptance checks.
- Any test snapshot updates required.
</output>
