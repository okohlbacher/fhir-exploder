---
phase: 26-app-shell-dedup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/ConnectionGatedOutlet.tsx
  - src/components/layout/__tests__/ConnectionGatedOutlet.test.tsx
  - src/components/quality/__tests__/quality-layout.test.tsx
  - src/components/quality/QualityLayout.tsx
  - src/components/explorer/ExplorerLayout.tsx
  - src/components/patients/PatientsLayout.tsx
autonomous: true
requirements: [SHELL-01]
tags: [react, mantine, react-router, layout, refactor, render-parity]

must_haves:
  truths:
    - "ConnectionGatedOutlet exists as a render-prop primitive and exposes a function-of-connected API"
    - "The three layouts (Explorer, Patients, Quality) no longer contain the 'Not connected' alert block"
    - "Connected state renders Outlet; disconnected state renders a single canonical alert with a link to '/'"
    - "QualityLayout still invokes migrateLegacyResourceTypeKey on mount and removes LEGACY_COHORT_KEY"
    - "ExplorerLayout and PatientsLayout each reach <=30 LOC; QualityLayout lands at a documented 'soft-miss' LOC with the legacy-migration essay still inline"
    - "MedplumProvider stays in AppLayout.tsx (NOT moved into ConnectionGatedOutlet)"
  artifacts:
    - path: "src/components/layout/ConnectionGatedOutlet.tsx"
      provides: "Render-prop primitive for connection-gated routing"
      exports: ["ConnectionGatedOutlet"]
    - path: "src/components/layout/__tests__/ConnectionGatedOutlet.test.tsx"
      provides: "Structural tests for the primitive (connected -> Outlet; disconnected -> alert)"
    - path: "src/components/quality/__tests__/quality-layout.test.tsx"
      provides: "Pre-migration snapshot of QualityLayout legacy-migration behavior"
      contains: "migrateLegacyResourceTypeKey"
    - path: "src/components/explorer/ExplorerLayout.tsx"
      provides: "Explorer layout using ConnectionGatedOutlet"
    - path: "src/components/patients/PatientsLayout.tsx"
      provides: "Patients layout using ConnectionGatedOutlet"
    - path: "src/components/quality/QualityLayout.tsx"
      provides: "Quality layout using ConnectionGatedOutlet with legacy-migration essay preserved"
  key_links:
    - from: "src/components/explorer/ExplorerLayout.tsx"
      to: "src/components/layout/ConnectionGatedOutlet.tsx"
      via: "import { ConnectionGatedOutlet }"
      pattern: "ConnectionGatedOutlet"
    - from: "src/components/patients/PatientsLayout.tsx"
      to: "src/components/layout/ConnectionGatedOutlet.tsx"
      via: "import { ConnectionGatedOutlet }"
      pattern: "ConnectionGatedOutlet"
    - from: "src/components/quality/QualityLayout.tsx"
      to: "src/components/layout/ConnectionGatedOutlet.tsx"
      via: "import { ConnectionGatedOutlet }"
      pattern: "ConnectionGatedOutlet"
---

<objective>
Extract the triplicated "Not connected" alert block out of ExplorerLayout / PatientsLayout / QualityLayout into a single `<ConnectionGatedOutlet>` render-prop primitive, and migrate the three layouts to consume it.

**Research correction honored:** `quality-layout.test.tsx` does NOT currently exist under `src/components/quality/__tests__/` despite being cited in SHELL-01 acceptance as "passes unchanged". Task 1 (Wave 0 of this plan) creates this test FIRST — asserting the legacy-migration behavior of the current QualityLayout — so the SHELL-01 migration in Task 3 has a real regression fence.

**Research correction honored (LOC soft-miss):** ROADMAP says "each layout <=30 LOC". RESEARCH.md §Focus 1 shows QualityLayout's 119 LOC is dominated by a ~50-LOC legacy-migration `useEffect` essay (lines 32-82) that is load-bearing until Phase 28 SWEEP-04 removes it. ExplorerLayout and PatientsLayout CAN hit <=30 LOC. QualityLayout will land around 55 LOC post-migration — the gate-duplication is gone, the legacy-migration essay remains. This is a documented soft-miss with a Phase 28 SWEEP-04 handoff, NOT a failure.

Purpose: Collapse 3 byte-identical alert blocks into one primitive; preserve render parity; establish the ConnectionGatedOutlet render-prop pattern for future layouts.
Output: One new primitive, one new test for the primitive, one new test for QualityLayout's legacy-migration, three migrated layouts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-app-shell-dedup/26-CONTEXT.md
@.planning/phases/26-app-shell-dedup/26-RESEARCH.md
@.planning/phases/25-quality-module-dedup/25-03-SUMMARY.md
@src/components/layout/AppLayout.tsx
@src/components/quality/QualityLayout.tsx
@src/components/explorer/ExplorerLayout.tsx
@src/components/patients/PatientsLayout.tsx
@src/components/quality/DrillDownShell.tsx

<interfaces>
<!-- Precedent: DrillDownShell render-prop pattern from Phase 25 QDDEP-02.
     ConnectionGatedOutlet mirrors its structure: a minimal primitive with a render-prop API,
     co-located with its test file. -->

Target API (from D-01, D-02):

```typescript
// src/components/layout/ConnectionGatedOutlet.tsx
import { ReactNode } from 'react';
import { Outlet } from 'react-router-dom';

export interface ConnectionGatedOutletProps {
  /** Function receiving `connected` boolean; caller decides what to render on each branch.
   *  By default, connected -> <Outlet />, disconnected -> canonical alert. */
  render?: (connected: boolean) => ReactNode;
}

export function ConnectionGatedOutlet(props: ConnectionGatedOutletProps): JSX.Element;
```

Canonical "Not connected" alert copy (D-04 — identical across all 3 layouts per RESEARCH.md §Focus 1):
"Not connected to a FHIR server. Return to the dashboard to connect."
with a `<Link to="/">Return to dashboard</Link>` (Mantine Alert, icon variant to be matched from QualityLayout:84-103).

Connection state source: read via `useMedplumContext()` hook from `@medplum/react` — check `medplum` client and its connection status. Match whatever check the 3 layouts currently use (all 3 are identical per RESEARCH.md Focus 1 — pick QualityLayout:84-103 as canonical).
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Create quality-layout.test.tsx pre-migration snapshot</name>
  <files>src/components/quality/__tests__/quality-layout.test.tsx</files>
  <read_first>
    - src/components/quality/QualityLayout.tsx (FULL file — know exactly what legacy-migration does)
    - src/utils/mii-modules.ts and any file defining `migrateLegacyResourceTypeKey` / `LEGACY_COHORT_KEY` (grep for these symbols)
    - src/components/quality/__tests__/DrillDownShell.test.tsx (existing vitest pattern in this directory — mirror its setup: MemoryRouter + MantineProvider wrappers)
    - src/components/quality/__tests__/SortableTh.test.tsx (another pattern reference for the quality test dir)
  </read_first>
  <behavior>
    This test is a regression fence: it asserts the CURRENT (pre-migration) behavior of QualityLayout so the SHELL-01 migration in Task 3 cannot break it silently.

    Test cases to create:
    - Test 1 (legacy-migration on mount): Render QualityLayout wrapped in MemoryRouter + MantineProvider with a connected Medplum client stub. Seed localStorage with `LEGACY_COHORT_KEY` set to some value. Assert: after mount, `localStorage.getItem(LEGACY_COHORT_KEY)` returns null (migration removed it). Also spy on `migrateLegacyResourceTypeKey` (via `vi.spyOn`) and assert it was called once.
    - Test 2 (connected renders Outlet): Render QualityLayout at the Quality route with a connected Medplum client; assert that the disconnected alert is NOT present (query by role="alert" with text matching /Not connected/i — should be null). Also assert a test-id stub Outlet child (e.g. route children = `<Routes><Route path="/quality" element={<QualityLayout />}><Route index element={<div data-testid="outlet-child">child</div>} /></Route></Routes>`) renders.
    - Test 3 (disconnected shows alert): Render QualityLayout with a disconnected Medplum client stub. Assert: alert with role="alert" and text /Not connected/i exists; link to "/" (Return to dashboard) is present; Outlet child `data-testid="outlet-child"` is NOT rendered.
    - Test 4 (QualityMetricsProvider present when connected): Assert that the connected-state tree includes QualityMetricsProvider (e.g. by asserting a context-consumer test component can read a default value). If direct provider detection is awkward, skip this assertion — focus on legacy-migration + alert presence.

    Use `vi.mock` for @medplum/react `useMedplumContext` to toggle connection state. Use `beforeEach` to reset localStorage and spies.

    The test MUST pass against the CURRENT QualityLayout.tsx (pre-migration). Run it first to confirm green before moving to Task 2.
  </behavior>
  <action>
    Create `src/components/quality/__tests__/quality-layout.test.tsx` with the 3-4 test cases above. Use vitest (`describe` / `it` / `expect` / `vi`). Use `@testing-library/react` `render` + `screen` + `queryByRole` / `getByTestId`. Wrap in MemoryRouter + MantineProvider exactly as DrillDownShell.test.tsx does.

    Mock `useMedplumContext` (from `@medplum/react-hooks`) to return `{ medplum: <stub-client>, profile: undefined }` where `<stub-client>` is either a connected-looking object or undefined depending on the test case. Use whatever connection-check pattern QualityLayout currently uses (grep QualityLayout.tsx for the check — likely `medplum` truthiness or a `connected` derivation); the test must mirror that.

    If `migrateLegacyResourceTypeKey` lives in a module import, use `vi.spyOn(module, 'migrateLegacyResourceTypeKey')`. If it's defined inline in QualityLayout.tsx, test the observable effect only (localStorage clearing).

    Run `npm test -- --run src/components/quality/__tests__/quality-layout.test.tsx` — MUST be green against the CURRENT QualityLayout.tsx.
  </action>
  <verify>
    <automated>npm test -- --run src/components/quality/__tests__/quality-layout.test.tsx</automated>
  </verify>
  <done>
    - File `src/components/quality/__tests__/quality-layout.test.tsx` exists.
    - `grep -c "migrateLegacyResourceTypeKey\|LEGACY_COHORT_KEY" src/components/quality/__tests__/quality-layout.test.tsx` returns >= 2.
    - `grep -c "Not connected" src/components/quality/__tests__/quality-layout.test.tsx` returns >= 1.
    - `npm test -- --run src/components/quality/__tests__/quality-layout.test.tsx` reports 3-4 passing tests against the pre-migration QualityLayout.
    - Commit: `test(26): add quality-layout legacy-migration regression test (SHELL-01)` — atomic, BEFORE Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ConnectionGatedOutlet primitive + its structural tests</name>
  <files>
    src/components/layout/ConnectionGatedOutlet.tsx,
    src/components/layout/__tests__/ConnectionGatedOutlet.test.tsx
  </files>
  <read_first>
    - src/components/quality/QualityLayout.tsx:84-103 (canonical alert block per D-04)
    - src/components/explorer/ExplorerLayout.tsx:24-43 (verify byte-identical)
    - src/components/patients/PatientsLayout.tsx:28-47 (verify byte-identical)
    - src/components/quality/DrillDownShell.tsx (render-prop + test-file precedent — mirror this style)
    - src/components/quality/__tests__/DrillDownShell.test.tsx (test setup precedent)
    - src/components/layout/AppLayout.tsx (confirm MedplumProvider placement — D-02 says it STAYS here, NOT moved)
  </read_first>
  <behavior>
    ConnectionGatedOutlet is a render-prop primitive. It owns only the "Not connected" alert — it does NOT wrap MedplumProvider.

    Tests (vitest + @testing-library/react, MemoryRouter + MantineProvider wrappers):
    - Test 1 (disconnected shows alert): Mock `useMedplumContext` to return disconnected state. Render `<ConnectionGatedOutlet />` at `/foo`. Assert: role="alert" element with text /Not connected/i is rendered; link to "/" exists; NO Outlet content (no `<Routes>` children-index rendered).
    - Test 2 (connected shows Outlet): Mock connected state. Render inside `<Routes><Route path="/foo" element={<ConnectionGatedOutlet />}><Route index element={<div data-testid="outlet-child" />} /></Route></Routes>`. Assert: `outlet-child` testid is in the document; no /Not connected/i alert.
    - Test 3 (render prop override, connected): `<ConnectionGatedOutlet render={(connected) => connected ? <div data-testid="custom-connected" /> : <div data-testid="custom-disconnected" />} />`. Assert connected branch renders custom node.
    - Test 4 (render prop override, disconnected): Same but disconnected; assert custom-disconnected renders and default alert is absent.

    (Tests establish the contract: default behavior = alert-or-Outlet; render-prop API = full override.)
  </behavior>
  <action>
    Create `src/components/layout/ConnectionGatedOutlet.tsx`:

    ```typescript
    import { ReactNode } from 'react';
    import { Outlet, Link } from 'react-router-dom';
    import { Alert, ... } from '@mantine/core';
    import { useMedplumContext } from '@medplum/react-hooks';
    // ... icon import matching QualityLayout:84-103

    export interface ConnectionGatedOutletProps {
      render?: (connected: boolean) => ReactNode;
    }

    export function ConnectionGatedOutlet({ render }: ConnectionGatedOutletProps): JSX.Element {
      const { medplum } = useMedplumContext();
      // Connection check — match whatever QualityLayout/ExplorerLayout/PatientsLayout currently use.
      // Per RESEARCH.md §Focus 1 all 3 are byte-identical; read QualityLayout.tsx:84-103 for the exact check.
      const connected = Boolean(medplum); // <- adjust to match existing layouts' check

      if (render) return <>{render(connected)}</>;
      if (connected) return <Outlet />;
      return (
        <Alert /* canonical props from QualityLayout:84-103 */>
          Not connected to a FHIR server. Return to the dashboard to connect.
          <Link to="/">Return to dashboard</Link>
        </Alert>
      );
    }
    ```

    The alert JSX must be COPIED VERBATIM from `QualityLayout.tsx:84-103` (per D-04). Do not rewrite the copy or restructure the icon props. This preserves render parity.

    Create `src/components/layout/__tests__/ConnectionGatedOutlet.test.tsx` with the 4 test cases above. Mirror `DrillDownShell.test.tsx` structure for MantineProvider + MemoryRouter wrapping + `vi.mock('@medplum/react-hooks', ...)`.

    Do NOT migrate the 3 layouts in this task. This task produces the primitive + its tests ONLY.

    Run `npm test -- --run src/components/layout/__tests__/ConnectionGatedOutlet.test.tsx` — MUST be green.
  </action>
  <verify>
    <automated>npm test -- --run src/components/layout/__tests__/ConnectionGatedOutlet.test.tsx</automated>
  </verify>
  <done>
    - `src/components/layout/ConnectionGatedOutlet.tsx` exists with named export `ConnectionGatedOutlet`.
    - `grep -c "ConnectionGatedOutlet" src/components/layout/ConnectionGatedOutlet.tsx` returns >= 2 (export + interface).
    - `grep -c "MedplumProvider" src/components/layout/ConnectionGatedOutlet.tsx` returns 0 (D-02 — provider STAYS in AppLayout, not moved).
    - `grep -c "Outlet\|Link" src/components/layout/ConnectionGatedOutlet.tsx` returns >= 2 (both imported).
    - `src/components/layout/__tests__/ConnectionGatedOutlet.test.tsx` passes with 4 tests.
    - `npx tsc -b --noEmit` passes.
    - Commit: `feat(26): add ConnectionGatedOutlet render-prop primitive (SHELL-01)` — atomic.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate ExplorerLayout, PatientsLayout, QualityLayout to consume ConnectionGatedOutlet</name>
  <files>
    src/components/explorer/ExplorerLayout.tsx,
    src/components/patients/PatientsLayout.tsx,
    src/components/quality/QualityLayout.tsx
  </files>
  <read_first>
    - The 3 layout files in their entirety (ExplorerLayout 57 LOC, PatientsLayout 61 LOC, QualityLayout 119 LOC per CONTEXT.md)
    - The newly-created `ConnectionGatedOutlet.tsx` (know the API you just built)
    - The newly-created `quality-layout.test.tsx` (know what you must not break)
    - CONTEXT.md D-02 (MedplumProvider stays at AppLayout root — the 3 layouts must NOT wrap MedplumProvider; any existing MedplumProvider wrapping inside the 3 layouts must stay or move per current file structure — verify by reading AppLayout.tsx)
  </read_first>
  <action>
    **ExplorerLayout.tsx** (target: <=30 LOC):
    - Delete the "Not connected" alert block (lines 24-43 per RESEARCH.md §Focus 3).
    - Replace the connection-branching JSX with `<ConnectionGatedOutlet />`. If the layout has NO per-route decoration (e.g. just renders the gate then Outlet), the default render-prop-less usage suffices.
    - Remove now-unused imports (Alert, AlertTriangle icon, Link — keep only what the post-migration JSX uses).
    - Keep any ExplorerLayout-specific decoration (if any exists outside the gate) by using the `render` prop: `<ConnectionGatedOutlet render={(connected) => connected ? <div className="explorer-specific"><Outlet/></div> : undefined} />`. RESEARCH.md §Focus 1 line-by-line table says ExplorerLayout has no such decoration — verify and use default form.
    - Target: <=30 LOC.

    **PatientsLayout.tsx** (target: <=30 LOC):
    - Same treatment as ExplorerLayout: delete alert block (lines 28-47 per §Focus 3), replace with `<ConnectionGatedOutlet />`.
    - Remove unused imports.
    - Target: <=30 LOC.

    **QualityLayout.tsx** (target: ~55 LOC — documented SOFT-MISS of the <=30 goal):
    - Delete ONLY the alert block (lines 84-103). DO NOT touch the legacy-migration `useEffect` at lines 32-82 (the ~50-LOC essay) — this is load-bearing per RESEARCH.md §Focus 1 and will be addressed by Phase 28 SWEEP-04.
    - Keep the `MedplumProvider` (if present in QualityLayout) and `QualityMetricsProvider` wrapping (per §Focus 1 lines 105-118 — those stay as layout-specific decoration).
    - The connection branching around `<Outlet />` becomes:
      ```tsx
      <QualityMetricsProvider>
        <ConnectionGatedOutlet />
      </QualityMetricsProvider>
      ```
      OR use the render prop if the QualityMetricsProvider should only wrap the connected branch:
      ```tsx
      <ConnectionGatedOutlet render={(connected) => connected
        ? <QualityMetricsProvider><Outlet /></QualityMetricsProvider>
        : undefined
      } />
      ```
      Pick whichever preserves current render behavior — read the pre-migration QualityLayout.tsx carefully. RESEARCH.md §Focus 1 shows `<MedplumProvider> + <QualityMetricsProvider> + <Outlet>` is the current decoration sequence; decide if QualityMetricsProvider wraps always (safe on disconnected too) or only when connected.
    - Remove unused imports (Alert, icon, Link if no longer used).
    - Acknowledge in a short top-of-file comment: `// NOTE: ~55 LOC includes the inline legacy-migration useEffect (lines ~30-80). That block is load-bearing until Phase 28 SWEEP-04 moves it into migrateLegacyResourceTypeKey.` — OR update the existing essay comment to mention Phase 28.

    After all 3 files:
    - Run the full quality-layout.test.tsx (from Task 1) — MUST still pass.
    - Run the full ConnectionGatedOutlet.test.tsx (from Task 2) — MUST still pass.
    - Run `npx tsc -b --noEmit` — 0 errors.
    - Run `npm test` full suite — zero NEW regressions vs. the 22 known-failing baseline.

    Do NOT wrap MedplumProvider around ConnectionGatedOutlet (D-02). Do NOT alter alert copy (D-04, already verbatim in the primitive).
  </action>
  <verify>
    <automated>npm test -- --run src/components/quality/__tests__/quality-layout.test.tsx src/components/layout/__tests__/ConnectionGatedOutlet.test.tsx && wc -l src/components/explorer/ExplorerLayout.tsx src/components/patients/PatientsLayout.tsx</automated>
  </verify>
  <done>
    - `grep -c "ConnectionGatedOutlet" src/components/explorer/ExplorerLayout.tsx` returns >= 1.
    - `grep -c "ConnectionGatedOutlet" src/components/patients/PatientsLayout.tsx` returns >= 1.
    - `grep -c "ConnectionGatedOutlet" src/components/quality/QualityLayout.tsx` returns >= 1.
    - `grep -rn "Not connected" src/components/explorer/ExplorerLayout.tsx src/components/patients/PatientsLayout.tsx src/components/quality/QualityLayout.tsx` returns 0 matches (alert block gone from all 3).
    - `wc -l src/components/explorer/ExplorerLayout.tsx` shows <= 30 lines.
    - `wc -l src/components/patients/PatientsLayout.tsx` shows <= 30 lines.
    - `wc -l src/components/quality/QualityLayout.tsx` shows <= 60 lines (SOFT-MISS acknowledged; legacy-migration essay preserved).
    - `grep -c "MedplumProvider" src/components/layout/ConnectionGatedOutlet.tsx` returns 0 (provider NOT moved).
    - `grep -c "migrateLegacyResourceTypeKey\|LEGACY_COHORT_KEY" src/components/quality/QualityLayout.tsx` returns >= 2 (legacy-migration essay preserved).
    - `npm test -- --run src/components/quality/__tests__/quality-layout.test.tsx` still passes (regression fence from Task 1 intact).
    - `npm test -- --run src/components/layout/__tests__/ConnectionGatedOutlet.test.tsx` passes.
    - `npx tsc -b --noEmit` reports 0 errors.
    - `npm test` full suite: 22 failed + 780 passed (zero NEW failures vs. baseline).
    - Commit: `refactor(26): migrate 3 layouts to ConnectionGatedOutlet (SHELL-01)` — atomic.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser <-> rendered app | Pure client-side React; no new boundary crossed by this refactor |

## STRIDE Threat Register (ASVS L1 — refactor phase, minimal surface)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | I (Information disclosure) | ConnectionGatedOutlet | accept | Component reads only `useMedplumContext().medplum` truthiness; does not read, log, or render PHI. The alert copy is a static string with no resource IDs or patient data. No logging introduced. |
| T-26-02 | T (Tampering) | Legacy-migration useEffect in QualityLayout | accept | Legacy-migration behavior PRESERVED — no change to what it reads/writes in localStorage. Regression fenced by `quality-layout.test.tsx` (Task 1). |
| T-26-03 | E (Elevation of privilege) | MedplumProvider placement | accept | D-02 forbids moving MedplumProvider into ConnectionGatedOutlet. MedplumProvider STAYS at AppLayout root — unchanged attack surface. Enforced by grep gate in Task 3 done criteria. |

**No PHI in logs. No auth change. No new settings persisted. No new network calls.**
</threat_model>

<verification>
- `npm test -- --run src/components/quality/__tests__/quality-layout.test.tsx` — passes (Task 1 regression fence).
- `npm test -- --run src/components/layout/__tests__/ConnectionGatedOutlet.test.tsx` — passes (Task 2 contract).
- `npx tsc -b --noEmit` — 0 errors.
- `npm test` full suite — no new failures vs. 22-failing / 780-passing baseline from Phase 25 end.
- Grep gates (see each task's `<done>` block).
- LOC gates: Explorer <=30, Patients <=30, Quality <=60 (SOFT-MISS documented).
</verification>

<success_criteria>
- SHELL-01 requirement met: ConnectionGatedOutlet replaces the triplicated alert block across the 3 layouts.
- Render parity preserved: existing quality-layout.test.tsx (created in Task 1) passes against post-migration QualityLayout.
- MedplumProvider still lives in AppLayout.tsx (NOT in ConnectionGatedOutlet).
- ExplorerLayout and PatientsLayout each <=30 LOC. QualityLayout lands around 55 LOC with a documented Phase 28 SWEEP-04 handoff for the legacy-migration essay.
- Zero new test regressions.
- Three atomic commits (test / feat / refactor).
</success_criteria>

<output>
After completion, create `.planning/phases/26-app-shell-dedup/26-01-SUMMARY.md` per template at `$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- Actual final LOC of each of the 3 layouts
- Confirmation of the soft-miss on QualityLayout with Phase 28 SWEEP-04 handoff
- All grep/LOC gate results
- Commit hashes (3 commits: test, feat, refactor)
</output>
