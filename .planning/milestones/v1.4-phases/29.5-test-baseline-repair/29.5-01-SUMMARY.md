---
phase: 29.5-test-baseline-repair
plan: 01
status: completed
completed: 2026-04-23
tests_before: 22 failed / 814 passed / 22 todo / 3 skipped
tests_after: 0 failed / 835 passed / 22 todo / 3 skipped
files_changed:
  - src/__tests__/fixtures/terminology.ts
  - src/__tests__/human-readable-view-terminology.test.tsx
  - src/__tests__/patient-detail.test.tsx
  - src/__tests__/patient-list.test.tsx
  - src/__tests__/patient-view-toggle.test.tsx
  - src/__tests__/quality-overview.test.tsx
  - src/__tests__/resource-type-landing-counts.test.tsx
  - src/__tests__/sidebar-terminology-row.test.tsx
---

# Summary — Plan 29.5-01: Test Baseline Repair

## Outcome

Repaired 22 pre-existing test failures across 8 test files so Phase 30 (layout
redesign) can enforce `npm test` as a per-step gate.

**Before:** 22 failed, 814 passed, 22 todo, 3 skipped.
**After:**   0 failed, 835 passed, 22 todo, 3 skipped.

Build remains clean (`npm run build` → 0 errors).

## Per-File Changes (Test Setup Only, Zero Production Changes)

| File | Failing | Fix |
|------|---------|-----|
| `fixtures/terminology.ts` | 1 | Expose `fhirUrl()` on mock `MedplumClient` and accept both `metadata` and path-suffixed matches in mock `get`. |
| `sidebar-terminology-row.test.tsx` | 4 | Wrap render in `<SettingsProvider>` + `<ConnectionProvider>` (Sidebar pulls both contexts via its child modals). |
| `patient-view-toggle.test.tsx` | 5 | Production moved `client.search(type, q)` → `client.get(client.fhirUrl(url).toString())`; mocks and URL-shape assertions rewritten. |
| `patient-detail.test.tsx` | 2 | Added `get/fhirUrl` to mock client. Swapped stale `data-testid="patient-header"` anchors (dead mock of Medplum `PatientHeader`) for existing `MII Modules` anchors. |
| `patient-list.test.tsx` | 5 | Added `useSearchParams` to router mock. Added `get/fhirUrl` to client mocks. Replaced obsolete field/placeholder copy and removed the dead "Browse Patients" empty-state test. |
| `quality-overview.test.tsx` | 2 | `Resource types` appears in two places (OverviewStrip tile + ResourceTypeSelector label) — relaxed to `getAllByText`. `Observation: 'loading'` is now intentionally filtered out of the counts table when `!includeEmpty` (current production behavior). |
| `resource-type-landing-counts.test.tsx` | 1 | Error badges are hidden by the default `!showEmpty` filter; the test now clicks **Show empty** to surface them. |
| `human-readable-view-terminology.test.tsx` | 2 | `HumanReadableView` renders the local `<ResourcePropertyTable>` now, not Medplum's `ResourceTable`. Switched the mock target; preserved the `rt-display` / `rt-code` data-testid contract. |

## Verification

- `npm run build` → exits 0, `tsc -b` clean.
- `npm test` → 835 passed / 22 todo / 0 failed.
- Commit: `8de3b37 test(29.5): repair baseline — 22 → 0 failing tests` on main.

## Handoff to Phase 30

Main is now green. The phase 30 feature branch
`gsd/phase-30-layout-redesign` was created before this repair, so it should be
rebased onto main (or merged forward) to inherit the test fixes before
beginning step 0:

```bash
git checkout gsd/phase-30-layout-redesign
git rebase main
```
