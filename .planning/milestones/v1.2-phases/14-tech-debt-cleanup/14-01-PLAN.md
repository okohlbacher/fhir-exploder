---
phase: 14-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/utils/fhir-helpers.ts
  - src/components/explorer/ResourceDetailPage.tsx
  - src/components/explorer/ResourcePropertyTable.tsx
  - src/components/explorer/SearchResultsPage.tsx
  - src/components/patients/FhirResourcesView.tsx
  - src/components/patients/MiiModuleTab.tsx
  - src/components/patients/PatientTimeline.tsx
  - src/utils/export.ts
  - src/quality/codingCoverageWalker.ts
  - src/quality/completenessWalker.ts
  - src/terminology/walker.ts
  - src/components/explorer/JsonTreeView.tsx
  - src/config/settings.ts
  - src/utils/timeline-utils.ts
  - src/__tests__/display-modes.test.tsx
  - src/__tests__/json-highlight.test.ts
autonomous: true
requirements:
  - DEBT-02

must_haves:
  truths:
    - "npx tsc -b --noEmit completes with zero errors"
    - "npx vitest run has no new failures compared to pre-fix baseline"
    - "All 47 inline 'as Record<string, unknown>' casts replaced with toRecord() calls"
    - "No unused declarations remain in source or test files"
  artifacts:
    - path: "src/utils/fhir-helpers.ts"
      provides: "toRecord() and getCodeDisplay() utility functions"
      exports: ["toRecord", "getCodeDisplay"]
    - path: "package.json"
      provides: "@testing-library/dom as devDependency"
      contains: "@testing-library/dom"
  key_links:
    - from: "src/components/explorer/SearchResultsPage.tsx"
      to: "src/utils/fhir-helpers.ts"
      via: "import { toRecord, getCodeDisplay }"
      pattern: "import.*toRecord.*fhir-helpers"
    - from: "src/components/patients/MiiModuleTab.tsx"
      to: "src/utils/fhir-helpers.ts"
      via: "import { toRecord, getCodeDisplay }"
      pattern: "import.*toRecord.*fhir-helpers"
---

<objective>
Eliminate all 54 TypeScript build errors across the codebase so `npx tsc -b --noEmit` exits with zero errors.

Purpose: DEBT-02 requires a clean build. This unblocks all future development by removing type errors that mask real issues.
Output: Zero-error TypeScript build, new `src/utils/fhir-helpers.ts` utility module, all inline casts migrated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tech-debt-cleanup/14-CONTEXT.md
@.planning/phases/14-tech-debt-cleanup/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install missing peer dependency and remove unused declarations</name>
  <files>package.json, package-lock.json, src/__tests__/display-modes.test.tsx, src/__tests__/json-highlight.test.ts, src/components/explorer/SearchResultsPage.tsx, src/components/patients/MiiModuleTab.tsx</files>
  <read_first>
    - package.json (current devDependencies)
    - src/__tests__/display-modes.test.tsx (find unused mockPatient declaration)
    - src/__tests__/json-highlight.test.ts (find unused JsonToken import)
    - src/components/explorer/SearchResultsPage.tsx (find unused 'includes' declaration at line 241)
    - src/components/patients/MiiModuleTab.tsx (find unused useCallback, useMemo imports at line 1)
  </read_first>
  <action>
Per D-05 and D-03 steps 1-2:

1. Install @testing-library/dom as a dev dependency:
   ```bash
   npm install --save-dev @testing-library/dom
   ```
   This clears all 31 TS2305 errors across 17 test files.

2. In `src/__tests__/display-modes.test.tsx`: Remove the `mockPatient` declaration (TS6133 at line 7). The variable is declared but never used. Remove the entire `const mockPatient = ...` block. If other test code references it, that would be caught by tsc.

3. In `src/__tests__/json-highlight.test.ts`: Remove the unused `JsonToken` import (TS6133 at line 3). Change the import line to only import the symbols actually used.

4. In `src/components/explorer/SearchResultsPage.tsx`: Remove the unused `includes` declaration (TS6133 at line 241). Find where `includes` is destructured or declared and remove it.

5. In `src/components/patients/MiiModuleTab.tsx`: Remove `useCallback` and `useMemo` from the React import at line 1. These are unused (TS6133). Keep only the React imports that are actually used in the file.

After these changes, 35 of 54 errors should be eliminated (31 TS2305 + 4 TS6133).
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/Exploder && npx tsc -b --noEmit 2>&1 | grep -c "TS2305\|TS6133" | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls @testing-library/dom` shows a version satisfying ^10.0.0 with no peer dep warnings
    - `npx tsc -b --noEmit 2>&1 | grep "TS2305" | wc -l` outputs 0
    - `npx tsc -b --noEmit 2>&1 | grep "TS6133" | wc -l` outputs 0
    - `grep -n "mockPatient" src/__tests__/display-modes.test.tsx` returns no matches (or file still compiles if mockPatient was needed elsewhere)
    - `grep -n "JsonToken" src/__tests__/json-highlight.test.ts` shows no import of JsonToken
    - `grep -n "useCallback\|useMemo" src/components/patients/MiiModuleTab.tsx` returns no matches on the import line
  </acceptance_criteria>
  <done>Zero TS2305 errors (testing-library types resolve), zero TS6133 errors (no unused declarations)</done>
</task>

<task type="auto">
  <name>Task 2: Create fhir-helpers utility, migrate all casts, fix TS2345</name>
  <files>src/utils/fhir-helpers.ts, src/components/explorer/ResourceDetailPage.tsx, src/components/explorer/ResourcePropertyTable.tsx, src/components/explorer/SearchResultsPage.tsx, src/components/patients/FhirResourcesView.tsx, src/components/patients/MiiModuleTab.tsx, src/components/patients/PatientTimeline.tsx, src/utils/export.ts, src/quality/codingCoverageWalker.ts, src/quality/completenessWalker.ts, src/terminology/walker.ts, src/components/explorer/JsonTreeView.tsx, src/config/settings.ts, src/utils/timeline-utils.ts</files>
  <read_first>
    - src/components/explorer/ResourceDetailPage.tsx (TS2345 at line 62, readResource call)
    - src/components/explorer/ResourcePropertyTable.tsx (3 TS2352 errors at lines 204, 205, 226)
    - src/components/explorer/SearchResultsPage.tsx (7 TS2352 errors at lines 20, 60, 422, 427, 428, 433 + existing casts)
    - src/components/patients/FhirResourcesView.tsx (2 TS2352 errors at lines 51, 70 + existing casts)
    - src/components/patients/MiiModuleTab.tsx (3 TS2352 errors at lines 14, 34, 129 + existing casts)
    - src/components/patients/PatientTimeline.tsx (2 TS2352 errors at line 58 + existing casts)
    - src/utils/export.ts (1 TS2352 error)
    - src/quality/codingCoverageWalker.ts (existing casts to migrate)
    - src/quality/completenessWalker.ts (existing casts to migrate)
    - src/terminology/walker.ts (existing casts to migrate)
    - src/components/explorer/JsonTreeView.tsx (existing casts to migrate)
    - src/config/settings.ts (existing casts to migrate)
    - src/utils/timeline-utils.ts (existing casts to migrate)
  </read_first>
  <action>
Per D-03 steps 3-4 and D-04:

**Step 1: Create `src/utils/fhir-helpers.ts`:**

```typescript
import type { CodeableConcept, Resource, ResourceType } from '@medplum/fhirtypes';

/**
 * Cast a FHIR Resource to a plain record for dynamic property access.
 * Centralizes the `as unknown as Record<string, unknown>` double-cast
 * needed because some Resource union members (e.g. VisionPrescription)
 * lack an index signature.
 */
export function toRecord(resource: Resource): Record<string, unknown> {
  return resource as unknown as Record<string, unknown>;
}

/**
 * Extract a human-readable display string from a CodeableConcept.
 * Prefers text, then first coding's display, then first coding's code.
 * Returns empty string if no display value is available.
 */
export function getCodeDisplay(concept: CodeableConcept | undefined): string {
  if (!concept) return '';
  if (concept.text) return concept.text;
  const coding = concept.coding?.[0];
  if (!coding) return '';
  return coding.display ?? coding.code ?? '';
}
```

**Step 2: Fix all 17 TS2352 error sites** by replacing `resource as Record<string, unknown>` with `toRecord(resource)`:
- Import `toRecord` from `../utils/fhir-helpers` (or appropriate relative path) in each file
- Replace every `someResource as Record<string, unknown>` that produces a TS2352 error with `toRecord(someResource)`

**Step 3: Migrate all ~30 existing working casts** across these additional files (per D-04 requirement to consolidate ALL casts):
- `src/quality/codingCoverageWalker.ts`
- `src/quality/completenessWalker.ts`
- `src/terminology/walker.ts`
- `src/components/explorer/JsonTreeView.tsx`
- `src/config/settings.ts`
- `src/utils/timeline-utils.ts`
- Any remaining sites in the 7 error files above that have working casts alongside the broken ones

For each file: add `import { toRecord } from '...'` and replace `x as Record<string, unknown>` with `toRecord(x)`. Only migrate casts where `x` is typed as `Resource` or a FHIR resource type. Do NOT migrate casts on non-Resource objects (e.g., plain objects, config values).

**Step 4: Migrate getCodeDisplay pattern** in files that have the repeated CodeableConcept extraction:
- `src/components/explorer/SearchResultsPage.tsx`
- `src/components/patients/MiiModuleTab.tsx`
- `src/components/patients/FhirResourcesView.tsx`
- `src/components/patients/PatientTimeline.tsx`

Look for patterns like `(r as Record<string, unknown>).code` followed by `.coding[0].display || .coding[0].code`. Replace with `getCodeDisplay((resource as any).code as CodeableConcept)` or use `toRecord` + proper typing.

**Step 5: Fix TS2345 in ResourceDetailPage.tsx** (line 62):
Add `as ResourceType` assertion to the `readResource` call:
```typescript
import type { ResourceType } from '@medplum/fhirtypes';
// In the readResource call:
client.readResource(resourceType as ResourceType, id)
```
This is safe because the value comes from URL params matching server-known resource types (per research assumption A1).

After all changes: `grep -rn "as Record<string, unknown>" src/` should return 0 matches for Resource-typed casts. Only non-Resource casts (if any) should remain.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/Exploder && npx tsc -b --noEmit 2>&1 | wc -l | grep "^0$" && grep -rn "as Record<string, unknown>" src/ | grep -v "fhir-helpers" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b --noEmit` exits with code 0 and produces zero output lines
    - `src/utils/fhir-helpers.ts` exists and contains `export function toRecord(` and `export function getCodeDisplay(`
    - `grep -rn "as Record<string, unknown>" src/ | grep -v fhir-helpers | wc -l` outputs 0 (all casts migrated except the one inside fhir-helpers.ts itself)
    - `grep -n "as ResourceType" src/components/explorer/ResourceDetailPage.tsx` returns at least one match
    - `npx vitest run 2>&1 | tail -5` shows test results with no new failures beyond the pre-existing 1 actual failure in terminology-health.test.ts
  </acceptance_criteria>
  <done>Zero TypeScript build errors. All inline Record casts consolidated into toRecord(). getCodeDisplay() extracted. TS2345 ResourceType string error fixed. DEBT-02 fully satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan is purely mechanical refactoring with no behavioral changes, no new user input handling, and no new data flows.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | T (Tampering) | fhir-helpers.ts toRecord() | accept | toRecord() performs a type assertion only -- no runtime behavior change. The cast was already present at each call site; centralizing it does not change the trust model. |
| T-14-02 | I (Information Disclosure) | @testing-library/dom | accept | Dev dependency only, not shipped in production bundle. No runtime impact. |
</threat_model>

<verification>
After both tasks complete:
1. `npx tsc -b --noEmit` exits 0 with no output
2. `npm run build` completes successfully (tsc -b + vite build)
3. `npx vitest run` shows no new failures vs baseline (181 passed, 1 failed in terminology-health, 22 todo)
4. `grep -rn "as Record<string, unknown>" src/ | grep -v fhir-helpers` returns 0 lines
</verification>

<success_criteria>
- DEBT-02 satisfied: `npm run build` (tsc -b) completes with zero errors
- All 47 scattered Record casts consolidated into toRecord() utility
- getCodeDisplay() utility extracts repeated CodeableConcept pattern
- No unused declarations in any source or test file
- Test suite baseline maintained (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/14-tech-debt-cleanup/14-01-SUMMARY.md`
</output>
