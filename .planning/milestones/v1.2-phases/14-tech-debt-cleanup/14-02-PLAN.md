---
phase: 14-tech-debt-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/terminology/walker.ts
  - src/terminology/TerminologyResolver.ts
  - src/terminology/terminologyKey.ts
  - src/__tests__/resolved-resource.test.tsx
  - public/settings.yaml
  - src/components/quality/CodingDrillDown.tsx
  - src/quality/completenessWalker.ts
  - src/utils/export.ts
  - src/components/quality/ValidationPanel.tsx
  - src/components/quality/CompletenessDrillDown.tsx
  - src/components/quality/SampleSizeControl.tsx
  - src/hooks/useValidationRun.ts
  - src/hooks/useCompletenessReport.ts
autonomous: true
requirements:
  - DEBT-01

must_haves:
  truths:
    - "All 5 Phase 4 info-level findings (IN-01 through IN-05) are addressed"
    - "All 12 Phase 5 info-level findings (IN-01 through IN-12) are addressed"
    - "Build still passes with zero errors after changes"
    - "Test suite has no new failures after changes"
  artifacts:
    - path: "src/terminology/terminologyKey.ts"
      provides: "Exported UNCONFIGURED_SERVER constant"
      contains: "UNCONFIGURED_SERVER"
    - path: "src/terminology/walker.ts"
      provides: "typeof child === 'object' guard in recursion"
      contains: "typeof child === 'object'"
  key_links:
    - from: "src/terminology/TerminologyResolver.ts"
      to: "src/terminology/terminologyKey.ts"
      via: "import { UNCONFIGURED_SERVER }"
      pattern: "import.*UNCONFIGURED_SERVER.*terminologyKey"
---

<objective>
Address all 17 info-level code review findings from Phase 4 (5 items) and Phase 5 (12 items) to satisfy DEBT-01.

Purpose: Clean codebase with no deferred review findings before building new features in Phases 15-19.
Output: All 17 info findings resolved with verifiable diffs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tech-debt-cleanup/14-CONTEXT.md
@.planning/phases/14-tech-debt-cleanup/14-RESEARCH.md
@.planning/phases/14-tech-debt-cleanup/14-01-SUMMARY.md

The code review findings are in git history. Read them with:
- Phase 4: `git show 46e2c82:.planning/phases/04-terminology-resolution/04-REVIEW.md`
- Phase 5: `git show 2596316:.planning/phases/05-data-quality-dashboard/05-REVIEW.md`

<interfaces>
<!-- From Plan 01 output: fhir-helpers.ts is already created and available -->
From src/utils/fhir-helpers.ts:
```typescript
export function toRecord(resource: Resource): Record<string, unknown>;
export function getCodeDisplay(concept: CodeableConcept | undefined): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Phase 4 info-level findings (IN-01 through IN-05)</name>
  <files>src/terminology/walker.ts, src/terminology/TerminologyResolver.ts, src/terminology/terminologyKey.ts, src/__tests__/resolved-resource.test.tsx, public/settings.yaml</files>
  <read_first>
    - src/terminology/walker.ts (IN-01: recursion into Coding scalar fields, around lines 29-31)
    - src/terminology/TerminologyResolver.ts (IN-02: '__unconfigured__' sentinel at line 63; IN-03: extractDisplay params.parameter guard at lines 23-34)
    - src/terminology/terminologyKey.ts (IN-02: check if UNCONFIGURED_SERVER already exists)
    - src/__tests__/resolved-resource.test.tsx (IN-04: ref mutation during render at line 34)
    - public/settings.yaml (IN-05: commented-out credential shape at line 7)
    - src/contexts/TerminologyContext.tsx (IN-02: find the other '__unconfigured__' usage)
  </read_first>
  <action>
Per D-01, fix all 5 Phase 4 info findings exactly as described in the review:

**IN-01** (`src/terminology/walker.ts:29-31`): Add `typeof child === 'object'` guard before recursing.
Change the loop body from:
```typescript
for (const key of Object.keys(v)) {
  const child = v[key];
  collectCodings(child, out);
}
```
to:
```typescript
for (const key of Object.keys(v)) {
  const child = v[key];
  if (child && typeof child === 'object') collectCodings(child, out);
}
```
This prevents wasted recursion into string/number/boolean scalar fields of Coding objects.

**IN-02** (`src/terminology/TerminologyResolver.ts:63` + `src/contexts/TerminologyContext.tsx`): Extract `'__unconfigured__'` sentinel to an exported constant.
In `src/terminology/terminologyKey.ts`, add:
```typescript
export const UNCONFIGURED_SERVER = '__unconfigured__';
```
Then in `TerminologyResolver.ts` line 63, replace `'__unconfigured__'` with `UNCONFIGURED_SERVER` (import from terminologyKey.ts).
In `src/contexts/TerminologyContext.tsx`, find the other `'__unconfigured__'` usage and replace it with the same import.

**IN-03** (`src/terminology/TerminologyResolver.ts:23-34`): Add `Array.isArray` guard for `params.parameter`.
Before the loop over `params.parameter`, add:
```typescript
if (!Array.isArray(params.parameter)) return null;
```

**IN-04** (`src/__tests__/resolved-resource.test.tsx:34`): Move ref mutation from render phase into `useEffect`.
Change from:
```typescript
captured.current = resolved;
```
(during render) to:
```typescript
useEffect(() => { captured.current = resolved; }, [resolved]);
```
Add the `useEffect` import if not already present.

**IN-05** (`public/settings.yaml:7`): Remove the commented-out credential/token example.
Delete any lines like `# token: "eyJ..."` or similar credential shape comments. Keep only the actual configuration values and comments about what fields are available (without example secrets).
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/Exploder && npx tsc -b --noEmit 2>&1 | wc -l | grep "^0$" && grep -n "typeof child === 'object'" src/terminology/walker.ts | head -1 && grep -n "UNCONFIGURED_SERVER" src/terminology/terminologyKey.ts | head -1 && grep -n "Array.isArray" src/terminology/TerminologyResolver.ts | head -1 && grep -n "useEffect.*captured" src/__tests__/resolved-resource.test.tsx | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b --noEmit` still exits 0 with no errors
    - `grep "typeof child === 'object'" src/terminology/walker.ts` returns at least one match (IN-01 fixed)
    - `grep "UNCONFIGURED_SERVER" src/terminology/terminologyKey.ts` returns `export const UNCONFIGURED_SERVER` (IN-02 constant created)
    - `grep "__unconfigured__" src/terminology/TerminologyResolver.ts src/contexts/TerminologyContext.tsx` returns no matches (both replaced with constant)
    - `grep "Array.isArray(params.parameter)" src/terminology/TerminologyResolver.ts` returns a match (IN-03 guard added)
    - `grep "useEffect.*captured" src/__tests__/resolved-resource.test.tsx` returns a match (IN-04 ref moved to effect)
    - `grep -i "token.*eyJ\|eyJ.*token" public/settings.yaml` returns no matches (IN-05 credential comment removed)
  </acceptance_criteria>
  <done>All 5 Phase 4 info-level findings (IN-01 through IN-05) resolved with verifiable code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Phase 5 info-level findings (IN-01 through IN-12)</name>
  <files>src/components/quality/CodingDrillDown.tsx, src/quality/completenessWalker.ts, src/utils/export.ts, src/components/quality/ValidationPanel.tsx, src/components/quality/CompletenessDrillDown.tsx, src/components/quality/SampleSizeControl.tsx, src/hooks/useValidationRun.ts, src/hooks/useCompletenessReport.ts</files>
  <read_first>
    - src/components/quality/CodingDrillDown.tsx (IN-01: duplicate React imports at lines 14 and 35; IN-08: double-fetch location)
    - src/quality/completenessWalker.ts (IN-02: isNonEmpty using for..in around line 76)
    - src/utils/export.ts (IN-03: filename generation with colons)
    - src/components/quality/ValidationPanel.tsx (IN-04: dead void NumberFormatter at line 73; IN-05: dead void backends; IN-12: bannerKey useMemo around line 84)
    - src/components/quality/CompletenessDrillDown.tsx (IN-06: singleTypeList array re-created each render)
    - src/components/quality/SampleSizeControl.tsx (IN-07: onChange coerces 0 to DEFAULT)
    - src/hooks/useValidationRun.ts (IN-09: errorMessage clearing comment; IN-10: duplicated progress-update block)
    - src/hooks/useCompletenessReport.ts (IN-11: cancel-return in next() comment)
  </read_first>
  <action>
Per D-01, fix all 12 Phase 5 info findings. Note: Some findings may already be partially addressed by Plan 01 TS error fixes. Check current state of each file before applying.

**IN-01** (`src/components/quality/CodingDrillDown.tsx:14,35`): Merge duplicate React imports into a single import statement. Combine all React hooks from both import lines into one `import { ... } from 'react'` line.

**IN-02** (`src/quality/completenessWalker.ts:76`): Replace the `isNonEmpty` function's `for..in` loop with `Object.keys(obj).length > 0`. Change from:
```typescript
function isNonEmpty(obj: Record<string, unknown>): boolean {
  for (const _ in obj) return true;
  return false;
}
```
to:
```typescript
function isNonEmpty(obj: Record<string, unknown>): boolean {
  return Object.keys(obj).length > 0;
}
```

**IN-03** (`src/utils/export.ts`): In the filename generation logic, replace colons (`:`) and dots (`.`) with hyphens (`-`) in exported filenames. Find the pattern that generates filenames with timestamp components and replace `[:.]` with `-`. For example, change from `2026-04-13T12:30:45.123Z` to `2026-04-13T12-30-45-123Z` in filenames.

**IN-04** (`src/components/quality/ValidationPanel.tsx:73`): Remove the dead `void NumberFormatter;` line entirely.

**IN-05** (`src/components/quality/ValidationPanel.tsx`): Find the `void backends` line. Instead of importing `backends` and voiding it, destructure only the fields actually used from whatever object provides `backends`. If `backends` comes from a destructuring like `const { backends, ...rest } = something`, remove `backends` from the destructuring.

**IN-06** (`src/components/quality/CompletenessDrillDown.tsx`): Wrap the `singleTypeList` array creation in `useMemo`. Find where `singleTypeList` (or equivalent single-type array) is created each render and wrap:
```typescript
const singleTypeList = useMemo(() => [resourceType], [resourceType]);
```
Add `useMemo` to the React import if not already present.

**IN-07** (`src/components/quality/SampleSizeControl.tsx`): In the `onChange` handler, change the behavior when value is `0` from coercing to DEFAULT to clamping to MIN (10). Find the condition like `if (val === 0) val = DEFAULT` and change to `if (val < MIN) val = MIN` where MIN is 10.

**IN-08** (`src/components/quality/CodingDrillDown.tsx`): Per D-01, add a TODO comment rather than building shared cache infrastructure. Add near the top of the component or near the fetch logic:
```typescript
// TODO: Consider shared sample cache to avoid double-fetching when switching between drill-down views (Phase 5 IN-08)
```

**IN-09** (`src/hooks/useValidationRun.ts`): Add a clarifying comment explaining why `errorMessage` is cleared on subsequent starts. Find where `setErrorMessage('')` or similar is called at the start of a run and add:
```typescript
// Clear previous error on new run start — user sees fresh state
```

**IN-10** (`src/hooks/useValidationRun.ts`): Extract the duplicated progress-update block (~40 lines) into a helper function. Find the two similar blocks that update progress state and extract into a local function like:
```typescript
function updateProgress(current: number, total: number, label: string) {
  setProgress({ current, total, label });
}
```
Or whatever the repeated pattern is. Read the file to identify the exact duplicated block before extracting.

**IN-11** (`src/hooks/useCompletenessReport.ts`): Add a clarifying comment at the cancel-return in the `next()` function. Find where the cancellation check returns early inside the async iteration and add:
```typescript
// Early return: effect cleanup set cancelled=true, abort remaining pages
```

**IN-12** (`src/components/quality/ValidationPanel.tsx`): Check if `bannerKey` is already wrapped in `useMemo`. Per research assumption A2, it may already be fixed (useMemo exists at line 84). If already memoized, no change needed. If not, wrap in `useMemo`.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/Exploder && npx tsc -b --noEmit 2>&1 | wc -l | grep "^0$" && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b --noEmit` still exits 0 with no errors
    - `grep -c "from 'react'" src/components/quality/CodingDrillDown.tsx` outputs 1 (single consolidated React import, IN-01)
    - `grep "Object.keys" src/quality/completenessWalker.ts` returns a match (IN-02)
    - `grep -E "replace\(.*[:\.].*-" src/utils/export.ts` or equivalent shows colon/dot replacement in filename (IN-03)
    - `grep "void NumberFormatter" src/components/quality/ValidationPanel.tsx` returns no matches (IN-04 removed)
    - `grep "void backends" src/components/quality/ValidationPanel.tsx` returns no matches (IN-05 removed)
    - `grep "useMemo.*resourceType\|useMemo.*singleType" src/components/quality/CompletenessDrillDown.tsx` returns a match (IN-06)
    - `grep -i "MIN\|min.*10\|< 10\|clamp" src/components/quality/SampleSizeControl.tsx` shows clamping to min (IN-07)
    - `grep "TODO.*sample cache\|TODO.*double-fetch\|TODO.*IN-08" src/components/quality/CodingDrillDown.tsx` returns a match (IN-08)
    - `grep -c "// .*clear\|// .*fresh\|// .*previous error" src/hooks/useValidationRun.ts` shows at least 1 (IN-09)
    - `grep -c "// .*cancel\|// .*abort\|// .*cleanup" src/hooks/useCompletenessReport.ts` shows at least 1 (IN-11)
    - `npx vitest run` shows no new test failures beyond the pre-existing 1 in terminology-health.test.ts
  </acceptance_criteria>
  <done>All 12 Phase 5 info-level findings resolved. Combined with Task 1, all 17 info-level findings from DEBT-01 are addressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All changes are refactoring existing code patterns without behavioral changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | T (Tampering) | settings.yaml credential comment removal | accept | Removing example credential shape reduces copy-paste risk. No runtime behavior change. |
| T-14-04 | S (Spoofing) | export filename sanitization | accept | Replacing colons with hyphens in filenames is a compatibility fix, not a security boundary. |
</threat_model>

<verification>
After both tasks complete:
1. `npx tsc -b --noEmit` exits 0
2. `npm run build` completes successfully
3. `npx vitest run` shows no new failures vs Plan 01 baseline
4. All 17 info findings verifiable via grep commands in acceptance criteria
</verification>

<success_criteria>
- DEBT-01 satisfied: All 17 info-level code review findings from phases 4+5 are addressed
- Each finding has a verifiable code change (grep-checkable)
- Build remains clean (zero TS errors maintained from Plan 01)
- Test suite has no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/14-tech-debt-cleanup/14-02-SUMMARY.md`
</output>
